# كتاب تعليمي شامل — مشروع eng-alaa.com (Portfolio Hub)

> **اسم المشروع:** Portfolio Hub — موقع المهندس علاء أحمد المصري  
> **الموقع:** eng-alaa.com  
> **التقنيات:** React + Vite + TypeScript + PHP + MySQL + Hostinger  
> **الغرض:** تعليمك المشروع **خطوة بخطوة** — **بدون** أن تكون مبرمجًا محترفًا

---

## عن هذا الكتاب

هذا **دليل تعليمي بالعربية** يشرح مشروع **eng-alaa.com** من الصفر: كيف يفتح الموقع، من أين تأتي البيانات، كيف تعمل لوحة التحكم، كيف تُنشر على Hostinger، وكيف تعدّل **بأمان** بمساعدة Cursor.

**هذا الكتاب للتعلّم والقراءة** — لا يعدّل الكود تلقائيًا. عندما تطبّق على المشروع الحقيقي، اتبع الفصول **16–19** (أمان، Git، Cursor، اختبار).

---

## لمن هذا الكتاب؟

| أنت… | هذا الكتاب يساعدك على… |
|------|-------------------------|
| **مبتدئ تمامًا** | فهم «ماذا يحدث» عند فتح الموقع |
| **صاحب المشروع** | تعديل المحتوى (كتب، مقالات، تصاميم) بثقة |
| **تتعلم React** | رؤية مشروع **حقيقي** — App.tsx، Components، state |
| **تستخدم Cursor** | طلب تعديلات **واضحة** دون تخريب الموقع |
| **تنشر على Hostinger** | checklist قبل وبعد الرفع |

**لا تحتاج** أن تحفظ كل شيء دفعة واحدة — **فصلًا في الأسبوع** كافٍ للبداية (انظر الفصل 15).

---

## كيف أستخدم هذا الكتاب؟

1. **ابدأ بالفصل 1** — لا تقفز إلى App.tsx قبل فهم `index.html → main.tsx → App.tsx`.
2. **شغّل المشروع محليًا** عند الفصل 2–3: `npm install` ثم `npm run dev`.
3. **افتح F12** (Console + Network) من الفصل 11 — استخدمها في كل فصل لاحق.
4. **اقرأ + جرّب** — بعد كل فصل: أسئلة تدريبية في نهاية الفصل.
5. **لا تعدّل الكود** أثناء القراءة الأولى — **الفصل 20** تدريب **نظري** فقط.
6. **ارجع للفهرس** — كل فصل مستقل نسبيًا؛ استخدم **الملاحق** في نهاية الكتاب للمراجعة السريعة.

### بنية كل فصل (معيار الكتاب)

| جزء | ماذا تجد؟ |
|-----|-----------|
| **هدف الفصل** | ماذا ستفهم بعد القراءة |
| **شرح مبسط** | لغة يومية + تشبيه |
| **أمثلة من المشروع** | ملفات ومسارات حقيقية |
| **ملخص / جداول** | مراجعة سريعة |
| **أسئلة / تمرين** | تثبيت بدون كسر المشروع |

---

## ⚠️ تنبيه مهم — قبل أي تعديل على الكود

```
┌─────────────────────────────────────────────────────────┐
│  1. خذ نسخة احتياطية (ZIP أو git commit)               │
│  2. npm run dev — تأكد أن الموقع يعمل قبل التعديل       │
│  3. عدّل شيئًا واحدًا فقط                               │
│  4. راجع diff في Cursor — لا Keep All بدون قراءة       │
│  5. اختبر — الفصل 19 — قبل رفع Hostinger               │
└─────────────────────────────────────────────────────────┘
```

**لا ترفع** `config.php` أو `.env` إلى Git — انظر الفصل **17**.

---

## 📚 فهرس الفصول (1–20)

| # | الفصل | الموضوع |
|---|--------|---------|
| **1** | الفصل الأول | التشغيل: index.html → main.tsx → App.tsx |
| **2** | الفصل الثاني | بنية المجلدات و package.json |
| **3** | الفصل الثالث | App.tsx — portal, state, components |
| **4** | الفصل الرابع | appData, data.json, MySQL |
| **5** | الفصل الخامس | ContentAdmin, auth, حفظ |
| **6** | الفصل السادس | PHP, Hostinger, schema |
| **7** | الفصل السابع | React, props, state |
| **8** | الفصل الثامن | index.css, responsive |
| **9** | الفصل التاسع | translations, ML, pickML |
| **10** | الفصل العاشر | public, Drive, uploads |
| **11** | الفصل الحادي عشر | Console, 404, 500 |
| **12** | الفصل الثاني عشر | قسم جديد — نظري |
| **13** | الفصل الثالث عشر | build:hostinger, SFTP |
| **14** | الفصل الرابع عشر | قاموس مصطلحات |
| **15** | الفصل الخامس عشر | خطة 30 يومًا |
| **16** | الفصل السادس عشر | تعديل آمن, Review |
| **17** | الفصل السابع عشر | Git, backup |
| **18** | الفصل الثامن عشر | أوامر Cursor |
| **19** | الفصل التاسع عشر | checklist اختبار |
| **20** | الفصل العشرون | ContactSection — تدريب |

### قائمة الفصول (نصية)

1. كيف يعمل المشروع من لحظة فتح الموقع؟  
2. شرح بنية المجلدات والملفات  
3. شرح App.tsx — قلب المشروع  
4. شرح البيانات والمحتوى  
5. شرح لوحة التحكم  
6. شرح السيرفر و Hostinger و PHP و MySQL  
7. شرح React و Components  
8. شرح التصميم و CSS  
9. شرح الترجمة واللغات  
10. شرح الصور والملفات والمسارات  
11. الأخطاء الشائعة وطريقة إصلاحها  
12. كيف أضيف قسمًا جديدًا؟  
13. كيف أنشر على Hostinger؟  
14. قاموس مصطلحات برمجية  
15. خطة تعلم 30 يومًا  
16. كيف أعدّل المشروع بأمان؟  
17. Git والنسخ الاحتياطي  
18. كيف أطلب من Cursor تعديلات صحيحة؟  
19. كيف أختبر الموقع قبل النشر؟  
20. مشروع تدريبي — قسم «تواصل معنا»  

---

## 📎 فهرس مرجعي موسّع (موضوعات مستقبلية)

> الفصول **1–20** أعلاه هي **محتوى الكتاب الكامل**. الجداول التالية **خريطة** لموضوعات إضافية — **لم تُكتب كفصول منفصلة** — للرجوع عند التوسع.

| المجال | أمثلة موضوعات |
|--------|----------------|
| **أقسام الزائر** | home, agri, graphics, software, cv — تفصيل كل portal |
| **لوحة التحكم** | CvDocEditor, FileExplorerAdmin, Analytics GPS |
| **ميزات متقدمة** | AI, PDF, Three.js, mediaUrl |
| **تطبيق عملي** | إضافة كتاب، مشروع gfx، خريطة «أي ملف أعدّل؟» |

---

## 📎 ملاحق هذا الكتاب (في نهاية الملف)

| ملحق | المحتوى |
|------|---------|
| **خاتمة** | ماذا أصبحت تفهم؟ |
| **أ** | أوامر جاهزة لـ Cursor |
| **ب** | قائمة الملفات المهمة |
| **ج** | checklist قبل أي تعديل |
| **د** | checklist قبل النشر على Hostinger |

---

## 📋 مراجعة بنية الفصول (1–20)

> كل فصل في هذا الكتاب يتبع **معيارًا موحّدًا**: هدف → شرح → أمثلة من المشروع → (تشبيه) → ملخص → أسئلة/تمرين. الجدول يلخّص **محتوى كل فصل** للمراجعة السريعة.

| # | الفصل | تشبيه / محور | ملخص | أسئلة |
|---|--------|--------------|------|-------|
| 1 | التشغيل | مسرحية | ✅ | ✅ |
| 2 | المجلدات | خريطة البيت | ✅ | ✅ |
| 3 | App.tsx | غرف + portal | ✅ | ✅ |
| 4 | البيانات | مخزن + دفتر | ✅ | ✅ |
| 5 | لوحة التحكم | مكتب المدير | ✅ | ✅ |
| 6 | Hostinger/PHP | مطبخ خلفي | ✅ | ✅ |
| 7 | React | Lego | ✅ | ✅ |
| 8 | CSS | ملابس | ✅ | ✅ |
| 9 | الترجمة | 3 نسخ كتاب | ✅ | ✅ |
| 10 | الصور | عنوان على باب | ✅ | ✅ |
| 11 | الأخطاء | إنذار السيارة | ✅ | ✅ |
| 12 | قسم جديد | مخطط بناء | ✅ | ✅ |
| 13 | النشر | رحلة + حقائب | ✅ | ✅ |
| 14 | قاموس | معجم | ✅ | ✅ |
| 15 | 30 يوم | خطة دراسة | ✅ | ✅ |
| 16 | تعديل آمن | مقبض الباب | ✅ | ✅ |
| 17 | Git | نقاط حفظ | ✅ | ✅ |
| 18 | Cursor | عامل صيانة | ✅ | ✅ |
| 19 | الاختبار | تجربة سيارة | ✅ | ✅ |
| 20 | Contact | غرفة جديدة | ✅ | ✅ |

---

## محتوى الكتاب — ابدأ من هنا

---

## الفصل الأول: كيف يعمل المشروع من لحظة فتح الموقع؟

> **هدف الفصل:** أن تفهم ماذا يحدث من اللحظة التي تكتب فيها `eng-alaa.com` في المتصفح حتى ترى الصفحة الرئيسية.

---

### 1. شرح بسيط

تخيّل أن الموقع **مسرحية**:

1. **`index.html`** = **المسرح الفارغ** — فيه خشبة واحدة فقط (`<div id="root">`) ينتظر عليها الممثلون.
2. **`main.tsx`** = **مدير المسرح** — يجلب الممثلين (React) ويقول لهم: «اصعدوا على الخشبة».
3. **`App.tsx`** = **المسرحية كلها** — كل المشاهد: الرئيسية، الزراعة، التصاميم، لوحة التحكم…

عندما تفتح الموقع، المتصفح يقرأ `index.html` أولًا، ثم يشغّل `main.tsx`، ثم `App.tsx` يرسم كل ما تراه.

---

### 2. أسماء الملفات المتعلقة بهذا الفصل

| الملف | دوره في هذه القصة |
|-------|-------------------|
| `index.html` | الباب — أول ما يفتحه المتصفح |
| `src/main.tsx` | المفتاح — يشغّل React |
| `src/App.tsx` | القلب — كل واجهة الموقع |
| `src/index.css` | ملابس الموقع — الألوان والخطوط |
| `src/print.css` | ملابس خاصة عند طباعة PDF |
| `src/translations.ts` | كتاب النصوص بثلاث لغات |
| `src/appData.ts` | خزانة البيانات — يُحمَّل داخل App |
| `package.json` | قائمة الأدوات (`npm run dev`) |
| `vite.config.ts` | إعدادات أداة التشغيل Vite |

---

### 3. ماذا يفعل كل ملف؟

#### 📄 `index.html` — الباب الرئيسي

**المكان:** في جذر المشروع (بجانب `package.json`).

**ماذا يفعل؟**
- يخبر المتصفح: «هذه صفحة عربية من اليمين لليسار» (`lang="ar" dir="rtl"`).
- يضع **عنوان التبويب**: Eng. Alaa Almasri | Portfolio Hub.
- يحمّل **خط Tajawal** من Google (للعربية الجميلة).
- يحمّل **مكتبات خارجية** من الإنترنت:
  - **Three.js** — للخلفية المتحركة (الشبكة/النجوم).
  - **jsPDF + html2canvas + html2pdf** — لتصدير السيرة والتقارير PDF.
- يترك **مكانًا فارغًا** `<div id="root"></div>` — هنا سترسم React كل شيء.
- في النهاية يشغّل: `<script type="module" src="/src/main.tsx"></script>`.

**سطر مهم:**
```html
<div id="root"></div>
```
بدون هذا الصندوق، React لا يعرف أين يرسم!

**سطر مهم آخر:**
```html
<script type="module" src="/src/main.tsx"></script>
```
هذا يقول: «بعد ما تجهّز الصفحة، اذهب شغّل main.tsx».

---

#### 📄 `src/main.tsx` — مدير التشغيل

**المكان:** داخل مجلد `src/`.

**الكود كامل (7 أسطر فقط!):**

```tsx
import { createRoot } from "react-dom/client";
import App from "./App";
import "@fortawesome/fontawesome-free/css/all.min.css";
import "./index.css";
import "./print.css";

createRoot(document.getElementById("root")!).render(<App />);
```

**ماذا يفعل كل سطر؟**

| السطر | المعنى |
|-------|--------|
| `import { createRoot }` | يجلب أداة React لوضع التطبيق في الصفحة |
| `import App from "./App"` | يجلب المكوّن الرئيسي — المسرحية كلها |
| `import ... fontawesome` | يجلب ملف الأيقونات (🔍 📁 ⭐…) |
| `import "./index.css"` | يجلب تنسيق الموقع العادي |
| `import "./print.css"` | يجلب تنسيق الطباعة/PDF |
| `createRoot(...).render(<App />)` | يجد `#root` ويرسم `<App />` داخله |

**`<App />` ماذا تعني؟**
- في React، `<App />` = «اعرض مكوّن App».
- App هو function في `App.tsx` ترجع HTML (JSX).

---

#### 📄 `src/App.tsx` — قلب الموقع

**المكان:** `src/App.tsx` — **أكبر ملف في المشروع** (آلاف الأسطر).

**ماذا يفعل باختصار؟**
1. **يستورد** عشرات الملفات (ترجمة، بيانات، مكوّنات فرعية).
2. **يحفظ حالة** (State) — مثل: أي لغة؟ أي قسم مفتوح؟ هل المدير مسجّل؟
3. **يحمّل البيانات** عند فتح الموقع (localStorage → MySQL → data.json).
4. **يرسم** الهيدر، القائمة، المحتوى حسب القسم، الفooter.
5. **يستجيب** للنقرات: تغيير لغة، فتح الزراعة، فتح التصاميم…

##### أ) الاستيراد (Imports) — أعلى الملف

```tsx
import { useEffect, useState, ... } from "react";
import { translations } from "./translations";
import { loadAppData, saveAppData, ... } from "./appData";
import { ContentAdmin } from "./ContentAdmin";
import { PlantDiagnostic } from "./PlantDiagnostic";
// ... وأكثر من 40 import
```

**معناها:** App لا يعمل وحده — يطلب مساعدة من ملفات أخرى.

##### ب) نوع `Portal` — أي «غرفة» مفتوحة؟

```tsx
type Portal =
  | "home"      // الرئيسية
  | "about"     // عن المهندس / السيرة
  | "agri"      // الزراعة
  | "graphics"  // التصاميم
  | "software"  // البرمجة
  | "cv"        // السيرة PDF
  | "admin";    // لوحة التحكم
```

المتغير `portal` يحدد **أي قسم يظهر** الآن.

##### ج) دالة `App()` — البداية

```tsx
export default function App() {
  const [lang, setLang] = useState<LangCode>(detectLang);
  const [portal, setPortal] = useState<Portal>("home");
  const [data, setData] = useState<AppData>(loadAppData);
  const [adminLoggedIn, setAdminLoggedIn] = useState(false);
  // ... عشرات المتغيرات الأخرى
```

**شرح للمبتدئ:**

| المصطلح | معنى بسيط |
|---------|-----------|
| `useState` | «ذاكرة» للصفحة — تتذكر اللغة والقسم المفتوح |
| `lang` | اللغة الحالية: ar / en / de |
| `portal` | القسم الحالي: home / agri / graphics… |
| `data` | **كل محتوى الموقع** — كتب، مقالات، تصاميم، مهارات… |
| `adminLoggedIn` | هل المدير دخل لوحة التحكم؟ |

##### د) تحميل البيانات عند الفتح — `useEffect`

```tsx
useEffect(() => {
  (async () => {
    let merged = loadAppData();           // 1) من localStorage
    const dbData = await loadAppDataFromDb(); // 2) من MySQL
    if (dbData) merged = { ...merged, ...dbData };
    const jsonPatch = await loadSiteContentFromJson(); // 3) من data.json
    if (jsonPatch) merged = mergeSiteContent(merged, jsonPatch);
    setData(applyDefaultCatalog(merged));
  })();
}, []);
```

**ترتيب التحميل (مبسّط):**

```
localStorage (المتصفح)
       ↓
MySQL عبر data.php (السيرفر)
       ↓
data.json (ملف على السيرفر)
       ↓
setData → الشاشة تتحدّث بالمحتوى
```

`useEffect(..., [])` = «نفّذ هذا **مرة واحدة** عند فتح الموقع».

##### هـ) دوال مهمة

```tsx
const openPortal = (p: Portal) => {
  setPortal(p);
  window.scrollTo({ top: 0, behavior: "smooth" });
};

const goHome = () => {
  setPortal("home");
  setActiveSkill(null);
};
```

- **`openPortal("agri")`** → ينقلك لقسم الزراعة.
- **`goHome()`** → يرجعك للرئيسية.

##### و) الرسم (Return) — JSX

في نهاية `App.tsx` يوجد `return (...)` ضخم فيه:
- خلفية Three.js (`canvasRef`)
- الهيدر (الاسم، اللغة، القائمة)
- **محتوى يتغير** حسب `portal`:
  - `{portal === "home" && (...)}`
  - `{portal === "agri" && (...)}`
  - `{portal === "graphics" && (...)}`
  - `{portal === "admin" && adminLoggedIn && (...)}`
- شريط تنقل سفلي للجوال
- Footer

---

### 4. كيف يرتبط الملفات ببعضها؟

```
┌─────────────────────────────────────────────────────────┐
│                    المتصفح (Chrome…)                     │
└─────────────────────────┬───────────────────────────────┘
                          │ يطلب index.html
                          ▼
┌─────────────────────────────────────────────────────────┐
│  index.html                                              │
│  • خطوط + Three.js + jsPDF                               │
│  • <div id="root">                                       │
│  • <script src="/src/main.tsx">                          │
└─────────────────────────┬───────────────────────────────┘
                          │ Vite يترجم main.tsx
                          ▼
┌─────────────────────────────────────────────────────────┐
│  src/main.tsx                                            │
│  • يحمّل index.css + print.css + أيقونات                 │
│  • createRoot(#root).render(<App />)                     │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  src/App.tsx                                             │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────┐ │
│  │translations │  │  appData.ts  │  │ ContentAdmin…   │ │
│  │  (نصوص)     │  │  (بيانات)    │  │ (مكوّنات)       │ │
│  └─────────────┘  └──────────────┘  └─────────────────┘ │
│                          │                               │
│              portal يختار أي قسم يُعرض                   │
└─────────────────────────────────────────────────────────┘
```

**قاعدة ذهبية:**
- `index.html` = هيكل فارغ + مكتبات
- `main.tsx` = تشغيل React
- `App.tsx` = العقل + العرض + ربط كل شيء

---

### 5. مثال من الحياة اليومية 🏠

تخيّل **مول تجاري**:

| جزء المول | ملف المشروع |
|-----------|-------------|
| مبنى المول الفارغ + الكهرباء + المصاعد | `index.html` |
| مدير المول الذي يفتح الأبواب ويشغّل الإضاءة | `main.tsx` |
| كل المحلات + اللوحات + موظف الاستقبال | `App.tsx` |
| `portal = "home"` | بهو الاستقبال |
| `portal = "agri"` | قسم الزراعة |
| `portal = "graphics"` | قسم التصاميم |
| `data` | مخزن البضاعة في كل محل |
| `lang` | لغة اللوحات (عربي/EN/DE) |

عندما تدخل المول (`index.html`)، المدير يشغّل كل شيء (`main.tsx`)، ثم أنت تمشي لمحل تختاره (`portal`).

---

### 6. أخطاء شائعة ⚠️

| الخطأ | لماذا يحدث؟ | ماذا تفعل؟ |
|-------|-------------|------------|
| صفحة بيضاء فارغة | خطأ في `App.tsx` أو `main.tsx` | افتح Console في المتصفح (F12) واقرأ الخطأ الأحمر |
| `#root` not found | حذفت `<div id="root">` من index.html | أعده كما كان |
| الموقع لا يتحدّث بعد التعديل | لم تحفظ الملف أو لم تشغّل `npm run dev` | احفظ + شغّل dev server |
| تعدّل `index.html` ولا ترى فرقًا | React يرسم داخل `#root` — التعديل خارج root لا يظهر في المحتوى | عدّل `App.tsx` أو CSS |
| تبحث عن HTML لكل صفحة | React = **صفحة واحدة** يتغير محتواها | ابحث عن `portal ===` في App.tsx |
| تفتح `App.tsx` وتُرهق | الملف ضخم — طبيعي! | ابحث بـ Ctrl+F عن `portal === "agri"` مثلاً |
| `npm run dev` لا يعمل | لم تثبت المكتبات | شغّل `npm install` أولًا |

---

### 7. ملخص الفصل الأول 📝

1. **`index.html`** = أول ملف يقرأه المتصفح. فيه `#root` ومكتبات (Three.js, PDF).
2. **`main.tsx`** = يشغّل React ويرسم `<App />` داخل `#root`.
3. **`App.tsx`** = كل الموقع: اللغة، القسم (`portal`)، البيانات (`data`)، لوحة التحكم.
4. **`portal`** يحدد أي قسم تراه: home, agri, graphics, software, admin…
5. **`useEffect`** يحمّل البيانات مرة عند الفتح من localStorage + MySQL + data.json.
6. باقي الملفات (translations, appData, ContentAdmin…) **تُستدعى من App** — ليست نقطة البداية.

---

### 8. أسئلة تدريبية 🎯

#### أسئلة فهم (بدون كود)

1. ما أول ملف يفتحه المتصفح عند زيارة الموقع؟
2. ما وظيفة `<div id="root">`؟
3. كم سطرًا تقريبًا في `main.tsx`؟ لماذا هو قصير؟
4. ما الفرق بين `index.html` و `App.tsx`؟
5. ماذا يعني `portal = "graphics"`؟
6. من أين يأتي محتوى الكتب والمقالات عند فتح الموقع؟ (اذكر 3 مصادر)
7. لماذا نحمّل Three.js في `index.html` وليس في `main.tsx`؟
8. ما الذي يحدث عندما تضغط زر «الزراعة» في القائمة؟

#### أسئلة تطبيقية

9. افتح `index.html` في مشروعك وعدّد: كم `<script>` خارجي؟ ما اسم كل واحد؟
10. افتح `main.tsx` — ما الملفات الثلاثة التي يستوردها غير App؟
11. في `App.tsx` ابحث عن `export default function App` — في أي سطر؟
12. ابحث عن `portal === "home"` — ماذا يُعرض تحته؟
13. شغّل `npm run dev` وافتح http://localhost:5173 — هل `#root` موجود في Elements (F12)؟

#### سؤال تفكير 🧠

14. لو حذفنا `App.tsx` بالكامل ومابقى `main.tsx` — ماذا سيحدث؟ ولماذا؟

---

## الفصل الثاني: شرح بنية المجلدات والملفات في المشروع

> **هدف الفصل:** أن تعرف «أين يعيش» كل جزء من المشروع — أي مجلد، أي ملف، ولماذا موجود.

---

### 1. شرح بسيط

المشروع مثل **منزل كبير**:

- **الطابق الأرضي (جذر المشروع)** = ملفات التشغيل: `package.json`, `index.html`, `vite.config.ts`
- **غرفة العمل (`src/`)** = كل كود React — الواجهة التي يراها الزائر
- **المخزن (`public/`)** = ملفات تُنسخ كما هي للموقع (صور، `data.json`…)
- **غرفة السيرفر (`hostinger_deploy/`)** = PHP وقاعدة البيانات — تعمل على Hostinger
- **ورشة التعبئة (`scripts/`)** = أدوات تجهّز الملفات للرفع

في هذا الفصل نفتح كل «غرفة» ونشرح محتوياتها.

---

### 2. شرح كل عنصر بالتفصيل

---

#### 📦 `package.json`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | في **جذر المشروع** — أول مجلد تفتحه، بجانب `index.html` |
| **ما وظيفته؟** | **بطاقة هوية المشروع** + **قائمة الأوامر** + **قائمة المكتبات** |
| **لماذا هو مهم؟** | بدونه لا تعرف كيف تشغّل المشروع أو ما المكتبات المثبتة |
| **من يستخدمه؟** | أنت (Terminal) عبر `npm` — و Vite عند البناء |
| **لو حُذف؟** | `npm run dev` و `npm install` **لن يعملا** — المشروع «يتيتم» |
| **مثال حياتي** | **قائمة مشتريات + وصفة** — فيها اسم الطبخة، المكونات، وخطوات «شغّل الفرن» |

**أهم أجزاء الملف:**

```json
"scripts": {
  "dev": "vite ...",              ← تشغيل محلي
  "build": "vite build ...",      ← بناء للإنتاج
  "build:hostinger": "...",       ← بناء + تجهيز Hostinger
  "typecheck": "tsc ..."          ← فحص أخطاء TypeScript
}
```

- **`dependencies`** = مكتبات يحتاجها الموقع عند التشغيل (مثل jsPDF).
- **`devDependencies`** = أدوات للتطوير فقط (React, Vite, Tailwind…).

---

#### 📄 `index.html`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | **جذر المشروع** (ليس داخل `src/`) |
| **ما وظيفته؟** | أول صفحة HTML يفتحها المتصفح — فيها `#root` ومكتبات CDN |
| **لماذا هو مهم؟** | بدون `#root` لا مكان لـ React — بدون Three.js/jsPDF بعض الميزات تتعطل |
| **من يستدعيه؟** | المتصفح مباشرة — Vite يحقنه بـ `main.tsx` |
| **لو حُذف؟** | الموقع **لا يفتح** — Vite يبحث عنه كنقطة دخول |
| **مثال حياتي** | **باب المنزل** — فيه عنوان البيت، الإضاءة، ومكان ينتظر فيه الضيوف |

*(شرح تفصيلي لمحتواه في الفصل الأول.)*

---

#### ⚙️ `vite.config.ts`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | **جذر المشروع** |
| **ما وظيفته؟** | **إعدادات Vite** — أداة التشغيل والبناء |
| **لماذا هو مهم؟** | يحدد المنفذ 5173، مجلد الإخراج `dist/`، و**بروكسي PHP** |
| **من يستخدمه؟** | Vite عند `npm run dev` و `npm run build` |
| **لو حُذف؟** | Vite يعمل بإعدادات افتراضية — **لكن** البروكسي لـ `/api` قد يتوقف محليًا |
| **مثال حياتي** | **لوحة تحكم المصنع** — سرعة الخط، أين تُخزَّن البضاعة، أين تُرسل الطلبات |

**ماذا يفعل في مشروعنا؟**

1. **`plugins: [react(), tailwindcss()]`** — يفعّل React و Tailwind.
2. **`build.outDir: "dist"`** — ناتج البناء يذهب لمجلد `dist/`.
3. **`server.proxy`** — عند التطوير المحلي، طلبات `/api/*` تُوجَّه لـ `eng-alaa.com` لأن **Vite لا يشغّل PHP**.

```
localhost:5173/api/data.php  →  eng-alaa.com/api/data.php
```

4. **`alias: { "@": "src" }`** — يمكنك كتابة `@/components/...` بدل مسارات طويلة.

---

#### 📁 `src/` — مجلد الكود الأمامي

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | داخل جذر المشروع — **`src/`** |
| **ما وظيفته؟** | **كل كود الواجهة** — React + TypeScript + CSS |
| **لماذا هو مهم؟** | هنا يُكتب 99% مما يراه الزائر ومدير الموقع |
| **من يستخدمه؟** | Vite يترجمه ويُدخله في المتصفح |
| **لو حُذف؟** | **لا موقع** — `main.tsx` و `App.tsx` يختفيان |
| **مثال حياتي** | **مطبخ المطعم** — كل الأطباق تُحضَّر هنا |

**أهم ما بداخله (بالإضافة للملفات المطلوبة):**

| ملف/مجلد | دور مختصر |
|----------|-----------|
| `ContentAdmin.tsx` | لوحة تحكم المحتوى |
| `AnalyticsDashboard.tsx` | إحصائيات الزوار |
| `PlantDiagnostic.tsx` | تشخيص النبات |
| `defaultCatalog.ts` | بيانات افتراضية أول مرة |
| `analytics.ts` | إرسال زيارات للسيرفر |
| `components/ui/` | أزرار وجداول جاهزة (shadcn) |
| `lib/utils.ts` | دوال مساعدة صغيرة |

---

#### 📄 `src/main.tsx`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `src/main.tsx` |
| **ما وظيفته؟** | **يشغّل React** ويرسم `<App />` داخل `#root` |
| **لماذا هو مهم؟** | الجسر بين `index.html` و `App.tsx` |
| **من يستدعيه؟** | `index.html` عبر `<script src="/src/main.tsx">` |
| **لو حُذف؟** | صفحة **بيضاء** — React لا يبدأ |
| **مثال حياتي** | **زر التشغيل** في جهاز التلفزيون |

*(7 أسطر فقط — لكنها نقطة لا غنى عنها.)*

---

#### 📄 `src/App.tsx`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `src/App.tsx` — **أكبر ملف** في المشروع |
| **ما وظيفته؟** | **العقل المركزي** — كل الأقسام، اللغة، البيانات، لوحة التحكم |
| **لماذا هو مهم؟** | يقرر ماذا ترى: رئيسية؟ زراعة؟ تصاميم؟ admin؟ |
| **من يستدعيه؟** | `main.tsx` |
| **لو حُذف؟** | الموقع **لا يعمل** — `main.tsx` يستورد ملفًا غير موجود |
| **مثال حياتي** | **مدير المطعم** — يوجّهك للطاولة، يختار القائمة، يستدعي الطباخين (ملفات أخرى) |

**يستورد من:** `appData.ts`, `translations.ts`, `ContentAdmin.tsx`, `analytics.ts`… وعشرات الملفات.

---

#### 📄 `src/appData.ts`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `src/appData.ts` — ملف **طويل جدًا** (~2200+ سطر) |
| **ما وظيفته؟** | **تعريف أنواع البيانات** + **حفظ/تحميل** محتوى الموقع |
| **لماذا هو مهم؟** | كل الكتب، المقالات، التصاميم، المهارات، إعدادات الموقع — هنا شكلها وقواعدها |
| **من يستخدمه؟** | `App.tsx`, `ContentAdmin.tsx`, وملفات admin أخرى |
| **لو حُذف؟** | **انهيار** — لا types، لا حفظ، لا اتصال بـ `data.php` |
| **مثال حياتي** | **دفتر مخزون + سجل محاسبة** — يحدد شكل كل صنف وكيف يُخزَّن |

**أهم مفاهيم:**

| مفهوم | معنى |
|-------|------|
| `ML` | نص بثلاث لغات `{ ar, en, de }` |
| `AppData` | **كل** محتوى الموقع في كائن واحد |
| `loadAppData()` | يقرأ من `localStorage` في المتصفح |
| `loadAppDataFromDb()` | يطلب `GET ./api/data.php` |
| `loadSiteContentFromJson()` | يقرأ `public/data.json` |
| `saveAppDataToDb()` | يحفظ في MySQL عبر `POST ./api/data.php` |

**ترتيب الدمج عند الفتح (في App.tsx):**
```
localStorage → MySQL → data.json → الشاشة
```

---

#### 📄 `src/translations.ts`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `src/translations.ts` |
| **ما وظيفته؟** | **نصوص الواجهة الثابتة** بثلاث لغات (عربي، إنجليزي، ألماني) |
| **لماذا هو مهم؟** | أزرار، عناوين، رسائل — ما يظهر لكل زائر حسب `lang` |
| **من يستخدمه؟** | `App.tsx` ومعظم المكوّنات عبر `translations[lang].xxx` |
| **لو حُذف؟** | آلاف النصوص **تختفي** — أخطاء في كل مكان |
| **مثال حياتي** | **كتيب ترجمة فورية** للموظفين — «زر الرجوع» = backHome |

**الفرق بين `translations.ts` و `appData.ts`:**

| | `translations.ts` | `appData.ts` |
|--|-----------------|--------------|
| المحتوى | نصوص **ثابتة** للواجهة | **محتوى الموقع** (كتب، مقالات، تصاميم…) |
| من يعدّله؟ | المبرمج | المدير من لوحة التحكم + data.json |
| مثال | `adminLoginBtn: 'التحقق والدخول'` | عنوان مقال زراعي، صورة مشروع تصميم |

---

#### 📊 `src/index.css`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `src/index.css` — ملف **ضخم** (~5000 سطر) |
| **ما وظيفته؟** | **كل التنسيق البصري** — ألوان، بطاقات، قوائم، responsive |
| **لماذا هو مهم؟** | بدونه الموقع «HTML عاري» بدون جمال |
| **من يستدعيه؟** | `main.tsx` → `import "./index.css"` |
| **لو حُذف؟** | الموقع يعمل **لكن** شكله ينهار — Tailwind + CSS مخصص يختفي |
| **مثال حياتي** | **ديكور المطعم** — الألوان، الإضاءة، ترتيب الطاولات |

**ملاحظة:** يبدأ بـ `@import "tailwindcss"` ثم متغيرات مثل `--navy`, `--glass` — نظام تصميم موحّد.

---

#### 📁 `public/` — الملفات العامة

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | مجلد **`public/`** في جذر المشروع |
| **ما وظيفته؟** | ملفات **تُنسخ كما هي** عند البناء — بدون معالجة |
| **لماذا هو مهم؟** | روابط ثابتة مثل `/favicon.svg`, `/data.json` |
| **من يستخدمه؟** | Vite ينسخه إلى `dist/` — المتصفح يطلبه مباشرة |
| **لو حُذف؟** | الصور والـ JSON **لا تُرفع** مع Build |
| **مثال حياتي** | **واجهة المحل** — اللوحة الخارجية والملصقات التي لا تتغير يوميًا |

**أهم محتوياته:**

| ملف | الدور |
|-----|-------|
| `favicon.svg` | أيقونة التبويب |
| `LogoAlaa.svg` | شعار |
| `data.json` | محتوى الكتب/المقالات/التصاميم (انظر أدناه) |
| `content-manager.html` | أداة CSV لإدارة data.json |
| `content-manager-templates/` | قوالب CSV جاهزة |
| `robots.txt` | تعليمات محركات البحث |

---

#### 📄 `public/data.json`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `public/data.json` — على السيرفر: `public_html/data.json` |
| **ما وظيفته؟** | **محتوى ثابت** للمكتبة والمقالات والتصاميم — **بدون إعادة Build** |
| **لماذا هو مهم؟** | ترفع الملف فقط عبر SFTP ويتحدّث الموقع (Ctrl+F5) |
| **من يستخدمه؟** | `loadSiteContentFromJson()` في `appData.ts` |
| **لو حُذف؟** | الموقع يعتمد على localStorage + MySQL فقط — لا «طبقة JSON» |
| **مثال حياتي** | **منيو مطبوع** على الحائط — تغيّره بدون إعادة بناء المطبخ |

**هيكله (لا تغيّر أسماء الأقسام الجذرية):**
```json
{
  "library": { "tree": [...], "books": [...], "view": "tree" },
  "articles": { "categories": [...], "items": [...] },
  "designs": { "categories": [...] }
}
```

**أولوية الدمج:** `data.json` **يُطبَّق فوق** localStorage عند التحميل.

---

#### 📁 `hostinger_deploy/` — ملفات السيرفر

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | مجلد **`hostinger_deploy/`** في جذر المشروع |
| **ما وظيفته؟** | **كل ما يعمل على Hostinger** — PHP، PDF، vendor، تقارير |
| **لماذا هو مهم؟** | React وحده **لا يكفي** — نحتاج PHP + MySQL على السيرفر |
| **من يستخدمه؟** | `prepare-hostinger.mjs` ينسخه إلى `hostinger_upload/` |
| **لو حُذف؟** | **لا API** — لا تسجيل دخول، لا حفظ DB، لا PDF سيرفر |
| **مثال حياتي** | **المستودع الخلفي + غرفة الخوادم** — الزبائن لا يرونها لكن بدونها لا خدمة |

**أهم محتوياته:**

| ملف/مجلد | الدور |
|----------|-------|
| `api/` | نقاط API (data, auth, analytics…) |
| `schema.sql` | هيكل قاعدة MySQL |
| `vendor/` | مكتبات PHP (dompdf, ar-php…) |
| `ai_proxy.php` | وسيط الذكاء الاصطناعي |
| `generate_report.php` | توليد تقارير PDF |
| `reports/` | مجلد حفظ التقارير المُولَّدة |

---

#### 📁 `hostinger_deploy/api/` — واجهة برمجة PHP

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `hostinger_deploy/api/` |
| **ما وظيفته؟** | **جسر** بين React (المتصفح) و MySQL (السيرفر) |
| **لماذا هو مهم؟** | المتصفح **لا يتصل** بـ MySQL مباشرة — PHP يفعل ذلك |
| **من يستدعيه؟** | `fetch('./api/...')` من `appData.ts`, `analytics.ts`… |
| **لو حُذف؟** | الموقع «عرض فقط» من localStorage — **لا مزامنة سحابية** |
| **مثال حياتي** | **موظف الاستقبال** — يستقبل طلبك ويسأل المخزن (MySQL) |

**ملفات مهمة إضافية:** `config.php` (بيانات DB — **لا يُرفع من Git**), `middleware.php` (JWT), `upload.php` (رفع ملفات).

---

#### 📄 `hostinger_deploy/api/data.php`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `hostinger_deploy/api/data.php` |
| **ما وظيفته؟** | **قراءة وحفظ** كل محتوى الموقع في MySQL |
| **لماذا هو مهم؟** | يجعل التعديلات من لوحة التحكم **دائمة** على السيرفر |
| **من يستدعيه؟** | `loadAppDataFromDb()`, `saveAppDataToDb()` في `appData.ts` |
| **لو حُذف؟** | **لا حفظ في قاعدة البيانات** — فقدان بيانات عند مسح المتصفح |
| **مثال حياتي** | **خزنة البنك** — تسحب رصيدك (GET) أو تودع (POST) |

**العمليات:**

| الطلب | ماذا يفعل |
|-------|-----------|
| `GET /api/data.php` | يُرجع كل AppData من الجداول |
| `GET /api/data.php?test` | يختبر اتصال DB (تشخيص) |
| `POST /api/data.php` | يحفظ AppData — **يتطلب JWT** (مدير مسجّل) |

---

#### 📄 `hostinger_deploy/api/auth.php`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `hostinger_deploy/api/auth.php` |
| **ما وظيفته؟** | **تسجيل دخول** المدير وإصدار **JWT token** |
| **لماذا هو مهم؟** | بدون token لا يمكن حفظ البيانات على السيرفر |
| **من يستدعيه؟** | `App.tsx` / `appData.ts` عند دخول لوحة التحكم |
| **لو حُذف؟** | **لا دخول admin** على السيرفر — حفظ DB مستحيل |
| **مثال حياتي** | **بطاقة الدخول** للمبنى — تثبت هويتك وتفتح الأبواب |

**العمليات:**

| الطلب | النتيجة |
|-------|---------|
| `POST { username, password }` | `{ token, role, displayName }` |
| `DELETE` + Bearer token | تسجيل خروج |

---

#### 📄 `hostinger_deploy/api/analytics.php`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `hostinger_deploy/api/analytics.php` |
| **ما وظيفته؟** | **تتبع الزوار** + **إحصائيات** للمدير (GPS، تنزيلات CV…) |
| **لماذا هو مهم؟** | تعرف من زار الموقع، من أين، وماذا حمّل |
| **من يستدعيه؟** | `analytics.ts` (زائر) + `AnalyticsDashboard.tsx` (مدير) |
| **لو حُذف؟** | **لا إحصائيات** — لوحة Analytics فارغة |
| **مثال حياتي** | **كاميرا + عدّاد** على باب المحل — تسجّل من دخل وماذا اشترى |

**العمليات:**

| الطلب | من | ماذا |
|-------|-----|------|
| `POST { action:"track", ... }` | أي زائر | يسجّل زيارة / heartbeat / تنزيل |
| `GET ?action=stats` | مدير + JWT | يُرجع جداول وإحصائيات |
| حذف انتقائي | مدير | يحذف جلسات/أحداث محددة |

---

#### 📄 `hostinger_deploy/schema.sql`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `hostinger_deploy/schema.sql` |
| **ما وظيفته؟** | **مخطط قاعدة البيانات** — CREATE TABLE لكل جدول |
| **لماذا هو مهم؟** | بدون جداول، `data.php` لا يجد أين يحفظ |
| **من يستخدمه؟** | **أنت** — تشغّله مرة في phpMyAdmin على Hostinger |
| **لو حُذف؟** | لا يؤثر على الموقع **الشغّال** — لكن صعب إعادة بناء DB من الصفر |
| **مثال حياتي** | **مخطط بناء المستودع** — أرفف، صفوف، أبواب |

**أهم الجداول:**

| جدول | المحتوى |
|------|---------|
| `admin_users` | حسابات المدير |
| `site_settings` | إعدادات الموقع العامة |
| `agri_books`, `agri_articles` | محتوى زراعي |
| `gfx_projects`, `gfx_categories` | التصاميم |
| `analytics_sessions`, `analytics_events` | إحصائيات الزوار |

---

#### 📁 `scripts/` — سكربتات مساعدة

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | مجلد **`scripts/`** في جذر المشروع |
| **ما وظيفته؟** | أدوات **لا تُشغَّل في المتصفح** — تجهيز، نشر، أتمتة |
| **لماذا هو مهم؟** | يسهّل رفع الموقع على Hostinger بخطوة واحدة |
| **من يستخدمه؟** | أنت عبر `npm run build:hostinger` |
| **لو حُذف؟** | `build:hostinger` **يفشل** — تنسخ يدويًا |
| **مثال حياتي** | **خط التعبئة** في المصنع — يجهّز الصندوق للشحن |

---

#### 📄 `scripts/prepare-hostinger.mjs`

| السؤال | الجواب |
|--------|--------|
| **أين مكانه؟** | `scripts/prepare-hostinger.mjs` |
| **ما وظيفته؟** | يجمع **`dist/`** + **`hostinger_deploy/`** → **`hostinger_upload/`** |
| **لماذا هو مهم؟** | مجلد واحد جاهز للرفع إلى `public_html/` |
| **من يستدعيه؟** | `npm run build:hostinger` (بعد `vite build`) |
| **لو حُذف؟** | تبني `dist/` لكن **بدون PHP** — موقع ناقص |
| **مثال حياتي** | **موظف الشحن** — يضع المنتج + الفاتورة + دليل الاستخدام في صندوق واحد |

**ماذا يفعل خطوة بخطوة:**

```
1. يتحقق أن dist/ موجود (بعد vite build)
2. يحذف hostinger_upload/ القديم
3. ينسخ hostinger_deploy/ → hostinger_upload/
   (ويستثني api/config.php — ترفعه يدويًا على السيرفر)
4. ينسخ dist/ → hostinger_upload/ (React المبني)
5. ينشئ مجلد reports/
6. يطبع: "ارفع محتويات hostinger_upload/ إلى public_html/"
```

---

### 3. ملخص بسيط جدًا 📝

1. **جذر المشروع** = تشغيل (`package.json`, `vite.config.ts`, `index.html`).
2. **`src/`** = واجهة React — `App.tsx` العقل، `appData.ts` البيانات، `translations.ts` النصوص، `index.css` الشكل.
3. **`public/`** = ملفات ثابتة — أهمها **`data.json`** للمحتوى بدون Build.
4. **`hostinger_deploy/`** = PHP + MySQL على السيرفر — `data.php` حفظ، `auth.php` دخول، `analytics.php` إحصائيات.
5. **`scripts/prepare-hostinger.mjs`** = يجهّز **`hostinger_upload/`** للرفع.

---

### 4. جدول يربط كل ملف بوظيفته

| الملف / المجلد | الوظيفة بجملة واحدة |
|----------------|---------------------|
| `package.json` | أوامر npm + قائمة المكتبات |
| `index.html` | باب المتصفح + `#root` + مكتبات CDN |
| `vite.config.ts` | إعدادات Vite + بروكسي PHP |
| `src/` | كل كود الواجهة (React) |
| `src/main.tsx` | تشغيل React |
| `src/App.tsx` | كل أقسام الموقع ولوحة التحكم |
| `src/appData.ts` | أنواع البيانات + حفظ/تحميل |
| `src/translations.ts` | نصوص الواجهة (ar/en/de) |
| `src/index.css` | التنسيق والألوان |
| `public/` | ملفات تُنسخ كما هي للموقع |
| `public/data.json` | محتوى JSON بدون إعادة Build |
| `hostinger_deploy/` | PHP + PDF + vendor للسيرفر |
| `hostinger_deploy/api/` | نقاط API |
| `hostinger_deploy/api/data.php` | قراءة/حفظ MySQL |
| `hostinger_deploy/api/auth.php` | تسجيل دخول + JWT |
| `hostinger_deploy/api/analytics.php` | تتبع زوار + إحصائيات |
| `hostinger_deploy/schema.sql` | مخطط جداول MySQL |
| `scripts/` | أدوات مساعدة للنشر |
| `scripts/prepare-hostinger.mjs` | تجميع `hostinger_upload/` |

---

### 5. رسمة نصية — علاقة المجلدات

```
                    ┌─────────────────────────────────────┐
                    │         جذر المشروع (Root)          │
                    │  package.json  vite.config.ts       │
                    │  index.html                         │
                    └──────────┬──────────────────────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
   ┌───────────┐        ┌────────────┐       ┌──────────────────┐
   │   src/    │        │  public/   │       │ hostinger_deploy/│
   │           │        │            │       │                  │
   │ main.tsx──┼──►     │ data.json  │       │  api/            │
   │ App.tsx   │        │ favicon    │       │   ├ data.php     │
   │ appData.ts│◄───────┤ content-   │       │   ├ auth.php     │
   │ translat..│ fetch  │ manager    │       │   └ analytics.php│
   │ index.css │        └────────────┘       │  schema.sql      │
   └─────┬─────┘                             │  vendor/ PDF…    │
         │                                   └────────┬─────────┘
         │ fetch ./api/*                              │
         └──────────────────┬─────────────────────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │  Hostinger      │
                   │  public_html/   │
                   │  (موقع حي)      │
                   └─────────────────┘
                            ▲
                            │ npm run build:hostinger
                   ┌────────┴────────┐
                   │    scripts/     │
                   │ prepare-hostinger│
                   │       ↓         │
                   │ hostinger_upload/│
                   └─────────────────┘
```

---

### 6. أسئلة تدريبية للمبتدئ 🎯

#### أسئلة فهم

1. أي ملف تكتب فيه `npm run dev`؟
2. ما الفرق بين `src/` و `public/`؟
3. لماذا `vite.config.ts` يوجّه `/api` إلى eng-alaa.com محليًا؟
4. أين تُحفظ نصوص الأزرار (مثل «العودة للرئيسية»)؟
5. أين تُحفظ عناوين المقالات الزراعية؟
6. ما الفرق بين `data.json` و `data.php`؟
7. ماذا يفعل `auth.php`؟
8. لماذا لا نرفع `api/config.php` من Git تلقائيًا؟
9. ما مجلد الناتج بعد `npm run build:hostinger`؟
10. ماذا يحدث لو حذفت `appData.ts`؟

#### أسئلة تطبيقية

11. افتح `package.json` — ما الأمر الذي يبني ويجهّز Hostinger معًا؟
12. افتح `vite.config.ts` — على أي منفذ يعمل dev server؟
13. في `public/` — هل `data.json` موجود؟ إن لم يكن، أين يُنشأ عادة؟
14. عدّ ملفات `hostinger_deploy/api/` — كم ملف PHP (بدون config)؟
15. شغّل `npm run build:hostinger` (إن أمكن) — هل ظهر مجلد `hostinger_upload/`؟

#### سؤال تفكير 🧠

16. زائر يفتح الموقع لأول مرة بدون إنترنت للسيرفر — ماذا يرى؟ (فكّر: localStorage vs API)

---

> **الفصل التالي (3):** «شرح ملف App.tsx — قلب المشروع» — *(هذا الفصل أدناه)*

---

## الفصل الثالث: شرح ملف App.tsx — قلب المشروع

> **هدف الفصل:** أن تفهم `App.tsx` من الداخل — ليس كملف «ضخم ومخيف»، بل كـ **مدير** ينسّق كل شيء في الموقع.

> **حجم الملف:** أكثر من **3600 سطر** — لا تتوقع قراءته دفعة واحدة. استخدم Ctrl+F وابحث عن `portal ===` أو اسم أي دالة.

---

### 1. ما هو ملف App.tsx؟

#### 1.1 تعريف بسيط

`App.tsx` هو ملف TypeScript + React يحتوي على **مكوّن واحد ضخم** اسمه `App`.

هذا المكوّن:
- يرسم **كل** ما يراه الزائر والمدير.
- يحفظ **حالة** الموقع (أي قسم مفتوح؟ أي لغة؟ هل المدير داخل؟).
- يربط **البيانات** بالواجهة.
- يستدعي **عشرات المكوّنات** الأصغر (مثل `PlantDiagnostic`, `ContentAdmin`…).

#### 1.2 لماذا يعتبر قلب المشروع؟

تخيّل جسم الإنسان:

| الجزء | ملف المشروع |
|-------|-------------|
| القلب | `App.tsx` |
| الدم (البيانات) | `appData.ts` + MySQL + data.json |
| العظام (الهيكل) | `index.html` + `main.tsx` |
| الأعضاء (مهام متخصصة) | `ContentAdmin`, `PlantDiagnostic`… |

**القلب** لا يفعل كل شيء بنفسه — لكن **بدونه لا حياة**. كل الأقسام، كل الأزرار، كل تحميل بيانات — **يمر أو يُنسَّق** من `App.tsx`.

#### 1.3 لماذا معظم أجزاء الموقع تمر من خلاله؟

لأن المشروع مبني كـ **Single Page Application (SPA)** — **صفحة واحدة** يتغير محتواها:

- لا يوجد `agri.html` و `graphics.html` منفصلين.
- يوجد **ملف واحد** `App.tsx` يقول:
  - إذا `portal === "agri"` → اعرض الزراعة.
  - إذا `portal === "graphics"` → اعرض التصاميم.
  - وهكذا…

**كل** المكوّنات الفرعية تُستدعى **من داخل** `App.tsx` — إما في قسم الزائر أو في لوحة التحكم.

#### 1.4 ما الفرق بينه وبين main.tsx؟

| | `main.tsx` | `App.tsx` |
|--|-----------|-----------|
| **الحجم** | ~7 أسطر | ~3600+ سطر |
| **الدور** | **يشغّل** React | **يُشغّل** الموقع |
| **التشبيه** | زر التشغيل | المسرحية كاملة |
| **هل يتغير كثيرًا؟** | نادرًا | كثيرًا عند إضافة ميزات |
| **هل يحتوي state؟** | لا | نعم — عشرات المتغيرات |
| **هل يستورد مكوّنات؟** | فقط `App` | أكثر من 40 import |

**قاعدة:** `main.tsx` = «ابدأ العرض». `App.tsx` = «ماذا نعرض وكيف».

---

### 2. كيف يبدأ App.tsx بالعمل؟

#### 2.1 من يستدعيه؟

**`main.tsx`** — السطر الأخير:

```tsx
createRoot(document.getElementById("root")!).render(<App />);
```

هذا يعني: «خذ مكوّن App وارسمه داخل `#root`».

#### 2.2 كيف يصل من main.tsx إلى الشاشة؟

```
index.html
    │
    │  المتصفح يقرأ HTML ويجد #root
    ▼
main.tsx
    │
    │  createRoot(...).render(<App />)
    ▼
App.tsx
    │
    │  export default function App() { ... return (...) }
    ▼
React يحوّل JSX إلى HTML حقيقي داخل #root
    │
    ▼
الزائر يرى: navbar + hero + أقسام + footer
```

#### 2.3 كيف يظهر الموقع للزائر؟ (خطوة بخطوة)

1. **البداية الفورية:** `useState(loadAppData)` — يعرض **فورًا** ما في `localStorage` (لا انتظار).
2. **بعد جزء من الثانية:** `useEffect` يحمّل من MySQL و `data.json` ويحدّث الشاشة.
3. **الافتراضي:** `portal = "home"` — يرى الصفحة الرئيسية.
4. **الخلفية:** Three.js يعمل على `#three-canvas-wrapper`.
5. **اللغة:** `detectLang()` — من localStorage أو لغة المتصفح (افتراضي عربي).
6. **التتبع:** `trackPageView` يسجّل الزيارة (إلا إذا المدير مسجّل).

---

### 3. ما معنى Component (مكوّن)؟

#### 3.1 تشبيه Lego

**Component** = **قطعة Lego** جاهزة:
- لها شكل محدد.
- يمكنك **تركيبها** مع قطع أخرى.
- يمكنك **إعادة استخدامها** في أماكن مختلفة.

مثال Lego:
- قطعة «باب» → تستخدمها في كل غرفة.
- قطعة «نافذة» → نفس الشكل في المطبخ والصالة.

مثال React:
- `<PlantDiagnostic />` → قطعة «تشخيص نبات».
- `<CvRenderer />` → قطعة «عرض سيرة ذاتية».

#### 3.2 كيف يستخدم App.tsx المكوّنات الأخرى؟

**الخطوة 1 — Import (استيراد):**

```tsx
import { ContentAdmin } from "./ContentAdmin";
import { PlantDiagnostic } from "./PlantDiagnostic";
```

**الخطوة 2 — استخدام في JSX:**

```tsx
{portal === "agri" && activeKey === 'diag' && (
  <PlantDiagnostic data={data} lang={lang as LangKey} />
)}
```

**الخطوة 3 — تمرير Props (خصائص):**
- `data={data}` → «خذ كل محتوى الموقع».
- `lang={lang}` → «اعرض باللغة الحالية».
- `onSave={handleCvSave}` → «عند الحفظ، اتصل بهذه الدالة».

#### 3.3 أمثلة من المشروع

| المكوّن | أين يُستخدم في App.tsx | ماذا يفعل |
|---------|------------------------|-----------|
| **ContentAdmin** | لوحة التحكم — تبويبات زراعة/تصاميم/برمجة/إعدادات | إدارة المحتوى (كتب، مقالات، مشاريع…) |
| **AnalyticsDashboard** | لوحة التحكم — تبويب إحصائيات | عرض زوار، GPS، تنزيلات CV |
| **PlantDiagnostic** | قسم الزراعة — تبويب تشخيص | رفع صورة نبات + AI + PDF |
| **CvRenderer** | قسم about/cv + طباعة PDF | رسم صفحات السيرة A4 |
| **CvDocEditor** | لوحة التحكم — تبويب CV | تحرير صفحات السيرة |
| **SeasonNowPanel** | قسم الزراعة — «موسمك الآن» | نصائح موسمية |
| **SoilRequest** | قسم الزراعة — تحليل التربة | نموذج طلب تحليل |
| **FileExplorerAdmin** | لوحة التحكم — إدارة الملفات | رفع صور وملفات |
| **HeroNameDisplay** | الصفحة الرئيسية + about | عرض الاسم (نص/خط يد/شعار) |
| **AppPicker** | معرض التصاميم | قائمة منسدلة للفروع |
| **AlaaLogo** | Navbar | شعار SVG |
| **SkillIcon** | المهارات | أيقونة برنامج + نسبة |
| **BookCover** / **BookAccessRibbon** | مكتبة الكتب | غلاف الكتاب + شارة مجاني/مدفوع |

**ملاحظة للمبتدئ:** لا تخلط بين:
- **`Portal` (نوع في App.tsx)** = القسم الحالي (home, agri…).
- **`createPortal` (من react-dom)** = يرسم قائمة اللغة **فوق** كل شيء — اسم مختلف، وظيفة مختلفة!

---

### 4. ما معنى State و useState؟

#### 4.1 شرح بسيط جدًا

**State** = **ذاكرة** الصفحة.

المتصفح عادة «نسيان» — كل refresh يبدأ من الصفر.  
React + `useState` يجعل الصفحة **تتذكر**:
- أي قسم فتحت؟
- أي لغة اخترت؟
- هل المدير دخل؟

#### 4.2 تشبيه

**جهاز التلفزيون:**
- `useState` = **آخر قناة** شاهدتها.
- عندما تضغط «زر» → القناة تتغير **بدون** إطفاء التلفزيون وإعادة تشغيله.

**في الموقع:**
- تضغط «الزراعة» → `setPortal("agri")` → الشاشة تتغير **فورًا** بدون reload.

#### 4.3 الصيغة

```tsx
const [portal, setPortal] = useState<Portal>("home");
```

| الجزء | المعنى |
|-------|--------|
| `portal` | **القيمة الحالية** — مثل: `"home"` |
| `setPortal` | **دالة التغيير** — مثل: `setPortal("agri")` |
| `useState("home")` | **القيمة الابتدائية** عند أول فتح |

**قاعدة React:** عندما تستدعي `setPortal(...)` → React **يعيد رسم** الجزء المتأثر تلقائيًا.

#### 4.4 أمثلة State مهمة في App.tsx

##### أ) `portal` — القسم الحالي

```tsx
const [portal, setPortal] = useState<Portal>("home");
```

- **القيم الممكنة:** `"home" | "about" | "agri" | "graphics" | "software" | "cv" | "admin"`
- **الاستخدام:** `{portal === "agri" && (...)}` — يقرر ماذا يُعرض.

##### ب) `lang` — اللغة

```tsx
const [lang, setLang] = useState<LangCode>(detectLang);
const t = translations[lang];  // كل النصوص الجاهزة
```

- عند `switchLang("en")` → كل `{t.xxx}` يتحول للإنجليزي.
- يُحفظ في localStorage عبر `saveLang(l)`.

##### ج) `data` — كل محتوى الموقع

```tsx
const [data, setData] = useState<AppData>(loadAppData);
```

- **أهم state في المشروع.**
- فيه: الاسم، المهارات، الكتب، المقالات، التصاميم، إعدادات الموقع…
- عند التعديل: `setData(updated)` ثم غالبًا `saveAppData(updated)`.

##### د) `adminLoggedIn` — هل المدير داخل؟

```tsx
const [adminLoggedIn, setAdminLoggedIn] = useState(false);
```

- `false` → الزائر يرى الموقع العادي فقط.
- `true` + `portal === "admin"` → لوحة التحكم الكاملة.
- عند `true` → **يتوقف** تتبع الزوار (`trackPageView` لا يعمل للمدير).

##### هـ) state فرعية للأقسام

| State | الغرض |
|-------|-------|
| `agriTab` | أي تبويب في الزراعة (تشخيص، موسم، كتب…) |
| `gfxBrowseView` | عرض الكل vs حسب التصنيف |
| `articlePage` | مقال مفتوح بصفحة كاملة |
| `webProjectPage` | مشروع برمجي مفتوح |
| `adminPanel` | أي تبويب في لوحة التحكم (0–9) |
| `bookPreview` | نافذة معاينة كتاب |

**فكرة:** `portal` = **أي غرفة**. State الفرعية = **أي رف داخل الغرفة**.

---

### 5. ما معنى portal في هذا المشروع؟

#### 5.1 لماذا يستخدم المشروع portal؟

بدل **7 صفحات HTML** منفصلة، نستخدم **متغير واحد** يحدد «أين أنا الآن».

**المزايا:**
- انتقال **سريع** بين الأقسام (بدون reload).
- **Navbar واحد** يبقى ثابتًا.
- **البيانات** (`data`) مشتركة بين كل الأقسام.

#### 5.2 تشبيه: أزرار تنقل بين غرف بيت واحد

```
┌─────────────────────────────────────────────────┐
│                    البيت (App.tsx)               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐ │
│  │  home   │ │  agri   │ │ graphics│ │ admin  │ │
│  │ الرئيسية│ │ الزراعة │ │ التصاميم│ │ تحكم   │ │
│  └─────────┘ └─────────┘ └─────────┘ └────────┘ │
│       ↑           ↑           ↑           ↑      │
│   setPortal   setPortal   setPortal   setPortal  │
└─────────────────────────────────────────────────┘
```

- **البيت واحد** — لا تغيّر عنوان URL (تقريبًا).
- **الغرفة** تتغير حسب `portal`.
- **الأثاث المشترك** (data, lang, navbar) يبقى في كل الغرف.

#### 5.3 كيف يقرر App.tsx ماذا يعرض؟

**تعريف النوع:**

```tsx
type Portal =
  | "home"      // الرئيسية
  | "about"     // السيرة / عن المهندس
  | "agri"      // الهندسة الزراعية
  | "graphics"  // التصاميم
  | "software"  // البرمجة
  | "cv"        // محرك CV
  | "admin";    // لوحة التحكم
```

**في JSX — شروط العرض:**

| portal | ماذا يظهر |
|--------|-----------|
| `"home"` | Hero، الاسم، المهارات، بطاقات الأقسام الثلاث |
| `"about"` | صورة، سيرة، أزرار تنزيل CV |
| `"agri"` | تبويبات: تشخيص، موسم، كتب، مقالات، تربة |
| `"graphics"` | معرض التصاميم، بحث، تصنيفات |
| `"software"` | مشاريع + مختبر أكواد |
| `"cv"` | محرك السيرة التفاعلي |
| `"admin"` | **فقط** إذا `adminLoggedIn === true` |

**دوال التنقل:**

```tsx
const openPortal = (p: Portal) => {
  setPortal(p);
  window.scrollTo({ top: 0, behavior: "smooth" });
};

const goHome = () => {
  setPortal("home");
  setActiveSkill(null);
};
```

**من أين تُستدعى؟**
- أزرار Navbar: `onClick={() => openPortal("about")}`
- بطاقات الرئيسية: `onClick={() => openPortal("agri")}`
- Bottom nav (جوال): `onClick={() => openPortal(item.key)}`

---

### 6. كيف يتم تحميل البيانات داخل App.tsx؟

#### 6.1 العلاقة مع الملفات الأخرى

```
App.tsx
   │
   ├── loadAppData()          ← appData.ts ← localStorage
   ├── loadAppDataFromDb()    ← appData.ts ← fetch ./api/data.php ← MySQL
   ├── loadSiteContentFromJson() ← appData.ts ← fetch /data.json
   └── applyDefaultCatalog()  ← appData.ts ← defaultCatalog.ts
```

#### 6.2 الكود الفعلي (مرة واحدة عند الفتح)

```tsx
useEffect(() => {
  (async () => {
    let merged = loadAppData();                    // ① localStorage
    const dbData = await loadAppDataFromDb();    // ② MySQL
    const dbLoaded = !!dbData;
    if (dbData) merged = { ...merged, ...dbData };
    const jsonPatch = await loadSiteContentFromJson();  // ③ data.json
    if (jsonPatch) {
      if (dbLoaded && jsonPatch.gfxCategories) {
        const { gfxCategories: _skipGfx, ...rest } = jsonPatch;
        merged = mergeSiteContent(merged, rest);
      } else {
        merged = mergeSiteContent(merged, jsonPatch);
      }
    }
    merged = { ...merged, skills: normalizeSkillIconList(merged.skills || []) };
    setData(applyDefaultCatalog(merged));        // ④ تحديث الشاشة
  })().catch(() => { /* stay with localStorage */ });
}, []);
```

#### 6.3 من أين تأتي البيانات أول مرة؟

**الترتيب الزمني للزائر:**

| اللحظة | المصدر | ماذا يرى |
|--------|--------|----------|
| **0 ms** | `loadAppData()` → localStorage | آخر نسخة حفظها المتصفح (أو افتراضي) |
| **~100–8000 ms** | `data.php` → MySQL | بيانات السيرفر (إن وُجدت و `is_seeded=1`) |
| **بعدها** | `data.json` | يدمج كتب/مقالات/تصاميم |

#### 6.4 إذا كان السيرفر متاحًا

1. `GET ./api/data.php` ينجح.
2. إذا `dbIsSeeded === true` → بيانات MySQL **تُدمج** فوق localStorage.
3. ثم `data.json` يُدمج (مع استثناء: إن DB محمّل، **لا** نستبدل `gfxCategories` من JSON القديم).
4. `setData(...)` → الشاشة **تتحدّث** تلقائيًا.

#### 6.5 إذا لم يكن السيرفر متاحًا

1. `loadAppDataFromDb()` يرجع `null` (timeout 8 ثوانٍ أو خطأ شبكة).
2. الكود **لا يتوقف** — `.catch()` يلتقط الخطأ.
3. يبقى على **localStorage** + ما نجح من `data.json`.
4. الموقع **يعمل** — لكن بدون مزامنة سحابية.

#### 6.6 متى تُستخدم البيانات الافتراضية؟

| الحالة | المصدر |
|--------|--------|
| أول زيارة + localStorage فارغ | `DEFAULT_DATA` من `defaultCatalog.ts` |
| localStorage تالف | `applyDefaultCatalog` يصلح ويكمل |
| DB غير مُهيّأ (`is_seeded=0`) | **تُتجاهل** DB — localStorage يبقى المصدر |

#### 6.7 كيف تصل البيانات للأقسام؟

**كل الأقسام تقرأ من `data`:**

```tsx
// الزراعة — الكتب
data.libraryTree
data.agriBooks

// التصاميم
data.gfxCategories

// البرمجة
data.webProjects
data.softwareSnippets

// الرئيسية
data.name
data.bio
data.skills
```

**لا** يوجد fetch منفصل في كل قسم — **App.tsx** يحمّل مرة و**يوزّع** عبر `data={data}`.

---

### 7. كيف يتم حفظ التعديلات؟

#### 7.1 المسار الكامل

```
┌─────────────────────────────────────────────────────────────┐
│  ContentAdmin.tsx (أو CvDocEditor أو saveGlobalBio…)        │
│  المدير يعدّل كتابًا / مقالًا / إعدادات…                     │
│  يضغط «حفظ»                                                  │
└──────────────────────────┬──────────────────────────────────┘
                           │ onSave(partial) أو saveAppData
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  App.tsx — handleCvSave / saveGlobalBio / saveSkills…        │
│  setData(updated)  ← تحديث State فورًا (الشاشة تتغير)        │
└──────────────────────────┬──────────────────────────────────┘
                           │ saveAppData(updated)
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  appData.ts — saveAppData()                                  │
│  ① localStorage.setItem(...)  ← دائمًا                       │
│  ② إن وُجد JWT token → POST ./api/data.php                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
     localStorage (المتصفح)      data.php → MySQL
```

**ملاحظة:** `data.json` **لا يُحدَّث تلقائيًا** من لوحة التحكم — يُعدَّل يدويًا أو عبر `content-manager.html` ويُرفع SFTP.

#### 7.2 دالة الحفظ المركزية في App.tsx

```tsx
function handleCvSave(partial: Partial<AppData>): Promise<boolean> {
  return new Promise(resolve => {
    setData(prev => {
      const updated = { ...prev, ...partial };
      void saveAppData(updated).then(resolve);
      return updated;
    });
  });
}
```

**ماذا يحدث خطوة بخطوة عند «حفظ» في ContentAdmin؟**

1. **ContentAdmin** يجمع التعديلات في كائن `partial` (مثلاً `{ agriBooks: [...] }`).
2. يستدعي `onSave(partial)` → **`handleCvSave`** في App.
3. **`setData`** يدمج التعديل مع `data` الحالي → **الواجهة تتحدّث فورًا**.
4. **`saveAppData`**:
   - يكتب في **localStorage** (حتى بدون سيرفر).
   - إن المدير **متصل بالسيرفر** (`getApiToken()` موجود) → **POST** إلى `data.php` → **MySQL**.
5. يرجع `true/false` — ContentAdmin قد يُظهر «تم الحفظ» أو «فشل المزامنة».

#### 7.3 الاتصال بالسيرفر (منفصل عن دخول لوحة التحكم)

| الخطوة | الدالة | ماذا تفعل |
|--------|--------|-----------|
| دخول admin محلي | `handleAdminLogin()` | يتحقق من ADMIN_EMAIL/PASS → `adminLoggedIn=true` |
| ربط MySQL | `handleServerConnect(user, pass)` | `loginToApi()` → `auth.php` → JWT |
| مزامنة يدوية | `handleServerSync()` | `saveAppData(data)` |
| قطع الاتصال | `handleServerDisconnect()` | `clearApiToken()` |

**مهم:** يمكنك الدخول للوحة التحكم **محليًا** بدون MySQL — لكن **الحفظ على السيرفر** يحتاج JWT من `auth.php`.

---

### 8. كيف يتحكم App.tsx بالواجهة؟

#### 8.1 كيف يقرر ماذا يظهر وماذا يخفي؟

**الشرط `&&` في JSX:**

```tsx
{portal === "home" && (
  <main className="hero">...</main>
)}

{portal === "admin" && adminLoggedIn && (
  <div className="admin-layout">...</div>
)}
```

- إذا الشرط **false** → React **لا يرسم** هذا الجزء (يختفي من DOM).
- **لا** حاجة لـ `display: none` — المكوّن غير موجود أصلًا.

#### 8.2 كيف يعرض الأقسام؟

كل قسم = **كتلة JSX كبيرة** داخل `return (...)`:

```
return (
  <div>
    <nav>...</nav>                    ← دائمًا ظاهر

    {portal === "home" && (...)}      ← قسم 1
    {portal === "about" && (...)}     ← قسم 2
    {portal === "agri" && (...)}      ← قسم 3
    ...

    <nav className="bottom-nav">      ← جوال — دائم
    {adminGate && (...)}              ← نافذة دخول — عند الحاجة
    {bookPreview && (...)}            ← modal كتاب — عند الحاجة
  </div>
);
```

#### 8.3 كيف يربط القائمة بالمحتوى؟

**Desktop — Navbar:**
```tsx
<button onClick={() => openPortal("about")}>{t.aboutNavBtn}</button>
```

**Mobile — Bottom nav:**
```tsx
onClick={() => item.key === 'home' ? goHome() : openPortal(item.key as Portal)}
className={`bottom-nav-item${portal === item.key ? ' active' : ''}`}
```

**Admin sidebar:**
```tsx
onClick={() => setAdminPanel(i)}
className={`admin-panel-btn${adminPanel === i ? " active" : ""}`}
```

**Agri tabs:**
```tsx
onClick={() => setAgriTab(i)}
// ثم activeKey يحدد أي مكوّن يُعرض
```

#### 8.4 كيف يغيّر المحتوى بدون إعادة تحميل الصفحة؟

**React SPA:**
1. حدث (click) → `setPortal("agri")`.
2. React يعيد حساب JSX.
3. `{portal === "home" && ...}` يصبح false → يُزال من DOM.
4. `{portal === "agri" && ...}` يصبح true → يُضاف للDOM.
5. **نفس** `#root` — **لا** طلب جديد لـ `index.html`.

**هذا أسرع** من موقع تقليدي متعدد الصفحات.

---

### 9. أهم الدوال والمتغيرات — شرح واحد واحد

#### 9.1 متغيرات أساسية

| الاسم | الوظيفة | لماذا نحتاجها | أين تُستخدم |
|-------|---------|---------------|-------------|
| `lang` | اللغة الحالية | ترجمة الواجهة | `t = translations[lang]`, كل `pickML(..., lang)` |
| `portal` | القسم المفتوح | التنقل | `{portal === "..." && ...}` |
| `data` | كل محتوى الموقع | المصدر الوحيد للعرض | كل الأقسام + admin |
| `t` | نصوص الواجهة | اختصار | `{t.backHome}`, `{t.adminDashTitle}` |
| `theme` | dark/light | ألوان وخلفية | `data-theme`, Three.js, CSS vars |
| `adminLoggedIn` | جلسة المدير | حماية admin | `{portal === "admin" && adminLoggedIn && ...}` |
| `adminPanel` | تبويب لوحة التحكم | 0=بيانات، 3=زراعة… | `{adminPanel === 3 && <ContentAdmin mode="agri" />}` |
| `isMobileView` | هل جوال؟ | تخطيط responsive | grids، bottom nav |
| `serverConnected` | JWT موجود؟ | مزامنة MySQL | ContentAdmin mode="site" |

#### 9.2 دوال التنقل

| الدالة | الوظيفة | مثال |
|--------|---------|------|
| `openPortal(p)` | ينقل لقسم + scroll للأعلى | `openPortal("graphics")` |
| `goHome()` | رئيسية + إلغاء skill filter | زر الشعار |
| `switchLang(l)` | تغيير لغة + حفظ | `switchLang("de")` |

#### 9.3 دوال Admin والحفظ

| الدالة | الوظيفة |
|--------|---------|
| `handleAdminLogin()` | تحقق محلي → `adminLoggedIn=true` → `openPortal("admin")` |
| `handleServerConnect()` | `loginToApi` → JWT → `saveAppData` |
| `handleCvSave(partial)` | **الحفظ المركزي** — `setData` + `saveAppData` |
| `saveGlobalBio()` | حفظ الاسم والنبذة من تبويب 0 |
| `saveSkills()` | حفظ المهارات من تبويب 1 |
| `saveArticleInline()` | حفظ مقال معدّل من صفحة المقال |
| `injectPage()` | إضافة صفحة HTML محقونة |

#### 9.4 دوال CV و PDF

| الدالة | الوظيفة |
|--------|---------|
| `requestVisitorCv(doc)` | يفتح نافذة اختيار لغة CV |
| `printCvDoc(doc, lang)` | يجهّز CvRenderer مخفي → PDF → `trackCvDownload` |
| `mountCvForPrint(...)` | ينتظر حتى صفحات A4 جاهزة |

#### 9.5 useEffect المهمة

| useEffect | متى | ماذا |
|-----------|-----|------|
| `[]` (فارغ) | مرة عند الفتح | تحميل localStorage + DB + data.json |
| `[portal, lang]` | تغيير قسم/لغة | `trackPageView` |
| `[adminLoggedIn]` | — | heartbeat كل 60 ثانية |
| `[theme]` | تغيير ثيم | لون خلفية body + Three.js |

#### 9.6 Refs (مراجع DOM)

| Ref | الغرض |
|-----|-------|
| `canvasRef` | حاوية Three.js |
| `langDdRef` | موقع قائمة اللغة |
| `cvPrintMountRef` | حاوية طباعة CV مخفية |
| `previewFrame` | iframe معاينة مختبر الأكواد |

---

### 10. مخطط نصي — العلاقة الكاملة

```
index.html
    │  #root + CDN (Three, jsPDF)
    ▼
main.tsx
    │  createRoot().render(<App />)
    ▼
┌───────────────────────────────────────────────────────────────────┐
│                         App.tsx                                    │
│  ┌─────────────┐   ┌──────────────┐   ┌─────────────────────────┐ │
│  │ State       │   │ useEffect    │   │ return (JSX)            │ │
│  │ portal      │   │ load data    │   │ navbar                  │ │
│  │ lang        │   │ analytics    │   │ {portal==="home"&&...}  │ │
│  │ data        │   │ theme/bg     │   │ {portal==="agri"&&...}  │ │
│  │ admin...    │   └──────┬───────┘   │ {portal==="admin"&&...} │ │
│  └─────────────┘          │           └───────────┬─────────────┘ │
└───────────────────────────┼───────────────────────┼───────────────┘
                            │                       │
         ┌──────────────────┼───────────────────────┼──────────────┐
         ▼                  ▼                       ▼              ▼
   localStorage        data.php              data.json      Components
   (فوري)              (MySQL)               (SFTP)         PlantDiagnostic
         │                  │                       │         ContentAdmin
         └──────────────────┴───────────────────────┘         CvRenderer
                            │                                 AnalyticsDashboard
                            ▼
                    setData(merged)
                            │
                            ▼
              ┌─────────────────────────┐
              │  عرض للزائر / المدير   │
              └────────────┬────────────┘
                           │ حفظ
                           ▼
              handleCvSave → saveAppData
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
        localStorage              POST data.php → MySQL
```

---

### 11. تشبيه: App.tsx = مدير فندق / مول تجاري

#### 11.1 استقبال الزائر

- **البوابة** = `index.html` + `main.tsx` (يفتح الفندق).
- **موظف الاستقبال** = `App.tsx` — يرحّب، يحدد اللغة (`lang`)، يعطي **مفتاح الغرفة** (`portal`).

#### 11.2 فتح القسم المطلوب

- الزائر يقول: «أريد قسم الزراعة» → `openPortal("agri")`.
- المدير **لا يبني غرفة جديدة** — الغرف **جاهزة** في JSX، يفتح الباب فقط (`portal === "agri"`).

#### 11.3 جلب البيانات

- **المخزن** = `data` (من localStorage ثم السيرفر).
- موظف الاستقبال يطلب: «أحضر قائمة الكتب» → `data.agriBooks`.
- **PlantDiagnostic** = طباخ متخصص — الاستقبال **يناديه** لكن لا يطبخ بنفسه.

#### 11.4 التعديلات والحفظ

- **المدير** يدخل غرفة خلفية (`admin` + `adminLoggedIn`).
- **ContentAdmin** = سكرتير يكتب التعديلات.
- عند «حفظ» → الاستقبال يرسل للمخزن (`saveAppData`) → **localStorage** + **خزنة السحابة** (MySQL).

#### 11.5 بدون إعادة بناء الفندق

- تغيير القسم = تغيير `portal` — **نفس المبنى**.
- تغيير اللغة = `setLang` — **نفس اللوحات** بترجمة مختلفة.
- **لا هدم ولا بناء** — فقط تبديل ما يُعرض.

---

### 12. نهاية الفصل

#### 12.1 ملخص بسيط جدًا

1. **`App.tsx`** = قلب الموقع — كل الأقسام والبيانات والتحكم.
2. **`main.tsx`** يشغّله فقط — الفرق: تشغيل vs إدارة.
3. **`portal`** = أي «غرفة» مفتوحة (home, agri, graphics…).
4. **`useState`** = ذاكرة الصفحة (لغة، قسم، بيانات…).
5. **`data`** = كل المحتوى — يُحمَّل من localStorage → MySQL → data.json.
6. **Components** = قطع Lego — App يجمعها.
7. **الحفظ:** ContentAdmin → `handleCvSave` → `saveAppData` → localStorage + MySQL.
8. **SPA:** تغيير المحتوى **بدون** reload — React يعيد الرسم.

#### 12.2 أهم 10 أشياء يجب أن تفهمها من App.tsx

1. الموقع **صفحة واحدة** — `portal` يختار المحتوى.
2. **`data`** مصدر الحقيقة للعرض — كل الأقسام تقرأ منه.
3. **`setData` + `saveAppData`** = تحديث الشاشة + حفظ.
4. **`handleCvSave`** = بوابة الحفظ من كل admin panels.
5. **تحميل البيانات** مرة واحدة في `useEffect([])`.
6. **`adminLoggedIn`** يفتح لوحة التحكم و**يوقف** تتبع الزوار.
7. **JWT** مطلوب لحفظ MySQL — من `auth.php` وليس من `handleAdminLogin` المحلي فقط.
8. **`translations[lang]`** للنصوص الثابتة — **`data`** للمحتوى المتغير.
9. **لا تخلط** `Portal` (قسم) مع `createPortal` (قائمة لغة).
10. الملف **كبير** — ابحث بـ Ctrl+F، لا تقرأ linear.

#### 12.3 أخطاء شائعة عند تعديل App.tsx

| الخطأ | النتيجة | الحل |
|-------|---------|------|
| تعديل `data` مباشرة بدون `setData` | الشاشة لا تتحدّث | استخدم `setData({...data, ...})` |
| نسيان `saveAppData` بعد التعديل | يضيع عند refresh | استدعِ `saveAppData` |
| إضافة `portal` جديد بدون JSX | زر لا يفعل شيئًا | أضف `{portal === "x" && (...)}` |
| حذف `&& adminLoggedIn` من admin | لوحة تحكم مكشوفة! | أعد الشرط |
| تعديل CSS داخل App | يخالف قواعد المشروع | عدّل `index.css` أو admin |
| copy-paste JSX ضخم | أخطاء أقواس | عدّل تدريجيًا + typecheck |
| خلط props خاطئة للمكوّن | crash | اقرأ تعريف المكوّن |

#### 12.4 ماذا يجب ألا ألمس كمبتدئ؟

- **`useThreeBackground`** — خلفية Three.js معقدة.
- **`printCvDoc` / `mountCvForPrint`** — سلسلة PDF حساسة.
- **`accentVars` / HSL parsing** — ألوان ديناميكية.
- **Drag-and-drop المهارات** — dnd-kit.
- **منطق دمج DB + data.json** في `useEffect` — افهم قبل التعديل.
- **أي منطق JWT / auth** — أمان.

#### 12.5 ماذا يمكنني تعديله بأمان؟

- **نصوص** في `translations.ts` (أفضل من App).
- **محتوى** من لوحة التحكم / `data.json`.
- **إضافة زر** يستدعي `openPortal` موجود.
- **تغيير ترتيب** عناصر داخل قسم **إذا** فهمت JSX.
- **تعليق مؤقت** `{false && (...)}` لإخفاء جزء أثناء التجربة.

#### 12.6 أسئلة تدريبية للمبتدئ

**فهم:**
1. ما الفرق بين `App.tsx` و `ContentAdmin.tsx`؟
2. ماذا يحدث عند `setPortal("graphics")`؟
3. من أين يأتي `data.agriBooks`؟
4. لماذا `handleCvSave` وليس `saveAppData` مباشرة من ContentAdmin؟
5. ما الفرق بين `handleAdminLogin` و `handleServerConnect`؟
6. متى يُتجاهل `data.json` لمعرض التصاميم؟
7. ما دور `adminPanel === 4`؟
8. لماذا `trackPageView` لا يعمل للمدير؟

**تطبيق:**
9. ابحث في App.tsx عن `{portal === "software"` — كم موضع؟
10. ابحث عن `<ContentAdmin` — كم mode مختلف؟
11. في أي سطر `export default function App`؟
12. ابحث عن `handleCvSave` — من يستدعيه؟

**تفكير:**
13. لو غيّرت `portal` الافتراضي من `"home"` إلى `"agri"` — ماذا يرى الزائر فورًا؟
14. لو حذفت `useEffect` تحميل البيانات — هل يبقى الموقع يعمل؟ بأي بيانات؟

#### 12.7 تمرين عملي (نظري — بدون تنفيذ)

**المطلوب:** إضافة قسم جديد اسمه `"contact"` (اتصل بنا) — **على الورق فقط**:

**الخطوة 1 — نوع Portal:**
```tsx
type Portal = ... | "contact";
```

**الخطوة 2 — State (اختياري):**
```tsx
// قد تحتاج حقول نموذج:
const [contactName, setContactName] = useState('');
```

**الخطوة 3 — JSX للقسم:**
```tsx
{portal === "contact" && (
  <main className="content-wrap">
    <h2>{lang === 'ar' ? 'اتصل بنا' : 'Contact'}</h2>
    {/* نموذج أو بيانات من data.personalInfo */}
  </main>
)}
```

**الخطوة 4 — زر تنقل:**
```tsx
<button onClick={() => openPortal("contact")}>اتصل بنا</button>
```

**الخطوة 5 — (اختياري) bottom-nav:** أضف عنصر `{ key: 'contact', ... }`.

**الخطوة 6 — translations:** أضف `contactTitle` في `translations.ts`.

**الخطوة 7 — اختبار:** `npm run dev` → اضغط الزر → تأكد `{portal === "contact"}` يظهر.

**لماذا لا ننفّذ الآن؟** لأن تعديل App.tsx يحتاج حذر — تعلّم البنية أولًا.

---

> **الفصل التالي (4):** «شرح البيانات والمحتوى» — *(هذا الفصل أدناه)*

---

## الفصل الرابع: شرح البيانات والمحتوى — من أين يأتي محتوى الموقع؟

> **هدف الفصل:** أن تفهم **المحتوى** (الكتب، المقالات، التصاميم، النصوص…) — من أين يأتي، أين يُحفظ، وكيف يصل للشاشة.

---

### 1. ما معنى «بيانات الموقع»؟

#### 1.1 تعريف بسيط

**بيانات الموقع** = كل ما **يمكن تغييره** بدون «برمجة» من جديد:
- اسم المهندس
- المقالات
- الكتب
- مشاريع التصاميم
- السيرة الذاتية
- الصور والروابط
- إعدادات الموقع (ألوان، شعار…)

**ليست بيانات:** الكود الذي يبني الصفحة (React, TypeScript, CSS structure).

#### 1.2 الفرق بين الكود والمحتوى

| | **الكود** | **المحتوى (البيانات)** |
|--|-----------|------------------------|
| **أين؟** | `src/App.tsx`, `index.css`… | `data`, `data.json`, MySQL |
| **من يعدّله؟** | المبرمج | المدير / أنت عبر لوحة التحكم |
| **مثال** | «اعرض بطاقة إذا portal=agri» | «عنوان المقال: زراعة الأنسجة» |
| **التغيير يحتاج Build؟** | غالبًا نعم | **لا** — لو عدّلت data.json فقط |

**تشبيه:** الكود = **قالب Word**. المحتوى = **النص** الذي تكتبه داخل القالب.

#### 1.3 الفرق بين التصميم والبيانات

| | **التصميم** | **البيانات** |
|--|-------------|--------------|
| **ماذا؟** | **كيف** يبدو الشيء | **ماذا** يُعرض |
| **أين؟** | `index.css`, classes | `AppData` |
| **مثال** | حجم البطاقة، لون الخلفية | عنوان الكتاب، صورة الغلاف |

**قاعدة المشروع:** لا تعدّل CSS إلا بطلب — **عدّل البيانات** من لوحة التحكم أو `data.json`.

#### 1.4 أمثلة من موقع eng-alaa.com

| نوع | أين في البيانات | مثال |
|-----|-----------------|------|
| **مقال** | `data.agriArticles[]` | عنوان + محتوى + صور + تاريخ |
| **مشروع تصميم** | `data.gfxCategories[].subCategories[].items[]` | عنوان، صور، glbUrl |
| **مشروع برمجي** | `data.webProjects[]` | عنوان، وصف، liveUrl |
| **سيرة ذاتية** | `data.cvDocs[]` | صفحات A4، أقسام، PDF |
| **صورة** | URL داخل حقل مثل `mainImg` | رابط Google Drive أو `/uploads/...` |
| **نص** | `ML` = `{ ar, en, de }` | «المهندس علاء…» |
| **قسم** | `data.articleCategories[]` | «زراعة مائية» |

---

### 2. من أين يأتي المحتوى عند فتح الموقع؟

#### 2.1 مصادر البيانات (5 مصادر)

```
① DEFAULT_DATA + defaultCatalog.ts   ← مدمج في الكود
② localStorage                      ← متصفح الزائر
③ data.php → MySQL                  ← سيرفر Hostinger
④ public/data.json                  ← ملف على السيرفر
⑤ applyDefaultCatalog()             ← يكمّل النواقص
```

#### 2.2 الترتيب الفعلي في App.tsx

**لحظة 0 (فورية):**
```tsx
const [data, setData] = useState<AppData>(loadAppData);
```
→ يقرأ **localStorage** فورًا (أو `DEFAULT_DATA` إن فارغ).

**لحظة 1 (بعد التحميل — useEffect مرة واحدة):**
```tsx
let merged = loadAppData();              // ① localStorage
const dbData = await loadAppDataFromDb(); // ② MySQL
if (dbData) merged = { ...merged, ...dbData };
const jsonPatch = await loadSiteContentFromJson(); // ③ data.json
if (jsonPatch) merged = mergeSiteContent(merged, jsonPatch);
setData(applyDefaultCatalog(merged));    // ④ defaultCatalog
```

#### 2.3 أي مصدر يُستخدم أولًا؟

| الترتيب | المصدر | ملاحظة |
|---------|--------|--------|
| **1** | localStorage | **أول ما تراه** — فوري |
| **2** | MySQL (عبر data.php) | **يُدمج فوق** localStorage إن `dbIsSeeded=1` |
| **3** | data.json | **يُدمج فوق** ما سبق (كتب/مقالات/تصاميم) |
| **4** | defaultCatalog | **يكمّل** تصنيفات فارغة وترجمات ناقصة |

**ليس** «أول ناجح يكفي» — بل **دمج طبقات** (layer merge).

#### 2.4 ماذا يحدث إذا فشل مصدر؟

| المصدر | إذا فشل | البديل |
|--------|---------|--------|
| localStorage فارغ | — | `DEFAULT_DATA` داخل appData.ts |
| localStorage تالف | catch | `DEFAULT_DATA` |
| data.php / MySQL | `return null` | يبقى localStorage |
| DB غير مُهيّأ (`dbIsSeeded=0`) | يُتجاهل DB | localStorage |
| data.json غير موجود | `return null` | لا تغيير من JSON |
| شبكة بطيئة | timeout 8 ثوانٍ | localStorage |

**نعم — يوجد fallback (بديل)** في كل طبقة.

#### 2.5 كيف «يعرف» الموقع أن يستخدم هذه البيانات؟

- **الكود مكتوب مسبقًا** في `App.tsx` و `appData.ts`.
- لا «ذكاء» — فقط: `fetch` → `if (ok)` → `merge` → `setData`.
- React يراقب `data` — أي تغيير → **إعادة رسم** الأقسام.

---

### 3. شرح ملف `src/appData.ts`

#### 3.1 ما وظيفة هذا الملف؟

**ملف البيانات المركزي** (~2266 سطر). يجمع:

1. **تعريف الشكل** — interfaces (ما شكل المقال؟ الكتاب؟)
2. **البيانات الافتراضية** — `DEFAULT_DATA`
3. **التحميل** — localStorage, MySQL, data.json
4. **الحفظ** — localStorage + POST data.php
5. **دوال مساعدة** — ترجمة ML، تطبيع JSON، رفع ملفات

#### 3.2 ما معنى TypeScript types / interfaces؟

**interface** = **نموذج فارغ** يصف شكل الشيء.

```tsx
export interface AgriArticle {
  id: string;
  categoryId: string;
  title: ML;        // { ar, en, de }
  content: ML;
  images: string[];
  date: string;
}
```

**لماذا؟**
- المحرر يُنبّهك إذا نسيت `id` أو كتبت `title: 123`.
- المشروع «يفهم» البيانات — أقل أخطاء.

**تشبيه:** interface = **استمارة رسمية** — فيها خانات إلزامية.

#### 3.3 أهم أنواع البيانات (interfaces)

| النوع | ماذا يمثل |
|-------|-----------|
| `ML` | نص ثلاثي اللغة `{ ar, en, de }` |
| `Skill` | مهارة + نسبة + أيقونة |
| `AgriBook` | كتاب في المكتبة |
| `AgriArticle` | مقال زراعي |
| `GfxCategory` | تصنيف تصاميم + فروع + مشاريع |
| `GfxProjectItem` | مشروع تصميم واحد |
| `WebProject` | مشروع برمجي |
| `CvDoc` | مستند سيرة A4 |
| `SiteSettings` | شعار، footer، theme |
| **`AppData`** | **كل** ما سبق في كائن واحد |

#### 3.4 كيف يستخدمه App.tsx؟

```tsx
import { loadAppData, saveAppData, AppData, pickML, ... } from "./appData";

const [data, setData] = useState<AppData>(loadAppData);

// عرض:
pickML(article.title, lang)

// حفظ:
saveAppData(updated);
```

**App.tsx لا يخزّن JSON خام** — يستخدم `AppData` جاهزًا.

#### 3.5 أهم الدوال والمتغيرات — واحدة واحدة

##### `ML` و `ml()` و `pickML()`

| | |
|--|--|
| **الوظيفة** | `ML` = نوع ثلاثي اللغة. `ml("عربي","EN","DE")` ينشئه. `pickML(obj, lang)` يختار النص للغة الحالية |
| **من يستدعيه؟** | App.tsx, ContentAdmin, كل العرض |
| **ماذا يُرجع؟** | `string` |
| **مثال حياتي** | قائمة طعام في 3 لغات — النادل يعطيك حسب لغتك |

##### `STORAGE_KEY` = `'alaa_portfolio_state'`

| | |
|--|--|
| **الوظيفة** | اسم المفتاح في localStorage |
| **من يستدعيه؟** | `loadAppData`, `saveAppData` |
| **مثال** | اسم درج في مكتبك |

##### `DEFAULT_DATA`

| | |
|--|--|
| **الوظيفة** | نسخة كاملة افتراضية من `AppData` — اسم، مهارات، نبذة… |
| **متى؟** | localStorage فارغ أو تالف |
| **مثال** | «عرض تجريبي» في متجر فارغ |

##### `loadAppData()`

| | |
|--|--|
| **الوظيفة** | يقرأ localStorage → ي parse JSON → ي normalizes → يرجع `AppData` |
| **من يستدعيه؟** | App.tsx (فوري + داخل useEffect) |
| **ماذا يُرجع؟** | `AppData` |
| **مثال** | فتح دفتر ملاحظاتك من الدرج |

##### `loadAppDataFromDb()`

| | |
|--|--|
| **الوظيفة** | `GET ./api/data.php` → دمج مع local → حفظ في localStorage |
| **من يستدعيه؟** | App.tsx useEffect |
| **ماذا يُرجع؟** | `AppData \| null` |
| **مهم** | يرجع `null` إذا `dbIsSeeded=0` |
| **مثال** | سحب نسخة محدّثة من السحابة |

##### `loadSiteContentFromJson()`

| | |
|--|--|
| **الوظيفة** | `fetch ./data.json` → `contentPatchFromJson` |
| **من يستدعيه؟** | App.tsx useEffect |
| **ماذا يُرجع؟** | `Partial<AppData> \| null` — **جزء** فقط |
| **مثال** | ملحق مطبوع يُلصق فوق الدفتر |

##### `mergeSiteContent(base, patch)`

| | |
|--|--|
| **الوظيفة** | يدمج **فقط**: library, books, articles, designs |
| **مثال** | تحديث فصل الكتب دون لمس السيرة |

##### `contentPatchFromJson(raw)`

| | |
|--|--|
| **الوظيفة** | يحوّل شكل `data.json` (library/articles/designs) → حقول AppData |
| **مثال** | مترجم من لغة JSON إلى لغة المشروع |

##### `applyDefaultCatalog(data)`

| | |
|--|--|
| **الوظيفة** | يستدعي `resolveArticleCategories` + `resolveGfxCategories` |
| **من يستدعيه؟** | بعد كل تحميل/حفظ |
| **مثال** | إذا رف فارغ — ضع تصنيفات افتراضية |

##### `saveAppData(data)`

| | |
|--|--|
| **الوظيفة** | ① localStorage دائمًا ② POST data.php إن JWT موجود |
| **من يستدعيه؟** | App.tsx, ContentAdmin عبر handleCvSave |
| **ماذا يُرجع؟** | `Promise<boolean>` — true = نجحت مزامنة السيرفر |
| **مثال** | حفظ في دفترك + إرسال نسخة للخزنة |

##### `loginToApi(username, password)`

| | |
|--|--|
| **الوظيفة** | POST auth.php → حفظ JWT في localStorage |
| **مثال** | بطاقة دخول المخزن |

##### `getApiToken()` / `setApiToken()` / `clearApiToken()`

| | |
|--|--|
| **الوظيفة** | إدارة JWT في localStorage (`__admin_jwt__`) |
| **مثال** | مفتاح خزنة |

##### `uploadMediaFile(file, folder)`

| | |
|--|--|
| **الوظيفة** | رفع صورة → upload.php → URL عام |
| **مثال** | إرسال صورة للأرشيف |

##### `flattenLibrary(nodes, lang)`

| | |
|--|--|
| **الوظيفة** | يحوّل شجرة المكتبة لقائمة مسارات |
| **مثال** | «هندسة زراعية / سنة 3 / فصل 1» |

---

### 4. شرح ملف `src/defaultCatalog.ts`

#### 4.1 ما هو الكتالوج الافتراضي؟

ملف يحتوي **تصنيفات جاهزة** لا تُحذف:
- `DEFAULT_ARTICLE_CATEGORIES` — 7 تصنيفات مقالات (أنسجة، مائية، طيران…)
- `DEFAULT_GFX_CATEGORIES` — شجرة تصاميم (لاندسكape، ديكور، مطبوعات…)

#### 4.2 لماذا يحتاج المشروع بيانات افتراضية؟

1. **أول تشغيل** — الموقع لا يظهر فارغًا.
2. **ترجمات EN/DE** — إن نسيت الإنجليزي، يُكمّل من الافتراضي.
3. **هيكل التصنيفات** — فروع التصاميم لها IDs ثابتة.

#### 4.3 متى يستخدمها الموقع؟

عبر `applyDefaultCatalog()` → `resolveArticleCategories()` / `resolveGfxCategories()`:

- إذا `articleCategories` **فارغة** → كل الافتراضي.
- إذا **موجودة** → دمج ترجمات ناقصة + إضافة فروع ناقصة (بدون إعادة مشاريع محذوفة).

#### 4.4 ماذا لو لا بيانات على السيرفر؟

→ localStorage أو `DEFAULT_DATA` + defaultCatalog.

#### 4.5 هل هو «نسخة احتياطية»؟

**جزئيًا نعم** — للتصنيفات والهيكل، **ليس** للمقالات والكتب الحقيقية.

#### 4.6 تعديل بحذر للمبتدئ

- **آمن:** تصحيح ترجمة EN/DE لاسم تصنيف.
- **حذر:** تغيير `id` — يكسر الربط مع مقالات موجودة.
- **بعد التعديل:** `npm run dev` + تحقق من الأقسام.

---

### 5. شرح `public/data.json`

#### 5.1 ما معنى JSON؟

**JSON** = نص منظم `{ "مفتاح": "قيمة" }` — يفهمه المتصفح وPHP.

```json
{
  "library": { "tree": [], "books": [], "view": "tree" },
  "articles": { "categories": [], "items": [] },
  "designs": { "categories": [] }
}
```

#### 5.2 لماذا داخل `public/`؟

Vite ينسخ `public/` **كما هو** → على السيرفر: `public_html/data.json`  
الرابط: `https://eng-alaa.com/data.json`

#### 5.3 الفرق بين data.json و appData.ts

| | `data.json` | `appData.ts` |
|--|-------------|--------------|
| **النوع** | ملف بيانات | كود TypeScript |
| **المحتوى** | كتب/مقالات/تصاميم فقط | **كل** AppData + دوال |
| **من يعدّله؟** | SFTP / content-manager | المبرمج |
| **Build؟** | **لا** — ارفع الملف فقط | نعم عند تغيير الكود |

#### 5.4 محتوى حقيقي أم احتياطي؟

**محتوى حقيقي** للزوار — طبقة نشر سريعة بجانب MySQL.

#### 5.5 متى يقرأه الموقع؟

في `useEffect` عند الفتح — `fetch('./data.json?t=...')`  
`?t=` يمنع cache قديم.

#### 5.6 كيف أقرأه كمبتدئ؟

1. افتحه في VS Code.
2. ثبّت `{` و `}` — كل قوس له زوج.
3. `"title": { "ar": "...", "en": "...", "de": "..." }` = عنوان ML.
4. استخدم [jsonlint.com](https://jsonlint.com) إذا شككت.

**ملاحظة:** قد لا يكون الملف في نسختك المحلية — يُنشأ عبر `content-manager.html` أو لوحة التحكم ثم يُرفع.

---

### 6. شرح localStorage

#### 6.1 ما معنى localStorage؟

**خزانة داخل متصفحك** — تبقى بعد إغلاق التبويب.

#### 6.2 أين يُحفظ؟

| | |
|--|--|
| **المكان** | جهاز الزائر (Chrome, Firefox…) |
| **المفتاح** | `alaa_portfolio_state` |
| **JWT** | `__admin_jwt__` (منفصل) |

#### 6.3 على جهازي أم السيرفر؟

**جهازك فقط** — كل متصفح/جهاز له نسخته.

#### 6.4 لماذا يستخدمه المشروع؟

1. **عرض فوري** — لا انتظار شبكة.
2. **عمل offline جزئي** — بعد زيارة سابقة.
3. **تسريع** — DB يُحفظ محليًا بعد أول جلب.

#### 6.5 متى مفيد؟

- تطوير محلي.
- زائر عاد للموقع.
- أثناء انقطاع API مؤقت.

#### 6.6 ما مشكلته؟

| المشكلة | الشرح |
|---------|--------|
| **ليس مشتركًا** | تعديلك **لا** يظهر لزوار آخرين |
| **يُمسح** | Clear data → يختفي |
| **حد الحجم** | ~5MB — صور base64 كبيرة تفشل |
| **ليس backup رسمي** | MySQL + data.json أهم للنشر |

#### 6.7 هل التعديلات تظهر للجميع؟

**لا** — localStorage **لك أنت فقط**.  
للجميع: **MySQL** أو **data.json** على السيرفر.

#### 6.8 متى لا تعتمد عليه؟

- عند النشر النهائي للمحتوى.
- عند مشاركة محتوى بين أجهزة.
- عند اختبار «ماذا يرى زائر جديد».

---

### 7. شرح `hostinger_deploy/api/data.php`

#### 7.1 ما وظيفته؟

**جسر** بين React (متصفح) و MySQL (سيرفر).

#### 7.2 لماذا PHP؟

المتصفح **لا يتصل** بـ MySQL مباشرة (أمان).  
PHP على Hostinger يقرأ/يكتب الجداول.

#### 7.3 ما معنى API؟

**API** = عنوان URL يرد بJSON:
- `GET /api/data.php` → «أعطني البيانات»
- `POST /api/data.php` → «احفظ هذه البيانات»

#### 7.4 كيف يطلب الموقع البيانات؟

```tsx
fetch('./api/data.php', { method: 'GET' })
```

#### 7.5 كيف يرسل البيانات؟

```tsx
fetch('./api/data.php', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}`,
  },
  body: JSON.stringify(data),
})
```

#### 7.6 GET vs POST

| | GET | POST |
|--|-----|------|
| **الغرض** | قراءة | كتابة |
| **Auth** | لا (عام) | JWT مطلوب |
| **Body** | لا | JSON كامل AppData |

#### 7.7 كيف يتعامل مع MySQL؟

- **GET:** `buildAppData($pdo)` — SELECT من كل الجداول → JSON واحد.
- **POST:** `syncAppData($pdo, $body)` — INSERT/UPDATE/DELETE.

#### 7.8 إذا فشل DB؟

- GET → 500 + `{ ok: false, error: "..." }`
- Frontend → `null` → يبقى localStorage
- POST → فشل — localStorage محفوظ لكن السيرفر لم يتحدّث

**ملفات مرتبطة:** `config.php` (بيانات DB), `middleware.php` (JWT), `upload.php` (ملفات).

---

### 8. شرح `schema.sql` و MySQL

#### 8.1 ما معنى قاعدة بيانات؟

**مخزن منظم** — ليس ملفًا واحدًا بل **جداول** مترابطة.

#### 8.2 ما معنى جدول؟

**جدول** = Excel — صفوف وأعمدة.

| id | title_ar | drive_url |
|----|----------|-----------|
| b1 | كتاب 1 | https://… |

#### 8.3 ما معنى schema.sql؟

**مخطط البناء** — أوامر `CREATE TABLE` — تشغّله **مرة** في phpMyAdmin.

#### 8.4 لماذا MySQL؟

- محتوى **مشترك** لكل الزوار.
- **دائم** على السيرفر.
- لوحة التحكم تحفظ هنا.

#### 8.5 أهم الجداول

| الجدول | المحتوى |
|--------|---------|
| `admin_users` | حسابات المدير |
| `site_settings` | إعدادات الموقع + `is_seeded` |
| `skills` | المهارات |
| `article_categories` | تصنيفات المقالات |
| `agri_articles` | المقالات |
| `library_nodes` | شجرة المكتبة |
| `library_books` | الكتب |
| `gfx_categories` / `gfx_subcategories` / `gfx_items` | التصاميم |
| `web_projects` | مشاريع برمجية |
| `code_snippets` | مختبر الأكواد |
| `cv_docs` / `cv_sections` | السيرة |
| `uploaded_files` | ملفات مرفوعة |
| `analytics_*` | إحصائيات (منفصلة) |

#### 8.6 كيف تحفظ وتُسترجع؟

**حفظ:** POST → PHP يوزّع JSON على الجداول.  
**استرجاع:** GET → PHP يجمع الجداول → JSON → `setData`.

---

### 9. مسار تحميل البيانات

#### 9.1 المخطط

```
المتصفح يفتح eng-alaa.com
         │
         ▼
    App.tsx
         │
         ├─► loadAppData() ──────────────► localStorage
         │         │ (فارغ/تالف)
         │         └─► DEFAULT_DATA (appData.ts)
         │
         ├─► loadAppDataFromDb() ───────► GET data.php ─► MySQL
         │         │ (فشل / dbIsSeeded=0)
         │         └─► null → تخطي
         │
         ├─► loadSiteContentFromJson() ► GET data.json
         │         │ (404 / خطأ)
         │         └─► null → تخطي
         │
         ├─► mergeSiteContent() ────────► دمج الطبقات
         │
         └─► applyDefaultCatalog() ─────► defaultCatalog.ts
                   │
                   ▼
              setData(merged)
                   │
                   ▼
         عرض في الأقسام (agri, graphics…)
```

#### 9.2 خطوة بخطوة

1. **App.tsx يبدأ** — `useState(loadAppData)` → عرض فوري.
2. **localStorage** — مفتاح `alaa_portfolio_state`.
3. **data.php** — إن DB مُهيّأة، تُدمج فوق المحلي.
4. **data.json** — يُحدّث الكتب/مقالات/تصاميم (استثناء gfx إن DB محمّل).
5. **defaultCatalog** — يملأ تصنيفات فارغة.
6. **`setData`** — React يُحدّث كل `{data.xxx}`.

---

### 10. مسار حفظ البيانات من لوحة التحكم

#### 10.1 المخطط

```
ContentAdmin.tsx
    │  المدير يعدّل ويضغط «حفظ»
    │  onSave({ agriArticles: [...] })
    ▼
App.tsx — handleCvSave(partial)
    │  setData(prev => ({ ...prev, ...partial }))
    ▼
appData.ts — saveAppData(updated)
    │  ① localStorage.setItem(...)
    │  ② إن JWT: POST data.php
    ▼
data.php — syncAppData()
    │  UPDATE/INSERT جداول MySQL
    ▼
MySQL — البيانات محفوظة
    │
    ▼
الزائر التالي: GET data.php → يرى الجديد
    │
    ▼
(اختياري) data.json — تحديث يدوي SFTP للمحتوى الثابت
```

#### 10.2 خطوة بخطوة

1. **ContentAdmin** يجمع التعديلات → `onSave`.
2. **handleCvSave** يحدّث `data` **فورًا** (المدير يرى التغيير).
3. **saveAppData** يكتب localStorage **دائمًا**.
4. إن **متصل بالسيرفر** → POST → **MySQL**.
5. **زائر جديد** يجلب GET — يرى المحتوى المحدّث.
6. **data.json** — منفصل؛ لا يُحدَّث تلقائيًا من الحفظ.

---

### 11. التطوير المحلي vs Hostinger

#### 11.1 محليًا (`npm run dev`)

| | |
|--|--|
| **العنوان** | http://localhost:5173 |
| **PHP** | Vite **لا يشغّل** PHP — يوجّه `/api` لـ eng-alaa.com (proxy) |
| **data.json** | من `public/data.json` محليًا إن وُجد |
| **localStorage** | خاص بمتصفحك على جهازك |

#### 11.2 على Hostinger

| | |
|--|--|
| **العنوان** | https://eng-alaa.com |
| **PHP + MySQL** | يعملان مباشرة |
| **data.json** | `public_html/data.json` |
| **localStorage** | لكل زائر على جهازه |

#### 11.3 هل البيانات من نفس المكان؟

**نفس المنطق** — مصادر مختلفة حسب البيئة:
- محلي: proxy → السيرفر حي + localStorage محلي.
- حي: data.php + data.json مباشرة.

#### 11.4 لماذا يعمل محليًا ولا على السيرفر؟

| السبب | الحل |
|-------|------|
| `config.php` ناقص | ارفعه يدويًا |
| DB غير مُنشأة | شغّل schema.sql |
| `is_seeded=0` | مزامنة من لوحة التحكم |
| JWT منتهي | أعد loginToApi |
| data.json غير مرفوع | SFTP |
| CORS / proxy | تحقق vite.config.ts |

#### 11.5 علاقة data.json و MySQL

- **MySQL** = مصدر admin + مزامنة كاملة.
- **data.json** = تحديث سريع للمحتوى **بدون build** — SFTP فقط.
- **الاثنان** يُدمجان عند التحميل — مع قواعد أولوية (انظر الفصل 2).

---

### 12. أخطاء شائعة في البيانات

| الخطأ | الأعراض | الحل |
|-------|---------|------|
| **JSON مكسور** | data.json لا يُقرأ | jsonlint، تحقق من `,` و `{}` |
| **حذف فاصلة/قوس** | parse error | أصلح JSON |
| **تغيير اسم حقل** | حقل يُ ignored | لا تغيّر أسماء الحقول في الكود |
| **MySQL غير متصل** | لا حفظ سحابي | config.php, phpMyAdmin |
| **data.php 403/500** | POST fails | صلاحيات، error_log |
| **localStorage فقط** | زوار لا يرون التعديل | loginToApi + save |
| **رفع ناقص Hostinger** | API 404 | ارفع api/ كاملًا |
| **dbIsSeeded=0** | DB تُتجاهل | مزامنة أولى |
| **صور base64 ضخمة** | localStorage full | upload.php |
| **gfx من JSON قديم** | مشاريع شبح | DB محمّل → gfx من JSON يُتخطى |

---

### 13. ماذا يمكنني تعديله بأمان كمبتدئ؟

#### 13.1 آمن (بحذر)

| ماذا | أين |
|------|-----|
| مقال / كتاب / تصميم | لوحة التحكم → ContentAdmin |
| محتوى bulk | content-manager.html → data.json → SFTP |
| نصوص أزرار | translations.ts |
| data.json | بعد تعلّم JSON |

#### 13.2 لا تلمس قبل الفهم

| ماذا | لماذا |
|------|-------|
| `appData.ts` — دوال norm/merge | كسر تحميل |
| `data.php` | كسر API |
| `schema.sql` على DB حية | فقدان بيانات |
| أسماء حقول AppData | TypeScript + DB |

#### 13.3 إضافة مقال/تصميم (نظري)

1. لوحة التحكم → محتوى الزراعة / معرض التصاميم.
2. املأ الحقول (ML).
3. حفظ → handleCvSave → MySQL.
4. أو: CSV → content-manager → data.json → SFTP.

#### 13.4 تغيير صورة/عنوان

- **عنوان:** حقل title في admin.
- **صورة:** URL أو رفع → upload.php.

#### 13.5 التأكد أنك لم تكسر

1. F12 → Console — أخطاء حمراء؟
2. `npm run typecheck`
3. JSON validator لـ data.json
4. `GET /api/data.php?test` على السيرفر
5. Ctrl+F5 بعد رفع data.json

---

### 14. تشبيه: المكتبة الكبيرة

| جزء المشروع | دور في المكتبة |
|-------------|----------------|
| **appData.ts** | **دفتر القوانين** — شكل الرف، أنواع الكتب، قواعد الإعارة |
| **defaultCatalog.ts** | **كتب احتياطية** على رف «تصنيفات» — إن فرغ رف |
| **data.json** | **نسخة مطبوعة** tُعلّق على الحائط — تحدّثها بدون بناء مكتبة جديدة |
| **MySQL** | **المخزن الرئيسي** — كل الكتب الحقيقية |
| **data.php** | **موظف الاستقبال** — يعطي ويستلم من المخزن |
| **localStorage** | **حقيبة الزائر** — نسخة شخصية مؤقتة |
| **App.tsx** | **أمين المكتبة** — يطلب ويعرض للزائر |
| **ContentAdmin** | **موظف الإدخال** — يكتب بطاقات كتب جديدة |

---

### 15. نهاية الفصل

#### 15.1 ملخص بسيط

1. **المحتوى ≠ الكود** — البيانات في `AppData`.
2. **مصادر متعددة** تُدمج: localStorage → MySQL → data.json → defaultCatalog.
3. **appData.ts** = تعريف + تحميل + حفظ.
4. **data.json** = تحديث SFTP بدون build.
5. **localStorage** = سريع لكن **شخصي**.
6. **MySQL + data.php** = مصدر **مشترك** للزوار.
7. **الحفظ:** admin → saveAppData → localStorage + POST.

#### 15.2 جدول مصادر البيانات

| المصدر | المكان | متى | مشترك؟ | Build؟ |
|--------|--------|-----|--------|--------|
| DEFAULT_DATA | appData.ts | أول زيارة | نعم | نعم |
| defaultCatalog | defaultCatalog.ts | تصنيفات فارغة | نعم | نعم |
| localStorage | متصفح | كل زيارة | **لا** | لا |
| MySQL | Hostinger | بعد seed | **نعم** | لا |
| data.json | public_html | كل زيارة | **نعم** | **لا** |

#### 15.3 أهم 10 أشياء

1. `data` في App.tsx = كل المحتوى المعروض.
2. `loadAppData()` = localStorage أولًا.
3. `dbIsSeeded=0` → DB تُتجاهل.
4. `data.json` = library + articles + designs فقط.
5. `saveAppData` = localStorage **+** MySQL (إن JWT).
6. localStorage **لا** يصل للزوار الآخرين.
7. `ML` = `{ ar, en, de }` دائمًا.
8. defaultCatalog = هيكل تصنيفات، ليس كل المحتوى.
9. content-manager.html = أداة CSV لـ data.json.
10. `?test` على data.php = تشخيص DB.

#### 15.4 أخطاء وحلول (مرجع سريع)

| المشكلة | الحل |
|---------|------|
| محتوى قديم | Ctrl+F5 + تحقق DB/data.json |
| لا حفظ | loginToApi + JWT |
| JSON error | jsonlint |
| DB فارغة | schema.sql + مزامنة |
| محلي لا API | proxy vite → eng-alaa.com |

#### 15.5 أسئلة تدريبية

**فهم:**
1. ما الفرق بين الكود والمحتوى؟
2. ما `ML`؟
3. ترتيب تحميل المصادر؟
4. لماذا localStorage ليس كافيًا للنشر؟
5. ما `dbIsSeeded`؟
6. الفرق بين data.json و MySQL؟
7. ماذا يفعل `mergeSiteContent`؟
8. GET vs POST في data.php؟

**تطبيق:**
9. ما مفتاح localStorage للبيانات؟
10. أين `SITE_CONTENT_JSON_URL`؟
11. ابحث `contentPatchFromJson` — ماذا يقرأ من JSON؟
12. عدّ جداول schema.sql — كم جدول محتوى (بدون analytics)؟

**تفكير:**
13. زائر جديد بدون localStorage — ماذا يرى؟
14. مدير حفظ بدون JWT — أين تُحفظ البيانات؟

#### 15.6 تمرين نظري: إضافة مشروع تصميم

**الهدف:** مشروع «هوية بصرية لصيدلية» في تصنيف «مطبوعات → بزنس كارد».

**الخطوات (بدون تنفيذ):**

1. **لوحة التحكم** → معرض التصاميم (adminPanel 4).
2. اختر تصنيف `gfx-prints` → فرع `gfx-prints-cards`.
3. «إضافة مشروع»:
   - `title`: `{ ar: "هوية صيدلية", en: "...", de: "..." }`
   - `mainImg`: URL صورة
   - `desc`: ML
4. **حفظ** → `onSave` → `handleCvSave` → `saveAppData` → MySQL.
5. **تحقق:** افتح graphics portal → تصنيف مطبوعات → فرع بزنس كارد.
6. **(بديل)** أضف صفًا في CSV → content-manager → data.json → SFTP.
7. **تحقق JSON:** `designs.categories[].subCategories[].items[]`.
8. Ctrl+F5 على الموقع الحي.

**لماذا نظري؟** لتتعلّم المسار قبل لمس بيانات حقيقية.

---

> **الفصل التالي (5):** «شرح لوحة التحكم» — *(هذا الفصل أدناه)*

---

## الفصل الخامس: شرح لوحة التحكم — كيف أعدّل محتوى الموقع بدون برمجة؟

> **هدف الفصل:** أن تدخل لوحة التحكم بثقة — تفهم **ماذا تعدّل**، **كيف يُحفظ**، و**لماذا** أحيانًا يراك الزوار التعديل وأحيانًا لا.

---

### 1. ما هي لوحة التحكم؟

#### 1.1 تعريف بسيط

**لوحة التحكم (Admin Dashboard)** = **المكتب الداخلي** للموقع.

- **الزائر** يرى: مقالات، تصاميم، سيرة، كتب…
- **أنت (المدير)** ترى: **نفس الموقع + أدوات تعديل** — حقول، أزرار حفظ، رفع صور، إحصائيات.

#### 1.2 لماذا نحتاج لوحة تحكم؟

بدونها، كل تعديل يحتاج:
- فتح ملف JSON يدويًا، أو
- تعديل كود React.

**لوحة التحكم** تجعل التعديل **بصريًا** — مثل WordPress أو لوحة متجر.

#### 1.3 الفرق بين صفحة الزائر وصفحة المدير

| | **الزائر** | **المدير** |
|--|-----------|------------|
| **portal** | home, agri, graphics… | **admin** (بعد الدخول) |
| **يرى** | محتوى نهائي | محتوى + نماذج تحرير |
| **يمكنه** | تصفح، تحميل CV | تعديل، حفظ، مزامنة |
| **تتبع** | نعم (analytics) | **لا** أثناء الجلسة |

#### 1.4 ما يمكن تعديله من لوحة التحكم

| التبويب (في App.tsx) | ماذا تعدّل |
|----------------------|------------|
| **0 — البيانات الشخصية** | الاسم، النبذة، شعار الاسم |
| **1 — المهارات** | Photoshop، AutoCAD… + النسب |
| **2 — السيرة CV** | صفحات A4، أقسام، PDF |
| **3 — محتوى الزراعة** | مقالات، كتب، مكتبة، تربة، تقارير |
| **4 — معرض التصاميم** | تصنيفات، فروع، مشاريع |
| **5 — المشاريع البرمجية** | web projects + مختبر أكواد |
| **6 — حقن صفحات** | HTML/CSS مخصص |
| **7 — إعدادات الموقع** | شعار، ألوان، footer، **مزامنة MySQL** |
| **8 — إدارة الملفات** | رفع صور وملفات |
| **9 — إحصائيات الزوار** | زيارات، GPS، تنزيلات |

#### 1.5 ما **لا** يجب تعديله من لوحة التحكم

| لا تفعل | لماذا |
|---------|--------|
| تغيير **هيكل** React/CSS | يحتاج مبرمج |
| تعديل **translations.ts** | نصوص الأزرار الثابتة — من الكود |
| حذف **جداول MySQL** يدويًا | خطر فقدان بيانات |
| مشاركة **كلمة مرور** admin | أمان |
| الاعتماد على localStorage فقط | الزوار لن يروا التعديل |

---

### 2. كيف يدخل المدير إلى لوحة التحكم؟

#### 2.1 أين زر الدخول؟

**زر مخفي نسبيًا** في **Footer** (أسفل الصفحة):

```tsx
<button className="footer-admin-btn" onClick={() => setAdminGate(true)}>
  {t.adminBtn}  // «لوحة التحكم»
</button>
```

**ليس** في القائمة الرئيسية — حتى لا يراه الزوار بسهولة.

#### 2.2 خطوات الدخول

```
1. scroll للأسفل → زر «لوحة التحكم»
2. تظهر نافذة (modal) — adminGate = true
3. إدخال email + password
4. handleAdminLogin() يتحقق
5. إن صح → adminLoggedIn = true → openPortal("admin")
6. تظهر لوحة التحكم الكاملة
```

#### 2.3 ما معنى adminLoggedIn؟

```tsx
const [adminLoggedIn, setAdminLoggedIn] = useState(false);
const [adminGate, setAdminGate] = useState(false);
```

| المتغير | المعنى |
|---------|--------|
| `adminGate` | **نافذة** تسجيل الدخول مفتوحة؟ |
| `adminLoggedIn` | **دخلت** فعلًا؟ — لوحة admin تظهر فقط إذا `true` |

**شرط العرض:**
```tsx
{portal === "admin" && adminLoggedIn && (
  <div className="admin-layout">...</div>
)}
```

#### 2.4 التحقق من كلمة المرور — **نوعان من الدخول**

**مهم جدًا للمبتدئ:** يوجد **دخولان مختلفان**:

| | **دخول لوحة التحكم (محلي)** | **دخول السيرفر (MySQL)** |
|--|---------------------------|---------------------------|
| **الدالة** | `handleAdminLogin()` | `loginToApi()` / `handleServerConnect()` |
| **التحقق** | `ADMIN_EMAIL` + `ADMIN_PASS` في `appData.ts` | `auth.php` + جدول `admin_users` |
| **النتيجة** | `adminLoggedIn = true` | JWT token في localStorage |
| **الغرض** | فتح لوحة التحكم | **حفظ** على Hostinger |

**يمكنك** فتح اللوحة محليًا **بدون** ربط MySQL — لكن **الحفظ للزوار** يحتاج JWT من `auth.php`.

#### 2.5 دور auth.php

```php
POST /api/auth.php  { username, password }
→ { token, role, displayName }
```

1. يقرأ username/password من body.
2. يبحث في `admin_users` في MySQL.
3. `password_verify()` — يتحقق من hash.
4. ينشئ **JWT token** — «بطاقة دخول مؤقتة».
5. المتصفح يحفظها: `setApiToken(token)`.

#### 2.6 إذا كانت كلمة المرور صحيحة (لوحة محلية)

```tsx
function handleAdminLogin() {
  if (adminEmail === ADMIN_EMAIL && adminPass === ADMIN_PASS) {
    setAdminGate(false);
    setAdminLoggedIn(true);
    // ... تحميل editName, editSkills من data
    openPortal("admin");
  } else {
    setAdminError("بيانات خاطئة!");
  }
}
```

→ تُغلق النافذة → تفتح لوحة admin.

#### 2.7 إذا كانت خاطئة

→ رسالة حمراء «بيانات خاطئة!» — **لا** `adminLoggedIn` — **لا** لوحة.

---

### 3. شرح ملف `ContentAdmin.tsx`

#### 3.1 ما وظيفته؟

**أكبر ملف في لوحة التحكم** (~2900+ سطر) — **واجهة تحرير المحتوى**:
- مقالات، كتب، تصاميم، مشاريع، إعدادات موقع.

#### 3.2 لماذا هو الأهم؟

لأن **90%** من تعديلات المحتوى اليومية تمر منه — بدونه تعدّل JSON يدويًا.

#### 3.3 الأقسام (modes)

```tsx
type Mode = 'agri' | 'gfx' | 'lab' | 'site';

export function ContentAdmin({ mode, data, onSave, ... }) {
  if (mode === 'agri') return <AgriAdmin ... />;
  if (mode === 'gfx') return <GfxAdmin ... />;
  if (mode === 'site') return <SiteSettingsAdmin ... />;
  return <LabAdmin ... />;
}
```

| mode | المكوّn الداخلي | المحتوى |
|------|----------------|---------|
| `agri` | `AgriAdmin` | زراعة: مقالات، كتب، مكتبة… |
| `gfx` | `GfxAdmin` | تصاميم: تصنيفات، مشاريع |
| `lab` | `LabAdmin` | برمجة: snippets + WebProjAdmin |
| `site` | `SiteSettingsAdmin` | إعدادات + مزامنة السيرفر |

#### 3.4 Props — كيف يتواصل مع App.tsx

```tsx
interface Props {
  mode: Mode;
  data: AppData;                              // كل المحتوى الحالي
  onSave: (u: Partial<AppData>) => void;      // دالة الحفظ
  serverConnected?: boolean;                   // site mode فقط
  serverSyncing?: boolean;
  onServerConnect?: (user, pass) => Promise<boolean>;
  onServerSync?: () => void;
  onServerDisconnect?: () => void;
}
```

**في App.tsx:**
```tsx
<ContentAdmin mode="agri" data={data} onSave={handleCvSave} />
```

#### 3.5 كيف يعرض الحقول؟

- **`MlNameInputs`** — 3 حقول (ar/en/de) لكل عنوان.
- **`RichEditor`** — محرر HTML للمقالات.
- **`input type="file"`** + `uploadMediaFile()` — صور.
- **`TabBar`** — تبويبات داخل AgriAdmin (مقالات، كتب…).

#### 3.6 كيف يحتفظ بالتعديلات قبل الحفظ؟

**State محلي** داخل كل Admin:

```tsx
function AgriAdmin({ data, onSave }) {
  const [articles, setArticles] = useState(data.agriArticles);
  const [editArticle, setEditArticle] = useState(null);
  // ...
}
```

- تكتب في الحقول → `setArticles(...)` — **مؤقت** في الذاكرة.
- عند «حفظ» → `onSave({ agriArticles: articles })`.

#### 3.7 أهم الدوال والمكوّnات — واحدة واحدة

##### `ContentAdmin` (التصدير الرئيسي)

| | |
|--|--|
| **الوظيفة** | يختار Agri/Gfx/Lab/Site حسب `mode` |
| **أين** | App.tsx adminPanel 3,4,5,7 |
| **مثال** | موظف است reception يوجّهك للقسم |

##### `AgriAdmin`

| | |
|--|--|
| **الوظيفة** | إدارة كل محتوى الزراعة |
| **تبويبات** | articles, books, library, soil, videos, reports, ai… |
| **حفظ** | `saveArticles`, `saveBooks`, `saveTree`… → `onSave({...})` |
| **مثال** | قسم المكتبة في مكتبة جامعة |

##### `GfxAdmin`

| | |
|--|--|
| **الوظيفة** | تصنيفات → فروع → مشاريع |
| **حفظ** | `commitCats(next)` → `onSave({ gfxCategories: next })` |
| **مثال** | أرفف معرض فني |

##### `LabAdmin` / `WebProjAdmin`

| | |
|--|--|
| **الوظيفة** | مشاريع ويب + snippets أكواد |
| **حفظ** | `onSave({ webProjects })` / `{ softwareSnippets }` |

##### `SiteSettingsAdmin`

| | |
|--|--|
| **الوظيفة** | شعار، theme، footer + **ربط MySQL** |
| **حفظ** | `commit(settings)` + `connectServer()` |
| **رسالة** | «تم الحفظ ✓» لثانيتين |

##### `MlNameInputs`

| | |
|--|--|
| **الوظيفة** | حقول ML ثلاثية + زر ترجمة AI |
| **مثال** | استمارة اسم بثلاث لغات |

##### `onSave` (prop)

| | |
|--|--|
| **الوظيفة** | يرسل `Partial<AppData>` للأب |
| **في App** | = `handleCvSave` |
| **مثال** | «اعتمد هذا التعديل يا مدير» |

##### `uploadMediaFile(file, folder)`

| | |
|--|--|
| **الوظيفة** | رفع صورة → upload.php → URL |
| **يحتاج** | JWT token |
| **مثال** | إرسال صورة للأرشيف |

---

### 4. ماذا يحدث عند تعديل نص أو صورة؟

#### الرحلة الكاملة

```
① المدير يغيّر عنوان مقال في حقل MlNameInputs
         │
         ▼
② onChange → setArticles([...])  ← ContentAdmin state محلي
         │
         ▼
③ React يعيد رسم الحقل — ترى التغيير فورًا
         │
         ▼
④ تضغط «حفظ المقال» → saveArticles(updated)
         │
         ▼
⑤ onSave({ agriArticles: updated })  ← ContentAdmin → App
         │
         ▼
⑥ handleCvSave(partial) في App.tsx
         │
         ▼
⑦ setData(prev => ({ ...prev, ...partial }))  ← الشاشة العامة تتحدّث
         │
         ▼
⑧ saveAppData(updated) في appData.ts
         │
         ├─► localStorage.setItem('alaa_portfolio_state', ...)
         │
         └─► إن JWT موجود: POST ./api/data.php
                    │
                    ▼
              data.php → syncAppData() → MySQL
```

**ملاحظة:** كثير من الإعدادات (مثل grid settings) تُحفظ **فورًا** عند التغيير — بدون زر منفصل.

---

### 5. ماذا يحدث عند الضغط على «حفظ»؟

#### 5.1 أي دالة أولًا؟

1. دالة محلية في ContentAdmin (مثل `saveArticles` أو `commit`).
2. ثم **`onSave(partial)`** → **`handleCvSave`**.
3. ثم **`saveAppData`**.

#### 5.2 ما البيانات المُرسَلة؟

**عادةً القسم الحالي فقط** — `Partial<AppData>`:

```tsx
onSave({ agriArticles: newList })     // مقالات فقط
onSave({ gfxCategories: next })      // تصاميم فقط
onSave({ siteSettings: next })        // إعدادات فقط
```

**لكن** `saveAppData` يأخذ **`data` الكامل** بعد الدمج:

```tsx
const updated = { ...prev, ...partial };
saveAppData(updated);  // يُرسل كل AppData للسيرفر
```

#### 5.3 هل يُحفظ كل الموقع أم القسم فقط؟

| الطبقة | ماذا يُحفظ |
|--------|------------|
| **React merge** | دمج partial في data الكامل |
| **localStorage** | **كل** AppData |
| **POST data.php** | **كل** AppData (normalized) |
| **MySQL** | PHP يوزّع على الجداول |

#### 5.4 كيف يعرف أن الحفظ نجح؟

```tsx
function handleCvSave(partial): Promise<boolean> {
  return new Promise(resolve => {
    setData(prev => {
      const updated = { ...prev, ...partial };
      void saveAppData(updated).then(resolve);  // true/false
      return updated;
    });
  });
}
```

- `true` = POST نجح (JWT + data.php OK).
- `false` = localStorage فقط (لا JWT أو فشل شبكة).

**SiteSettingsAdmin** يعرض: «تم الحفظ ✓» أو رسالة مزامنة.

#### 5.5 إذا فشل الحفظ

- **localStorage** غالبًا **محفوظ** — أنت ترى التعديل.
- **MySQL** لم تتحدّث — **الزوار لا يرون**.
- رسالة خطأ في تبويب إعدادات الموقع أو Console (F12).

---

### 6. علاقة ContentAdmin مع App.tsx

#### 6.1 كيف يرسل التعديلات؟

عبر **props** — خاصة **`onSave`**:

```tsx
<ContentAdmin mode="gfx" data={data} onSave={handleCvSave} />
```

ContentAdmin **لا يستورد** saveAppData مباشرة (في الغالب) — يمر عبر الأب.

#### 6.2 ما معنى props؟

**Props** = **خصائص** تمرّر من الأب للابن:

```tsx
// الأب يعطي:
data={data}           // «هذا محتواك»
onSave={handleCvSave} // «عند الحفظ اتصل بهذه الدالة»
```

**تشبيه:** الأب يعطي الابن **دفترًا** + **رقم fax** للإرسال للمخزن.

#### 6.3 لماذا ContentAdmin لا يحفظ وحده؟

1. **`data` واحد** في App — مصدر الحقيقة.
2. **حفظ موحّد** — نفس `handleCvSave` لـ CV و Agri و Gfx.
3. **فصل المسؤوليات** — ContentAdmin = واجهة، App = orchestrator.

#### 6.4 لماذا App.tsx وسيط؟

- يملك `setData` — **State الرئيسي**.
- يملك `handleCvSave`, `handleServerConnect`.
- يقرر **متى** adminLoggedIn و **متى** track analytics.

---

### 7. علاقة لوحة التحكم مع appData.ts (الحفظ فقط)

#### 7.1 دور appData.ts

| الدالة | متى |
|--------|-----|
| `saveAppData(data)` | **كل** حفظ من admin |
| `loginToApi(user, pass)` | ربط MySQL |
| `getApiToken()` | هل JWT موجود؟ |
| `uploadMediaFile()` | رفع صور |

#### 7.2 saveAppData — ماذا يفعل؟

```tsx
export async function saveAppData(data: AppData): Promise<boolean> {
  const normalized = applyDefaultCatalog(data);
  // ① تنظيف صور base64 ضخمة للـ localStorage
  localStorage.setItem(STORAGE_KEY, JSON.stringify(forLocal));
  // ② إن JWT:
  const res = await fetch('./api/data.php', {
    method: 'POST',
    headers: { Authorization: `Bearer ${token}` },
    body: JSON.stringify(normalized),
  });
  return res.ok;
}
```

#### 7.3 لماذا لا نكتب الحفظ داخل ContentAdmin؟

- **تكرار** — 4 modes × عشرات الأزرار.
- **أخطاء** — نسيان localStorage أو JWT.
- **صيانة** — تغيير data.php مرة واحدة في appData.

---

### 8. علاقة لوحة التحكم مع data.php و MySQL

#### 8.1 كيف تصل التعديلات إلى Hostinger؟

```
المتصفح (لوحة التحكم)
    │  fetch POST https://eng-alaa.com/api/data.php
    │  Header: Authorization: Bearer eyJ...
    │  Body: { name, bio, agriArticles, gfxCategories, ... }
    ▼
data.php (PHP على Hostinger)
    │  requireAuth() — يتحقق JWT
    │  syncAppData($pdo, $body)
    ▼
MySQL — UPDATE/INSERT في جداول:
    agri_articles, library_books, gfx_items, site_settings, ...
```

#### 8.2 Request و Response

| | **Request** | **Response** |
|--|-------------|--------------|
| **معنى** | «طلب» من المتصفح | «رد» من الالسيرفر |
| **مثال** | POST + JSON body | `{ "ok": true }` |
| **فشل** | شبكة/401/500 | `{ "ok": false, "error": "..." }` |

#### 8.3 إذا DB لا تعمل

- POST → 500
- `saveAppData` → `false`
- localStorage **محفوظ** — أنت ترى، الزوار **لا**.

#### 8.4 إذا data.php غير مرفوع

- fetch → 404
- نفس النتيجة — **محلي فقط**.

---

### 9. شرح `AnalyticsDashboard.tsx`

#### 9.1 الوظيفة

**لوحة إحصائيات** — **قراءة فقط** (لا تعدّل مقالات).

#### 9.2 ماذا تعرض؟

| تبويب | المحتوى |
|-------|---------|
| **ملخص** | زوار، page views، CV downloads |
| **GPS دقيق** | زوار وافقوا على الموقع |
| **تقارير** | يوم / شهر / سنة |
| **نشاط** | أحداث حديثة، حذف انتقائي |

#### 9.3 من أين الأرقام؟

```tsx
fetchAnalyticsStats(getApiToken())
// GET ./api/analytics.php?action=stats
```

#### 9.4 الفرق عن ContentAdmin

| | ContentAdmin | AnalyticsDashboard |
|--|--------------|-------------------|
| **يعدّل محتوى؟** | نعم | **لا** |
| **API** | data.php | analytics.php |
| **JWT** | للحفظ | لقراءة stats |

---

### 10. شرح `analytics.php`

#### 10.1 الوظيفة

| الطلب | من | ماذا |
|-------|-----|------|
| `POST { action:"track" }` | **أي زائر** | يسجّل زيارة/heartbeat/تنزيل |
| `GET ?action=stats` | **مدير + JWT** | إحصائيات كاملة |

#### 10.2 كيف يسجل الزيارات؟

**من `analytics.ts` في App.tsx:**

```tsx
trackPageView(`${portal}:${lang}`);  // عند تغيير قسم
trackHeartbeat();                       // كل 60 ثانية
trackCvDownload(...);                 // عند PDF
```

**لا يُتتبّع** المدير: `if (adminLoggedIn) return;`

#### 10.3 أين تُحفظ؟

جداول MySQL:
- `analytics_sessions`
- `analytics_events`
- `ip_geo_cache`

#### 10.4 أخطاء شائعة

| خطأ | الحل |
|-----|------|
| لا بيانات | analytics.php غير مرفوع |
| stats فارغ | JWT منتهي — أعد loginToApi |
| GPS فارغ | الزوار لم يوافقوا |

---

### 11. الفرق بين الحفظ المحلي والحفظ الحقيقي

| | **localStorage** | **MySQL** |
|--|------------------|-----------|
| **أين** | متصفحك | Hostinger |
| **من يرى** | **أنت** على **هذا الجهاز** | **كل الزوار** |
| **متى** | دائمًا مع saveAppData | إن JWT + data.php OK |
| **data.json** | **منفصل** — SFTP يدوي | لا يُحدَّث تلقائيًا |

#### كيف تتأكد أن الحفظ على Hostinger؟

1. تبويب **إعدادات الموقع** → «متصل ✔».
2. عدّل شيئًا → احفظ → «تم الاتصال ونقل البيانات».
3. افتح الموقع من **جهاز آخر** / متصفح خفي.
4. أو: phpMyAdmin → تحقق من الجدول.

---

### 12. أمان لوحة التحكم

#### 12.1 لماذا الحماية؟

من يدخل admin يمكنه **تغيير كل المحتوى** — وحذف بيانات.

#### 12.2 قواعد بسيطة

| افعل | لا تفعل |
|------|---------|
| كلمة مرور قوية | مشاركة ADMIN_PASS |
| JWT سري | نشر token |
| auth.php على HTTPS | HTTP بدون SSL |
| حساب admin_users في DB | كلمة مرور في Git |

#### 12.3 هل النظام مناسب كبداية؟

**نعم للتعلّم والموقع الشخصي** — لكن للإنتاج الكبير:
- نقل ADMIN_EMAIL/PASS من الكود إلى env.
- تقوية JWT، rate limiting، 2FA لاحقًا.

---

### 13. أخطاء شائعة في لوحة التحكم

| المشكلة | السبب | الحل |
|---------|--------|------|
| حفظت ولا أرى | لم تُحفظ فعلًا | تحقق رسالة ✓ |
| عندي فقط | localStorage بدون MySQL | loginToApi + connect |
| كلمة مرور خاطئة | ADMIN_* أو admin_users | راجع البيانات |
| data.php لا يستجيب | غير مرفوع / config | SFTP + config.php |
| CORS محلي | proxy vite | `VITE_PHP_PROXY_TARGET` |
| MySQL error | schema / credentials | ?test + phpMyAdmin |
| JSON تالف | تعديل يدوي خاطئ | jsonlint |

---

### 14. ماذا يمكنني تعديله بأمان؟

#### ✅ آمن (من اللوحة)

| التعديل | أين |
|---------|-----|
| عنوان / وصف | AgriAdmin, GfxAdmin |
| مقال جديد | تبويب مقالات |
| مشروع تصميم | GfxAdmin |
| صورة | رفع URL أو upload |
| تصنيف | إعدادات التصنيفات |
| سيرة CV | CvDocEditor (تبويب 2) |
| مهارات | تبويب 1 |

#### ❌ لا تلمس الآن

- `handleCvSave`, `saveAppData`
- `auth.php`, `data.php`, `schema.sql`
- أسماء حقول `AppData`
- `ADMIN_*` constants (إلا بفهم)

---

### 15. تشبيه: المحل التجاري

| جزء | في المشروع |
|-----|------------|
| **واجهة المحل** | صفحات الزائر (portal ≠ admin) |
| **المكتب الداخلي** | `portal === "admin"` |
| **موظف الكتابة** | ContentAdmin.tsx |
| **المدير المعتمد** | App.tsx + handleCvSave |
| **موظف البريد** | data.php |
| **المستودع** | MySQL |
| **دفتر المبيعات** | AnalyticsDashboard + analytics.php |
| **حقيبة شخصية** | localStorage |

---

### 16. نهاية الفصل

#### 16.1 ملخص

1. لوحة التحكم = تعديل **بدون برمجة**.
2. دخولان: **admin محلي** + **JWT للالسيرفر**.
3. ContentAdmin = واجهة — App = وسيط — appData = حفظ.
4. onSave → handleCvSave → saveAppData → data.php → MySQL.
5. Analytics = **عرض فقط** — analytics.php.
6. localStorage ≠ حفظ للزوار — MySQL أو data.json.

#### 16.2 جدول الملفات

| الملف | الوظيفة |
|-------|---------|
| `App.tsx` | دخول admin، handleCvSave، تبويبات |
| `ContentAdmin.tsx` | تحرير محتوى (agri/gfx/lab/site) |
| `AnalyticsDashboard.tsx` | عرض إحصائيات |
| `appData.ts` | saveAppData, loginToApi, upload |
| `analytics.ts` | track + fetchAnalyticsStats |
| `auth.php` | JWT login |
| `data.php` | GET/POST AppData |
| `analytics.php` | track + stats |
| `CvDocEditor.tsx` | تحرير CV |
| `FileExplorerAdmin.tsx` | ملفات |
| `upload.php` | رفع وسائط |

#### 16.3 مخطط التعديل والحفظ

```
[Footer: لوحة التحكم]
        ↓ adminGate modal
   handleAdminLogin ✓
        ↓ adminLoggedIn + portal=admin
   ContentAdmin (mode=agri/gfx/...)
        ↓ onSave(partial)
   App.handleCvSave
        ↓ setData + saveAppData
   localStorage + POST data.php
        ↓
   MySQL → الزوار يرون التحديث
```

#### 16.4 أهم 10 أشياء

1. زر admin في **Footer** — ليس Navbar.
2. `adminLoggedIn` ≠ JWT — **اثنان منفصلان**.
3. ContentAdmin عبر **`onSave={handleCvSave}`**.
4. الحفظ **غالبًا فوري** عند التغيير.
5. POST يرسل **AppData كامل** — ليس partial في PHP.
6. **Site Settings** = ربط MySQL.
7. Analytics **لا تعدّل** محتوى.
8. المدير **لا يُتتبّع** في analytics.
9. «تم الحفظ ✓» ≠ «على الالسيرفر» — تحقق «متصل ✔».
10. data.json **لا** يُحدَّث من لوحة التحكم تلقائيًا.

#### 16.5 أخطاء وحلول (مرجع)

| # | مشكلة | حل |
|---|--------|-----|
| 1 | لا admin | Footer → gear |
| 2 | محلي فقط | Site → connectServer |
| 3 | 401 POST | loginToApi |
| 4 | stats فارغ | analytics.php + JWT |
| 5 | صورة لا ترفع | JWT + upload.php |

#### 16.6 أسئلة تدريبية

**فهم:**
1. أين زر لوحة التحكم؟
2. الفرق adminLoggedIn vs JWT؟
3. ما modes في ContentAdmin؟
4. ماذا يفعل onSave؟
5. ContentAdmin vs AnalyticsDashboard؟

**تطبيق:**
6. كم تبويب adminPanel في App.tsx؟
7. أي mode لإعدادات الموقع؟
8. ما API للإحصائيات؟
9. ابحث `handleCvSave` — ماذا يستدعي؟
10. متى trackPageView لا يعمل؟

**تفكير:**
11. حفظت مقالًا بدون JWT — ماذا يرى زائر جديد؟

#### 16.7 تمرين نظري: إضافة مقال

**الهدف:** مقال «زراعة مائية للمبتدئين» — **بدون كود**.

1. Footer → **لوحة التحكم** → email/password → دخول.
2. Sidebar → **محتوى الزراعة** (adminPanel 3).
3. تبويب **المقالات**.
4. **+ مقال جديد**:
   - categoryId: تصنيف «زراعة مائية»
   - title ML: ar/en/de
   - content ML
   - images: [URLs]
   - date: 2026-06-22
5. **حفظ** → onSave → handleCvSave.
6. **(مهم)** Site Settings → **connectServer** إن لم تكن متصلًا.
7. افتح portal agri → تبويب مقالات → **تحقق**.
8. جهاز آخر / Ctrl+Shift+N → **هل يظهر؟**
9. إن لا → JWT أو data.php.
10. **(بديل)** CSV → content-manager → data.json → SFTP.

---

> **الفصل التالي (6):** «شرح السيرفر وHostinger» — *(هذا الفصل أدناه)*

---

## الفصل السادس: شرح السيرفر وHostinger وملفات PHP وMySQL

> **هدف الفصل:** أن تفهم **أين يعيش موقعك على الإنترنت** — Hostinger، PHP، MySQL، وكيف ترفع نسخة جديدة **بدون خوف**.

---

### 1. ما معنى السيرفر؟

#### 1.1 تعريف بسيط

**السيرفر (Server)** = **جهاز كمبيوتر** (أو جزء منه) **متصل بالإنترنت 24/7** — يخدم الزوار طوال الوقت.

#### 1.2 جهازك المحلي vs السيرفر

| | **جهازك (محلي)** | **السيرفر (Hostinger)** |
|--|------------------|---------------------------|
| **العنوان** | `localhost:5173` | `https://eng-alaa.com` |
| **من يراه** | **أنت** فقط | **العالم** |
| **PHP + MySQL** | لا (إلا proxy) | **نعم** — يعملان |
| **متى يعمل** | عند `npm run dev` | **دائمًا** |

#### 1.3 ما هو Hostinger؟

**Hostinger** = شركة **استضافة (Hosting)** — تؤجرك:
- مساحة على سيرفر (`public_html/`)
- قاعدة بيانات MySQL
- تشغيل PHP

**تشبيه:** Hostinger = **مبنى تجاري** تؤجر فيه «محل» (موقعك).

#### 1.4 لماذا تحتاج Hostinger؟

- React **بعد Build** = ملفات ثابتة — تحتاج مكانًا يخدمها.
- **PHP + MySQL** = حفظ بيانات، تسجيل دخول، إحصائيات — **لا يعمل** على جهازك بدون سيرفر.

#### 1.5 ماذا يحدث عندما يفتح الزائر الموقع؟

```
1. يكتب eng-alaa.com في المتصفح
2. DNS يوجّهه لسيرفر Hostinger
3. Hostinger يرسل index.html + JS + CSS (من public_html)
4. المتصفح يشغّل React
5. App.tsx يطلب ./api/data.php
6. PHP يقرأ MySQL ويرجع JSON
7. الزائر يرى المحتوى
```

#### 1.6 أين ملفات الموقع؟ أين قاعدة البيانات؟

| ماذا | أين على Hostinger |
|------|-------------------|
| **ملفات الواجهة** | `public_html/` — index.html, assets/, api/ |
| **قاعدة البيانات** | **MySQL** — لوحة Hostinger → Databases → phpMyAdmin |
| **لا ترى DB كملف** | البيانات **داخل** MySQL — ليس ملف `.db` في public_html |

#### 1.7 شرح أعمق للمبتدئ — تخيّل «مكتبة عامة»

تخيّل أن موقعك **مكتبة عامة**:
- **الرفوف والكتب الظاهرة** = ملفات HTML/CSS/JS (الواجهة) — أي زائر يستطيع «رؤيتها».
- **غرفة الأرشيف خلف الكاونتر** = MySQL — فيها سجلات: مقالات، كتب، إعدادات.
- **موظف الاستعلامات** = PHP — الزائر لا يدخل الأرشيف؛ يطلب من الموظف فيقوم بجلب الكتاب.
- **المبنى الذي تستأجر فيه المكتبة** = Hostinger.

عندما تطور على جهازك (`npm run dev`):
- المكتبة «مؤقتة» على طاولتك — فقط أنت تراها.
- PHP غالبًا **لا يعمل محليًا** — Vite يوجّه طلبات `/api` إلى موقعك الحي على Hostinger (proxy).

#### 1.8 DNS — كيف يجد المتصفح Hostinger؟

عندما تكتب `eng-alaa.com`:
1. المتصفح يسأل **DNS** (دفتر عناوين الإنترنت): «أين يعيش هذا النطاق؟»
2. DNS يرد بـ **IP** (عنوان رقمي لسيرفر Hostinger).
3. المتصفح يتصل بذلك IP ويطلب `index.html`.

**لا تحتاج فهم DNS يوميًا** — يكفي أن تعرف: النطاق مربوط بـ Hostinger من لوحة النطاق (Domain).

---

### 2. Frontend vs Backend

#### 2.1 Frontend — ما يراه الزائر

- React + HTML + CSS
- يعمل **في المتصفح** (Chrome, Safari…)
- مجلد `src/` → بعد Build → `dist/`

#### 2.2 Backend — الجزء المخفي

- PHP في `hostinger_deploy/api/`
- يعمل **على السيرفر** — الزائر **لا يرى** كود PHP
- يتصل بـ **MySQL**

#### 2.3 كيف يتكلمان؟

```
Frontend (React)  ──fetch──►  Backend (PHP)  ──SQL──►  MySQL
                ◄──JSON───                ◄──rows───
```

| الدور | من |
|-------|-----|
| **المتصفح** | يشغّل Frontend + يرسل fetch |
| **السيرفر** | يشغّل PHP |
| **MySQL** | يخزّن البيانات |

#### 2.4 مثال من حياتك اليومية

عندما تفتح تطبيق توصيل طعام:
- **الواجهة** (قائمة المطاعم، الصور) = Frontend.
- **السيرفر** (يحفظ طلبك، يحسب السعر) = Backend.
- **قاعدة البيانات** (سجل الطلبات السابقة) = MySQL.

في مشروعك: React يعرض المقالات؛ PHP يجلبها من MySQL.

#### 2.5 لماذا نفصل Frontend عن Backend؟

1. **أمان:** كلمة مرور MySQL لا تُوضع أبدًا في React (أي زائر يستطيع قراءة JS).
2. **مركزية:** كل الزوار يرون **نفس** البيانات من DB واحدة.
3. **حفظ دائم:** إغلاق المتصفح لا يحذف المقالات — البيانات في MySQL.

---

### 3. ما معنى PHP؟

#### 3.1 لماذا PHP في المشروع؟

- **React لا يتصل بـ MySQL مباشرة** (أمان).
- **PHP** = لغة السيرفر — تقرأ/تكتب MySQL.

#### 3.2 لماذا React وحده لا يكفي؟

| React | PHP |
|-------|-----|
| يعمل في المتصفح | يعمل على السيرفر |
| لا يخزّن بيانات دائمة للجميع | يخزّن في MySQL |
| لا يتحقق من JWT بأمان | يتحقق من JWT |

#### 3.3 هل PHP يظهر للزائر؟

**لا** — الزائر يرى **نتيجة** PHP فقط: JSON مثل `{ "ok": true }`.

#### 3.4 كيف يستقبل ويرد؟

```
1. fetch('./api/data.php')  ← طلب HTTP
2. Hostinger يشغّل data.php
3. PHP يقرأ MySQL
4. echo json_encode(...)  ← response JSON
5. المتصفح ي parse JSON
```

---

### 4. ما معنى API؟

#### 4.1 تشبيه طفل

**API** = **نافذة طلبات** — تقول «أعطني القائمة» أو «احفظ هذا» — **بدون** دخول المطبخ.

#### 4.2 مجلد `api/`

```
hostinger_deploy/api/
├── data.php       ← البيانات
├── auth.php       ← تسجيل دخول
├── analytics.php  ← إحصائيات
├── upload.php     ← رفع ملفات
├── middleware.php ← JWT
└── config.php     ← أسرار DB (على السيرفر فقط)
```

#### 4.3 Request و Response

| | **Request** | **Response** |
|--|-------------|--------------|
| **معنى** | طلب من المتصفح | رد من السيرفر |
| **مثال** | `GET /api/data.php` | `{ "name": {...}, "agriArticles": [...] }` |
| **JSON response** | — | نص `{ "key": "value" }` يفهمه JavaScript |

**مثال حياتي:** تطلب قهوة (request) → الباريستا يعطيك فنجان (response).

---

### 5. شرح مجلد `hostinger_deploy/`

#### 5.1 لماذا موجود؟

**ليس** للتطوير اليومي — **مخصص للرفع على Hostinger** + PHP + PDF + vendor.

#### 5.2 هل هو نفس `src/`؟

**لا:**

| | `src/` | `hostinger_deploy/` |
|--|--------|---------------------|
| **لغة** | TypeScript/React | PHP + SQL + vendor |
| **يُرفع؟** | **لا** — يُبنى إلى dist | **نعم** (جزء منه) |
| **الغرض** | تطوير | إنتاج + سيرفر |

#### 5.3 ماذا يحتوي؟

| محتوى | الدور |
|-------|-------|
| `api/` | نقاط API |
| `schema.sql` | مخطط MySQL |
| `vendor/` | مكتبات PHP (dompdf, ar-php…) |
| `ai_proxy.php` | وسيط AI |
| `generate_report.php` | تقارير PDF |
| `reports/` | مجلد حفظ تقارير |
| `config.sample.php` | مثال إعدادات |

#### 5.4 ما يُرفع وما لا يُرفع

| ✅ يُرفع (عبر hostinger_upload) | ❌ لا يُرفع |
|--------------------------------|-------------|
| محتويات `dist/` (React built) | `src/` |
| `api/*.php` (عدا config من Git) | `node_modules/` |
| `vendor/` | `.git/` |
| `ai_proxy.php`, PDF scripts | `package.json` (اختياري) |
| `data.json` (من public) | ملفات التطوير |

**`api/config.php`:** تُنشئه **يدويًا** على السيرفر — **لا** من Git.

#### 5.5 ملفات PHP إضافية في المشروع

| الملف | الوظيفة | متى تحتاجه |
|-------|---------|------------|
| `api/upload.php` | رفع صور/ملفات من لوحة التحكم | عند رفع وسائط |
| `api/img-proxy.php` | جلب صور خارجية عبر السيرفر | تجاوز CORS للصور |
| `api/reset_pass.php` | إعادة تعيين كلمة مرور admin | مرة عند الإعداد |
| `api/storage_info.php` | معلومات مساحة التخزين | تشخيص |
| `api/generate_customer_report.php` | تقارير PDF للعملاء | portal الزراعة |
| `ai_proxy.php` | وسيط لـ Gemini AI | تشخيص AI |
| `generate_report.php` | تقارير PDF عامة | مشاركة تقارير |
| `vendor/` | dompdf + ar-php | توليد PDF عربي |

**`config.sample.php`** في الجذر = مثال لإعدادات Gemini — **ليس** بيانات MySQL.

---

### 6. شرح `data.php` بالتفصيل

#### 6.1 أين مكانه؟

`hostinger_deploy/api/data.php` → على السيرفر: `public_html/api/data.php`

#### 6.2 الوظيفة الرئيسية

| الطلب | ماذا يفعل |
|-------|-----------|
| `GET` | **يجلب** كل AppData من MySQL |
| `GET ?test` | **تشخيص** اتصال DB |
| `POST` + JWT | **يحفظ** AppData في MySQL |

#### 6.3 عندما يطلب App.tsx البيانات

```tsx
fetch('./api/data.php', { method: 'GET' })
```
→ `buildAppData($pdo)` → SELECT من كل الجداول → JSON واحد → `loadAppDataFromDb()`

#### 6.4 عندما تضغط «حفظ» في لوحة التحكم

```tsx
POST ./api/data.php
Authorization: Bearer <JWT>
Body: { ...كل AppData... }
```
→ `requireAuth()` → `syncAppData($pdo, $body)` → UPDATE/INSERT جداول → `{ "ok": true }`

#### 6.5 كيف يعرف أين يحفظ؟

`syncAppData` يوزّع الحقول:

```php
syncSiteSettings($db, $d);
syncAgriArticles($db, $d['agriArticles']);
syncGfxCategories($db, $d['gfxCategories']);
// ... إلخ
```

#### 6.6 أجزاء مهمة في data.php

| القسم / الدالة | الوظيفة | لو تعطّل |
|----------------|---------|----------|
| `require config.php` | اتصال PDO | 500 — «config not found» |
| `jd()` / `je()` | فك/ترميز JSON من DB | بيانات ML تالفة |
| `readMLField()` | قراءة ar/en/de | نصوص فارغة |
| `buildAppData()` | GET — تجميع JSON | لا تحميل من DB |
| `buildNodeTree()` | شجرة المكتبة | مكتبة مسطحة |
| `syncAppData()` | POST — حفظ كامل | لا مزامنة |
| `syncSiteSettings()` | إعدادات + `is_seeded=1` | DB «غير مهيّأ» |
| `register_shutdown_function` | أخطاء PHP → JSON | صفحة بيضاء بدل JSON |

**نجاح POST:** `{ "ok": true }`  
**فشل:** `{ "ok": false, "error": "..." }` + HTTP 500

#### 6.7 شرح كل دالة مهمة في data.php (بالتفصيل)

> **ملاحظة:** لا تحتاج تعديل هذه الدوال كمبتدئ — فقط **افهم** ماذا تفعل لو احتجت تشخيص مشكلة.

##### أ) دوال الترميز والقراءة (JSON + لغات)

| الدالة | الوظيفة | لماذا نحتاجها | لو تعطّل | مثال |
|--------|---------|---------------|----------|------|
| **`jd()`** | **J**son **D**ecode — يحوّل نص JSON من DB إلى مصفوفة PHP | حقول ar/en/de مخزّنة كـ JSON في MySQL | العناوين تظهر فارغة أو `null` | `jd('{"ar":"مرحبا"}')` → `['ar'=>'مرحبا']` |
| **`je()`** | **J**son **E**ncode — يحوّل مصفوفة PHP إلى نص للحفظ | عند POST نحفظ ML كـ JSON | فشل الحفظ أو بيانات تالفة | `je(['ar'=>'x'])` → `'{"ar":"x"}'` |
| **`cleanMLPart()`** | ينظّف نصًا واحدًا من قيم غريبة مثل `"Array"` | بيانات قديمة من إصدارات سابقة | نصوص غريبة في الواجهة | يحوّل `"Array"` → `""` |
| **`readMLField()`** | يقرأ حقل ML من DB (JSON أو نص قديم) ويرجع `{ar,en,de}` | كل العناوين ثلاثية اللغة | مقال يظهر بلغة واحدة فقط | — |
| **`writeMLField()`** | يجهّز ML للكتابة في DB | عند syncSiteSettings للاسم والbio | — | — |
| **`genId()`** | يولّد id فريدًا (UUID بسيط) | عناصر جديدة بدون id | تعارض ids | — |

##### ب) دوال القراءة (GET — buildAppData)

| الدالة | الوظيفة | لماذا | لو تعطّل | مثال |
|--------|---------|-------|----------|------|
| **`buildAppData()`** | **قلب GET** — SELECT من كل الجداول ويجمع كائن AppData واحد | App.tsx يتوقع شكلًا واحدًا للبيانات | الموقع فارغ أو fallback لـ data.json | يرجع `{ name, agriArticles, gfxCategories, ... }` |
| **`buildNodeTree()`** | يحوّل صفوف `library_nodes` المسطّحة إلى **شجرة** متداخلة | الواجهة تعرض مجلدات داخل مجلدات | المكتبة تظهر مسطّحة بدون فروع | parent_id → children[] |
| **`buildGfxCategories()`** | يجمع gfx_categories + subcategories + items | portal التصاميم | تصاميم ناقصة | — |
| **`buildCvDocs()`** | يجمع cv_docs + cv_sections | السيرة الذاتية | CV فارغ | — |
| **`gfxItemFromMeta()` / `gfxItemMetaToJson()`** | حقول إضافية للتصاميم في عمود meta | glbUrl, videoUrl… | روابط 3D مفقودة | — |
| **`ensureGfxItemMetaColumn()`** | يضيف عمود meta إن لم يكن موجودًا (migration خفيف) | قواعد قديمة | — | — |
| **`defaultCvProfile()`** | قيم افتراضية للسيرة | CV جديد | — | — |

##### ج) دوال الحفظ (POST — syncAppData)

| الدالة | الوظيفة | الجدول | لو تعطّل |
|--------|---------|--------|----------|
| **`syncAppData()`** | تبدأ **transaction** — تستدعي كل sync* — ثم commit أو rollback | كل الجداول | **لا يُحفظ شيء** — rollback |
| **`syncSiteSettings()`** | إعدادات الموقع + **is_seeded=1** | `site_settings` | DB «غير مهيّأ» دائمًا |
| **`syncPersonalInfo()`** | هاتف، email، صورة | `personal_info` | Footer تواصل فارغ |
| **`syncSkills()`** | المهارات | `skills` | — |
| **`syncArticleCategories()`** | تصنيفات المقالات | `article_categories` | — |
| **`syncAgriArticles()`** | المقالات الزراعية | `agri_articles` | مقالات لا تُحفظ |
| **`syncLibraryTree()` + `flattenTree()`** | شجرة المكتبة → صفوف nodes | `library_nodes` | شجرة تتكسّر |
| **`syncLibraryBooks()`** | الكتب | `library_books` | — |
| **`syncAgriVideos()`** | فيديوهات | `agri_videos` | — |
| **`syncPublicReports()`** | تقارير عامة | `public_reports` | — |
| **`syncGfxCategories()`** | تصنيفات + فروع + items | `gfx_*` | — |
| **`syncCodeSnippets()`** | مختبر الأكواد | `code_snippets` | — |
| **`syncWebProjects()`** | مشاريع برمجية | `web_projects` | — |
| **`syncCvDocs()`** | السيرة | `cv_docs`, `cv_sections` | — |
| **`syncSoilAnalysis()`** | تحليل التربة | `soil_analysis` | — |
| **`syncReportTemplate()`** | قالب التقارير | `report_templates` | — |
| **`syncCustomerReports()`** | تقارير العملاء | `customer_reports` | — |
| **`syncAiVault()`** | خزنة AI | `ai_vault` | — |
| **`syncInjectedPages()`** | صفحات HTML محقونة | `injected_pages` | — |

##### د) رأس الملف — قبل الدوال

| القسم | الوظيفة | لو تعطّل |
|-------|---------|----------|
| **`register_shutdown_function`** | أي fatal error → JSON بدل صفحة PHP بيضاء | صعب تشخيص الأخطاء |
| **CORS headers** | يسمح لـ fetch من نفس الموقع (وOPTIONS) | مشاكل preflight نادرة |
| **`require config.php`** | PDO + JWT_SECRET | 500 فورًا |
| **GET `?test`** | تشخيص: pdo_ok, tables, jwt_set | — |
| **POST `requireAuth()`** | يتحقق من JWT قبل الحفظ | 401 Unauthorized |

#### 6.8 سيناريو كامل: زائر يفتح الموقع

1. `App.tsx` في `useEffect` يستدعي `loadAppDataFromDb()`.
2. `fetch('./api/data.php')` — طلب GET.
3. Hostinger ينفّذ `data.php` → `buildAppData($pdo)`.
4. PHP يقرأ `site_settings`, `agri_articles`, … ويبني JSON ضخم.
5. المتصفح يستلم JSON → `setData(...)` → React يعيد الرسم.
6. إن فشل DB → ي fallback إلى `data.json` أو localStorage (حسب appData.ts).

#### 6.9 سيناريو كامل: مدير يحفظ من لوحة التحكم

1. المدير يعدّل مقالًا في `ContentAdmin` → `onSave`.
2. `App.tsx` → `handleCvSave` → `saveAppData(data)`.
3. `fetch('./api/data.php', { method:'POST', headers:{ Authorization:'Bearer '+token }, body: JSON.stringify(data) })`.
4. `requireAuth()` يتحقق من JWT.
5. `syncAppData` يوزّع البيانات على الجداول داخل transaction.
6. `{ "ok": true }` → toast نجاح في الواجهة.

---

### 7. شرح `auth.php`

#### 7.1 الوظيفة

**تسجيل دخول المدير للسيرفر** — إصدار **JWT token**.

#### 7.2 هل يحفظ كلمة المرور؟

**لا في الملف** — كلمة المرور **مُشفّرة (hash)** في جدول `admin_users` في MySQL.

#### 7.3 التحقق

```php
password_verify($password, $user['password_hash'])
```

#### 7.4 كلمة مرور صحيحة

→ `{ "token": "...", "role": "...", "displayName": "..." }`  
→ المتصفح: `setApiToken(token)`

#### 7.5 كلمة مرور خاطئة

→ HTTP **401** + `{ "error": "Invalid username or password" }`

#### 7.6 هل آمن كبداية؟

**كافٍ لموقع شخصي** — JWT + password_hash.  
**للتحسين لاحقًا:** rate limiting، 2FA، إزالة ADMIN_* من Frontend، secrets في env فقط.

#### 7.7 شرح middleware.php (مرتبط بـ auth)

| الدالة | الوظيفة |
|--------|---------|
| **`createToken()`** | يبني JWT: payload (id, role, exp) + توقيع HMAC بـ JWT_SECRET |
| **`requireAuth()`** | يقرأ `Authorization: Bearer …` — يتحقق من التوقيع والانتهاء |

**JWT** = «تذكرة دخول رقمية» — صالحة ~24 ساعة (86400 ثانية).  
**لا تُخزّن** في auth.php — تُرسل للمتصفح مرة واحدة عند login.

#### 7.8 خطوات تسجيل الدخول للسيرفر (connectServer)

```
1. المدير يدخل username + password في Site Settings
2. fetch POST ./api/auth.php  { username, password }
3. auth.php يبحث في admin_users
4. password_verify → createToken()
5. React: setApiToken(token) — يُحفظ في الذاكرة/localStorage
6. كل POST لـ data.php يرسل: Authorization: Bearer <token>
```

---

### 8. شرح `analytics.php`

#### 8.1 الوظيفة

| | |
|--|--|
| **POST track** | أي زائر — يسجّل زيارة/heartbeat/تنزيل |
| **GET stats** | مدير + JWT — إحصائيات كاملة |
| **POST delete/reset** | مدير — حذف/تصفير |

#### 8.2 الربط مع AnalyticsDashboard

```tsx
fetchAnalyticsStats(getApiToken())
// GET ./api/analytics.php?action=stats
```

#### 8.3 أين تُحفظ؟

- `analytics_sessions`
- `analytics_events`
- `ip_geo_cache`

#### 8.4 لو لم يعمل

- الموقع **يعمل** — track **silent fail**
- لوحة الإحصائيات **فارغة**

#### 8.5 ما البيانات التي يستقبلها track؟

```json
{
  "action": "track",
  "sessionId": "uuid-...",
  "event": "page_view",
  "path": "/",
  "label": "home",
  "meta": { "timezone": "...", "gps": {...} }
}
```

**أنواع events:** `page_view`, `cv_download`, `file_download`, `heartbeat`

#### 8.6 ما يرجعه stats للمدير؟

GET `./api/analytics.php?action=stats` + JWT → JSON فيه:
- عدد الجلسات والزيارات
- توزيع الدول والمدن
- أحداث تنزيل CV
- بيانات GPS (إن وافق الزائر)

#### 8.7 ensureAnalyticsTables

عند أول track — analytics.php **ينشئ الجداول تلقائيًا** إن لم تكن موجودة (بديل جزئي لـ schema.sql للإحصائيات فقط).

---

### 9. شرح `schema.sql`

#### 9.1 مصطلحات

| المصطلح | معنى | مثال |
|---------|------|------|
| **SQL** | لغة أوامر قواعد البيانات | `SELECT`, `CREATE TABLE` |
| **MySQL** | برنامج قاعدة البيانات | على Hostinger |
| **database** | «مخزن» كامل | `u123456_portfolio` |
| **table** | جدول | `agri_articles` |
| **column** | عمود | `title`, `category_id` |
| **row** | صف | مقال واحد |
| **schema** | **مخطط** الجداول | `schema.sql` |

#### 9.2 كيف نستخدمه؟

1. Hostinger → **phpMyAdmin**
2. اختر قاعدة البيانات
3. **Import** → ارفع `schema.sql`
4. **Run** — تُنشأ الجداول

#### 9.3 الجداول الرئيسية

| الجدول | الوظيفة | أعمدة مهمة |
|--------|---------|------------|
| `admin_users` | حسابات المدير | username, password_hash, role |
| `site_settings` | إعدادات الموقع | logo_type, theme_mode, **is_seeded** |
| `skills` | المهارات | name, percent, icon |
| `article_categories` | تصنيفات مقالات | id, name (JSON) |
| `agri_articles` | المقالات | title, content, images, category_id |
| `library_nodes` | شجرة المكتبة | parent_id, name |
| `library_books` | الكتب | node_id, drive_url, is_paid |
| `gfx_categories` | تصنيفات تصاميم | id, name, icon |
| `gfx_subcategories` | فروع | category_id, name |
| `gfx_items` | مشاريع | title, main_img, glb_url |
| `web_projects` | مشاريع برمجية | title, live_url, github_url |
| `code_snippets` | مختبر أكواد | code_html, code_css |
| `cv_docs` / `cv_sections` | السيرة | sections JSON |
| `personal_info` | تواصل | phone, email, photo |
| `uploaded_files` | ملفات مرفوعة | url, folder |
| `analytics_sessions` | جلسات زوار | ip, country, gps_* |
| `analytics_events` | أحداث | event_type, path |

**جداول إضافية في schema.sql:**

| الجدول | الوظيفة |
|--------|---------|
| `agri_videos` | فيديوهات قسم الزراعة |
| `public_reports` | تقارير PDF عامة للزوار |
| `soil_analysis` | سجل تحليل التربة |
| `customer_reports` | تقارير عملاء (PDF) |
| `report_template` | قالب HTML/CSS للتقارير |
| `cv_docs` / `cv_sections` | مستندات السيرة وأقسامها |
| `uploaded_files` | سجل الملفات المرفوعة عبر upload.php |
| `ai_vault` | عناصر خزنة AI |
| `injected_pages` | صفحات HTML مخصصة |
| `ip_geo_cache` | cache لموقع IP (تسريع analytics) |

**حقل مهم:** `site_settings.is_seeded` — يصبح `1` بعد أول حفظ ناجح من لوحة التحكم. قبلها قد يعتمد الموقع على data.json.

---

### 10. MySQL على Hostinger

#### 10.1 أين تنشئ DB؟

**hPanel** → **Databases** → **MySQL Databases** → Create

#### 10.2 معلومات الاتصال

| معلومة | مثال | سرّية |
|--------|------|--------|
| **DB_HOST** | `localhost` أو host Hostinger | ⚠️ |
| **DB_NAME** | `u123456_dbname` | ⚠️ |
| **DB_USER** | `u123456_user` | ⚠️ |
| **DB_PASS** | كلمة مرور قوية | ⚠️⚠️ |

**لا تشاركها** — من يملكها يقرأ/يحذف كل البيانات.

#### 10.3 أين توضع؟

في **`public_html/api/config.php`** (تُنشأ يدويًا — **لا** في Git):

```php
define('DB_HOST', 'localhost');
define('DB_NAME', '...');
define('DB_USER', '...');
define('DB_PASS', '...');
define('JWT_SECRET', '...');  // 64+ حرف عشوائي
```

#### 10.4 إذا معلومات خاطئة

- `GET ?test` → `pdo_ok: false`
- `data.php` → 500
- لوحة التحكم → «فشل الاتصال»

---

### 11. شرح `scripts/prepare-hostinger.mjs`

#### 11.1 الوظيفة

يجمع **`dist/`** + **`hostinger_deploy/`** → **`hostinger_upload/`** — مجلد **جاهز للرفع**.

#### 11.2 الخطوات

```
1. يتحقق dist/ موجود (بعد vite build)
2. يحذف hostinger_upload/ القديم
3. ينسخ hostinger_deploy/ → hostinger_upload/
   (يستثني api/config.php)
4. ينسخ dist/ → hostinger_upload/
5. ينشئ reports/
6. يطبع: «ارفع محتويات hostinger_upload/ إلى public_html/»
```

#### 11.3 متى تشغّله؟

```bash
npm run build:hostinger
```

**لا يبني وحده** — يشغّل `vite build` **ثم** السكربت.

#### 11.4 لو لم تشغّله

- لديك `dist/` فقط — **بدون** PHP/api/vendor.

---

### 12. `package.json` — أوامر النشر

| الأمر | ماذا يفعل | متى |
|-------|-----------|-----|
| `npm install` | يثبت المكتبات | أول مرة / بعد pull |
| `npm run dev` | Vite محلي :5173 | **تجربة** يومية |
| `npm run build` | يبني → `dist/` | قبل نشر |
| `npm run build:hostinger` | build + `hostinger_upload/` | **نشر** |
| `npm run serve` | معاينة dist محليًا | اختبار Build |
| `npm run typecheck` | فحص TypeScript | قبل commit |

| للتجربة | للنشر | نادر |
|---------|-------|------|
| dev, serve | build:hostinger | typecheck |

---

### 13. `vite.config.ts` — النشر

#### 13.1 ما هو Vite؟

أداة **بناء وتشغيل** React — سريعة للتطوير.

#### 13.2 `build`

```ts
build: {
  outDir: "dist",
  emptyOutDir: true,
}
```

يحوّل `src/` → ملفات JS/CSS **محسّنة** في `dist/`.

#### 13.3 `base: "/"`

المسارات من **جذر** الموقع — `eng-alaa.com/assets/...`  
إذا `base` خاطئ → **404** للصور والـ JS.

#### 13.4 Proxy محلي

```ts
proxy: { "/api": phpProxy }  // → eng-alaa.com
```

**Vite لا يشغّل PHP** — يوجّه `/api` للسيرفر الحي.

---

### 14. رحلة فتح الموقع المنشور

```
الزائر → eng-alaa.com
    ↓
Hostinger → public_html/index.html + assets/*.js
    ↓
المتصفح → يشغّل React
    ↓
main.tsx → App.tsx
    ↓
useEffect → GET ./api/data.php
    ↓
data.php → buildAppData() → SELECT MySQL
    ↓
JSON → setData()
    ↓
الزائر يرى: مقالات، تصاميم، كتب…
```

**خطوة بخطوة:**

1. **DNS:** يربط `eng-alaa.com` بـ IP سيرفر Hostinger.
2. **index.html:** أول ملف — يحمّل React من `assets/`.
3. **React SPA:** صفحة واحدة ديناميكية داخل `#root`.
4. **main.tsx → App.tsx:** تشغيل التطبيق.
5. **GET data.php:** جلب AppData من MySQL.
6. **JSON → setData:** عرض المحتوى للزائر.
7. **analytics track (اختياري):** تسجيل زيارة — لا يوقف الموقع إن فشل.

---

### 15. رحلة حفظ من لوحة التحكم

```
المدير → Footer → لوحة التحكم
    ↓
handleAdminLogin (محلي) → adminLoggedIn
    ↓
Site Settings → connectServer → auth.php → JWT
    ↓
ContentAdmin → تعديل → onSave(partial)
    ↓
handleCvSave → saveAppData
    ↓
POST data.php + Bearer JWT
    ↓
syncAppData → MySQL COMMIT
    ↓
{ "ok": true }
    ↓
زائر جديد → GET → يرى التعديل
```

**خطوة بخطوة:**

1. **Footer → لوحة التحكم:** يفتح overlay الإدارة.
2. **handleAdminLogin:** تحقق **محلي** من ADMIN_EMAIL/PASSWORD — `adminLoggedIn=true` (هذا **ليس** JWT بعد).
3. **connectServer:** المدير يدخل username/password **السيرفر** → auth.php → JWT.
4. **ContentAdmin:** تعديل حقول → `onSave(partial)` يرسل جزءًا من AppData.
5. **handleCvSave:** يدمج partial في `data` state الكامل.
6. **saveAppData:** POST كامل (أو merged) إلى data.php مع Bearer token.
7. **syncAppData:** transaction — كل الجداول تُحدَّث معًا.
8. **{ ok: true }:** toast — البيانات الآن في MySQL.
9. **زائر آخر:** GET جديد — يرى التعديل (لا يعتمد على localStorage).

**مهم:** حفظ على السيرفر **يتطلب** JWT — الدخول المحلي للوحة وحده **لا يكفي** للمزامنة.

---

### 16. محلي vs منشور

| | `npm run dev` | Hostinger |
|--|---------------|-----------|
| **URL** | localhost:5173 | eng-alaa.com |
| **PHP** | proxy للسيرفر | مباشر |
| **MySQL** | سيرفر Hostinger | نفس DB |
| **src/** | حي — hot reload | **لا** — dist فقط |

**لماذا يعمل محليًا ولا على السيرفر؟**
- نسيت رفع `api/`
- `config.php` ناقص
- schema.sql لم يُشغَّل
- مسارات `base` خاطئة

#### 16.1 localhost vs الرابط الحقيقي

| localhost:5173 | https://eng-alaa.com |
|----------------|----------------------|
| Vite dev server على جهازك | Apache/Nginx على Hostinger |
| Hot reload — تغيير فوري | تحتاج build + رفع |
| `/api` → proxy للسيرفر الحي | `/api` → PHP مباشر |

#### 16.2 لماذا PHP لا يعمل داخل Vite؟

**Vite = Node.js** — يفهم JavaScript فقط، **لا PHP**.  
لذلك `vite.config.ts` يوجّه `/api` إلى `https://eng-alaa.com` أثناء التطوير.

يمكن تغيير الهدف عبر `.env`:
```
VITE_PHP_PROXY_TARGET=https://eng-alaa.com
```

#### 16.3 لماذا MySQL تحتاج إعدادات على السيرفر؟

MySQL **برنامج منفصل** على Hostinger — PHP يحتاج:
- اسم DB صحيح
- user + password مربوطين بـ DB
- جداول من schema.sql

بدون هذا trio — `pdo_ok: false`.

---

### 17. ما الملفات التي أرفعها؟

#### 17.1 بعد `npm run build:hostinger`

**المجلد:** `hostinger_upload/`  
**ارفع كل محتوياته** إلى `public_html/`

#### 17.2 هيكل على السيرفر

```
public_html/
├── index.html          ← من dist
├── assets/             ← JS/CSS من dist
├── data.json           ← من public (اختياري)
├── api/
│   ├── data.php
│   ├── auth.php
│   ├── analytics.php
│   ├── upload.php
│   ├── middleware.php
│   └── config.php      ← يدوي — سري
├── vendor/             ← PDF
├── ai_proxy.php
├── generate_report.php
└── reports/
```

#### 17.3 أسئلة شائعة

| ملف | أرفعه؟ |
|-----|--------|
| `src/` | **لا** |
| `node_modules/` | **لا** |
| `schema.sql` | **لا** إلى public_html — **phpMyAdmin فقط** |
| `package.json` | **لا** (ليس للزوار) |

---

### 18. أخطاء شائعة — Hostinger (تفصيل لكل خطأ)

#### 18.1 صفحة بيضاء بعد الرفع

| | |
|--|--|
| **السبب** | JS لم يُحمَّل (مسار خاطئ)، أو خطأ runtime في Console |
| **الفحص** | F12 → Console — ابحث عن 404 لـ `assets/*.js` |
| **الحل** | `npm run build:hostinger` من جديد؛ تأكد `base: "/"` في vite.config؛ ارفع كل `assets/` |

#### 18.2 الصور لا تظهر

| | |
|--|--|
| **السبب** | URL نسبي خاطئ، أو صورة على Drive محجوبة |
| **الفحص** | Network tab — status 404 للصورة |
| **الحل** | استخدم URLs كاملة؛ أو upload.php ثم استخدم الرابط المُرجَع |

#### 18.3 الروابط لا تعمل (404)

| | |
|--|--|
| **السبب** | React Router يحتاج `.htaccess` أو كل المسارات → index.html |
| **الفحص** | افتح `/some-path` مباشرة |
| **الحل** | تأكد إعداد Hostinger لـ SPA (أو افتح من الصفحة الرئيسية فقط) |

#### 18.4 data.php يرجع خطأ 500

| | |
|--|--|
| **السبب** | `config.php` ناقص، أو بيانات DB خاطئة، أو جداول غير موجودة |
| **الفحص** | افتح `https://eng-alaa.com/api/data.php?test` |
| **الحل** | أنشئ config.php؛ import schema.sql؛ تحقق DB_USER/DB_PASS |

#### 18.5 تسجيل الدخول للسيرفر لا يعمل

| | |
|--|--|
| **السبب** | لا مستخدم في `admin_users`، أو hash خاطئ |
| **الفحص** | phpMyAdmin → SELECT * FROM admin_users |
| **الحل** | INSERT user مع password_hash من reset_pass.php أو BACKEND_DOCS |

#### 18.6 التعديلات لا تُحفظ / لا تظهر للزوار

| | |
|--|--|
| **السبب** | لا JWT (لم connectServer)، أو POST 401 |
| **الفحص** | Network → POST data.php — status 401؟ |
| **الحل** | Site Settings → اتصل بالسيرفر → auth ناجح → احفظ مجددًا |

#### 18.7 MySQL لا يتصل

| | |
|--|--|
| **السبب** | DB_HOST غالبًا `localhost` على Hostinger — لكن NAME/USER/PASS خطأ |
| **الفحص** | ?test → pdo_ok: false, error message |
| **الحل** | انسخ القيم **بالضبط** من hPanel → MySQL |

#### 18.8 خطأ 404 لـ /api/data.php

| | |
|--|--|
| **السبب** | مجلد `api/` غير مرفوع أو في مسار خاطئ |
| **الفحص** | File Manager → public_html/api/ موجود؟ |
| **الحل** | ارفع من hostinger_upload/api/ |

#### 18.9 خطأ 500 عام (Internal Server Error)

| | |
|--|--|
| **السبب** | syntax PHP، أو vendor ناقص، أو صلاحيات |
| **الفحص** | Hostinger error logs؛ ?test |
| **الحل** | أعد رفع api/ كاملًا؛ تحقق PHP version ≥ 8 |

#### 18.10 مشكلة CORS

| | |
|--|--|
| **السبب** | طلب من domain مختلف (نادر على نفس eng-alaa.com) |
| **الفحص** | Console: CORS blocked |
| **الحل** | محليًا: vite proxy؛ على السيرفر: headers موجودة في PHP |

#### 18.11 اختلاف المسارات محلي vs سيرفر

| | |
|--|--|
| **السبب** | `base` في vite ≠ مسار النشر الفعلي |
| **الفحص** | compare asset URLs محلي vs منشور |
| **الحل** | `base: "/"` للجذر |

#### 18.12 نسيان رفع api أو vendor

| | |
|--|--|
| **السبب** | رفع dist فقط |
| **الفحص** | public_html فيه api/data.php؟ |
| **الحل** | استخدم `build:hostinger` وليس `build` فقط |

#### 18.13 نسيان schema.sql

| | |
|--|--|
| **السبب** | DB فارغة — لا جداول |
| **الفحص** | ?test → tables: [] |
| **الحل** | phpMyAdmin → Import schema.sql |

---

### 19. الأمان الأساسي

| قاعدة | لماذا |
|-------|--------|
| **config.php سري** | DB + JWT |
| **HTTPS** | تشفير الطلبات |
| **لا كلمات مرور في Git** | أي من يرى Repo يخترق |
| **غيّر admin password** | بعد أول نشر |
| **JWT قوي** | 64+ حرف عشوائي |

**HTTPS** = قفل 🔒 في المتصفح — `https://` وليس `http://`.

---

### 20. ماذا تفعل بأمان كمبتدئ؟

#### ✅ آمن

- `npm run build:hostinger`
- رفع `hostinger_upload/` عبر File Manager / SFTP
- import `schema.sql` في phpMyAdmin
- اختبار `data.php?test`
- تعديل محتوى من لوحة التحكم

#### ❌ لا تلمس الآن

- دوال PDO في config
- `syncAppData` / `buildAppData`
- أسماء الجداول والأعمدة
- `middleware.php` JWT logic
- `vite.config.ts` بدون فهم

---

### 21. تشبيه: المطعم

| جزء | في المشروع |
|-----|------------|
| **صالة المطعم** | Frontend — ما يراه الزبون |
| **النادل** | React / App.tsx |
| **مدير الصالة** | App.tsx orchestration |
| **شباك الطلبات** | API (`/api/*`) |
| **موظف الشباك** | PHP (data.php, auth.php) |
| **المخزن** | MySQL |
| **المبنى** | Hostinger |
| **مخطط المخزن** | schema.sql |
| **تجهيز الصناديق** | prepare-hostinger.mjs |
| **دفتر المبيعات** | analytics.php |

---

### 22. نهاية الفصل

#### 22.1 ملخص

1. **Hostinger** = مكان الموقع + MySQL على الإنترنت.
2. **Frontend** = React في المتصفح — **Backend** = PHP + MySQL.
3. **data.php** = GET جلب + POST حفظ.
4. **auth.php** = JWT — **analytics.php** = track + stats.
5. **schema.sql** = إنشاء الجداول — مرة في phpMyAdmin.
6. **build:hostinger** → `hostinger_upload/` → `public_html/`.
7. **config.php** سري — على السيرفر فقط.

#### 22.2 جدول ملفات السيرفر

| الملف | الوظيفة |
|-------|---------|
| `data.php` | GET/POST AppData ↔ MySQL |
| `auth.php` | login → JWT |
| `analytics.php` | track + stats |
| `middleware.php` | verify JWT |
| `upload.php` | رفع وسائط |
| `config.php` | DB + JWT secrets |
| `schema.sql` | CREATE TABLE |
| `prepare-hostinger.mjs` | تجميع hostinger_upload |
| `vite.config.ts` | build + proxy |
| `ai_proxy.php` | AI backend |
| `generate_report.php` | PDF |

#### 22.3 جدول أوامر npm

| الأمر | الوظيفة |
|-------|---------|
| npm install | تثبيت |
| npm run dev | تطوير محلي |
| npm run build | dist/ |
| npm run build:hostinger | dist + PHP جاهز |
| npm run serve | معاينة dist |
| npm run typecheck | فحص TS |

#### 22.4 مخطط React ↔ PHP ↔ MySQL

```
┌─────────────┐   fetch JSON    ┌─────────────┐   SQL    ┌─────────────┐
│   React     │ ◄──────────────►│    PHP      │◄────────►│   MySQL     │
│  App.tsx    │  /api/data.php │  data.php   │          │  Hostinger  │
│ ContentAdmin│  /api/auth.php │  auth.php   │          │   DB        │
│ Analytics   │  /api/analytics│ analytics   │          │             │
└─────────────┘                 └─────────────┘          └─────────────┘
       ▲
       │ dist/ (بعد vite build)
       │
  Hostinger public_html/
```

#### 22.5 أهم 10 أشياء

1. **public_html** = جذر الموقع على Hostinger.
2. **لا ترفع src/** — ارفع **hostinger_upload/**.
3. **config.php** يدوي — سري.
4. **schema.sql** في phpMyAdmin — ليس public_html.
5. **GET data.php** عام — **POST** يحتاج JWT.
6. **Vite proxy** محلي — PHP على Hostinger.
7. **is_seeded=1** بعد أول مزامنة ناجحة.
8. **?test** لتشخيص DB.
9. **HTTPS** إلزامي للإنتاج.
10. **data.json** تحديث منفصل — SFTP بدون build.

#### 22.6 أخطاء وحلول (مرجع)

| # | مشكلة | حل |
|---|--------|-----|
| 1 | 404 api | ارفع api/ |
| 2 | 500 data | config + schema |
| 3 | لا حفظ | JWT auth |
| 4 | بيضاء | Console + rebuild |
| 5 | stats فارغ | analytics.php |

#### 22.7 أسئلة تدريبية

1. ما الفرق localhost و eng-alaa.com؟
2. لماذا React لا يتصل MySQL مباشرة؟
3. ماذا يفعل GET vs POST في data.php؟
4. أين config.php؟
5. ما hostinger_upload؟
6. هل نرفع schema.sql لـ public_html؟
7. ما دور middleware.php؟
8. لماذا Vite proxy؟
9. ما is_seeded؟
10. فرق build و build:hostinger؟

**تفكير:** رفعت dist بدون api — ماذا يرى الزائر؟

#### 22.8 تمرين نظري: رفع نسخة جديدة

**الهدف:** نشر تحديث واجهة + PHP — **بدون تنفيذ**.

1. **محلي:** `npm run typecheck` (اختياري).
2. **محلي:** `npm run build:hostinger`.
3. **تحقق:** مجلد `hostinger_upload/` موجود.
4. **تحقق:** فيه `index.html`, `assets/`, `api/data.php`, `vendor/`.
5. **Hostinger:** File Manager أو SFTP → `public_html/`.
6. **نسخ احتياطي:** حمّل نسخة من public_html القديم (اختياري).
7. **ارفع:** **كل** محتويات hostinger_upload/ (استبدال).
8. **لا تمسح:** `api/config.php` الموجود على السيرفر!
9. **اختبار:** `https://eng-alaa.com/api/data.php?test`.
10. **اختبار:** افتح الموقع Ctrl+F5.
11. **لوحة تحكم:** connectServer → عدّل → احفظ → تحقق من جهاز آخر.
12. **محتوى فقط:** إن غيّرت data.json فقط → SFTP data.json — **بدون** build.

---

> **الفصل التالي (7):** «شرح React و Components» — *(هذا الفصل أدناه)*

---

## الفصل السابع: شرح React و Components — كيف بُنيت واجهة الموقع من قطع صغيرة؟

> **هدف الفصل:** أن تفهم **كيف تُبنى الواجهة** — React، المكوّنات، props، state، events — بأسلوب مبتدئ تمامًا.

---

### 1. ما معنى React؟

#### 1.1 تعريف بسيط جدًا

**React** = مكتبة JavaScript (مكتوبة بلغة TypeScript في مشروعنا) تبني **واجهة المستخدم** بطريقة **تفاعلية**.

**تشبيه:** HTML العادي = لوحة إعلانات **ثابتة** على الحائط.  
React = شاشة **رقمية** تتغير فورًا عندما تضغط أو تكتب — **بدون** إعادة تحميل الصفحة من السيرفر.

#### 1.2 لماذا React بدل HTML عادي؟

| HTML عادي | React |
|-----------|-------|
| تعدّل المحتوى يدويًا في كل مكان | تعدّل **مرة** في state → React يحدّث الشاشة |
| إعادة تحميل كاملة للانتقال بين الأقسام | **قسم واحد** يختفي وآخر يظهر (SPA) |
| صعب إدارة آلاف السطور | تقسيم إلى **Components** صغيرة |

#### 1.3 كيف يجعل الموقع تفاعليًا؟

1. React يحفظ **حالة** (state) — مثل: «القسم الحالي = زراعة».
2. عندما تضغط زرًا → **event** يغيّر state.
3. React **يعيد الرسم** (re-render) فقط الأجزاء التي تغيّرت.

#### 1.4 «تتغير بدون إعادة تحميل كاملة» — ماذا يعني؟

**إعادة تحميل كاملة:** المتصفح يطلب `index.html` من جديد — الشاشة تومض، كل شيء يبدأ من الصفر.

**React (SPA):** المتصفح يبقى على **نفس** `index.html` — فقط **JavaScript** يبدّل ما تراه داخل `<div id="root">`.

#### 1.5 مثال من موقع eng-alaa.com

عندما تضغط بطاقة **«الهندسة الزراعية»** في الصفحة الرئيسية:

```
1. onClick → openPortal("agri")
2. setPortal("agri")  ← state يتغير
3. React يخفي hero (portal === "home" يصبح false)
4. React يظهر قسم agri (portal === "agri" يصبح true)
5. أنت ترى المكتبة والمقالات — بدون reload
```

الكود الفعلي في `App.tsx`:

```tsx
const openPortal = useCallback((p: Portal) => {
  setPortal(p);
  window.scrollTo({ top: 0, behavior: "smooth" });
}, []);
```

---

### 2. ما معنى Component؟

#### 2.1 تشبيه Lego

**Component** = **قطعة Lego** — جزء صغير من الواجهة له **اسم** و**وظيفة**.

- قطعة «العجلات» = `SkillIcon`
- قطعة «اللوحة» = `HeroNameDisplay`
- قطعة «صندوق الأدوات الكبير» = `ContentAdmin`
- **الطفل الذي يجمع اللعبة** = `App.tsx`

#### 2.2 كل قطعة لها وظيفة

| Component | الوظيفة |
|-----------|---------|
| `AlaaLogo` | شعار SVG متحرك |
| `AppPicker` | أيقونات البرامج (Photoshop, AutoCAD…) |
| `BookCover` | غلاف كتاب في المكتبة |
| `PlantDiagnostic` | تشخيص النبات بالذكاء الاصطناعي |
| `CvRenderer` | عرض السيرة الذاتية A4 |
| `AnalyticsDashboard` | لوحة إحصائيات الزوار |

#### 2.3 App.tsx يجمع القطع

`App.tsx` **لا يعرض كل شيء بنفسه** — يستدعي مكوّنات:

```tsx
<HeroNameDisplay name={...} display={...} shimmer={...} />
<PlantDiagnostic data={data} lang={lang} />
<CvRenderer doc={selectedDoc} lang={cvLang} name={...} skills={data.skills} />
<ContentAdmin mode="agri" data={data} onSave={handleCvSave} />
```

---

### 3. الفرق بين الصفحة والـ Component

#### 3.1 هل كل Component صفحة؟

**لا.** `SkillIcon` = أيقونة صغيرة — **ليس** صفحة.  
`ReorderBtns` داخل `ContentAdmin` = زرّان للأعلى/للأسفل — **ليس** صفحة.

#### 3.2 هل كل صفحة Component؟

في مشروعنا **لا توجد صفحات HTML منفصلة** — «الصفحة» = **portal** في `App.tsx`:

```tsx
type Portal = "home" | "about" | "agri" | "graphics" | "software" | "cv" | "admin";
```

كل portal = **قسم كامل** يُرسم داخل `App.tsx` — وقد يحتوي Components فرعية.

#### 3.3 صفحة = عدة Components

**لوحة التحكم (`portal === "admin"`):**
- `ContentAdmin` (محتوى)
- `CvDocEditor` (سيرة)
- `AnalyticsDashboard` (إحصائيات)
- `FileExplorerAdmin` (ملفات)

**قسم الزراعة (`portal === "agri"`):**
- تبويبات (مقالات، كتب، تشخيص…)
- `PlantDiagnostic` (تبويب التشخيص)
- `SeasonNowPanel` (موسمك الآن)
- `SoilRequest` (تحليل التربة)
- `BookCover`, `BookAccessRibbon` (المكتبة)

**السيرة (`portal === "cv"`):**
- قائمة اختيار نوع CV
- `CvRenderer` (المعاينة A4)

**التصاميم (`portal === "graphics"`):**
- تصنيفات، بحث، معرض
- `GfxSourceFileDownload` (تحميل ملفات المصدر)

---

### 4. شرح `src/main.tsx` من ناحية React

#### 4.1 الكود كامل (9 أسطر فقط!)

```tsx
import { createRoot } from "react-dom/client";
import App from "./App";
import "@fortawesome/fontawesome-free/css/all.min.css";
import "./index.css";
import "./print.css";

createRoot(document.getElementById("root")!).render(<App />);
```

#### 4.2 كيف يبدأ React؟

1. المتصفح يحمّل `index.html`.
2. يجد `<div id="root"></div>` — **حاوية فارغة**.
3. يحمّل `main.tsx` (كـ module).
4. `createRoot(...).render(<App />)` — React **يملأ** `#root` بتطبيق `App`.

#### 4.3 ما معنى root؟

**root** = **الجذر** — المكان الذي «تُزرع» فيه شجرة React.

في `index.html`:

```html
<div id="root"></div>
<script type="module" src="/src/main.tsx"></script>
```

#### 4.4 علاقة main.tsx بـ index.html

| index.html | main.tsx |
|------------|----------|
| هيكل HTML بسيط | يشغّل React |
| يحمّل Three.js, jsPDF (CDN) | يستورد CSS و Font Awesome |
| `#root` فارغ | يضع `<App />` داخله |

#### 4.5 لماذا لا نكتب كل شيء في index.html؟

- الموقع **ضخم** (آلاف الأسطر في App + Components).
- HTML ثابت **لا يتفاعل** — كل تغيير = reload.
- React + TypeScript = **تنظيم** + **أمان أنواع** + **تفاعل**.

---

### 5. شرح App.tsx كمدير للمكونات

#### 5.1 App.tsx = «مدير العرض»

`App.tsx` (~3600 سطر) **ليس** ملفًا واحدًا بسيطًا — هو:
- **مدير state** (البيانات + القسم + اللغة + Admin)
- **موجه عرض** (أي portal يظهر)
- **وسيط** بين المكوّنات و `appData.ts` / السيرفر

#### 5.2 كيف يستدعي المكوّنات؟

**import** في أعلى الملف:

```tsx
import { ContentAdmin } from "./ContentAdmin";
import { PlantDiagnostic } from "./PlantDiagnostic";
import { CvRenderer } from "./CvRenderer";
import { AnalyticsDashboard } from "./AnalyticsDashboard";
```

**استخدام** داخل JSX:

```tsx
{portal === "agri" && ( ... <PlantDiagnostic data={data} lang={lang} /> ... )}
{portal === "admin" && adminLoggedIn && ( ... <ContentAdmin ... /> ... )}
```

#### 5.3 كيف يقرر أي Component يظهر؟

**شرط portal:**

```tsx
{portal === "home" && ( <main className="hero"> ... </main> )}
{portal === "agri" && ( ... )}
{portal === "admin" && adminLoggedIn && ( ... )}
```

**شرط إضافي:** لوحة التحكم تحتاج `adminLoggedIn === true`.

**شرط adminPanel:** داخل admin — أي لوحة فرعية:

```tsx
{adminPanel === 3 && <ContentAdmin mode="agri" ... />}
{adminPanel === 9 && <AnalyticsDashboard lang={lang} />}
```

#### 5.4 كيف يمرر البيانات؟

عبر **props** — مثل:

```tsx
<ContentAdmin mode="agri" data={data} onSave={handleCvSave} />
<PlantDiagnostic data={data} lang={lang as LangKey} />
<CvRenderer doc={selectedDoc} lang={cvLang} name={pickML(data.name, cvLang)} skills={data.skills} />
```

#### 5.5 كيف يستقبل التعديلات؟

عبر **callbacks** (دوال تُمرَّر كـ props):

```tsx
function handleCvSave(partial: Partial<AppData>): Promise<boolean> {
  return new Promise(resolve => {
    setData(prev => {
      const updated = { ...prev, ...partial };
      void saveAppData(updated).then(resolve);
      return updated;
    });
  });
}
```

`ContentAdmin` يستدعي `onSave({ agriArticles: [...] })` → `handleCvSave` يدمج ويحفظ.

#### 5.6 props و state — ملخص سريع

| | **props** | **state** |
|--|-----------|-----------|
| **من؟** | الأب يمرّر للابن | داخل المكوّن نفسه |
| **تتغير؟** | من الأب فقط | بـ setState |
| **مثال** | `data={data}` | `const [portal, setPortal] = useState("home")` |

#### 5.7 ربط الأقسام

- **Navbar + bottom-nav** — أزرار تستدعي `openPortal("agri")` أو `goHome()`.
- **state `portal`** — يحدد القسم.
- **state `data`** — محتوى مشترك بين **كل** الأقسام.

---

### 6. شرح Props بالتفصيل

#### 6.1 ما معنى props؟

**Props** = **Properties** = **خصائص** تمرّرها من مكوّن **أب** إلى مكوّن **ابن**.

**تشبيه:** تعليمات مكتوبة على ظرف تعطيه للطفل: «اعرض هذه البيانات، وعند الانتهاء اتصل بي».

#### 6.2 props مهمة من المشروع (من الكود الفعلي)

##### 1) `ContentAdmin`

| | |
|--|--|
| **من يرسل؟** | `App.tsx` |
| **من يستقبل؟** | `ContentAdmin` |

| Prop | المحتوى | لماذا |
|------|---------|-------|
| `mode` | `'agri' \| 'gfx' \| 'lab' \| 'site'` | أي لوحة إدارة |
| `data` | كائن `AppData` كامل | مصدر الحقول |
| `onSave` | `(u: Partial<AppData>) => void` | إرسال التعديلات للأب |
| `serverConnected?` | boolean | هل JWT موجود (mode=site) |
| `serverSyncing?` | boolean | جاري المزامنة |
| `onServerConnect?` | `(user, pass) => Promise<boolean>` | تسجيل دخول السيرفر |
| `onServerSync?` | `() => void` | مزامنة يدوية |
| `onServerDisconnect?` | `() => void` | قطع JWT |

**مثال:**

```tsx
<ContentAdmin mode="agri" data={data} onSave={handleCvSave} />
```

##### 2) `PlantDiagnostic`

| Prop | من → إلى | المحتوى |
|------|----------|---------|
| `data` | App → PlantDiagnostic | `AppData` — إعدادات AI، قالب تقرير، تحليل تربة |
| `lang` | App → PlantDiagnostic | `'ar' \| 'en' \| 'de'` |

##### 3) `CvRenderer`

| Prop | المحتوى | لماذا |
|------|---------|-------|
| `doc` | `CvDoc` | مستند السيرة (أقسام، ألوان، تخطيط) |
| `lang` | `LangKey` | لغة العرض |
| `name` | `string` | اسم المهندس |
| `skills` | `Skill[]` | المهارات للعرض |
| `part?` | `'all' \| 'header' \| 'footer'…` | جزء للتصدير PDF |
| `editMode?` | boolean | وضع التعديل في المحرر |
| `onEditTarget?` | callback | عند النقر لتعديل قسم |

##### 4) `AnalyticsDashboard`

| Prop | المحتوى |
|------|---------|
| `lang` | `'ar' \| 'en' \| 'de'` — نصوص التبويبات |

##### 5) `HeroNameDisplay` (من الصفحة الرئيسية)

| Prop | المحتوى |
|------|---------|
| `name` | نص الاسم |
| `display` | `'text' \| 'handwriting' \| 'logo'` |
| `logo`, `logoColor` | شعار مخصص |
| `shimmer*` | إعدادات تأثير اللمعان |

##### 6) `CvDocEditor`

| Prop | المحتوى |
|------|---------|
| `data` | AppData |
| `onSave` | handleCvSave |
| `onExport` | printCvDoc |

##### 7) `FileExplorerAdmin`

| Prop | المحتوى |
|------|---------|
| `data` | AppData |
| `onSave` | handleCvSave |

#### 6.3 props داخلية صغيرة (داخل ContentAdmin)

```tsx
function ReorderBtns({ index, total, onMove }: { index: number; total: number; onMove: (dir: -1 | 1) => void })
function MlNameInputs({ value, onChange, dark, stacked, showTranslate }: { value: ML; onChange: (v: ML) => void; ... })
function TabBar({ tabs, active, color, onChange }: ...)
```

هذه **مكوّنات فرعية** — نفس فكرة props لكن على مستوى أصغر.

---

### 7. شرح State

#### 7.1 ما معنى state؟

**State** = **ذاكرة** المكوّن — «ماذا يحدث **الآن**؟»

React **يراقب** state — عند تغييره → **إعادة رسم** تلقائية.

#### 7.2 useState

```tsx
const [portal, setPortal] = useState<Portal>("home");
```

- `portal` = القيمة الحالية
- `setPortal("agri")` = تغيير + إعادة رسم

#### 7.3 state vs props

| | **props** | **state** |
|--|-----------|-----------|
| **الاتجاه** | من الأب للابن | داخل المكوّن |
| **من يغيّر؟** | الأب | `setXxx(...)` |
| **مثال** | `data` من App | `agriTab` داخل App |

#### 7.4 أمثلة state في App.tsx (من الكود)

| State | الغرض |
|-------|-------|
| `lang` | اللغة الحالية ar/en/de |
| `portal` | القسم: home, agri, admin… |
| `data` | **كل** محتوى الموقع (AppData) |
| `adminLoggedIn` | هل دخل المدير لوحة التحكم؟ |
| `serverConnected` | هل JWT للسيرفر موجود؟ |
| `agriTab` | أي تبويب في قسم الزراعة |
| `gfxProjectPage` | مشروع تصميم مفتوح للتفاصيل |
| `articlePage` | مقال مفتوح للقراءة |
| `printingCv` | جاري تصدير PDF |

#### 7.5 state داخل ContentAdmin (AgriAdmin)

`ContentAdmin` ينسخ `data` إلى **state محلي** للتحرير:

```tsx
const saveArticles = (a: AgriArticle[]) => { setArticles(a); onSave({ agriArticles: a }); };
```

- `setArticles` — تحديث **فوري** للواجهة
- `onSave` — إرسال للأب + حفظ

#### 7.6 state داخل PlantDiagnostic

```tsx
const [images, setImages] = useState<string[]>([]);
const [loading, setLoading] = useState(false);
const [results, setResults] = useState<Partial<Record<LangKey, DiagResult>>>({});
```

---

### 8. شرح Events

#### 8.1 ما معنى event؟

**Event** = **حدث** — شيء فعله المستخدم: ضغط، كتابة، إرسال نموذج.

#### 8.2 ماذا يحدث عند الضغط على زر؟

```tsx
<button onClick={() => openPortal("agri")}>الزراعة</button>
```

1. المتصفح يلتقط **click**
2. React يستدعي الدالة داخل `onClick`
3. `setPortal("agri")` → إعادة رسم

#### 8.3 onClick

**أكثر event شيوعًا** — أزرار، بطاقات، تبويبات.

أمثلة:
- `onClick={goHome}` — العودة للرئيسية
- `onClick={() => setAgriTab(2)}` — تبويب الكتب
- `onClick={saveSkills}` — حفظ المهارات في admin

#### 8.4 onChange

عند **تغيير** حقل إدخال:

```tsx
<input value={value[lk]} onChange={e => onChange({ ...value, [lk]: e.target.value })} />
```

في `MlNameInputs` — كل حرف تكتبه → `onChange` → state يتحدث.

#### 8.5 onSubmit

عند **إرسال** نموذج (Enter أو زر Submit) — يُستخدم في forms لتجنب reload:

```tsx
<form onSubmit={e => { e.preventDefault(); handleAdminLogin(); }}>
```

#### 8.6 أمثلة من لوحة التحكم

| Event | أين | ماذا يفعل |
|-------|-----|-----------|
| `onClick` على «حفظ» | ContentAdmin | `onSave({ ... })` |
| `onChange` على عنوان مقال | MlNameInputs | تحديث ML ar/en/de |
| `onClick` ReorderBtns | ContentAdmin | نقل عنصر لأعلى/أسفل |
| `onClick` connectServer | SiteSettingsAdmin | `onServerConnect(user, pass)` |

---

### 9. شرح ContentAdmin.tsx

#### 9.1 لماذا Component؟

ملف **~2900 سطر** — لو وُضع كله في App.tsx سيكون **مستحيل** الصيانة.  
`ContentAdmin` = **صندوق أدوات** لإدارة المحتوى.

#### 9.2 البنية الداخلية

```tsx
export function ContentAdmin({ mode, data, onSave, ... }: Props) {
  if (mode === 'agri') return <AgriAdmin data={data} onSave={onSave} />;
  if (mode === 'gfx') return <GfxAdmin data={data} onSave={onSave} />;
  if (mode === 'site') return <SiteSettingsAdmin data={data} onSave={onSave} ... />;
  return <LabAdmin data={data} onSave={onSave} />;
}
```

| mode | المكوّن | المحتوى |
|------|---------|---------|
| `agri` | AgriAdmin | مقالات، كتب، مكتبة، تربة، فيديو |
| `gfx` | GfxAdmin | تصنيفات، مشاريع، watermark |
| `lab` | LabAdmin | مشاريع web + snippets |
| `site` | SiteSettingsAdmin | logo, footer, nav, اتصال السيرفر |

#### 9.3 ماذا يستقبل؟

انظر **القسم 6** — `data`, `onSave`, وprops السيرفر لـ site.

#### 9.4 ماذا يرسل؟

**Partial AppData** عبر `onSave`:

```tsx
onSave({ agriArticles: newList });
onSave({ gfxCategories: next });
onSave({ siteSettings: {...}, name: {...} });
```

#### 9.5 الحفظ المؤقت vs النهائي

1. **مؤقت (محلي):** `setArticles(...)`, `setDraft(...)` — تحدّث الواجهة فورًا.
2. **نهائي:** `onSave({ agriArticles })` → App → `handleCvSave` → `saveAppData` → localStorage + POST data.php.

**لا يوجد** زر «حفظ» منفصل لكل حقل — كثير من التعديلات **تحفظ مباشرة** عند `onSave`.

#### 9.6 أجزاء مهمة

| جزء | الوظيفة |
|-----|---------|
| `MlNameInputs` | حقول ar/en/de |
| `TabBar` | تبويبات داخل AgriAdmin |
| `RichEditor` | محرر HTML للمقالات |
| `AiArticleGeneratePanel` | توليد مقال AI |
| `BookCoverGenerator` | غلاف كتاب |
| `GridFontControls` | إعدادات شبكة العرض |
| `CustomerReportsAdmin` | تقارير العملاء |

---

### 10. شرح AnalyticsDashboard.tsx

#### 10.1 ماذا يعرض؟

- ملخص زيارات
- متواجدون الآن
- دول، مدن، GPS
- تنزيلات CV
- تقارير فترات

#### 10.2 من أين البيانات؟

**ليس** من `data` state — يجلب **مباشرة** من API:

```tsx
const data = await fetchAnalyticsStats(token);  // GET analytics.php?action=stats
```

يحتاج `getApiToken()` — JWT من connectServer.

#### 10.3 عرض فقط أم تعديل؟

**عرض + إدارة:** حذف جلسات، reset كامل — **لا** يعدّل محتوى الموقع.

#### 10.4 state داخلي

```tsx
const [stats, setStats] = useState<AnalyticsStats | null>(null);
const [view, setView] = useState<AnalyticsView>('summary');
const [loading, setLoading] = useState(true);
```

#### 10.5 كيف يظهر للمدير؟

`App.tsx` → `adminPanel === 9` → `<AnalyticsDashboard lang={lang} />`

---

### 11. شرح PlantDiagnostic.tsx

#### 11.1 الوظيفة

**تشخيص نبات** من صور — AI — تقرير PDF.

#### 11.2 أين يظهر؟

**جزء** من `portal === "agri"` — **ليس** صفحة مستقلة:

```tsx
<PlantDiagnostic data={data} lang={lang as LangKey} />
```

(داخل تبويب «افحص نباتك» — إن `aiDiagnosticsEnabled`)

#### 11.3 البيانات المستخدمة

- `data.aiDiagnosticsEnabled` — هل التبويب ظاهر؟
- `data.reportTemplate` — قالب التقرير
- `data.soilAnalysis` — جدول تحليل التربة في PDF
- صور يرفعها **المستخدم** (state محلي `images`)

#### 11.4 التفاعل

1. رفع صور (`fileRef`, `onChange`)
2. زر «تشخيص» → طلب AI
3. معاينة A4 + تصدير PDF

#### 11.5 الربط بالمشروع

- يستخدم `AlaaLogo`, `resolveImageSrc`
- `captureReportSnapshot` للـ PDF
- لا يستدعي `onSave` — نتائج **محلية** للزائر (لا تُحفظ في DB)

---

### 12. شرح CvRenderer.tsx

#### 12.1 الوظيفة

**يرسم السيرة الذاتية** بمقاس A4 — للمعاينة والطباعة/PDF.

#### 12.2 مصدر البيانات

| | |
|--|--|
| **doc** | `data.cvDocs[]` — من AppData / MySQL |
| **name** | `data.name` (ML) |
| **skills** | `data.skills` |

#### 12.3 كيف يحوّل البيانات لواجهة؟

1. يقرأ `doc.sections[]` — كل قسم (تعليم، خبرة…)
2. `pickML(..., lang)` — نص بلغة الزائر
3. `cvLayout(doc)` — أبعاد header/footer
4. `planCvPageOffsets` — تقسيم صفحات متعددة
5. `SkillIcon` — أيقونات المهارات

#### 12.4 part = تقطيع للتصدير

```tsx
part?: 'all' | 'body' | 'header' | 'footer' | 'continued-header' | 'continued-footer'
```

لتصدير PDF — كل جزء يُلتقط منفصلًا.

#### 12.5 التعديل بأمان

- **آمن:** تغيير **نصوص** CV من لوحة `CvDocEditor` (محتوى)
- **حذر:** `CvRenderer.tsx` — منطق **تخطيط** معقد — لا تلمس `planCvPageOffsets` كمبتدئ
- **editMode:** للنقر على قسم في المحرر — `onEditTarget`

---

### 13. شرح index.css

#### 13.1 ما معنى CSS؟

**CSS** = **Cascading Style Sheets** — يتحكم **بالشكل**: ألوان، خطوط، مسافات، تخطيط.

#### 13.2 وظيفة index.css

- **~5000 سطر** — تصميم الموقع الكامل
- يستورد Tailwind: `@import "tailwindcss";`
- متغيرات: `--navy`, `--glass`, `--font`
- classes: `.hero`, `.navbar`, `.glass`, `.btn-pill`, `.admin-layout`…

#### 13.3 CSS vs JavaScript

| JavaScript/React | CSS |
|------------------|-----|
| **ماذا** يعرض | **كيف** يبدو |
| منطق، بيانات | ألوان، خطوط |
| `portal === "agri"` | `.hero { min-height: 100vh }` |

#### 13.4 لو حذفنا index.css؟

- الموقع **يعمل** منطقيًا — لكن **بدون تنسيق**
- نصوص فوق بعض، لا glass، لا navbar ثابت

#### 13.5 تغيير لون/خط بحذر

1. ابحث عن class في DevTools (F12)
2. عدّل في `index.css` **class واحد**
3. Ctrl+F5 — تحقق **كل** الأقسام
4. **لا** تغيّر `*, *::before` بدون فهم — يؤثر على Font Awesome

---

### 14. شرح translations.ts

#### 14.1 الوظيفة

**نصوص واجهة ثابتة** — أزرار، عناوين، تسميات — **ليس** محتوى المقالات.

#### 14.2 ثلاث لغات

```tsx
export type LangCode = 'ar' | 'en' | 'de';
export const translations = { ar: { ... }, en: { ... }, de: { ... } };
export interface T { dir: 'rtl' | 'ltr'; adminBtn: string; ... }
```

#### 14.3 اختيار اللغة

```tsx
const [lang, setLang] = useState<LangCode>(detectLang);
const t: T = translations[lang];
```

- `detectLang()` — localStorage أو لغة المتصفح
- `saveLang(l)` — حفظ الاختيار

#### 14.4 كيف تصل للمكونات؟

```tsx
<h2>{t.agriTitle}</h2>
<button>{t.adminSaveBio}</button>
```

**AnalyticsDashboard** — نصوص **داخلية** (VIEW_TABS) + `ar = lang === 'ar'`.

#### 14.5 محتوى vs ترجمة

| translations.ts | appData / data.json |
|-----------------|---------------------|
| «حفظ»، «إلغاء» | عنوان المقال، نص المقال |
| واجهة UI | محتوى الزائر |

#### 14.6 إضافة ترجمة بحذر

1. أضف المفتاح في `ar`, `en`, `de` **معًا**
2. أضف النوع في `interface T`
3. استخدم `t.newKey` في JSX

---

### 15. العلاقة بين البيانات والمكونات

#### 15.1 من السيرفر إلى App

```
loadAppData() → localStorage
loadAppDataFromDb() → GET data.php
loadSiteContentFromJson() → data.json
        ↓ merge
setData(applyDefaultCatalog(merged))
```

#### 15.2 من App إلى المكوّنات

```tsx
<PlantDiagnostic data={data} lang={lang} />
<ContentAdmin data={data} onSave={handleCvSave} />
```

#### 15.3 هل المكوّن يخزّن؟

| | |
|--|--|
| **عرض فقط** | `CvRenderer`, `HeroNameDisplay` — يقرأ props |
| **state محلي + onSave** | `ContentAdmin` — يحرّر ثم يرسل |
| **state محلي فقط** | `PlantDiagnostic` results — لا DB |

#### 15.4 متى يرسل تعديلًا؟

عند `onSave(...)` — عادة **فور** تغيير (حفظ مقال، نقل كتاب…).

---

### 16. مخطط البنية الكامل

```
index.html
    ↓  (#root + main.tsx)
main.tsx
    ↓  createRoot().render(<App />)
App.tsx
    ↓  portal + data + lang
Components
    ├── ContentAdmin.tsx      ← onSave → handleCvSave
    ├── AnalyticsDashboard.tsx ← fetch analytics.php
    ├── PlantDiagnostic.tsx   ← data + AI
    ├── CvRenderer.tsx        ← cvDocs
    ├── HeroNameDisplay.tsx
    ├── SeasonNowPanel.tsx
    ├── SoilRequest.tsx
    ├── CvDocEditor.tsx
    └── … (SkillIcon, AppPicker, …)
    ↓
index.css + print.css  → الشكل
appData.ts             → تحميل/حفظ AppData
translations.ts        → t.adminBtn, t.agriTitle…
```

**خطوة بخطوة:**

1. **index.html** — هيكل + scripts CDN
2. **main.tsx** — تشغيل React + CSS
3. **App.tsx** — state مركزي + routing بـ portal
4. **Components** — قطع متخصصة
5. **index.css** — مظهر `.glass`, `.hero`…
6. **appData.ts** — `data` object shape + save/load
7. **translations.ts** — `t.*` للواجهة

---

### 17. دورة حياة التفاعل — مثال قسم الزراعة

```
الزائر يضغط بطاقة «الزراعة»
        ↓
onClick → openPortal("agri")
        ↓
setPortal("agri")
        ↓
React re-render
        ↓
{portal === "agri" && (...)} → true
        ↓
يظهر: t.agriTitle، تبويبات، PlantDiagnostic…
        ↓
trackPageView("agri:ar")  ← analytics
```

**بساطة:** ضغطة → state → شاشة جديدة — **بدون** reload.

---

### 18. تعديل نص في لوحة التحكم

```
ContentAdmin → AgriAdmin → حقل عنوان مقال (MlNameInputs)
        ↓
المدير يكتب في input ar
        ↓
onChange → onChange({ ...value, ar: e.target.value })
        ↓
setArticles(updated)  ← state محلي
        ↓
(عند حفظ/إضافة/حذف) saveArticles → onSave({ agriArticles })
        ↓
App.handleCvSave → setData + saveAppData
        ↓
POST data.php → MySQL
        ↓
زائر جديد → GET → يرى العنوان الجديد
```

**ملاحظة:** بعض الحقول تحفظ **عند كل** `onSave` — ليس بالضرورة زر «حفظ» كبير.

---

### 19. أخطاء شائعة في React

#### 19.1 تعديل Component بدون فهم props

| | |
|--|--|
| **السبب** | حذف `data` أو `onSave` من الاستدعاء |
| **كيف تعرف** | Console: `undefined` errors |
| **تجنّب** | لا تحذف prop — اقرأ interface `Props` |

#### 19.2 حذف import

| | |
|--|--|
| **السبب** | `import { ContentAdmin }` محذوف |
| **كيف تعرف** | `ContentAdmin is not defined` |
| **تجنّب** | IDE يظهر خطأ أحمر — لا تحفظ |

#### 19.3 تغيير اسم متغير

| | |
|--|--|
| **السبب** | `portal` → `section` في مكان واحد فقط |
| **كيف تعرف** | TypeScript error أو قسم لا يظهر |
| **تجنّب** | Rename Symbol في IDE |

#### 19.4 JSX مكسور

| | |
|--|--|
| **السبب** | `<div>` بدون `</div>` |
| **كيف تعرف** | Vite error: Unterminated JSX |
| **تجنّب** | كل tag مغلق — أو `<Component />` self-closing |

#### 19.5 CSS يؤثر على كل الموقع

| | |
|--|--|
| **السبب** | تعديل `body { }` أو `*` |
| **تجنّب** | عدّل class **محدد** مثل `.hero-bio` |

#### 19.6 حذف state مهم

| | |
|--|--|
| **السبب** | حذف `const [data, setData]` |
| **النتيجة** | الموقع فارغ |
| **تجنّب** | لا تمس `data` state في App |

---

### 20. ماذا تعدّل بأمان؟

#### ✅ آمن (كمبتدئ)

- نصوص في `translations.ts` (الثلاث لغات)
- محتوى من **لوحة التحكم** (ContentAdmin)
- class CSS **واحد** بعد فحص
- عنوان قسم في translations

#### ❌ لا تلمس الآن

- `handleCvSave`, `saveAppData`, `loadAppDataFromDb`
- props: `onSave`, `data` structure
- `adminLoggedIn`, JWT logic
- `portal` routing logic
- `CvRenderer` pagination
- بنية `AppData` في appData.ts

---

### 21. تشبيه Lego

| Lego | المشروع |
|------|---------|
| **الطفل يجمع** | App.tsx |
| **قطعة Lego** | Component |
| **تعليمات على الظرف** | props |
| **ذاكرة الطفل** | state |
| **الأزرار** | events (onClick…) |
| **لون القطع** | index.css |
| **صندوق المعلومات** | appData.ts / data |
| **قاموس اللغات** | translations.ts |

---

### 22. نهاية الفصل

#### 22.1 ملخص

1. **React** = واجهة تفاعلية بدون reload.
2. **Component** = قطعة UI — App يجمعها.
3. **props** = من الأب للابن (data, onSave, lang…).
4. **state** = ذاكرة (portal, data, lang…).
5. **events** = onClick, onChange → تغيير state.
6. **main.tsx** → `#root` → `<App />`.
7. **translations** = UI — **appData** = محتوى.

#### 22.2 جدول Components رئيسية

| Component | الوظيفة | props رئيسية |
|-----------|---------|--------------|
| App.tsx | مدير كل شيء | — (جذر) |
| ContentAdmin | إدارة محتوى | mode, data, onSave |
| AnalyticsDashboard | إحصائيات | lang |
| PlantDiagnostic | تشخيص AI | data, lang |
| CvRenderer | عرض CV A4 | doc, lang, name, skills |
| CvDocEditor | تحرير CV | data, onSave, onExport |
| HeroNameDisplay | اسم الرئيسية | name, display, shimmer* |
| SeasonNowPanel | موسمك الآن | (داخل agri) |
| SoilRequest | طلب تحليل تربة | — |
| FileExplorerAdmin | ملفات | data, onSave |
| SkillIcon | أيقونة مهارة | skill |
| AppPicker | أيقونات برامج | apps, active |

#### 22.3 Component vs props vs state vs event

| | **Component** | **props** | **state** | **event** |
|--|---------------|-----------|-----------|-----------|
| **ما هو** | قطعة UI | مدخلات | ذاكرة | فعل مستخدم |
| **مثال** | ContentAdmin | data={data} | portal | onClick |
| **من يغيّر** | — | الأب | setState | المستخدم |

#### 22.4 أهم 10 أشياء عن React في المشروع

1. **SPA** — portal واحد يبدّل الأقسام.
2. **App.tsx** = state مركزي + routing.
3. **data** = AppData — مصدر الحقيقة للمحتوى.
4. **onSave** = طريق العودة من admin للأب.
5. **translations[lang]** = نصوص UI.
6. **main.tsx** = 9 أسطر — لا تبالغ بملفات أخرى قبل فهمها.
7. **Conditional render:** `{portal === "x" && (...)}`.
8. **ContentAdmin** ينسخ data لـ state محلي.
9. **AnalyticsDashboard** مستقل — fetch من API.
10. **CSS منفصل** — لا تخلط منطق وشكل.

#### 22.5 أخطاء وحلول

| مشكلة | حل |
|-------|-----|
| شاشة بيضاء | F12 Console |
| prop undefined | تحقق من App.tsx استدعاء |
| لا يحفظ | connectServer + JWT |
| لغة خاطئة | lang state + translations |
| CSS كسر الكل | تراجع عن تعديل global |

#### 22.6 أسئلة تدريبية

1. ما الفرق React و HTML عادي؟
2. ما `#root`؟
3. ما portal في App.tsx؟
4. فرق props و state؟
5. ماذا يفعل onSave؟
6. من أين `data`؟
7. لماذا ContentAdmin منفصل؟
8. هل PlantDiagnostic صفحة؟
9. فرق translations و appData؟
10. ماذا يفعل main.tsx؟

#### 22.7 تمرين نظري: ContactSection

**الهدف:** Component جديد «اتصل بنا» — **بدون كود**.

1. أنشئ mentally `src/ContactSection.tsx`.
2. props: `phone`, `email`, `lang`.
3. استخدم `pickML` أو `t.*` للنصوص.
4. في App.tsx: `{portal === "home" && <ContactSection ... />}` أو portal جديد.
5. CSS: class `.contact-section` في index.css.
6. **لا** تعدّل handleCvSave — عرض فقط.
7. اختبر: npm run dev → هل يظهر؟

#### 22.8 ملفات للدراسة بعد هذا الفصل

| الترتيب | الملف | لماذا |
|---------|-------|-------|
| 1 | `src/App.tsx` | portal + state |
| 2 | `src/ContentAdmin.tsx` | admin patterns |
| 3 | `src/translations.ts` | i18n UI |
| 4 | `src/index.css` | تصميم |
| 5 | `src/CvRenderer.tsx` | layout معقد |
| 6 | `src/HeroNameDisplay.tsx` | component متوسط |
| 7 | `src/appData.ts` | شكل البيانات |
| 8 | `src/PlantDiagnostic.tsx` | AI + PDF |

---

> **الفصل التالي (8):** «شرح التصميم و CSS» — *(هذا الفصل أدناه)*

---

## الفصل الثامن: شرح التصميم و CSS — كيف يأخذ الموقع شكله الجميل؟

> **هدف الفصل:** أن تفهم **من أين يأتي الشكل** — الألوان، الخطوط، البطاقات، الجوال — بدون لمس React logic.

---

### 1. ما معنى CSS؟

#### 1.1 تشبيه طفل مبتدئ

تخيّل أن **React** بنى **غرفة** (عنوان، فقرة، زر).  
**CSS** = **الدهان، الستائر، الإضاءة، حجم الأثاث** — يجعل الغرفة **جميلة ومريحة**.

بدون CSS: الغرفة موجودة لكن **بيضاء وفوضوية**.  
بدون React: لا توجد غرف أصلًا.

#### 1.2 HTML vs React vs CSS

| | **HTML / JSX** | **React** | **CSS** |
|--|----------------|-----------|---------|
| **ماذا؟** | **ماذا** يوجد (عنوان، زر) | **كيف** يتغير المحتوى | **كيف** يبدو |
| **مثال** | `<button>حفظ</button>` | `onClick` + state | `.btn-prime { background: navy }` |
| **أين؟** | App.tsx (JSX) | App.tsx (logic) | index.css |

#### 1.3 لماذا نحتاج CSS؟

- المتصفح **افتراضيًا** يعرض HTML بشكل بسيط جدًا (Times New Roman، أزرار رمادية).
- CSS يقول: «الزر **أزرق داكن**، **زوايا دائرية**، **يتوهج** عند hover».

#### 1.4 كيف يعرف المتصفح الشكل؟

1. React يضع `className="btn-pill"` على الزر.
2. المتصفح يقرأ `index.css`.
3. يجد قاعدة `.btn-pill { ... }`.
4. **يطبّق** الألوان والخط على العنصر.

---

### 2. شرح ملف `src/index.css`

#### 2.1 الوظيفة

**ملف التصميم الرئيسي** — ~**5000 سطر** — يُحمَّل من `main.tsx`:

```tsx
import "./index.css";
```

**يؤثر على كل الموقع** — كل class المستخدمة في App والمكوّنات.

#### 2.2 Tailwind + CSS عادي — ماذا يستخدم المشروع؟

في **أعلى** index.css:

```css
@import "tailwindcss";
```

**Tailwind** مُثبّت (عبر `@tailwindcss/vite`) — لكن **الواجهة الرئيسية** (App.tsx) تعتمد على **classes مخصصة** مثل `.hero`, `.glass`, `.card` — **ليس** `className="flex p-4 text-blue-500"`.

| أين | أسلوب CSS |
|-----|-----------|
| **App.tsx + أغلب المكوّنات** | classes مخصصة في index.css |
| **src/components/ui/** | Tailwind utilities (Radix/shadcn) — **نادر** في الموقع العام |
| **ContentAdmin** | mix: classes + `style={{ ... }}` inline |

**خلاصة:** تعلّم **index.css** أولًا — هذا «دفتر الديكور» الحقيقي.

#### 2.3 أهم الأقسام داخل index.css

| القسم (تعليق في الملف) | المحتوى |
|------------------------|---------|
| **Design System** `:root` | متغيرات الألوان والخط |
| **Glassmorphism** `.glass` | لوحات شفافة ضبابية |
| **Canvas BG** `#three-canvas-wrapper` | خلفية Three.js |
| **Navbar** `.navbar` | شريط علوي ثابت |
| **Hero** `.hero`, `.hero-name` | الصفحة الرئيسية |
| **Portal Gates** `.portal-grid`, `.portal-card` | بطاقات الأقسام الثلاث |
| **Content Wrapper** `.content-wrap` | حاوية الأقسام الداخلية |
| **Sub Tabs** `.sub-tabs`, `.tab-btn` | تبويبات الزراعة/غيرها |
| **Card Grid** `.card`, `.card-grid` | بطاقات المقالات/التصاميم |
| **CV** `.cv-layout`, `.a4-page` | السيرة A4 |
| **Admin** `.admin-layout`, `.admin-panel-btn` | لوحة التحكم |
| **Analytics** `.analytics-dashboard`, `.analytics-stat-card` | الإحصائيات |
| **Bottom Nav** `.bottom-nav` | تنقل الجوال |
| **Responsive** `@media (max-width: …)` | شاشات صغيرة |

#### 2.4 الألوان الأساسية (`:root`)

```css
:root {
  --bg:           #FFFFFF;
  --navy:         #003366;
  --navy-light:   rgba(0,51,102,0.08);
  --navy-glow:    rgba(0,51,102,0.15);
  --glass:        rgba(255,255,255,0.5);
  --glass-border: rgba(0,51,102,0.12);
  --text:         #001F3F;
  --muted:        #556677;
  --radius:       16px;
  --font:         'Tajawal', system-ui, sans-serif;
}
```

**ملاحظة:** App.tsx **يعدّل** `--navy`, `--glass`… ديناميكيًا من `siteSettings.accentColor` و `themeMode` (dark/light).

#### 2.5 الخطوط

| الخط | الاستخدام | من أين |
|------|-----------|--------|
| **Tajawal** | كل النصوص | `index.html` Google Fonts + `--font` |
| **Aref Ruqaa Ink** | اسم بخط اليد `.hero-name--handwriting` | Google Fonts |
| **Font Awesome** | أيقونات `<i className="fa-solid fa-...">` | npm في main.tsx |

#### 2.6 أحجام العناوين (أمثلة)

| Class | حجم تقريبي |
|-------|------------|
| `.hero-name` | `clamp(2rem, 5vw, 3.4rem)` — يتكيف مع الشاشة |
| `.section-title` | `1.9rem`, وزن 800 |
| `.portal-card h3` | `1.05rem` |
| `.card-title` | `1rem` |
| `.admin-main h4` | `1rem` |

**clamp** = حد أدنى + نسبة viewport + حد أقصى — **responsive** تلقائيًا.

#### 2.7 تنسيق الأزرار

| Class | الشكل |
|-------|-------|
| `.btn-pill` | كapsule (حبة) — navbar، لغة |
| `.btn-back` | navy، rounded 25px — «عودة» |
| `.btn-prime` | زر رئيسي — حفظ، تصدير CV |
| `.tab-btn` | تبويب — `.tab-btn.active` = navy |
| `.admin-panel-btn.active` | sidebar admin — navy |
| `.analytics-filter-tab.active` | تبويب إحصائيات |
| `.bottom-nav-item.active` | قسم نشط على الجوال |

#### 2.8 تنسيق البطاقات

```css
.card {
  border-radius: var(--radius);
  display: flex; flex-direction: column;
}
.card-img { height: 195px; object-fit: cover; }
.card:hover { transform: translateY(-5px); box-shadow: ... }
```

**Portal cards** مختلفة — `.portal-card` — dark glass على hero.

#### 2.9 تنسيق الأقسام

```css
.content-wrap { max-width: 1200px; margin: 0 auto; padding: 60px 4% 80px; }
.section-head { border-bottom: 2px solid var(--glass-border); ... }
.fade-up { animation: fadeUp 0.5s ease forwards; }
```

---

### 3. كيف يرتبط CSS مع React؟

#### 3.1 className في JSX

```tsx
<nav className={`navbar${theme === "dark" ? " dark" : " glass"}`}>
<button className={`tab-btn${agriTab === i ? " active" : ""}`}>
<div className="card glass">
```

#### 3.2 كيف يطبّق CSS؟

```
JSX: className="card glass"
        ↓
HTML في المتصفح: class="card glass"
        ↓
CSS: .card { ... }  و  .glass { ... }
        ↓
الشكل النهائي = دمج القواعد
```

#### 3.3 className vs class

في **HTML** العادي: `class="hero"`.  
في **React/JSX**: `className="hero"` — لأن `class` كلمة محجوزة في JavaScript.

#### 3.4 className ديناميكي + state

```tsx
className={`bottom-nav-item${portal === item.key ? ' active' : ''}`}
```

- `portal === "agri"` → يضاف class `active`
- CSS: `.bottom-nav-item.active { color: #fff; }`

#### 3.5 style inline (بالإضافة لـ CSS)

أحيانًا App يضع `style={{ ...accentVars }}` على `<div>` الجذر — **CSS variables** ديناميكية:

```tsx
<div style={{ ...accentVars }} data-theme={theme}>
```

---

### 4. شرح تصميم الأقسام الرئيسية

#### 4.1 الصفحة الرئيسية (`portal === "home"`)

| | |
|--|--|
| **الشكل** | خلفية Three.js داكنة، اسم متوهج، 3 بطاقات portal، أيقونات social |
| **الملفات** | App.tsx + index.css (hero, portal-*) |
| **Classes** | `.hero`, `.hero-name`, `.portal-grid`, `.portal-card`, `.social-btn`, `.fade-up` |
| **Navbar** | `.navbar.dark` على الوضع الداكن |

**البطاقات:** grid 3 أعمدة → عمود واحد على جوال (`@media max-width: 900px`).

#### 4.2 صفحة «عني» (`portal === "about"`)

| | |
|--|--|
| **الشكل** | صفحة داكنة — صورة + badge اسم + مهارات |
| **Classes** | `.about-dark-page`, `.about-dark-layout`, `.about-dark-skill-fill` |
| **Flex/Grid** | layout أفقي — يتكدس على جوال |

#### 4.3 قسم الزراعة (`portal === "agri"`)

| | |
|--|--|
| **الشكل** | `.content-wrap` + `.section-head` + تبويبات |
| **Classes** | `.sub-tabs`, `.tab-btn`, `.card`, `.articles-dynamic-grid`, `.books-dynamic-grid`, `.book-grid-card` |
| **موبايل** | `.agri-mobile-quicktabs`, `.desktop-sub-tabs` (يختفي/يظهر) |
| **مكوّنات** | PlantDiagnostic, SeasonNowPanel — classes خاصة + `.glass` |

**المقالات:** `.card.glass` + `.article-card-img` + `.card-body`.  
**الكتب:** `.book-grid-card` + `bookGridStyleResponsive()` من JS للأعمدة.

#### 4.4 قسم التصاميم (`portal === "graphics"`)

| | |
|--|--|
| **الشكل** | معرض بطاقات، تصنيفات، `.gfx-dyn-grid` |
| **Classes** | `.gfx-cat-divider`, `.gfx-sub-divider`, `.card-img`, `.gfx-cat-picker` |
| **Theme** | `[data-theme="dark"] .gfx-cat-divider` — ألوان مختلفة dark/light |

**البطاقة:** `.card.glass` + صورة `.card-img` (object-fit: cover).

#### 4.5 قسم البرمجة (`portal === "software"`)

| | |
|--|--|
| **الشكل** | `.web-proj-grid` + `.lab-grid` |
| **Classes** | `.card`, `.lab-drawer` (مختبر أكواد — split editor/preview) |
| **Grid** | `lab-grid`: `repeat(auto-fill, minmax(260px, 1fr))` |

#### 4.6 السيرة الذاتية (`portal === "cv"`)

| | |
|--|--|
| **الشكل** | `.cv-layout` — sidebar + معاينة A4 |
| **Classes** | `.cv-scroll-wrap`, `.cv-preview-stack`, `.a4-page`, `.btn-prime` |
| **CvRenderer** | classes `.cv-head`, `.cv-sec-title`, `.cv-footer-full-bleed` |
| **طباعة** | `print.css` — `@media print` |

#### 4.7 لوحة التحكم (`portal === "admin"`)

| | |
|--|--|
| **الشكل** | `.admin-layout` — sidebar 220px + main |
| **Classes** | `.admin-sidebar`, `.admin-panel-btn`, `.admin-main`, `.glass` |
| **Responsive** | `@media 900px` → عمود واحد |

ContentAdmin يستخدم **inline styles** كثيرة للحقول — بالإضافة لـ `.btn-outline-sm`.

#### 4.8 لوحة الإحصائيات (`AnalyticsDashboard`)

| | |
|--|--|
| **الشكل** | dark panel داخل admin — بطاقات stats + جداول |
| **Classes** | `.analytics-dashboard`, `.analytics-stat-card`, `.analytics-filter-tab`, `.analytics-table` |
| **ألوان** | نص أبيض، accent `#5ec8ff`, borders شفافة |

---

### 5. شرح الأزرار والقوائم

#### 5.1 تصميم الأزرار

**نمط موحّد:**
- `border-radius` كبير (pill)
- `transition: var(--ease)` — حركة ناعمة
- `:hover` — تغيير لون + `translateY(-2px)`

#### 5.2 الزر النشط (active)

| مكان | كيف |
|------|-----|
| **تبويب agri** | `className={... agriTab === i ? " active" : ""}` → `.tab-btn.active` |
| **admin sidebar** | `.admin-panel-btn.active` |
| **bottom nav** | `portal === item.key` → `.bottom-nav-item.active` |
| **analytics** | `.analytics-filter-tab.active` |

#### 5.3 التنقل

**Desktop:** navbar `.btn-pill` + بطاقات portal.  
**Mobile (≤768px):** `.bottom-nav` fixed أسفل الشاشة — **5 أيقونات**.

#### 5.4 كيف يعرف المستخدم قسمه؟

1. **Visual:** tab/nav **active** بلون مختلف.
2. **عنوان:** `.section-title` — «بوابة الهندسة الزراعية».
3. **State React:** `portal`, `agriTab` — **CSS يتبع** class active.

#### 5.5 علاقة التصميم بـ state

```
state: portal = "agri"
        ↓
JSX: className="... active"  (conditional)
        ↓
CSS: .bottom-nav-item.active { color: #fff; }
```

**بدون state** — لا يوجد class `active` — **بدون CSS** — active لا معنى له.

---

### 6. شرح البطاقات (Cards)

#### 6.1 ما معنى Card؟

**صندوق** يحتوي: **صورة + عنوان + وصف** — مثل بطاقة منتج في متجر.

#### 6.2 أين في المشروع؟

| مكان | Class |
|------|-------|
| الرئيسية | `.portal-card` (بطاقات أقسام — ليست card عادية) |
| مقالات | `.card.glass` |
| تصاميم | `.card` + `.card-img` |
| كتب | `.book-grid-card.glass` |
| مشاريع web | `.card` في `.web-proj-grid` |
| lab | `.lab-grid` items |

#### 6.3 تنسيق البطاقة

```css
.card { border-radius: 16px; flex-direction: column; overflow: hidden; }
.card-img { width: 100%; height: 195px; object-fit: cover; }
.card-body { padding: 18px; }
.card-title { font-weight: 800; color: var(--navy); }
.card-desc { color: var(--muted); font-size: 0.84rem; }
```

#### 6.4 تعديل بحذر

1. عدّل **`.card-title` font-size** فقط — تحقق من agri + gfx + software.
2. **لا** تغيّر `.card` `display` — يكسر grid.
3. جرّب hover على **بطاقة واحدة** قبل الكل.

---

### 7. شرح الصور والخلفيات

#### 7.1 من أين الصور؟

| المصدر | مثال |
|--------|------|
| **appData / MySQL** | `article.images[0]`, `item.mainImg` |
| **public/** | `/favicon.svg`, `data.json` assets |
| **Google Drive / URLs** | `driveUrl`, روابط خارجية |
| **رفع admin** | upload.php → URL محفوظ |

#### 7.2 كيف تظهر؟

```tsx
<img src={resolveImageSrc(article.images[0])} className="article-card-img" />
<img src={resolveImageSrc(item.mainImg)} className="card-img" />
```

`resolveImageSrc()` — يحوّل المسار ليعمل محليًا وعلى Hostinger.

#### 7.3 public vs رابط خارجي

| | **public/** | **URL خارجي** |
|--|-------------|---------------|
| **مسار** | `/favicon.svg` | `https://...` |
| **الرفع** | مع build | في JSON/DB |
| **CORS** | نفس الموقع | قد يحتاج proxy |

#### 7.4 CSS يتحكم بالحجم

```css
.card-img { width: 100%; height: 195px; object-fit: cover; }
.hero-name-logo-img { max-height: clamp(72px, 14vw, 120px); object-fit: contain; }
```

#### 7.5 object-fit

| القيمة | المعنى |
|--------|--------|
| **cover** | يملأ الإطار — **قد يقص** أطراف الصورة |
| **contain** | يظهر الصورة كاملة — **قد يترك** فراغ |

#### 7.6 صورة مقصوصة أو مشوهة؟

- **مقصوصة:** `object-fit: cover` + ارتفاع ثابت 195px.
- **مشوهة:** نادر — إذا `width/height` بدون object-fit.
- **كبيرة جدًا:** بدون `max-width: 100%` — تكسر layout.

---

### 8. Responsive Design

#### 8.1 ما معنى responsive؟

الموقع **يتكيف** مع عرض الشاشة — كمبيوتر، تابلت، جوال.

#### 8.2 media queries في المشروع

```css
@media (max-width: 900px) { .portal-grid { grid-template-columns: 1fr; } }
@media (max-width: 768px) { .bottom-nav { display: flex; } }
@media (max-width: 640px) { ... }
```

**الفكرة:** «إذا الشاشة **أضيق** من X بكسل — غيّر التنسيق».

#### 8.3 grid و flex

| | **Flex** | **Grid** |
|--|----------|----------|
| **فكرة** | صف أو عمود مرن | شبكة أعمدة/صفوف |
| **مثال** | `.navbar`, `.social-row` | `.portal-grid`, `.card-grid` |
| **responsive** | `flex-wrap` | `grid-template-columns: 1fr` |

#### 8.4 جوال vs desktop

| Desktop | Mobile |
|---------|--------|
| navbar علوي | + bottom-nav |
| 3 portal cards | 1 عمود |
| sub-tabs أفقية | `.agri-mobile-quicktabs` |
| admin sidebar جنب | sidebar فوق |

#### 8.5 useIsMobile

App يستخدم `useIsMobile()` hook — **JS** يغيّر grid columns أحيانًا (بالإضافة لـ CSS).

---

### 9. Flexbox و Grid — شرح بسيط

#### 9.1 Flex — «صف كراسي»

```css
.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
```

**تشبيه:** كراسي في صف — تتوسط أو تتوزع.

#### 9.2 Grid — «رفوف سوبرماركت»

```css
.portal-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}
```

**تشبيه:** 3 صناديق متساوية في صف — على جوال: صندوق واحد في كل صف.

#### 9.3 أين في المشروع؟

| Flex | Grid |
|------|------|
| `.hero`, `.nav-actions`, `.card-body` | `.portal-grid`, `.card-grid`, `.admin-layout`, `.cv-layout`, `.lab-drawer-body` |

---

### 10. الألوان والخطوط

#### 10.1 الألوان الأساسية

| اللون | الاستخدام |
|-------|-----------|
| **#003366 navy** | أزرار، عناوين، brand |
| **#001F3F text** | نص أساسي |
| **#556677 muted** | وصف ثانوي |
| **rgba blues** | hero dark theme |
| **accentColor** | من siteSettings — يُحقن عبر `--navy` |

#### 10.2 أين تغيّر؟

| ماذا | أين |
|------|-----|
| **لون brand** | لوحة التحكم → Site Settings → accent **أو** `:root --navy` |
| **خلفية body** | App `useEffect` theme → `#000c1a` dark / `#eef4ff` light |
| **لون نص** | `:root --text` |
| **خط** | `:root --font` + Google Fonts في index.html |

#### 10.3 تحذيرات

- **لا** تضع نصًا أبيض على خلفية بيضاء.
- **لا** تغيّر `*, *::before` بدون فهم — يكسر Font Awesome.
- **accent** يُعاد حسابه في App — تعديل `:root` فقط قد **يُ overridden**.

---

### 11. المسافات والحواف

#### 11.1 المصطلحات

| | **معنى** | **مثال في المشروع** |
|--|----------|---------------------|
| **margin** | مسافة **خارج** العنصر | `.content-wrap { margin: 0 auto }` |
| **padding** | مسافة **داخل** العنصر | `.card-body { padding: 18px }` |
| **border-radius** | زوايا دائرية | `--radius: 16px`, `.btn-pill { border-radius: 30px }` |
| **box-shadow** | ظل | `.glass { box-shadow: 0 8px 32px ... }` |
| **gap** | مسافة بين عناصر flex/grid | `.portal-grid { gap: 20px }` |

#### 11.2 مظهر احترافي

- **تباعد متسق** — `--radius` واحد للبطاقات.
- **ظلال خفيفة** — `.glass`, hover على cards.
- **transitions** — `--ease: all 0.4s cubic-bezier(...)`.

---

### 12. أخطاء شائعة في CSS

#### 12.1 تغيير class مستخدم في أماكن كثيرة

| | |
|--|--|
| **السبب** | `.card { display: block }` بدل flex |
| **كيف تعرف** | بطاقات agri + gfx + software كلها انكسرت |
| **تجنّب** | أنشئ class **جديد** `.card-compact` |

#### 12.2 حذف class من JSX

| | |
|--|--|
| **السبب** | حذفت `glass` من div |
| **النتيجة** | لوحة بدون blur/shadow |
| **تجنّب** | DevTools — قارن قبل/بعد |

#### 12.3 contrast سيء

| | |
|--|--|
| **السبب** | `--muted` على خلفية داكنة فاتحة جدًا |
| **تجنّب** | اقرأ النص على dark **و** light |

#### 12.4 صورة تكسر layout

| | |
|--|--|
| **السبب** | img بدون `max-width: 100%` |
| **الحل** | `.card-img { width: 100% }` موجود — لا تحذفه |

#### 12.5 تعديل CSS عام

| | |
|--|--|
| **السبب** | `body { font-size: 8px }` |
| **النتيجة** | **كل** الموقع صغير |
| **تجنّب** | عدّل class **محدد** |

#### 12.6 نسيان responsive

| | |
|--|--|
| **السبب** | `.portal-grid { 3 columns }` بدون media query |
| **النتيجة** | بطاقات ضيقة على جوال |
| **تجنّب** | اختبر F12 → Device toolbar |

---

### 13. ماذا تعدّل بأمان؟

#### ✅ آمن

- `--radius` من 16px → 20px
- `.card-title { font-size: 1.1rem }`
- `.btn-prime` hover opacity
- `.hero-bio { line-height: 1.8 }`
- لون `--muted` slightly darker

#### ❌ لا تلمس الآن

- `*, *::before` reset
- `@media` blocks
- `.admin-layout`, `.cv-layout`, `.a4-page`
- `.bottom-nav` mobile
- Font Awesome `::before` fix
- `accentVars` logic في App.tsx

---

### 14. تشبيه: البيت

| البيت | المشروع |
|-------|---------|
| **هيكل الغرف** | React + JSX |
| **الغرف والأثاث** | Components |
| **الدهان والديكور** | CSS |
| **دفتر الديكور** | index.css |
| **الملصق على الأثاث** | className |
| **ترتيب حسب حجم الزائر** | responsive + media queries |

---

### 15. نهاية الفصل

#### 15.1 ملخص

1. **CSS** = الشكل — React = المحتوى والتفاعل.
2. **index.css** = ~5000 سطر — **المصدر الرئيسي** للتصميم.
3. **Tailwind** موجود — لكن App يستخدم **classes مخصصة**.
4. **className** يربط JSX بـ CSS.
5. **:root variables** + **data-theme** + **accentVars** = ألوان ديناميكية.
6. **Cards, tabs, buttons** — أنماط متكررة.
7. **@media** = جوال — **bottom-nav** ≤768px.
8. **print.css** = طباعة CV/PDF منفصل.
9. **object-fit** = cover/contain للصور.
10. **عدّل class واحد** — اختبر كل الأقسام.

#### 15.2 جدول مفاهيم CSS

| مفهوم | معنى | مثال المشروع |
|-------|------|--------------|
| selector | `.card { }` | يستهدف class |
| variable | `--navy` | `:root` |
| flex | صف/عمود | `.navbar` |
| grid | شبكة | `.portal-grid` |
| hover | مرور الماوس | `.card:hover` |
| media query | شاشة صغيرة | `@media (max-width: 768px)` |
| transition | حركة ناعمة | `var(--ease)` |
| object-fit | ملء الإطار | `.card-img cover` |

#### 15.3 جدول ملفات الشكل

| الملف | الوظيفة |
|-------|---------|
| `src/index.css` | تصميم الموقع الكامل |
| `src/print.css` | طباعة A4 / PDF |
| `src/main.tsx` | import CSS |
| `index.html` | Google Fonts |
| `App.tsx` accentVars | ألوان ديناميكية |
| `GridFontControls.ts` | grid styles للمقالات/كتب |
| `vite.config.ts` | `@tailwindcss/vite` plugin |

#### 15.4 أهم 10 أشياء

1. **index.css** — ابدأ من `:root` و `.glass`.
2. **className** — الجسر بين React و CSS.
3. **لا Tailwind** في App — classes مخصصة.
4. **theme dark/light** — `data-theme` على div الجذر.
5. **accentColor** — من admin يغيّر `--navy`.
6. **portal-card** ≠ **card** — styles مختلفة.
7. **bottom-nav** — mobile only.
8. **card-img cover** — صور مقصوصة طبيعي.
9. **fade-up** — animation دخول أقسام.
10. **print.css** — منفصل عن الشاشة.

#### 15.5 أخطاء وحلول

| مشكلة | حل |
|-------|-----|
| أيقونات ▯ | لا تمس FA `::before` rule |
| بطاقات ضيقة | تحقق media 900px |
| ألوان غريبة | accentVars + theme |
| CV مكسور جوال | `.cv-scroll-wrap` — لا تعدّل a4 width |
| admin sidebar تحت | `@media 900px` admin-layout |

#### 15.6 أسئلة تدريبية

1. ما الفرق HTML/CSS/React؟
2. أين يُحمَّل index.css؟
3. لماذا className؟
4. ما `--navy`؟
5. فرق `.portal-card` و `.card`؟
6. ما object-fit cover؟
7. متى يظهر bottom-nav؟
8. ما media query؟
9. فرق flex و grid؟
10. ما print.css؟

#### 15.7 تمرين نظري: تغيير لون زر

**الهدف:** جعل `.btn-prime` أخضر — **بدون تنفيذ**.

1. افتح `index.css` — ابحث `.btn-prime`.
2. انسخ القاعدة — **لا** تحذف الأصل.
3. غيّر `background` فقط — مثلاً `#006633`.
4. **تحقق contrast** — نص أبيض على أخضر داكن.
5. `npm run dev` — اختبر: CV export, admin save, agri.
6. **hover** — هل `:hover` still readable؟
7. **light theme** — `data-theme="light"` — زر still OK؟
8. إن مشكلة — **تراجع** — لا تمس `.btn-back` أو `.tab-btn`.
9. **بديل آمن:** غيّر `--navy` في Site Settings من admin.
10. document: «غيّرت btn-prime background فقط — tested 3 pages».

---

> **الفصل التالي (9):** «شرح الترجمة واللغات» — *(هذا الفصل أدناه)*

---

## الفصل التاسع: شرح الترجمة واللغات — كيف يتغير نص الموقع بين اللغات؟

> **هدف الفصل:** أن تفهم **نظامين للنصوص** في المشروع — واجهة (`translations.ts`) ومحتوى (`ML` في البيانات) — وكيف يختار الزائر اللغة.

---

### 1. ما معنى موقع متعدد اللغات؟

**موقع متعدد اللغات (Multilingual)** = نفس الموقع يعرض **نفس الوظائف** — لكن **النصوص** تتغير حسب اختيار الزائر:

| اللغة | مثال |
|-------|------|
| **ar** | «بوابة الهندسة الزراعية» |
| **en** | «Agricultural Engineering Portal» |
| **de** | «Landwirtschaftsportal» (أو النص الألماني في الملف) |

في مشروع **eng-alaa.com** اللغات الثلاث: **عربي، إنجليزي، ألماني**.

**مهم:** تغيير اللغة **لا** يعيد تحميل الصفحة من السيرver — React يبدّل النصوص فورًا.

---

### 2. لماذا نحتاج ملف `translations.ts`؟

#### 2.1 المشكلة بدون ملف ترجمة

لو كتبت في App.tsx:

```tsx
<button>لوحة التحكم</button>  // عربي فقط!
```

عند التبديل للإنجليزية — **يبقى عربي** — أو تحتاج `if (lang === 'ar')` **في كل زر** — فوضى.

#### 2.2 الحل: ملف واحد للواجهة

`src/translations.ts` = **قاموس** لكل نصوص **واجهة المستخدم** (UI):
- عناوين الأقسام
- أزرار admin
- تسميات CV
- تبويبات

**مكان واحد** — تعدّل ar/en/de **معًا**.

---

### 3. الفرق: نص UI vs نص محتوى

هذا **أهم** تمييز في المشروع — **نظامان**:

| | **نصوص الواجهة (UI)** | **نصوص المحتوى (Content)** |
|--|------------------------|----------------------------|
| **المصدر** | `translations.ts` | `appData` / MySQL / `data.json` |
| **الشكل** | `t.agriTitle` | `pickML(article.title, lang)` |
| **من يعدّله؟** | مطوّر (ملف TS) | مدير (لوحة التحكم) |
| **أمثلة** | «حفظ»، «إلغاء»، «بوابة الزراعة» | عنوان مقال، نص bio، اسم كتاب |

#### 3.1 نص مكتوب مباشرة في Component

```tsx
{lang === 'ar' ? 'حفظ' : lang === 'de' ? 'Speichern' : 'Save'}
```

**موجود** في App.tsx لبعض النصوص **القصيرة** — لكن **الأفضل** استخدام `t.*` أو `ml()`.

#### 3.2 نص من ملف ترجمة

```tsx
<h2>{t.agriTitle}</h2>
<button>{t.adminSaveBio}</button>
```

**مركزي** — يقرأ من `translations[lang]`.

#### 3.3 نص محتوى متعدد اللغات (ML)

```tsx
<p>{pickML(data.bio, lang)}</p>
<div>{pickML(article.title, lang as LangKey)}</div>
```

**ML** = `{ ar: "...", en: "...", de: "..." }` — يُحرّر من **ContentAdmin**.

---

### 4. كيف يتم اختيار اللغة؟

#### 4.1 عند أول فتح الموقع

```tsx
function detectLang(): LangCode {
  const stored = localStorage.getItem(LANG_PREF_KEY);  // 'alaa_lang'
  if (stored && ["ar", "en", "de"].includes(stored)) return stored;
  const browser = navigator.language.toLowerCase();
  if (browser.startsWith("ar")) return "ar";
  if (browser.startsWith("de")) return "de";
  return "ar";  // افتراضي
}
```

**الترتيب:**
1. **localStorage** — اختار الزائر سابقًا؟
2. **لغة المتصفح** — ar / de
3. **افتراضي:** عربي

#### 4.2 عندما يغيّر الزائر اللغة

في **Navbar** — زر «اللغة» → قائمة:

```tsx
const LANG_OPTIONS: [LangCode, string, string][] = [
  ["ar", "العربية", "🇸🇾"],
  ["en", "English", "🇺🇸"],
  ["de", "Deutsch", "🇩🇪"],
];
```

```tsx
const switchLang = useCallback((l: LangCode) => {
  saveLang(l);           // localStorage
  setLang(l);            // React state
  setLangOpen(false);
}, []);
```

---

### 5. كيف يعرف App.tsx أي لغة يعرض؟

#### 5.1 state اللغة

```tsx
const [lang, setLang] = useState<LangCode>(detectLang);
const t: T = translations[lang];
```

- **`lang`** = `'ar' | 'en' | 'de'` — **الحالة الحالية**
- **`t`** = كائن **كل** نصوص UI للغة المختارة

#### 5.2 اتجاه الصفحة (RTL / LTR)

```tsx
useEffect(() => {
  document.documentElement.lang = lang;
  document.documentElement.dir = t.dir;  // ar → rtl, en/de → ltr
}, [lang, t.dir]);
```

**العربية:** من اليمين لليسار.  
**الإنجليزية/الألمانية:** من اليسار لليمين.

---

### 6. كيف تصل النصوص للأقسام؟

#### 6.1 مسار UI (translations)

```
lang = "en"
    ↓
t = translations["en"]
    ↓
<h2>{t.agriTitle}</h2>  → "Agricultural Engineering Portal"
```

#### 6.2 مسار المحتوى (ML)

```
lang = "en"
    ↓
data.agriArticles[0].title = { ar: "...", en: "...", de: "..." }
    ↓
pickML(article.title, "en")  → النص الإنجليزي
```

#### 6.3 مسار مكوّنات فرعية

```tsx
<PlantDiagnostic data={data} lang={lang as LangKey} />
<SeasonNowPanel data={data} lang={lang as LangKey} />
<AnalyticsDashboard lang={lang} />
```

كل مكوّن يستقبل **`lang`** ويختار النص المناسب داخليًا.

#### 6.4 PlantDiagnostic — ML محلي

في `PlantDiagnostic.tsx` — تسميات UI **داخل الملف**:

```tsx
const LBL = {
  reportTitle: ml('تقرير التشخيص...', 'Agricultural Diagnostic...', '...'),
  diagnoseBtn: ml('تشخيص فوري...', 'Instant diagnosis...', '...'),
};
const L = (m: ML) => pickML(m, lang);
```

**ليست** في translations.ts — لأنها **خاصة** بالمكوّن.

---

### 7. ما معنى key في ملف الترجمة؟

**Key** = **اسم** الحقل — مثل مفتاح في قاموس:

```tsx
// translations.ts — ar
agriTitle: 'بوابة الهندسة الزراعية',
adminSaveBio: 'حفظ وتحديث البيانات فوراً',

// الاستخدام
t.agriTitle
t.adminSaveBio
```

| Key | ar | en (مختصر) |
|-----|-----|------------|
| `agriTitle` | بوابة الهندسة الزراعية | Agricultural Engineering Portal |
| `cvExportBtn` | طباعة / حفظ PDF | Print / Save as PDF |
| `adminDashTitle` | لوحة الإدارة... | Comprehensive Admin Panel... |

**interface T** في نهاية translations.ts — **قائمة كل الـ keys** المطلوبة — TypeScript يتحقق أن ar/en/de **نفس المفاتيح**.

---

### 8. ماذا يحدث إذا كانت ترجمة ناقصة؟

| الحالة | النتيجة |
|--------|---------|
| **key ناقص في en/de** | خطأ TypeScript عند `npm run typecheck` |
| **key موجود لكن نص فارغ `""`** | زر/عنوان **فارغ** — يبدو مكسورًا |
| **ML محتوى: en فارغ** | `pickML` **يرجع ar** كـ fallback: `m[lang] \|\| m.ar \|\| m.en \|\| m.de` |
| **displayML للواجهة** | `pickMLStrict` — **لا fallback** — إن en فارغ يظهر فارغ |

**مثال fallback pickML:**

```tsx
// title = { ar: "مقال", en: "", de: "" }
pickML(title, "en")  → "" ثم ar → "مقال"  (يرجع ar)
```

---

### 9. كيف أضيف كلمة أو جملة جديدة للترجمة؟

#### 9.1 خطوات (UI — translations.ts)

1. **اختر key** — اسم إنجليزي camelCase: `contactSectionTitle`
2. **أضف في `ar`** داخل `translations.ar`:
   ```tsx
   contactSectionTitle: 'تواصل معنا',
   ```
3. **أضف نفس key في `en` و `de`**:
   ```tsx
   contactSectionTitle: 'Contact Us',        // en
   contactSectionTitle: 'Kontakt',           // de
   ```
4. **أضف في `interface T`**:
   ```tsx
   contactSectionTitle: string;
   ```
5. **استخدم في JSX**:
   ```tsx
   <h2>{t.contactSectionTitle}</h2>
   ```
6. **`npm run typecheck`** — تأكد لا أخطاء.

#### 9.2 محتوى ML (مقال، كتاب…)

من **لوحة التحكم** — ContentAdmin — حقول **ar / en / de** — **لا** translations.ts.

---

### 10. كيف أعدّل ترجمة موجودة بأمان؟

#### 10.1 تعديل UI

1. افتح `src/translations.ts`
2. ابحث عن key — مثل `adminSaveBio`
3. عدّل **الثلاث لغات** إن لزم
4. **لا** تغيّر **اسم** key — فقط النص
5. احفظ → `npm run dev` → Ctrl+F5
6. جرّب **الثلاث لغات** من قائمة اللغة

#### 10.2 تعديل محتوى

لوحة التحكم → ContentAdmin → حقول ML — **آمن** للمدير.

#### 10.3 قواعد أمان

| ✅ آمن | ❌ خطر |
|--------|--------|
| تغيير **نص** key موجود | حذف key مستخدم في App |
| إضافة key **+** interface T | تغيير `LangCode` بدون فهم |
| تعديل ar/en/de معًا | نسيان en/de → typecheck fail |

---

### 11. أخطاء شائعة في ملفات الترجمة

| الخطأ | السبب | الحل |
|-------|--------|------|
| **TypeScript: Property 'x' is missing** | key في ar فقط | أضف en/de + interface T |
| **نص فارغ** | `""` في ترجمة | املأ أو احذف الاستخدام |
| **خلط UI و content** | تعديل مقال في translations | مقالات في ContentAdmin |
| **فاصلة ناقصة** | syntax JSON-like في TS | تحقق من الفواصل |
| **علامات اقتباس** | `'` داخل `'...'` | استخدم `"..."` أو escape |
| **نسيان dir** | ar بدون rtl | `dir: 'rtl'` في ar |
| **تعديل key في App فقط** | `{t.oldKey}` والkey محذوف | ابحث كل المشروع عن key |

---

### 12. كيف أعرف: translations.ts أم نص مباشر؟

#### 12.1 علامات translations.ts

```tsx
import { translations, T } from "./translations";
const t: T = translations[lang];
{t.adminBtn}
{t.agriTitle}
```

#### 12.2 علامات ML / محتوى

```tsx
import { pickML, ml, displayML } from "./appData";
pickML(data.name, lang)
pickML(article.title, lang)
displayML(data.bio, cvLang)
```

#### 12.3 علامات نص مباشر (inline)

```tsx
{lang === 'ar' ? 'حفظ' : lang === 'de' ? 'Speichern' : 'Save'}
```

**كثير في App.tsx** — نصوص قصيرة لم تُنقل لـ translations بعد.

#### 12.4 علامات ML محلي في مكوّن

```tsx
const LBL = { uploadTitle: ml('...', '...', '...') };
```

في: `PlantDiagnostic.tsx`, `SoilRequest.tsx`, `SeasonNowPanel.tsx`…

#### 12.5 طريقة سريعة للبحث

في Cursor: `Ctrl+Shift+F`:
- `t.` → UI translations
- `pickML(` → محتوى
- `lang === 'ar' ?` → inline

---

### 13. ContentAdmin والترجمة

#### 13.1 MlNameInputs

```tsx
{(['ar', 'en', 'de'] as LangKey[]).map(lk => (
  <input value={value[lk]} onChange={e => onChange({ ...value, [lk]: e.target.value })} />
))}
```

**ثلاث حقول** — ar / en / de — لكل عنوان/وصف.

#### 13.2 MlTranslateControls

أزرار **«ترجمة تلقائية»** — من العربية → en/de عبر AI (`mlTranslate.ts`).

```tsx
<MlObjectTranslateButton value={value} onChange={onChange} />
```

**لا يستبدل** translations.ts — يترجم **محتوى** ML فقط.

#### 13.3 فلترة الكتب حسب اللغة

```tsx
const langFilteredBooks = data.agriBooks.filter(b =>
  !b.languages || b.languages.length === 0 || b.languages.includes(lang)
);
```

كتاب بـ `languages: ["ar"]` **لا يظهر** للزائر الإنجليزي.

---

### 14. AnalyticsDashboard والترجمة

```tsx
export function AnalyticsDashboard({ lang }: { lang: 'ar' | 'en' | 'de' }) {
  const ar = lang === 'ar';
```

تبويبات VIEW_TABS — **ar/en/de داخل الملف**:

```tsx
{ id: 'summary', ar: 'ملخص', en: 'Summary', de: 'Übersicht', ... }
```

**نمط:** `ar ? tab.ar : lang === 'de' ? tab.de : tab.en`

---

### 15. مخطط: رحلة اختيار اللغة

```
المستخدم يضغط «English» في Navbar
        ↓
switchLang("en")
        ↓
saveLang("en") → localStorage['alaa_lang'] = 'en'
        ↓
setLang("en") → React re-render
        ↓
t = translations["en"]
document.documentElement.dir = "ltr"
        ↓
{t.agriTitle} → نص إنجليزي
pickML(article.title, "en") → محتوى إنجليزي
        ↓
PlantDiagnostic, SeasonNowPanel → lang="en"
        ↓
الزائر يرى الموقع بالإنجليزية
```

---

### 16. تشبيه: القاموس

**translations.ts** = **قاموس كبير** بثلاث نسخ (ar, en, de).

**App.tsx** = شخص يسأل: «أعطني `agriTitle` **بالإنجليزية**».

**القاموس** يرد: `"Agricultural Engineering Portal"`.

**ML / appData** = **كتب** على الرف — كل كتاب له **ثلاث نسخ** (ar/en/de) — `pickML` يختار **الفصل** المناسب.

---

### 17. ملخصات ومراجع

#### 17.1 ملخص بسيط

1. **3 لغات:** ar, en, de.
2. **UI** → `translations.ts` + `t.key`.
3. **محتوى** → ML + `pickML`.
4. **lang state** في App + localStorage.
5. **dir** rtl/ltr تلقائي.
6. **لا تخلط** UI و content.
7. **أضف key** في الثلاث + interface T.
8. **ContentAdmin** — ML للمقالات/كتب.
9. **بعض النصوص inline** — `lang === 'ar' ?`.
10. **PlantDiagnostic** — ML محلي LBL.

#### 17.2 جدول ملفات الترجمة

| الملف | الوظيفة |
|-------|---------|
| `translations.ts` | قاموس UI — ar/en/de + interface T |
| `appData.ts` | `ML`, `pickML`, `displayML`, `ml()`, `LANG_PREF_KEY` |
| `App.tsx` | `lang` state, `switchLang`, `t = translations[lang]` |
| `ContentAdmin.tsx` | MlNameInputs — تحرير ML |
| `MlTranslateControls.tsx` | ترجمة AI للمحتوى |
| `mlTranslate.ts` | دوال ترجمة API |
| `PlantDiagnostic.tsx` | LBL — ML محلي |
| `AnalyticsDashboard.tsx` | نصوص تبويبات داخلية |

#### 17.3 أهم 10 أشياء

1. **نظامان:** UI (t.*) vs Content (pickML).
2. **`const t = translations[lang]`** — سطر س magic.
3. **`LANG_PREF_KEY = 'alaa_lang'`** — localStorage.
4. **`detectLang()`** — أول زيارة.
5. **`interface T`** — كل keys مطلوبة.
6. **`dir: rtl/ltr`** — مع اللغة.
7. **pickML fallback** → ar إن en فارغ.
8. **ContentAdmin** — ثلاث حقول ar/en/de.
9. **لا تعدّل مقالات** في translations.ts.
10. **typecheck** بعد أي تعديل translations.

#### 17.4 أخطاء وحلول

| مشكلة | حل |
|-------|-----|
| زر فارغ | تحقق key في translations[lang] |
| typecheck fail | أضف key للثلاث + T |
| مقال عربي للإنجليزي | املأ en في ContentAdmin |
| اتجاه معكوس | تحقق t.dir |
| لغة لا تُحفظ | localStorage / switchLang |

#### 17.5 أسئلة تدريبية

1. فرق `t.agriTitle` و `pickML(article.title, lang)`؟
2. أين تُحفظ اللغة؟
3. ما `LangCode`؟
4. لماذا interface T؟
5. ما pickML fallback؟
6. أين تعدّل «حفظ» في admin؟
7. ما ML؟
8. كيف PlantDiagnostic يترجم؟
9. ما dir rtl؟
10. كيف أضيف key جديد؟

#### 17.6 تمرين نظري: إضافة لغة رابعة (فرنسية)

**الهدف:** `fr` — **بدون تنفيذ** — فهم التعقيد.

1. **LangCode** → `'ar' | 'en' | 'de' | 'fr'`
2. **ML interface** → `{ ar, en, de, fr }`
3. **translations** → قسم `fr: { ... }` **كامل** — نسخ en وتعديل
4. **interface T** — unchanged keys
5. **detectLang** — `browser.startsWith('fr')`
6. **LANG_OPTIONS** — `['fr', 'Français', '🇫🇷']`
7. **pickML** — `m.fr`
8. **schema.sql / data.php** — JSON ML → 4 حقول
9. **ContentAdmin** — حقل رابع في MlNameInputs
10. **اختبار:** كل شاشة × 4 لغات

**الخلاصة:** لغة جديدة = **تغيير واسع** — كمبتدئ **لا** تفعل الآن.

---

> **الفصل التالي (10):** «شرح الصور والملفات والمسارات» — *(هذا الفصل أدناه)*

---

## الفصل العاشر: شرح الصور والملفات والمسارات — كيف يعرف الموقع مكان الصور والملفات؟

> **هدف الفصل:** أن تفهم **من أين تأتي الصور** — public، روابط، Google Drive، رفع Hostinger — ولماذا أحيانًا «تعمل عندي ولا تعمل على الموقع».

---

### مقدمة — ما الذي فحصناه في المشروع؟

قبل أن نبدأ الشرح، هذه **خريطة سريعة** لأماكن الصور في مشروع eng-alaa.com (Portfolio Hub):

| المكان | ماذا يوجد؟ |
|--------|------------|
| **`public/`** | `favicon.svg`, `LogoAlaa.svg`, `robots.txt`, `content-manager.html`, قوالب CSV — و **`data.json`** عندما تُنشئه (قد لا يكون داخل Git) |
| **`src/`** | كود React — **لا** تُخزَّن هنا صور التصاميم والمقالات عادةً |
| **`src/mediaUrl.ts`** | دوال تحويل روابط Drive → صورة تعمل في `<img>` |
| **`src/appData.ts`** | رفع ملفات، ضغط صور، جلب `data.json` |
| **`src/App.tsx`** | يعرض بطاقات التصاميم والمقالات والكتب — يمرّر `mainImg` إلى `<img>` |
| **`src/ContentAdmin.tsx`** | لوحة التحكم — حقول URL + معاينة + رفع |
| **`src/BookCover.tsx`** | غلاف الكتاب — `<img>` أو placeholder |
| **`hostinger_deploy/api/upload.php`** | يرفع ملفًا إلى مجلد `uploads/` على السيرفر |
| **MySQL** | **نص الرابط** فقط — مثل `"https://drive.google.com/..."` |

**تذكّر:** الموقع **لا يبحث** عن صورة باسمها داخل مجلدات React — يقرأ **رابطًا مكتوبًا** في البيانات، ثم المتصفح يحمّل الصورة من ذلك الرابط.

---

### 1. ما معنى path (مسار الملف)؟

**المسار (Path)** = **العنوان** الذي يخبر المتصفح **أين يجد** الملف.

**تشبيه:** مثل عنوان منزل — «شارع X، بناية Y، شقة 3».  
إذا كتبت العنوان **خطأ** — البريد (المتصفح) **لا يجد** الصورة.

**مثال من حياتك اليومية:**  
عندما تفتح موقعًا وتكتب في المتصفح `eng-alaa.com/favicon.svg` — أنت تطلب **مسارًا** محددًا. السيرفر يبحث عن ملف اسمه `favicon.svg` في جذر الموقع. إن وُجد → ترى الأيقونة. إن لم يُوجد → خطأ **404 Not Found**.

**في React:** لا «ننسخ» الصورة داخل الكود. نكتب في `<img src="...">` **عنوانًا** (مسارًا أو رابطًا). React يضع هذا العنوان في HTML، والمتصفح يتولى التحميل.

**أمثلة في المشروع:**

| المسار | المعنى |
|--------|--------|
| `/favicon.svg` | ملف في جذر الموقع (من مجلد `public/`) |
| `./data.json` | ملف JSON بجانب `index.html` على السيرفر |
| `https://eng-alaa.com/uploads/gfx/abc.png` | رابط كامل لصورة مرفوعة على Hostinger |
| `https://drive.google.com/file/d/XXX/view` | رابط مشاركة Google Drive |

**في `index.html`:**

```html
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
```

الشرطة `/` في البداية = «ابدأ من **جذر** الموقع» — أي `public_html/favicon.svg` بعد النشر.

---

### 2. الفرق: صورة في `public/` vs صورة في `src/`

#### 2.1 مجلد `public/`

```
public/
├── favicon.svg
├── LogoAlaa.svg
├── robots.txt
├── content-manager.html
├── content-manager-templates/   ← ملفات CSV
└── data.json                    ← (يُرفع — قد لا يكون في Git)
```

**قاعدة Vite:** كل ما في `public/` يُنسَخ **كما هو** إلى `dist/` ثم `public_html/`.  
**المتصفح يصل إليها** مباشرة: `https://eng-alaa.com/favicon.svg`

**لا** يمر عبر React import — **مسار ثابت**.

#### 2.2 مجلد `src/`

- ملفات **React/TypeScript** — تُ **import** في الكود.
- Vite **يبنيها** — صور مُ import تُدمج في `assets/` بأسماء hash.
- **مثال:** `import logo from './logo.png'` → `/assets/logo-abc123.png`

**في مشروعك:** أغلب **صور المحتوى** **ليست** في src — بل **روابط** في البيانات.

#### 2.3 `@assets` في vite.config

```ts
"@assets": path.resolve(import.meta.dirname, "attached_assets"),
```

مجلد **attached_assets** — لملفات إضافية إن وُجدت — alias `@assets/...`.

---

### 3. لماذا نضع بعض الملفات في `public/`؟

| السبب | مثال |
|-------|------|
| **URL ثابت** بدون build | `favicon.svg`, `data.json` |
| **تحديث SFTP بدون npm build** | `data.json` — محتوى كتب/مقالات |
| **أدوات جانبية** | `content-manager.html` |
| **robots.txt** للمحركات | SEO |

**data.json** — حسب `.cursorrules`:  
ارفع `public/data.json` → `public_html/data.json` — **Ctrl+F5** — **بدون** build.

---

### 4. كيف تظهر الصورة داخل React؟

#### 4.1 tag `<img>`

```tsx
<img src={resolveImageSrc(item.mainImg)} alt={pickML(item.title, lang)} className="card-img" />
```

**الخطوات:**
1. `item.mainImg` = **نص** (URL) من البيانات
2. `resolveImageSrc()` = يحوّل Drive → thumbnail يعمل
3. المتصفح يطلب URL من `src`
4. CSS `.card-img` يتحكم بالحجم

#### 4.2 BookCover

```tsx
<BookCover src={resolveImageSrc(book.thumbnail)} alt={pickML(book.title, lang)} />
```

داخل BookCover:

```tsx
{src ? <img src={src} alt={alt} loading="lazy" /> : <div className="book-cover-placeholder">📚</div>}
```

**لا صورة؟** → placeholder emoji.

#### 4.3 أي Components تعرض صورًا في المشروع؟

| الملف | ماذا يعرض؟ | كيف يصل للرابط؟ |
|-------|------------|-----------------|
| **`App.tsx`** | بطاقات التصاميم، المقالات، معرض الصور، العلامة المائية | `item.mainImg`, `article.images[]`, `data.watermarkImg` |
| **`ContentAdmin.tsx`** | معاينة أثناء التحرير | نفس الحقول + `resolveImageSrc` |
| **`BookCover.tsx`** | غلاف الكتاب | `src` يُمرَّر من الأب |
| **`FileExplorerAdmin.tsx`** | ملفات مرفوعة على السيرفر | قائمة من API |
| **`CvRenderer.tsx`** | صورة السيرة الشخصية | `personal_info.photo` |

**القاعدة:** كل هذه المكونات **لا تخزّن** الصورة — تستقبل **string** (URL) وتضعه في `src`.

#### 4.4 مثال: زائر يفتح قسم التصاميم

1. يفتح `eng-alaa.com` → `App.tsx` يحمّل البيانات من `data.php` أو `localStorage`.
2. يضغط «التصاميم» → `portal = "gfx"`.
3. React يرسم بطاقة لكل `GfxProjectItem`.
4. داخل البطاقة: `<img src={resolveImageSrc(item.mainImg)} />`.
5. المتصفح يرسل طلب HTTP GET إلى Google أو Hostinger.
6. تصل bytes الصورة → تُرسم على الشاشة.

**أنت كمبتدئ:** افتح **F12 → Network → Img** — ستشاهد كل طلبات الصور وروابطها وحالة الاستجابة (200 = نجاح، 404 = لم يُوجد).

### 5. رابط خارجي vs صورة داخل المشروع

| | **داخل المشروع / Hostinger** | **خارجي** |
|--|-------------------------------|-----------|
| **مثال** | `/uploads/gfx/xxx.png` | Google Drive, Imgur |
| **أين يُحفظ؟** | سيرفر Hostinger | سيرفر Google |
| **المشروع يحفظ** | **الرابط فقط** | **الرابط فقط** |
| **تحويل** | مباشر أو `./api/upload.php` | `resolveImageSrc()` لـ Drive |

**مهم:** الموقع **لا يخزّن ملف الصورة** في React — يخزّن **string URL** في JSON/MySQL.

---

### 6. كيف يعرف الموقع مكان الصورة؟

```
1. البيانات (MySQL / data.json / localStorage)
   → gfx_items.main_img = "https://drive.google.com/..."
2. App.tsx يقرأ data.gfxCategories
3. Component يمرّر mainImg
4. resolveImageSrc(mainImg) → URL يعمل في <img>
5. المتصفح GET → Google أو Hostinger
6. الصورة تظهر
```

**الدالة المركزية:** `src/mediaUrl.ts` → `resolveImageSrc()`

---

### 7. Relative path (مسار نسبي)

**نسبي** = بالنسبة **للصفحة الحالية** — **بدون** domain كامل.

| المسار | من `https://eng-alaa.com/` |
|--------|---------------------------|
| `./data.json` | `https://eng-alaa.com/data.json` |
| `./api/data.php` | `https://eng-alaa.com/api/data.php` |
| `/favicon.svg` | `https://eng-alaa.com/favicon.svg` |

**`.`** = «نفس المجلد» (جذر الموقع).  
**`/`** في البداية = «من **جذر** الدomain».

**في المشروع:** `base: "/"` في vite.config — المسارات من **جذر** eng-alaa.com.

---

### 8. Absolute path (مسار مطلق)

**مطلق** = عنوان **كامل** — protocol + domain + path.

```
https://eng-alaa.com/uploads/gfx/67890abc.png
https://drive.google.com/thumbnail?id=XXX&sz=w1200
data:image/png;base64,iVBORw0KGgo...
```

| النوع | متى |
|-------|-----|
| **https://...** | Drive, uploads Hostinger, CDN |
| **data:...** | صورة مضغوطة محليًا (fallback) |
| **blob:...** | معاينة مؤقتة قبل رفع |

---

### 9. لماذا لا تظهر الصورة بعد الرفع؟

| السبب | مثال |
|-------|------|
| **ملف لم يُرفع** | رابط `/uploads/...` لكن الملف ناقص على Hostinger |
| **مسار خاطئ** | `./images/x.png` بدل `/uploads/gfx/x.png` |
| **Drive link خاطئ** | رابط مشاركة لا يعمل في `<img>` بدون `resolveImageSrc` |
| **base path** | build بـ base خاطئ — assets 404 |
| **CORS** | Canvas/غلاف كتاب — يستخدم `img-proxy.php` |
| **case sensitive** | `Photo.PNG` vs `photo.png` على Linux |
| **مسافات/أحرف** | `my photo.jpg` — مشاكل URL |

---

### 10. Hostinger ومسارات الصور

#### 10.1 بعد النشر

```
public_html/
├── index.html
├── assets/          ← JS/CSS من build
├── favicon.svg      ← من public/
├── data.json        ← محتوى (SFTP)
├── uploads/         ← ملفات مرفوعة (upload.php)
│   ├── gfx/
│   ├── books/
│   ├── projects/
│   └── general/
└── api/
    └── upload.php
```

#### 10.2 upload.php

```php
// يرفع الملف → UPLOAD_BASE/folder/
// يرجع:
$url = UPLOAD_URL . '/' . $folder . '/' . $newName;
// { "url": "https://eng-alaa.com/uploads/gfx/..." }
```

**config.php** (سري) يحدّد:
- `UPLOAD_BASE` — مسار على القرص
- `UPLOAD_URL` — الرابط العام

#### 10.3 من Frontend

```tsx
const url = await uploadMediaFile(file, 'gfx');
// POST ./api/upload.php + JWT
// يرجع url → تضعه في mainImg
```

---

### 11. Vite ومسارات الملفات

| Vite | التأثير |
|------|---------|
| **`base: "/"`** | `/assets/xxx.js` من جذر الموقع |
| **`public/`** | ينسخ كما هو — `/favicon.svg` |
| **`import img from './x.png'`** | يُبنى إلى `/assets/x-hash.png` |
| **dev server :5173** | public متاح على localhost:5173/favicon.svg |
| **proxy `/api`** | PHP على Hostinger — ليس للصور static |

**محليًا:** `./data.json` → `localhost:5173/data.json` (من public).  
**منشورًا:** `eng-alaa.com/data.json`.

---

### 12. كيف أضيف صورة جديدة بطريقة صحيحة؟

#### 12.1 طريقة 1: Google Drive (الأكثر شيوعًا للمحتوى)

1. ارفع الصورة على Drive → **Anyone with link**
2. انسخ رابط المشاركة: `https://drive.google.com/file/d/ID/view`
3. في **لوحة التحكم** → حقل mainImg / thumbnail / images
4. الصق الرابط — `normalizeImageUrlForStorage` يحوّله عند blur
5. **احفظ** → MySQL
6. الموقع يستخدم `resolveImageSrc()` تلقائيًا

#### 12.2 طريقة 2: رفع على Hostinger (upload.php)

1. **connectServer** — JWT
2. ContentAdmin → زر رفع → `uploadMediaFile(file, 'gfx')`
3. يرجع URL مثل `https://eng-alaa.com/uploads/gfx/xxx.webp`
4. يُحفظ في `mainImg` — **احفظ** AppData

#### 12.3 طريقة 3: ملف static في public (نادر للمحتوى)

1. ضع `public/my-banner.png`
2. `npm run build:hostinger` + رفع
3. استخدم `/my-banner.png` في JSON أو siteSettings

**للمحتوى الديناميكي:** الطريقة 1 أو 2 — **ليس** public.

#### 12.4 طريقة 4: data.json (كتب/مقالات/تصاميم)

1. عدّل `public/data.json` أو content-manager.html
2. SFTP → `public_html/data.json`
3. Ctrl+F5 — **بدون build**

---

### 13. كيف أغير صورة مشروع أو تصميم؟

1. **لوحة التحكم** → ContentAdmin → mode `gfx`
2. اختر مشروعًا → حقل **mainImg** (أو رفع ملف)
3. **onSave** → `handleCvSave` → MySQL
4. زائر جديد → GET data.php → يرى الصورة الجديدة

**أو:** SFTP data.json إن التصميم من هناك (حسب merge rules في App).

---

### 14. ماذا لو غيرت اسم الصورة؟

| **السيناريو** | النتيجة |
|-----------|---------|
| **ملف على Hostinger** `abc.png` → `xyz.png` | كل الروابط القديمة **404** |
| **Drive** — نفس ID | الرابط يعمل |
| **في DB** mainImg لا يزال `.../abc.png` | **صورة مكسورة** |

**القاعدة:** غيّر **الرابط في البيانات** — أو **لا تغيّر** اسم الملف على السيرفر.

---

### 15. ماذا لو حذفت صورة مستخدمة؟

- **404** في Network tab
- `<img onError>` — بعض المكونات تخفى الصورة أو opacity 0.3
- **BookCover** → placeholder 📚
- **لا crash** — React يستمر — لكن **فراغ** بصري

---

### 16. أين تُخزَّن روابط الصور في البيانات؟

| نوع المحتوى | الحقل | نوع |
|-------------|-------|-----|
| **تصميم gfx** | `mainImg`, `images[]` | string URL |
| **مقال** | `images[]` | مصفوفة URLs |
| **كتاب** | `thumbnail`, `driveUrl` | string |
| **مشروع web** | `mainImg`, `images[]` | string |
| **مهارة** | `icon` | URL أو data: |
| **site settings** | `logoImg`, `watermarkImg` | string |
| **سيرة** | `photo` في personal_info | string |

**في MySQL:** أعمدة TEXT/JSON — **النص URL** — **ليس** BLOB للصورة.

#### 16.1 مثال من `data.json` — تصميم gfx

```json
{
  "designs": {
    "categories": [
      {
        "id": "cat-1",
        "subCategories": [
          {
            "id": "sub-1",
            "items": [
              {
                "id": "design-001",
                "title": { "ar": "مشروع زراعي", "en": "Farm project", "de": "..." },
                "mainImg": "https://drive.google.com/file/d/XXXX/view",
                "images": [
                  "https://drive.google.com/file/d/YYYY/view"
                ]
              }
            ]
          }
        ]
      }
    ]
  }
}
```

**لاحظ:** `mainImg` و `images` = **نصوص روابط** — ليست مسارات ملفات على قرصك. عند رفع `data.json` إلى `public_html`، الموقع يدمج هذه القيم فوق البيانات المحلية (حسب الفصل الرابع).

---

### 17. DB vs ملف — أين الصورة فعليًا؟

| | **في MySQL** | **على القرص / Drive** |
|--|--------------|------------------------|
| **ماذا يُحفظ؟** | `"https://..."` | **bytes** الصورة |
| **حجم DB** | صغير (نص) | — |
| **data: URL** | نص base64 **طويل** | داخل DB كنص! |

#### 17.1 هل يمكن حفظ الصور داخل MySQL؟

**نعم تقنيًا** (BLOB أو base64 في TEXT) — **لكن المشروع لا يفعل** للصور الكبيرة.

**يستخدم data: URL** أحيانًا:
- `compressImageFileForStorage()` — صور صغيرة مضغوطة
- fallback بدون upload.php

#### 17.2 الأفضل كمبتدئ

✅ **احفظ الصورة كملف** (Drive أو uploads/) + **الرابط في البيانات**

❌ **لا** تلصق base64 ضخم في كل حقل

---

### 18. `resolveImageSrc` — شرح مبسط

```tsx
export function resolveImageSrc(url: string, width = 1200): string {
  if (u.startsWith('data:') || u.startsWith('blob:')) return u;
  const id = extractDriveFileId(u);
  if (!id) return u;  // رابط عادي — كما هو
  return `https://drive.google.com/thumbnail?id=${id}&sz=w${width}`;
}
```

| المدخل | المخرج |
|--------|--------|
| Drive share link | thumbnail Google |
| `https://eng-alaa.com/uploads/...` | نفس الرابط |
| `data:image/jpeg;base64,...` | نفس data URL |
| فارغ `""` | فارغ — placeholder |

---

### 19. مخطط: من البيانات إلى الشاشة

```
ملف البيانات يحتوي رابط الصورة
   (MySQL عبر data.php — أو data.json — أو localStorage)
   مثال: mainImg = "https://drive.google.com/file/d/ABC/view"
        ↓
App.tsx يقرأ البيانات
   data.gfxCategories → items[] → كل item فيه mainImg
        ↓
Component يستلم رابط الصورة
   بطاقة التصميم / BookCover / مقال
        ↓
img tag يعرض الصورة
   <img src={resolveImageSrc(item.mainImg)} className="card-img" />
        ↓
المتصفح يطلب الصورة من public أو من رابط خارجي
   GET → drive.google.com/thumbnail  أو  eng-alaa.com/uploads/...
        ↓
الصورة تظهر للزائر
```

**ملاحظة:** خطوة `resolveImageSrc` **ضرورية** لروابط Drive — لأن رابط `/view` وحده لا يعمل دائمًا داخل `<img>`.

---

### 20. أخطاء شائعة

#### 20.1 تعمل محليًا ولا تعمل على Hostinger

| | |
|--|--|
| **ماذا ترى؟** | صورة محلياً — 404 على الموقع |
| **السبب** | `localhost` file path، أو uploads/ غير مرفوع |
| **أين تفحص؟** | Network → status 404 |
| **الحل** | URL كامل https://eng-alaa.com/uploads/... أو Drive |

#### 20.2 اسم فيه فراغات أو أحرف غريبة

| | |
|--|--|
| **السبب** | `my photo (1).jpg` — URL encoding |
| **الحل** | `-` أو `_` — بدون مسافات — أو encode |

#### 20.3 مسار يبدأ خطأ

| خطأ | صحيح |
|-----|------|
| `public/favicon.svg` | `/favicon.svg` |
| `C:\Users\...\img.png` | **لا** — مسارات Windows |
| `../src/logo.png` | import أو `/uploads/...` |

#### 20.4 صورة كبيرة جداً

| | |
|--|--|
| **السبب** | 10MB PNG — بطء + upload 413 |
| **الحل** | compress — WebP — max 5MB في upload.php |

#### 20.5 حذف صورة مستخدمة

| | |
|--|--|
| **الحل** | حدّث الرابط في ContentAdmin **قبل** الحذف |

#### 20.6 مجلد غير مناسب

| | |
|--|--|
| **السبب** | صورة في `src/` لكن URL في DB يشير لمسار قديم |
| **الحل** | uploads/gfx أو Drive — **رابط واحد** في DB |

---

### 21. تشبيه: الخزانة والعنوان

**الصور** = ملفات داخل **خزانات** (Google Drive، مجلد `uploads/` على Hostinger، مجلد `public/`).

**المسار في البيانات** = **العنوان المكتوب على باب الخزانة** — مثل: «الخزانة 3، الرف B، ملف abc.png».

**React / App.tsx** = موظف يقرأ العنوان من دفتر البيانات (MySQL أو JSON) ويذهب للخزانة.

**المتصفح** = الشخص الذي يفتح الباب فعليًا ويحمل الملف.

**إذا كتبت العنوان خطأ** — الباب فارغ — **404** — ترى أيقونة صورة مكسورة أو placeholder 📚.

**لا تخلط:** وجود ملف على جهازك في `Downloads` **لا يكفي** — يجب أن يكون **العنوان في البيانات** يشير لمكان يصل إليه **الزائر** (سيرفر على الإنترنت).

---

### 22. ملفات مهمة في المشروع

| الملف | الدور |
|-------|-------|
| `public/` | static: favicon, LogoAlaa, data.json |
| `src/mediaUrl.ts` | resolveImageSrc, Drive, video |
| `src/appData.ts` | uploadMediaFile, compress, data.json load |
| `hostinger_deploy/api/upload.php` | رفع ملفات |
| `hostinger_deploy/api/img-proxy.php` | CORS لـ Drive في Canvas |
| `ContentAdmin.tsx` | حقول URL + رفع |
| `FileExplorerAdmin.tsx` | إدارة ملفات مرفوعة |
| `BookCover.tsx` | عرض غلاف كتاب |
| `vite.config.ts` | base, @assets |

---

### 23. نهاية الفصل

#### 23.1 ملخص

1. **المسار** = عنوان الملف.
2. **public/** = static — `/favicon.svg`.
3. **المحتوى** = **روابط** في DB — ليس ملفات في src.
4. **`resolveImageSrc`** = Drive → thumbnail.
5. **`uploadMediaFile`** = رفع Hostinger → URL.
6. **relative** `./data.json` — **absolute** `https://...`.
7. **Vite base `/`** — من جذر eng-alaa.com.
8. **data.json** — SFTP بدون build.
9. **لا تحذف** ملفًا قبل تحديث الرابط.
10. **الأفضل:** ملف + رابط — **ليس** base64 ضخم.

#### 23.2 جدول أنواع المسارات

| النوع | مثال | الاستخدام |
|-------|------|-----------|
| Root absolute | `/favicon.svg` | public static |
| Relative site | `./data.json` | JSON محتوى |
| Full URL | `https://eng-alaa.com/uploads/gfx/x.webp` | uploads |
| Drive share | `drive.google.com/file/d/ID/view` | + resolveImageSrc |
| data URL | `data:image/png;base64,...` | fallback صغير |
| blob | `blob:http://localhost:5173/...` | معاينة مؤقتة |

#### 23.3 أين أضع الصور؟

| نوع الصورة | أين | كيف في DB |
|------------|-----|-----------|
| Favicon, logo static | `public/` | `/LogoAlaa.svg` |
| تصميم/مقال/كتاب | Drive أو uploads | URL في mainImg/thumbnail |
| محتوى CSV/json | data.json | SFTP public_html |
| skill icon | uploads/skills أو URL | icon field |
| CV photo | uploads/cv أو URL | personal_info.photo |

#### 23.4 أهم 10 أشياء

1. DB يحفظ **رابط** — **ليس** الملف.
2. **`resolveImageSrc`** دائمًا للعرض.
3. **public/** = URL من الجذر `/`.
4. **upload.php** يحتاج JWT.
5. **Drive** — رابط مشاركة — ليس direct download فقط.
6. **لا مسارات Windows** في URLs.
7. **Hostinger Linux** — حساس لحالة الأحرف.
8. **data.json** منفصل عن build.
9. **Network tab** — أفضل تشخيص.
10. **غيّر الرابط** قبل حذف الملف.

#### 23.5 أخطاء وحلول (مرجع)

| # | مشكلة | حل |
|---|--------|-----|
| 1 | 404 image | تحقق URL + رفع الملف |
| 2 | Drive broken | resolveImageSrc |
| 3 | محلي OK / live no | absolute URL |
| 4 | upload fail | JWT + 5MB limit |
| 5 | بطء | ضغط WebP |

#### 23.6 أسئلة تدريبية

1. فرق public vs src للصور؟
2. أين mainImg مخزّن؟
3. ما resolveImageSrc؟
4. فرق relative vs absolute؟
5. كيف uploadMediaFile؟
6. لماذا data.json SFTP؟
7. ماذا لو حذفت uploads/file؟
8. هل MySQL يخزّن bytes الصورة؟
9. ما base في Vite؟
10. كيف أفحص 404؟

#### 23.7 تمرين نظري: صورة لتصميم gfx

**الهدف:** mainImg جديد — **بدون تنفيذ**.

1. جهّز صورة WebP < 500KB.
2. **خيار A:** ارفع Drive → انسخ share link.
3. **خيار B:** connectServer → ContentAdmin gfx → upload → انسخ URL.
4. افتح مشروعًا في ContentAdmin.
5. الصق في **mainImg** — preview يظهر؟
6. **احفظ** → handleCvSave → data.php.
7. Ctrl+F5 كزائر — قسم graphics — Network → img 200؟
8. جوال — نفس الصورة؟
9. **لا** تضع `C:\Users\...` في الحقل.
10. document: URL النهائي + مشروع id.

---

> **الفصل التالي (11):** «الأخطاء الشائعة وطريقة إصلاحها» — *(هذا الفصل أدناه)*

---

## الفصل الحادي عشر: الأخطاء الشائعة وطريقة إصلاحها — كيف أفهم المشكلة بدل الخوف منها؟

> **هدف الفصل:** أن تتعلّم **قراءة** الخطأ — أين يظهر، ماذا يعني، وكيف تصلّحه خطوة صغيرة بدل الذعر.

---

### 1. ما معنى error (خطأ)؟

**Error** = رسالة تقول: «شيء **توقف** أو **فشل** — لا يمكن المتابعة بهذا الشكل».

**تشبيه:** مثل إنذار السيارة — الضوء الأحمر يقول «هناك مشكلة — توقّف وافحص».

**في مشروعك:** قد يكون خطأ في **Terminal** (`build failed`)، أو في **Console المتصفح** (`undefined is not a function`)، أو من **السيرفر** (`500 Internal Server Error`).

**مهم:** الخطأ **ليس** عدوك — هو **دليل** يشير لمكان المشكلة.

---

### 2. ما معنى warning (تحذير)؟

**Warning** = تنبيه **أصفر** — «شيء غير م ideal لكن البرنامج **قد** يستمر».

**فرق Error vs Warning:**

| | Error | Warning |
|--|-------|---------|
| **اللون غالبًا** | أحمر | أصفر |
| **هل يوقف؟** | غالبًا نعم | غالبًا لا |
| **مثال** | syntax error — لا build | متغير غير مستخدم |

**في مشروعك:** تحذير TypeScript في Cursor قد لا يمنع `npm run dev` — لكن **يستحسن** إصلاحه قبل النشر.

---

### 3. أين تظهر الأخطاء؟

| المكان | ماذا ترى؟ | متى تستخدمه؟ |
|--------|-----------|--------------|
| **Cursor (المحرر)** | خطوط حمراء، Problems panel | قبل التشغيل — أخطاء TypeScript |
| **Terminal** | `npm run dev` / `build` فشل | مشاكل build أو أوامر |
| **Console (المتصفح F12)** | أخطاء JavaScript أثناء التصفح | صفحة بيضاء، أزرار لا تعمل |
| **Network tab** | 404, 500, failed fetch | صور، data.php، auth |
| **Hostinger** | صفحة بيضاء، 500، Error logs | بعد النشر فقط |
| **لوحة Hostinger** | Error log, File Manager | PHP/MySQL على السيرفر |

**قاعدة:**  
- **قبل النشر** → Cursor + Terminal + Console محليًا  
- **بعد النشر** → Console + Network + Hostinger logs

---

### 4. كيف أقرأ رسالة الخطأ؟

**الترتيب الصحيح لقراءة أي خطأ:**

1. **السطر الأول** — ماذا حدث؟ (`Failed to fetch`, `Unexpected token`)
2. **اسم الملف ورقم السطر** — `App.tsx:2004`
3. **Stack trace** — سلسلة الدوال التي وصلت للخطأ (اقرأ **أول** سطر يخص **ملفك** في `src/`)
4. **الرسالة التفصيلية** — `Cannot read properties of undefined`

**مثال:**

```
TypeError: Cannot read properties of undefined (reading 'title')
    at App.tsx:1844
```

**الترجمة:** في السطر 1844 من App.tsx، شيء كان `undefined` وحاولت قراءة `.title` منه.

**لا تقرأ كل Stack** — ركّز على **أول ملف في src/**.

---

### 5. ما معنى Console؟

**Console** = «دفتر ملاحظات» المتصفح — يطبع رسائل JavaScript: `console.log`, أخطاء, تحذيرات.

**كيف تفتحه:** F12 → تبويب **Console**

**في مشروعك:**  
- أخطاء `fetch('./api/data.php')` تظهر هنا  
- أخطاء React (مثل key مفقود) تظهر هنا  
- `console.error` من الكود يظهر هنا

---

### 6. ما معنى Network tab؟

**Network** = قائمة **كل الطلبات** التي يرسلها المتصفح: HTML, JS, CSS, صور, API.

**كيف تفتحه:** F12 → **Network** → حدّث الصفحة (Ctrl+F5)

**ما تبحث عنه:**

| العمود | المعنى |
|--------|--------|
| **Name** | اسم الملف أو API |
| **Status** | 200 = OK, 404 = غير موجود, 500 = خطأ سيرفر |
| **Type** | fetch, img, script… |
| **Response** | محتوى رد data.php |

**في مشروعك:** افحص `./api/data.php`, `./data.json`, صور `mainImg`.

---

### 7. ما معنى 404؟

**404 Not Found** = «طلبت ملفًا أو صفحة — **السيرفر لا يجدها**».

**أمثلة في مشروعك:**
- `/uploads/gfx/photo.png` — الملف غير مرفوع
- `./api/data.php` — مجلد api ناقص على Hostinger
- `/assets/index-OLD.js` — build قديم بعد deploy جديد

**الحل:** تحقق **المسار** + **هل الملف موجود** على السيرفر.

---

### 8. ما معنى 500؟

**500 Internal Server Error** = «السيرver **وصل** للملف PHP لكن **حدث خطأ داخلي**».

**أسباب شائعة في مشروعك:**
- `config.php` ناقص أو بيانات DB خاطئة
- syntax error في PHP
- جدول MySQL غير موجود
- صلاحيات ملف خاطئة

**أين تفحص:** Hostinger Error Log + افتح `data.php` في المتصفح مباشرة.

---

### 9. ما معنى CORS؟

**CORS** = Cross-Origin Resource Sharing — قاعدة أمان: «هل هذا الموقع **مسموح** له يطلب بيانات من موقع آخر؟»

**متى تراه في مشروعك:**
- محليًا `localhost:5173` يطلب API من domain مختلف
- Canvas يحاول رسم صورة Drive — يستخدم **`img-proxy.php`** لحلّ المشكلة

**الرسالة الشائعة:**

```
Access to fetch at '...' has been blocked by CORS policy
```

**كمبتدئ:** إن ظهر CORS — المشكلة غالبًا **ليس** في React بل في **إعداد السيرفر** أو **نطاق الطلب**.

---

### 10. ما معنى build failed؟

**Build failed** = أمر `npm run build` **لم ينجح** — لم يُنتَج مجلد `dist/` صالح.

**أسباب:**
- syntax error في TypeScript/TSX
- import لملف غير موجود
- type error (إن كان strict)

**الحل:** اقرأ **آخر** رسائل Terminal — السطر الأحمر الأول غالبًا يحدد الملف.

**في مشروعك:** جرّب أيضًا `npm run typecheck` قبل build.

---

### 11. ما معنى syntax error؟

**Syntax error** = **قواعد اللغة** مكسورة — مثل نسيان `}` أو `)` أو فاصلة.

**مثال:**

```tsx
const x = { name: "علاء"   // ← ناقص }
```

**أين يظهر:** Cursor (خط أحمر), Terminal عند build.

---

### 12. ما معنى import error؟

**Import error** = الملف يحاول **استيراد** شيء **غير موجود** أو مسار خاطئ.

**مثال:**

```
Failed to resolve import "./NoSuchFile.tsx" from "App.tsx"
```

**الحل:** تحقق **اسم الملف**، **حالة الأحرف** (Linux حساس), **المسار النسبي** `./` vs `../`.

---

### 13. ما معنى undefined؟

**undefined** = «هذا المتغير **لا قيمة له** بعد» — فارغ من ناحية JavaScript.

**مثال شائع:**

```tsx
pickML(item.title, lang)  // إن item = undefined → crash
```

**الحل:** تحقق **هل البيانات حُمّلت** قبل العرض — `item?.title` أو شرط `{item && ...}`.

---

### 14. ما معنى JSON parse error؟

**JSON parse error** = النص **ليس** JSON صالح — غالبًا فاصلة زائدة، أو ملف PHP أرجع HTML خطأ بدل JSON.

**في مشروعك:**
- `data.json` فيه syntax خطأ
- `data.php` أرجع صفحة خطأ 500 (HTML) — `fetch` يحاول `json()` → فشل

**الحل:** افتح الرابط في المتصفح — هل ترى `{` و `}` صحيحة؟

---

### 15. ما معنى database connection failed؟

**Database connection failed** = PHP **لم يستطع** الاتصال بـ MySQL.

**أسباب على Hostinger:**
- `config.php` — host, user, password, database name خاطئة
- MySQL غير مُنشأ
- `schema.sql` لم يُستورد

**الأعراض:** data.php 500 — الحفظ لا يعمل — لوحة التحكم فارغة.

---

### 16. جدول الأخطاء الشائعة في هذا المشروع

#### 16.1 صفحة بيضاء

| | |
|--|--|
| **ماذا أرى؟** | شاشة فارغة — لا محتوى |
| **السبب المحتمل** | خطأ JavaScript يوقف React — ملف JS 404 — build ناقص |
| **أين أفحص؟** | F12 Console (خطأ أحمر), Network → index.html و assets/*.js |
| **كيف أصلح؟** | اقرأ أول خطأ Console — أعد build — تأكد رفع `assets/` كامل |
| **متى أطلب مساعدة؟** | بعد التحقق من Console + رفع dist كامل — مع لقطة شاشة الخطأ |

#### 16.2 الصور لا تظهر

| | |
|--|--|
| **ماذا أرى؟** | أيقونة مكسورة أو placeholder |
| **السبب** | URL خاطئ — Drive بدون resolveImageSrc — ملف uploads ناقص |
| **أين أفحص؟** | Network → Img → 404 |
| **كيف أصلح؟** | صحّح mainImg — resolveImageSrc — ارفع الملف |
| **متى أطلب مساعدة؟** | إن URL صحيح و200 لكن لا تظهر — أرسل Network screenshot |

#### 16.3 زر الحفظ لا يعمل

| | |
|--|--|
| **ماذا أرى؟** | ضغط «حفظ» — لا رسالة نجاح — بيانات تضيع |
| **السبب** | غير مسجّل JWT — data.php 401/500 — offline |
| **أين أفحص؟** | Network → POST data.php — Console |
| **كيف أصلح؟** | loginToApi — connectServer — تحقق config.php |
| **متى أطلب مساعدة؟** | إن POST يرجع 500 بعد login ناجح |

#### 16.4 لوحة التحكم لا تفتح

| | |
|--|--|
| **ماذا أرى؟** | admin لا يظهر — أو login يفشل |
| **السبب** | auth.php — كلمة مرور — portal !== admin |
| **أين أفحص؟** | Network → auth.php — Console |
| **كيف أصلح؟** | تحقق admin_users في MySQL — JWT في localStorage |
| **متى أطلب مساعدة؟** | إن auth 200 لكن portal لا يتغير |

#### 16.5 البيانات لا تتحدّث

| | |
|--|--|
| **ماذا أرى؟** | محتوى قديم بعد التعديل |
| **السبب** | cache — data.json قديم — لم تحفظ MySQL |
| **أين أفحص؟** | Network data.php — Ctrl+F5 |
| **كيف أصلح؟** | احفظ من admin — تحقق response — امسح cache |
| **متى أطلب مساعدة؟** | إن POST ناجح لكن GET يرجع قديم |

#### 16.6 التعديل يظهر عندي فقط

| | |
|--|--|
| **ماذا أرى؟** | أنت ترى التعديل — الزوار لا |
| **السبب** | localStorage فقط — لم يُرفع — لم يُحفظ server |
| **أين أفحص؟** | هل connectServer؟ — data.php على live |
| **كيف أصلح؟** | احفظ على السيرver — أو ارفع data.json |
| **متى أطلب مساعدة؟** | إن الحفظ «ناجح» محليًا لكن live لا يتغير |

#### 16.7 يعمل محليًا ولا على Hostinger

| | |
|--|--|
| **ماذا أرى؟** | localhost OK — eng-alaa.com لا |
| **السبب** | api ناقص — config.php — base path — .htaccess |
| **أين أفحص؟** | Network على live — Hostinger File Manager |
| **كيف أصلح؟** | `npm run build:hostinger` — ارفع hostinger_upload كامل |
| **متى أطلب مساعدة؟** | بعد رفع package كامل + config صحيح |

#### 16.8 مشكلة في data.php

| | |
|--|--|
| **ماذا أرى؟** | fetch failed — 500 — JSON parse error |
| **السبب** | DB — config — PHP error |
| **أين أفحص؟** | افتح `./api/data.php` — Error log |
| **كيف أصلح؟** | config.php — schema — صلاحيات |
| **متى أطلب مساعدة؟** | مع نص Response من data.php |

#### 16.9 مشكلة في MySQL

| | |
|--|--|
| **ماذا أرى؟** | connection failed — جداول فارغة |
| **السبب** | schema لم يُستورد — credentials |
| **أين أفحص؟** | phpMyAdmin — Hostinger Databases |
| **كيف أصلح؟** | استورد schema.sql — عدّل config |
| **متى أطلب مساعدة؟** | إن phpMyAdmin يعمل لكن PHP لا يتصل |

#### 16.10 مشكلة في المسارات

| | |
|--|--|
| **ماذا أرى؟** | 404 لملفات أو صور |
| **السبب** | `/` vs `./` — Windows path — اسم ملف |
| **أين أفحص؟** | Network — File Manager |
| **كيف أصلح؟** | راجع الفصل 10 — مسارات من جذر الموقع |
| **متى أطلب مساعدة؟** | إن المسار يبدو صحيحًا والملف موجود |

#### 16.11 مشكلة في CSS

| | |
|--|--|
| **ماذا أرى؟** | تنسيق مكسور — ألوان غريبة — responsive سيء |
| **السبب** | class خاطئ — cache CSS — تعديل inline بالخطأ |
| **أين أفحص؟** | F12 Elements — Styles |
| **كيف أصلح؟** | `.cursorrules`: لا تعدّل CSS إلا بطلب — Ctrl+F5 |
| **متى أطلب مساعدة؟** | إن لم تمس CSS ومع ذلك تغيّر بعد deploy |

#### 16.12 مشكلة في React

| | |
|--|--|
| **ماذا أرى؟** | component لا يظهر — crash — keys warning |
| **السبب** | state — props — portal condition |
| **أين أفحص؟** | Console — React DevTools |
| **كيف أصلح؟** | اقرأ Stack — راجع آخر تعديل App.tsx |
| **متى أطلب مساعدة؟** | مع رسالة الخطأ + السطر |

---

### 17. مخطط تفكير عند أي مشكلة

```
حدثت مشكلة
        ↓
أقرأ الرسالة (لا أتجاهلها)
        ↓
أحدد مكان المشكلة (Cursor / Terminal / Console / Network / Hostinger)
        ↓
أبحث عن الملف المذكور في الخطأ
        ↓
أفهم: ما آخر تعديل عملته؟
        ↓
أجرب حلًا صغيرًا واحدًا (تراجع / تصحيح مسار / Ctrl+F5)
        ↓
أختبر الموقع (محليًا ثم live)
        ↓
أحفظ النتيجة (Git commit / ملاحظة ماذا نفع)
```

---

### 18. نهاية الفصل

#### 18.1 ملخص

1. **Error** يوقف — **Warning** ينبه.
2. **Console** = JS — **Network** = طلبات HTTP.
3. **404** = غير موجود — **500** = خطأ سيرver.
4. اقرأ **أول** سطر خطأ + **ملف src/**.
5. **12 مشكلة** شائعة في الجدول أعلاه — ارجع لها.
6. **لا تغيّر 10 أشياء** — حل واحد → اختبار.

#### 18.2 جدول أخطاء وحلول (مرجع سريع)

| الخطأ | فحص سريع | حل سريع |
|-------|----------|---------|
| صفحة بيضاء | Console | أصلح JS / أعد build |
| صورة 404 | Network Img | URL + رفع ملف |
| حفظ fail | POST data.php | login + config |
| JSON parse | Response tab | أصلح JSON/PHP |
| DB fail | config.php | MySQL credentials |
| محلي ≠ live | compare Network | build:hostinger + api |

#### 18.3 أهم 10 قواعد عند ظهور خطأ

1. **تنفّس** — الخطأ معلومة.
2. **اقرأ** الرسالة كاملة مرة.
3. **حدّد** Cursor أم Browser أم Server.
4. **F12** قبل أي شيء في المتصفح.
5. **Ctrl+F5** قبل افتراض cache.
6. **تغيير واحد** ثم اختبار.
7. **لا Keep All** بدون مراجعة.
8. **نسخة احتياطية** قبل إصلاح كبير.
9. **document** ما نفع — للمرة القادمة.
10. **اطلب مساعدة** مع screenshot + رسالة الخطأ.

#### 18.4 أسئلة تدريبية

1. فرق Error و Warning؟
2. أين Console؟
3. ماذا يعني 404؟
4. ماذا يعني 500 في data.php؟
5. ما CORS باختصار؟
6. لماذا build failed؟
7. ما undefined؟
8. لماذا JSON parse error؟
9. ماذا تفعل مع صفحة بيضاء؟
10. لماذا التعديل عندي فقط؟

#### 18.5 تمرين نظري: صفحة بيضاء بعد رفع Hostinger

**بدون تنفيذ — خطواتك:**

1. افتح eng-alaa.com → F12 Console — انسخ **أول** خطأ أحمر.
2. Network — هل `index.html` 200؟ هل `assets/index-*.js` 200 أم 404؟
3. إن JS 404 → هل رفعت مجلد assets بعد build جديد؟
4. إن خطأ React → أي ملف وسطر؟
5. هل api/ موجود في public_html؟
6. هل config.php موجود (ليس config.sample)؟
7. جرّب `./api/data.php` في تبويب جديد — JSON أم HTML خطأ؟
8. قارن مع localhost:5173 — نفس الخطأ؟
9. اكتب **3** أسباب محتملة مرتبة.
10. اكتب **حلًا واحدًا** تجربه أولًا.

---

> **الفصل التالي (12):** «كيف أضيف قسمًا جديدًا إلى الموقع؟» — *(هذا الفصل أدناه)*

---

## الفصل الثاني عشر: كيف أضيف قسمًا جديدًا إلى الموقع؟

> **هدف الفصل:** فهم **الخطة الكاملة** لإضافة قسم جديد — **نظريًا فقط** — بدون تنفيذ في الكود.

> **تحذير:** هذا الفصل **تعليمي**. مشروع eng-alaa.com مبني على `portal` في App.tsx — إضافة قسم = تعديل عدة ملفات. **لا تنفّذ** حتى تفهم الفصول 1–11.

---

### 1. ما معنى «قسم جديد»؟

**قسم جديد** = **شاشة جديدة** في الموقع — مثل «الزراعة» أو «التصاميم» — لها:
- **زر** في القائمة للوصول إليها
- **محتوى** خاص (نصوص، صور، بيانات)
- **تصميم** CSS
- **ترجمة** ar/en/de (إن لزم)

**في مشروعك:** الأقسام الحالية تُدار بـ **`portal` state**:

```tsx
type Portal = "home" | "about" | "agri" | "graphics" | "software" | "cv" | "admin";
```

قسم جديد = قيمة جديدة في هذا النوع + كود يعرضه عند `portal === "contact"`.

---

### 2. الملفات التي تحتاج فهمها قبل إضافة قسم

| الملف | لماذا؟ |
|-------|--------|
| **`src/App.tsx`** | portal, openPortal, عرض الأقسام, bottom-nav |
| **`src/translations.ts`** | نصوص الأزرار والعناوين UI |
| **`src/appData.ts`** | بيانات المحتوى — إن القسم يحتاج JSON |
| **`src/index.css`** | تنسيق — classes جديدة |
| **`src/ContentAdmin.tsx`** | إن تريد تحرير القسم من لوحة التحكم |
| **`.cursorrules`** | قواعد — لا تكسر data.json structure |

---

### 3. كيف أقرر اسم القسم؟

**قواعد:**

1. **اسم تقني قصير** بالإنجليزية — `"contact"`, `"services"` — **بدون مسافات**.
2. **يتوافق** مع `Portal` type.
3. **لا يتعارض** مع أسماء موجودة (`home`, `agri`…).
4. **اسم للزائر** في translations — «تواصل معنا» / Contact / Kontakt.

**مثال:**  
- تقني: `contact`  
- Component: `ContactSection.tsx`  
- portal: `portal === "contact"`

---

### 4. أين أضيف بيانات القسم؟

**خياران:**

#### أ) بيانات ثابتة في translations.ts
مناسب لقسم «تواصل» — عنوان، نص، email — **بدون** MySQL.

#### ب) بيانات في appData.ts / MySQL
مناسب لقسم «خدمات» — قائمة خدمات قابلة للتعديل من admin.

**مثال نظري في appData:**

```tsx
contactSection: {
  email: "info@eng-alaa.com",
  phone: "+49...",
  items: [{ id: "1", title: ml("استشارة", "Consulting", "..."), desc: ml(...) }]
}
```

**في schema.sql:** جدول جديد أو عمود JSON — **قرار كبير** — كمبتدئ استخدم **حقل في site_settings JSON** أولًا نظريًا.

---

### 5. أين أضيف زر التنقل؟

**أماكن في App.tsx:**

1. **بطاقات الصفحة الرئيسية** — `portal-grid` — مثل بطاقات الأقسام.
2. **bottom-nav** — الشريط السفلي للجوال.
3. **header** — زر pill إن لزم.

**نظريًا:** تضيف `{ key: 'contact', icon: 'fa-envelope', label: t.contactNav }` لمصفوفة bottom-nav.

---

### 6. أين أضيف Component جديد؟

**الطريقة الصحيحة:**

1. أنشئ **`src/ContactSection.tsx`** (اسم واضح).
2. Component يستقبل **props**: `lang`, `data`, `t`.
3. **export** الدالة.
4. **import** في App.tsx.

**لماذا ملف منفصل؟** App.tsx **3500+** سطر — قسم جديد في ملفه يصعب الصيانة.

---

### 7. كيف أجعل App.tsx يعرض القسم؟

**الخطوات النظرية:**

1. أضف `"contact"` إلى `type Portal`.
2. import ContactSection.
3. أضف block:

```tsx
{portal === "contact" && (
  <ContactSection lang={lang} data={data.contactSection} t={t} />
)}
```

4. تأكد **openPortal("contact")** يُستدعى من الزر.

---

### 8. كيف أضيف CSS؟

1. **أولًا:** استخدم classes **موجودة** — `portal-card`, `btn-pill`, `glass-panel`.
2. **إن لزم:** أضف section في **`index.css`** — مثل `.contact-section { ... }`.
3. **تذكّر `.cursorrules`:** لا تعدّل CSS إلا **بقصد** — قسم جديد = استثناء **مخطّط**.

**Responsive:** استخدم `@media` الموجودة — mobile-first.

---

### 9. كيف أضيف ترجمة؟

1. **`translations.ts`** — أضف keys:
   - `contactNav`, `contactTitle`, `contactDesc`, `contactEmailLabel`…
2. **في interface T** — نفس keys للثلاث لغات.
3. **محتوى ديناميكي** — ML في appData + `pickML`.

**لا تخلط:** عنوان الزر = `t.contactNav` — وصف خدمة = `pickML(item.desc, lang)`.

---

### 10. كيف أجعله في لوحة التحكم؟

**نظريًا:**

1. في **ContentAdmin** — mode جديد `'contact'` أو تبويب في site settings.
2. حقول edit لـ email, phone, items.
3. **onSave** → handleCvSave → data.php.

**بديل أبسط:** عدّل `siteSettings` الموجود — بدون mode جديد.

---

### 11. مثال نظري: ContactSection

**الفكرة:** صفحة «تواصل معنا» — email, نموذج (مستقبلًا), روابط social.

#### الخطوات (نظريًا فقط):

| # | الخطوة |
|---|--------|
| 1 | **فكرة:** ما يراه الزائر؟ عنوان + email + خريطة؟ |
| 2 | **بيانات:** contactSection في appData + defaultCatalog |
| 3 | **Component:** ContactSection.tsx — JSX بسيط |
| 4 | **App.tsx:** Portal + import + `{portal === "contact" && ...}` |
| 5 | **زر:** بطاقة home + bottom-nav |
| 6 | **CSS:** `.contact-hero`, `.contact-grid` |
| 7 | **ترجمة:** t.contactTitle × 3 لغات |
| 8 | **اختبار:** npm run dev — كل لغة — جوال |
| 9 | **لا يكسر:** تأكد home/agri لا يزال يعمل |
| 10 | **admin (اختياري):** حقل email في ContentAdmin |

#### مثال ServicesSection

نفس الخطوات — بيانات = مصفوفة `services[]` — كل خدمة: title ML, desc ML, icon URL.

---

### 12. مخطط نصي

```
فكرة القسم (ماذا يرى الزائر؟)
        ↓
بيانات القسم (appData / translations)
        ↓
Component جديد (ContactSection.tsx)
        ↓
ربط في App.tsx (portal + import + شرط عرض)
        ↓
زر في القائمة (home grid + bottom-nav)
        ↓
تصميم CSS (classes موجودة + جديدة إن لزم)
        ↓
اختبار (dev + لغات + جوال + admin)
```

---

### 13. نهاية الفصل

#### 13.1 ملخص

- قسم جديد = **portal** + **Component** + **بيانات** + **ترجمة** + **زر**.
- **ابدأ** بفكرة واضحة — **لا** تعدّل App.tsx عشوائيًا.
- **Component منفصل** — لا تضف 500 سطر في App.
- **translations** للUI — **ML** للمحتوى.
- **اختبر** أن الأقسام القديمة **لا تنكسر**.

#### 13.2 جدول الملفات

| الملف | ماذا تفعل نظريًا؟ |
|-------|-------------------|
| App.tsx | Portal type, import, render, nav |
| ContactSection.tsx | JSX القسم |
| appData.ts | نوع بيانات + default |
| translations.ts | keys UI |
| index.css | styles |
| ContentAdmin.tsx | تحرير (اختياري) |
| schema.sql / data.php | حفظ server (متقدم) |

#### 13.3 أخطاء شائعة

| خطأ | النتيجة |
|-----|---------|
| نسيت Portal type | typecheck fail |
| نسيت import | Component is not defined |
| زر بدون openPortal | لا شيء يحدث |
| نسيت translations en/de | نص فارغ |
| CSS يكسر layout | أقسام أخرى تتأثر |

#### 13.4 أسئلة تدريبية

1. ما portal؟
2. أين bottom-nav؟
3. فرق t.* و pickML للقسم الجديد؟
4. لماذا Component منفصل؟
5. ما خطوات ContactSection؟
6. كيف تختبر بعد الإضافة؟
7. متى تحتاج MySQL؟
8. ما .cursorrules تقول عن CSS؟
9. كيف تضيف زر في home؟
10. ما ServicesSection يحتاج بيانات؟

#### 13.5 تمرين نظري: قسم «تواصل معنا»

**اكتب على ورقة (بدون كود):**

1. اسم portal: `contact`
2. 5 keys في translations
3. 3 حقول في contactSection (email, phone, address)
4. sketch: ماذا في ContactSection.tsx؟
5. أين الزر في home؟
6. 3 classes CSS مقترحة
7. checklist اختبار 8 نقاط
8. ماذا لو نسيت `"contact"` في Portal type؟
9. هل تحتاج ContentAdmin؟ لماذا؟
10. ترتيب التنفيذ — أي ملف **أولًا**؟

---

> **الفصل التالي (13):** «كيف أنشر نسخة جديدة على Hostinger؟» — *(هذا الفصل أدناه)*

---

## الفصل الثالث عشر: كيف أنشر نسخة جديدة على Hostinger خطوة خطوة؟

> **هدف الفصل:** دليل **عملي نظري** — تفهم **ماذا تفعل** قبل وبعد النشر — **بدون تنفيذ** الأوامر هنا.

---

### 1. ماذا يعني «نشر الموقع»؟

**النشر (Deploy)** = نقل نسخة **جاهزة** من مشروعك على جهازك → إلى **سيرفر Hostinger** → ليراها **الزوار** على `eng-alaa.com`.

**تشبيه:** كتابكتك في بيتك — **النشر** = وضع نسخة في **مكتبة عامة** (public_html).

---

### 2. فرق نسخة التطوير ونسخة النشر

| | **تطوير (Development)** | **نشر (Production)** |
|--|-------------------------|----------------------|
| **أين؟** | جهازك — `localhost:5173` | Hostinger — eng-alaa.com |
| **الأمر** | `npm run dev` | `npm run build:hostinger` + رفع |
| **السرعة** | hot reload | ملفات ثابتة مُحسّنة |
| **PHP/MySQL** | proxy إلى live أو mock | حقيقي على السيرver |
| **الأخطاء** | تظهر تفصيلية | قد تكون صفحة بيضاء |

---

### 3. ماذا أفعل قبل النشر؟

1. **نسخة احتياطية** — الملفات + export MySQL من phpMyAdmin.
2. **اختبار محلي** — `npm run dev` — كل الأقسام.
3. **typecheck** — `npm run typecheck` (إن أمكن).
4. **build** — تأكد نجاح بدون أخطاء.
5. **راجع** آخر تعديلات — هل كلها مقصودة؟
6. **config.php** على السيرver — **لا** ترفعه من Git (سري).

---

### 4. كيف أتأكد أن الموقع يعمل محليًا؟

1. `npm install` (أول مرة أو بعد تحديث package.json).
2. `npm run dev`.
3. افتح `http://localhost:5173`.
4. جرّب: home, agri, graphics, software, cv, admin (login).
5. F12 — **لا** أخطاء حمراء في Console.
6. Network — data.php (قد ي proxy لـ live).

---

### 5. ما معنى npm install؟

**npm install** = تحميل **المكتبات** (React, Vite…) من `package.json` إلى `node_modules/`.

**متى:** أول clone — بعد pull يغيّر dependencies.

**لا ترفع** `node_modules/` إلى Hostinger — **فقط** ناتج build.

---

### 6. ما معنى npm run build؟

**npm run build** = Vite **يبني** نسخة إنتاج في مجلد **`dist/`**:
- HTML, JS, CSS **مضغوطة**
- صور public **منسوخة**
- أسماء hash للـ cache

**الأمر في مشروعك:**

```json
"build": "vite build --config vite.config.ts"
```

---

### 7. ما معنى npm run build:hostinger؟

**build:hostinger** = build **+** تجهيز حزمة جاهزة للرفع:

```json
"build:hostinger": "vite build --config vite.config.ts && node scripts/prepare-hostinger.mjs"
```

**prepare-hostinger.mjs** يفعل:
1. ينسخ `hostinger_deploy/` → `hostinger_upload/`
2. **يستثني** `api/config.php` (لا يمسّ إعداداتك على السيرver)
3. ينسخ `dist/` فوقها
4. ينشئ مجلد `reports/`

**النتيجة:** مجلد **`hostinger_upload/`** — ارفع **كل** محتوياته إلى `public_html/`.

---

### 8. أين تظهر ملفات النشر؟

| مجلد | المحتوى |
|------|---------|
| **`dist/`** | ناتج Vite فقط |
| **`hostinger_upload/`** | dist + api + PHP + كل hostinger_deploy |

**Console بعد build:hostinger:**

```
Hostinger upload package ready:
  .../hostinger_upload
Upload ALL contents of hostinger_upload/ to public_html/
```

---

### 9. ما الملفات التي أرفعها؟

**ارفع كل** محتويات `hostinger_upload/`:

- `index.html`
- `assets/` (JS, CSS)
- `favicon.svg`, `LogoAlaa.svg`, `robots.txt`…
- `api/` (data.php, auth.php, upload.php…) — **مع** config.php **على السيرver**
- `data.json` (محتوى)
- `uploads/` (إن وُجد)
- `vendor/`, `generate_report.php`, `ai_proxy.php` (PDF/AI)

---

### 10. ما الملفات التي لا أرفعها؟

| لا ترفع | لماذا؟ |
|---------|--------|
| `node_modules/` | ضخم — يُبنى محليًا |
| `src/` | مصدر — ليس للزوار |
| `.env` | أسرار |
| `api/config.php` من Git | **على السيرver فقط** — credentials |
| `.git/` | ليس للموقع |

---

### 11. أين أضع مجلد api؟

```
public_html/
└── api/
    ├── config.php      ← سري — على السيرver
    ├── data.php
    ├── auth.php
    ├── upload.php
    └── analytics.php
```

**URL:** `https://eng-alaa.com/api/data.php`

**Frontend يستدعي:** `./api/data.php` (نسبي من جذر الموقع).

---

### 12. أين أضع ملفات PHP؟

- **`api/`** — REST APIs
- **جذر public_html** — `ai_proxy.php`, `generate_report.php` (حسب hostinger_deploy)
- **لا** داخل `assets/`

---

### 13. كيف أجهّز MySQL؟

1. Hostinger Panel → **Databases** → Create MySQL.
2. سجّل: hostname, database name, user, password.
3. أنشئ **`api/config.php`** من `config.sample.php` — ضع القيم.
4. **لا** ترفع config.php في Git.

---

### 14. كيف أستورد schema.sql؟

1. phpMyAdmin → اختر database.
2. **Import** → اختر `hostinger_deploy/schema.sql`.
3. Execute — تظهر جداول: `site_data`, `admin_users`, `uploaded_files`…
4. تحقق — جدول `admin_users` فيه مستخدم admin (حسب schema).

---

### 15. كيف أختبر data.php؟

1. افتح `https://eng-alaa.com/api/data.php` في المتصفح.
2. **توقع:** JSON يبدأ بـ `{` — **ليس** HTML خطأ.
3. F12 Network — GET — Status **200**.
4. إن **500** → config أو schema.

---

### 16. كيف أختبر لوحة التحكم؟

1. افتح الموقع → admin login (زر مخفي أو مسار admin).
2. `loginToApi` → JWT.
3. ContentAdmin يظهر.
4. عدّل حقلًا بسيطًا → **احفظ**.
5. Network POST data.php → 200.
6. Ctrl+F5 كزائر — التعديل ظاهر؟

---

### 17. كيف أتأكد أن التعديلات تحفظ فعليًا؟

1. **POST** data.php ناجح في Network.
2. **GET** data.php بعدها — نفس القيمة.
3. **جهاز آخر** / incognito — نفس المحتوى.
4. **ليس** localStorage فقط — connectServer مفعّل.

---

### 18. صفحة بيضاء بعد النشر؟

→ **الفصل 11** — Console, assets 404, JS error.

**سريع:** F12 → أول خطأ → هل assets/ مرفوع؟

---

### 19. الصور لا تظهر بعد النشر؟

→ **الفصل 10** — URLs, uploads/, Drive.

**سريع:** Network Img — 404؟

---

### 20. الحفظ لا يعمل بعد النشر؟

→ auth + data.php + config + MySQL.

**سريع:** Network POST — status؟ Response؟

---

### 21. Checklist قبل النشر

- [ ] نسخة احتياطية (ملفات + DB)
- [ ] اختبار محلي (`npm run dev`)
- [ ] build ناجح (`npm run build:hostinger`)
- [ ] رفع محتويات `hostinger_upload/` كاملة
- [ ] مجلد `api/` + `config.php` صحيح
- [ ] MySQL + schema.sql مستورد
- [ ] اختبار `data.php` → JSON
- [ ] اختبار الموقع — كل الأقسام
- [ ] اختبار لوحة التحكم — حفظ
- [ ] اختبار الجوال

---

### 22. مخطط النشر

```
تعديل المشروع (src / public)
        ↓
اختبار محلي (npm run dev)
        ↓
Build (npm run build:hostinger)
        ↓
تجهيز hostinger_upload/
        ↓
رفع الملفات (SFTP / File Manager)
        ↓
تجهيز MySQL (schema + config.php)
        ↓
اختبار الرابط (eng-alaa.com)
        ↓
اختبار لوحة التحكم (حفظ + تحقق)
```

**تحديث محتوى فقط (data.json):**  
عدّل `public/data.json` → SFTP → `public_html/data.json` → Ctrl+F5 — **بدون build**.

---

### 23. نهاية الفصل

#### 23.1 ملخص

1. **النشر** = dist + PHP على public_html.
2. **`build:hostinger`** = dist + hostinger_deploy → hostinger_upload.
3. **لا** node_modules — **لا** src على السيرver.
4. **config.php** سري — على السيرver فقط.
5. **data.json** — SFTP منفصل بدون build.
6. **اختبر** data.php + admin + جوال.

#### 23.2 جدول أوامر النشر

| الأمر | الوظيفة |
|-------|---------|
| `npm install` | تثبيت المكتبات |
| `npm run dev` | تطوير محلي |
| `npm run typecheck` | فحص TypeScript |
| `npm run build` | بناء dist |
| `npm run build:hostinger` | حزمة Hostinger كاملة |
| `npm run serve` | معاينة dist محليًا |

#### 23.3 مشاكل النشر وحلولها

| المشكلة | الحل |
|---------|------|
| assets 404 | أعد رفع assets/ بعد build |
| data.php 500 | config + schema |
| admin لا login | admin_users + auth.php |
| محتوى قديم | Ctrl+F5 + تحقق GET |
| صور 404 | uploads + URLs |

#### 23.4 أسئلة تدريبية

1. فرق dev و production؟
2. أين hostinger_upload؟
3. لماذا لا نرفع node_modules؟
4. أين config.php؟
5. كيف تستورد schema؟
6. كيف تختبر data.php؟
7. متى تحتاج build؟
8. متى يكفي SFTP data.json؟
9. ماذا يفعل prepare-hostinger.mjs؟
10. checklist قبل النشر — 3 أهم نقاط؟

#### 23.5 تمرين نظري: نشر نسخة جديدة

**اكتب خطة على ورقة:**

1. اليوم — ماذا تعدّل؟
2. backup — ماذا تنزّل؟
3. أوامر terminal بالترتيب.
4. ما ترفعه — قائمة 10 ملفات/مجلدات.
5. ما **لا** ترفعه — 5 أشياء.
6. بعد الرفع — 5 URLs تختبرها.
7. ماذا لو صفحة بيضاء؟
8. ماذا لو data.php 500؟
9. كيف تتحقق الحفظ؟
10. rollback — كيف ترجع نسخة قديمة؟

---

> **الفصل التالي (14):** «قاموس مصطلحات برمجية مبسط» — *(هذا الفصل أدناه)*

---

## الفصل الرابع عشر: قاموس مصطلحات برمجية مبسط من داخل مشروعي

> **هدف الفصل:** معجم **مبسّط** — كل مصطلح: معنى + مثال من المشروع + تشبيه + أين تراه.

---

### كيف تقرأ هذا القاموس؟

لكل مصطلح أربعة أجزاء: **المعنى** — **من المشروع** — **التشبيه** — **أين**.

---

#### React
- **المعنى:** مكتبة تبني الواجهة من **قطع** (Components) تتغير حسب البيانات.
- **من المشروع:** كل الموقع في `App.tsx` + ContentAdmin.
- **التشبيه:** LEGO — قطع تركّبها وتعيد ترتيبها.
- **أين:** `package.json`, `src/*.tsx`

#### Vite
- **المعنى:** أداة **تشغيل** و **بناء** المشروع بسرعة.
- **من المشروع:** `npm run dev`, `vite.config.ts`.
- **التشبيه:** مطبخ جاهز — يسخّن المشروع فورًا.
- **أين:** `vite.config.ts`, `package.json`

#### TypeScript
- **المعنى:** JavaScript + **أنواع** — يقلّل الأخطاء.
- **من المشروع:** `type Portal`, `interface AppData`.
- **التشبيه:** ملصقات على الصناديق — «هذا رقم لا نص».
- **أين:** كل `.ts`, `.tsx`

#### TSX / JSX
- **المعنى:** HTML داخل JavaScript/TypeScript.
- **من المشروع:** `<div className="portal-card">` في App.tsx.
- **التشبيه:** مسرحية فيها حوار + مشهد.
- **أين:** `App.tsx`, Components

#### Component
- **المعنى:** قطعة UI مستقلة — تُعاد استخدامها.
- **من المشروع:** `BookCover`, `ContentAdmin`, `PlantDiagnostic`.
- **التشبيه:** قطعة فساتين — تلبسها في أماكن مختلفة.
- **أين:** `src/*.tsx`

#### Props
- **المعنى:** **مدخلات** تمرّرها للComponent من الأب.
- **من المشروع:** `<BookCover src={...} alt={...} />`.
- **التشبيه:** تعليمات للطباخ.
- **أين:** أي `<Component prop={value} />`

#### State
- **المعنى:** **ذاكرة** متغيرة — عند تغيّرها يُعاد الرسم.
- **من المشروع:** `portal`, `lang`, `data`, `adminLoggedIn`.
- **التشبيه:** عدّاد السرعة — يتغير فيتغير العرض.
- **أين:** `useState` في App.tsx

#### useState
- **المعنى:** دالة React تنشئ state + دالة تحديثه.
- **من المشروع:** `const [portal, setPortal] = useState<Portal>("home")`.
- **التشبيه:** صندوق + مفتاح.
- **أين:** App.tsx, ContentAdmin

#### useEffect
- **المعنى:** «افعل شيئًا **بعد** الرسم» — تحميل، تتبع.
- **من المشروع:** تحميل data.php, trackPageView, Three.js.
- **التشبيه:** «عندما أفتح الباب، شغّل النور».
- **أين:** App.tsx

#### Event / onClick / onChange
- **المعنى:** حدث — نقرة، كتابة — + دوال الاستجابة.
- **من المشروع:** `onClick={() => openPortal("agri")}`.
- **التشبيه:** زر مصعد.
- **أين:** App.tsx, forms

#### Function / Variable
- **المعنى:** كتلة كود مسماة / اسم يحمل قيمة.
- **من المشروع:** `resolveImageSrc`, `const t = translations[lang]`.
- **التشبيه:** وصفة / ملصق على علبة.
- **أين:** كل `.ts`

#### Array / Object
- **المعنى:** قائمة `[a,b,c]` / `{ key: value }`.
- **من المشروع:** `data.skills`, `ML { ar, en, de }`.
- **التشبيه:** صف مدرسة / بطاقة هوية.
- **أين:** appData, JSON

#### JSON
- **المعنى:** نص منظم للتبادل بين البرامج.
- **من المشروع:** `data.json`, response data.php.
- **التشبيه:** طرد بعنوان واضح.
- **أين:** public/data.json

#### API / Request / Response / GET / POST
- **المعنى:** واجهة سيرver — طلب/رد — قراءة/حفظ.
- **من المشروع:** GET/POST `./api/data.php`.
- **التشبيه:** نافذة بنك — سؤال وإجابة.
- **أين:** hostinger_deploy/api/, Network

#### PHP / MySQL / Database
- **المعنى:** لغة سيرver / قاعدة بيانات / تخزين دائم.
- **من المشروع:** data.php, schema.sql, site_data.
- **التشبيه:** موظف خلف العداد / خزانة ملفات.
- **أين:** api/, phpMyAdmin

#### Table / Row / Column / SQL / Schema
- **المعنى:** جدول / صف / عمود / لغة DB / مخطط جداول.
- **من المشروع:** `uploaded_files`, CREATE TABLE في schema.sql.
- **التشبيه:** Excel / مخطط بناء.
- **أين:** schema.sql

#### localStorage
- **المعنى:** تخزين في **متصفح** الزائر.
- **من المشروع:** `alaa_lang`, JWT.
- **التشبيه:** جيبك — محلي.
- **أين:** appData.ts

#### public / src
- **المعنى:** ملفات static للزوار / مصدر الكود.
- **من المشروع:** favicon vs App.tsx.
- **التشبيه:** واجهة المحل / المطبخ.
- **أين:** `public/`, `src/`

#### build / deploy / Hostinger
- **المعنى:** بناء dist / رفع للسيرver / الاستضافة.
- **من المشروع:** build:hostinger, hostinger_upload.
- **التشبيه:** تعبئة طلب / فتح فرع / الأرض.
- **أين:** package.json, prepare-hostinger.mjs

#### frontend / backend
- **المعنى:** React للزائر / PHP+MySQL على السيرver.
- **من المشروع:** src vs api/.
- **التشبيه:** صالة عرض / مخزن.
- **أين:** src, api

#### CSS / className / Responsive
- **المعنى:** تنسيق / class في React / جوال+desktop.
- **من المشروع:** index.css, portal-card, @media.
- **التشبيه:** ملابس / سائل يأخذ شكل الكأس.
- **أين:** index.css, App.tsx

#### Import / Export / Package / npm
- **المعنى:** جلب/تصدير كود / مكتبة / مدير حزم.
- **من المشروع:** import App, react في package.json.
- **التشبيه:** استعارة كتاب / شراء مكوّنات.
- **أين:** package.json

#### Terminal / Console / Error / 404 / 500
- **المعنى:** أوامر / دفتر المتصفح / فشل / غير موجود / خطأ سيرver.
- **من المشروع:** npm run dev, F12, data.php.
- **التشبيه:** لوحة تحكم / إنذار سيارة.
- **أين:** Cursor, Browser F12

---

### أهم 20 مصطلحًا — احفظها أولًا

1. React  2. Component  3. Props  4. State  5. useState  
6. App.tsx  7. portal  8. JSON  9. API  10. fetch  
11. PHP  12. MySQL  13. build  14. deploy  15. public  
16. src  17. npm  18. Console  19. 404  20. localStorage

---

### مصطلحات لا داعي لفهمها بعمق الآن

| مصطلح | لماذا تؤجل؟ |
|-------|-------------|
| pg / Drizzle | PHP+MySQL على Hostinger |
| Radix internals | مكوّنات جاهزة |
| Three.js shaders | خلفية زخرفية |
| JWT crypto | يكفي: token بعد login |
| Webpack | المشروع Vite |

---

### أسئلة تدريبية

1. ما React بجملة؟
2. فرق props و state؟
3. أين frontend وأين backend؟
4. ما JSON؟
5. ما portal؟
6. فرق GET و POST؟
7. ما build؟
8. ما public vs src؟
9. ما 404؟
10. اربط 5 مصطلحات بملفات في مشروعك.

---

> **الفصل التالي (15):** «خطة تعلم 30 يومًا» — *(هذا الفصل أدناه)*

---

## الفصل الخامس عشر: خطة تعلم شهر كامل لفهم مشروعي وتعديله بثقة

> **هدف الفصل:** **30 يومًا** — كل يوم خطوة صغيرة — لتفهم eng-alaa.com وتعدّله **بدون خوف**.

> **قاعدة الذهب:** **30–45 دقيقة يوميًا** أفضل من 8 ساعات يوم واحد ثم توقف.

---

### نظرة على الأسابيع الأربعة

#### الأسبوع 1 — البنية والتشغيل
- بنية المجلدات — index.html → main.tsx → App.tsx
- قراءة الفصول 1–3 + 11 (أخطاء)
- `npm install`, `npm run dev`
- F12 Console و Network

#### الأسبوع 2 — React والبيانات
- props, state, Components
- ContentAdmin, appData.ts, data.json
- الفصول 4–5, 7

#### الأسبوع 3 — التصميم والمحتوى
- CSS (قراءة فقط أولًا)
- صور ومسارات — الفصل 10
- ترجمة — الفصل 9
- تعديلات آمنة (data.json, translations)

#### الأسبوع 4 — السيرver والنشر
- Hostinger, PHP, MySQL — الفصل 6, 13
- build:hostinger نظريًا
- قسم جديد نظريًا — الفصل 12
- مراجعة أخطاء — الفصل 11, 14, 15

---

### قواعد مهمة (كل يوم)

1. **لا تعدّل أكثر من شيء** في نفس الوقت.
2. **خذ نسخة احتياطية** قبل أي تعديل.
3. **اقرأ الخطأ** قبل الخوف — الفصل 11.
4. **لا Keep All** في Cursor بدون مراجعة.
5. **لا تعدّل** api/config.php قبل فهم الفصل 6.
6. **لا تغيّر** أسماء أقسام data.json الجذرية (library, articles, designs).
7. **استخدم Git** — commit بعد كل نجاح صغير.

---

### خطة الـ 30 يومًا (يوم بيوم)

| يوم | الهدف | الملفات | ماذا أفهم | تمرين آمن | سؤال لـ Cursor |
|-----|-------|---------|-----------|-----------|----------------|
| **1** | صورة كاملة | الفصل 1 | index→main→App | شغّل dev — افتح home | ما portal؟ |
| **2** | المجلدات | الفصل 2 | public vs src | list مجلدات بدون تعديل | أين data.json؟ |
| **3** | package.json | package.json | scripts | اقرأ scripts — لا تشغّل build بعد | فرق dev و build؟ |
| **4** | App.tsx جزء 1 | App.tsx 1–600 | state, portal | ابحث useState — لا تعدّل | ما openPortal؟ |
| **5** | App.tsx جزء 2 | App.tsx home | portal-grid | F12 — Network عند الفتح | كيف home يُرسم؟ |
| **6** | الأخطاء | الفصل 11 | Console | افتح Console — سجّل ما تراه | ما 404؟ |
| **7** | مراجعة أ1 | الفصول 1–2 | تثبيت | ارسم مخطط التشغيل على ورقة | لخّص الأسبوع 1 |
| **8** | React | الفصل 7 | Component | افتح BookCover.tsx | ما props؟ |
| **9** | props/state | App.tsx | lang, data | غيّر لغة — راقب dir | فرق props/state؟ |
| **10** | ContentAdmin | ContentAdmin | modes | admin — **لا تحفظ** | ما mode gfx؟ |
| **11** | appData | appData.ts | AppData | ابحث pickML | ما ML؟ |
| **12** | data.json | الفصل 4, 17 | merge | افتح content-manager.html | كيف SFTP data.json؟ |
| **13** | حفظ | الفصل 5 | handleCvSave | Network POST — راقب | متى MySQL vs local؟ |
| **14** | مراجعة أ2 | الفصول 4–5, 7 | تثبيت | اكتب 5 Components | لخّص الأسبوع 2 |
| **15** | CSS | الفصل 8 | classes | Elements — portal-card | أين index.css؟ |
| **16** | responsive | index.css @media | bottom-nav | صغّر نافذة المتصفح | ما responsive؟ |
| **17** | صور | الفصل 10 | resolveImageSrc | Network Img في graphics | لماذا Drive؟ |
| **18** | ترجمة | الفصل 9 | t vs pickML | بدّل ar/en/de | أين translations؟ |
| **19** | تعديل آمن | translations | key واحد | **نظريًا** — key جديد | كيف أضيف key؟ |
| **20** | مراجعة أ3 | 8–10, 9 | تثبيت | checklist صور+ترجمة | لخّص الأسبوع 3 |
| **21** | PHP | الفصل 6 | data.php | افتح data.php live | ما config.php؟ |
| **22** | MySQL | schema.sql | جداول | phpMyAdmin — browse | ما site_data؟ |
| **23** | Hostinger | الفصل 13 | deploy | اقرأ prepare-hostinger | ما hostinger_upload؟ |
| **24** | build | package.json | build:hostinger | **نظريًا** — اقرأ dist | ماذا ترفع؟ |
| **25** | قسم جديد | الفصل 12 | portal | تمرين Contact نظري | خطوات ContactSection؟ |
| **26** | أخطاء | الفصل 11 | جدول 12 خطأ | scenario: صفحة بيضاء | أول 3 خطوات؟ |
| **27** | قاموس | الفصل 14 | 20 مصطلح | flashcards ورقية | 5 مصطلحات بملفات |
| **28** | Git | .git | commit | git status — commit صغير | كيف أرجع تعديل؟ |
| **29** | مراجعة | كل الفصول | خريطة | ارسم مخطط البيانات | ما أضعف نقطة؟ |
| **30** | مشروع نهائي | الفصل 12, 15 | تطبيق | خطة Contact كاملة على ورقة | جاهز للتنفيذ؟ |

---

### جدول متابعة 30 يومًا (اطبعه)

| يوم | ✓ | ملاحظة |
|-----|---|--------|
| 1–7 | ☐ | أسبوع 1 |
| 8–14 | ☐ | أسبوع 2 |
| 15–20 | ☐ | أسبوع 3 |
| 21–27 | ☐ | أسبوع 4 |
| 28–30 | ☐ | مراجعة + Git |

---

### «أصبحت أفهم المشروع عندما أقدر أن…»

- [ ] أشغّل `npm run dev` بدون مساعدة
- [ ] أفتح F12 وأقرأ Console و Network
- [ ] أشرح ما هو portal و AppData
- [ ] أفرّق translations و pickML
- [ ] أعرف أين mainImg مخزّن
- [ ] أفتح ContentAdmin وأفهم الحفظ
- [ ] أشرح مسار data.php → MySQL
- [ ] أعرف متى أرفع data.json فقط vs build كامل
- [ ] أخطّط لقسم جديد **نظريًا**
- [ ] أتعامل مع خطأ **بدون ذعر** — الفصل 11

---

### أسئلة اختبار نهائية

1. من index.html إلى الشاشة — 5 خطوات؟
2. ما 3 مصادر للبيانات؟
3. كيف تحفظ من admin؟
4. فرق public و src للصور؟
5. ماذا يفعل build:hostinger؟
6. ماذا لو صفحة بيضاء على live؟
7. كيف تضيف قسم contact نظريًا؟
8. 10 مصطلحات من الفصل 14؟
9. checklist قبل النشر — 5 نقاط؟
10. ما قواعد الأمان الـ 7؟

---

### مشروع تدريبي صغير: قسم «تواصل معنا» (نظري)

**بعد اليوم 30 — على ورقة:**

1. Portal: `contact`
2. بيانات: email, phone, socialLinks
3. Component: ContactSection — 3 أقسام UI
4. App.tsx: 5 أسطر **تصف** ماذا تضيف
5. translations: 8 keys × 3 لغات
6. CSS: 2 classes — `.contact-hero`, `.contact-grid`
7. ContentAdmin: هل تحتاج mode؟ نعم/لا + لماذا
8. اختبار: 10 نقاط checklist
9. مخاطر: 3 أشياء قد تكسر الموقع
10. **لا تنفّذ** حتى تمرّ بكل الفصول + typecheck

---

### ملخص الخطة

| أسبوع | التركيز | النتيجة |
|-------|---------|---------|
| 1 | تشغيل + بنية | الموقع يعمل محليًا — تعرف F12 |
| 2 | React + بيانات | تفهم AppData و admin |
| 3 | CSS + صور + لغات | تعديلات آمنة |
| 4 | سيرفر + نشر + مراجعة | جاهز للنشر بثقة |

**بعد 30 يومًا:** أنت **لا** «مبرمج كامل» — لكنك **تفهم مشروعك** وتعدّل المحتوى وتشخّص الأخطاء وتخطّط للتوسع.

---

> **الفصل التالي (16):** «كيف أعدّل المشروع بأمان؟» — *(هذا الفصل أدناه)*

---

## الفصل السادس عشر: كيف أعدّل المشروع بأمان بدون أن أخرب الموقع؟

> **هدف الفصل:** أن تتعلّم **كيف تغيّر** — محتوى، نص، لون — **بدون** أن تتوقف eng-alaa.com عن العمل.

---

### مقدمة — ما الذي فحصناه في المشروع؟

مشروع **Portfolio Hub** ليس «ملفًا واحدًا» — بل **شبكة ملفات مرتبطة**:

```
index.html → main.tsx → App.tsx (3600+ سطر)
                ↓
    translations.ts + appData.ts + index.css
                ↓
    ContentAdmin, CvRenderer, PlantDiagnostic…
                ↓
    ./api/data.php → MySQL على Hostinger
                ↓
    public/data.json (كتب / مقالات / تصاميم)
```

**قواعد `.cursorrules` في مشروعك:**
- المحتوى (كتب، مقالات، تصاميم) → **`public/data.json`** — SFTP بدون build.
- **ممنوع** تعديل CSS و App.tsx للتنسيق إلا **بطلب صريح**.
- لا تغيّر أسماء الأقسام الجذرية: `library`, `articles`, `designs`.

هذا الفصل يعلّمك **أين تمشي بأمان** — وأين **لا تلمس** كمبتدئ.

---

### 1. لماذا يجب أن أعدّل بحذر؟

#### 1.1 المشروع يعمل الآن — لا أريد تخريبه

الموقع **live** على `eng-alaa.com` — زوار، محتوى، سيرة، مكتبة، لوحة تحكم.  
كل تعديل **غير مدروس** قد:
- يوقف الصفحة (**صفحة بيضاء**)
- يمنع **الحفظ** على MySQL
- يكسر **الترجمة** أو **الصور**
- يتطلب **ساعات** إصلاح — أو استرجاع نسخة قديمة

**تشبيه:** السيارة تمشي — لا تفك المحرك لتغيير لون الباب.

#### 1.2 تغيير بسيط قد يؤثر على ملفات كثيرة

**مثال حقيقي من مشروعك:**

| تعديل «بسيط» | ما قد ينكسر |
|--------------|-------------|
| حذف `import` من App.tsx | Component كامل يختفي — crash |
| تغيير اسم `portal` | bottom-nav + كل `{portal === "..."}` |
| فاصلة زائدة في data.json | كل المكتبة لا تُحمّل |
| تغيير حقل في appData.ts | typecheck fail + ContentAdmin |
| خطأ في auth.php | لا أحد يدخل admin |

**App.tsx وحده** يستورد **عشرات** الملفات ويربط **كل الأقسام**.

#### 1.3 ما معنى «الملفات مرتبطة ببعضها»؟

**الارتباط (Dependency)** = ملف **A** يحتاج ملف **B** ليعمل.

```
translations.ts  ──import──►  App.tsx  ──props──►  ContentAdmin
appData.ts       ──types───►  App.tsx  ──fetch──►  data.php
data.json        ──merge────►  appData.ts ──►  App.tsx يعرض الكتب
index.css        ──classes──►  App.tsx (className="portal-card")
```

**إذا كسرت B** — **A** يتعطل — وأحيانًا **C** و **D** أيضًا.

---

### 2. القاعدة الذهبية: «لا تعدّل أكثر من شيء واحد في نفس الوقت»

#### لماذا؟

1. **تعرف السبب:** إن نجح → هذا التعديل. إن فشل → هذا التعديل.
2. **تسهّل الرجوع:** تراجع **خطوة واحدة** — ليس 5 ملفات.
3. **تقلّل Keep All الخطير:** Cursor يقترح أحيانًا 10 ملفات — **رفض** ما لا تفهمه.
4. **تتعلّم:** كل تعديل = درس — 5 دفعة واحدة = فوضى.

**مثال سيء:** «غيّر اللون + أضف قسم + عدّل data.php + حدّث package.json»  
**مثال جيد:** «غيّر نص `agriTitle` في translations.ts للإنجليزية فقط»

---

### 3. ماذا أفعل **قبل** أي تعديل؟

#### 3.1 أخذ نسخة احتياطية

| الطريقة | كيف؟ |
|---------|------|
| **ZIP** | انسخ مجلد المشروع كاملًا → `backup-2026-06-22.zip` |
| **Git** | `git status` → commit قبل التعديل |
| **ملف واحد** | انسخ `App.tsx` → `App.tsx.backup` |
| **data.json** | نزّل نسخة من Hostinger قبل التعديل |
| **MySQL** | Export من phpMyAdmin |

**قاعدة:** backup **قبل** — ليس **بعد** الكارثة.

#### 3.2 معرفة الملف الذي سأعدّله

**اسأل نفسك:**
- هل هذا **محتوى**؟ → data.json أو ContentAdmin
- هل **نص واجهة**؟ → translations.ts
- هل **لون/شكل**؟ → index.css (بحذر — `.cursorrules`)
- هل **منطق الموقع**؟ → **توقّف** — اقرأ الفصل أولًا

#### 3.3 سؤال Cursor **قبل** التعديل

**لا تقل:** «حسّن الموقع»  
**قل:**

> «أريد تغيير نص العنوان في الصفحة الرئيسية للعربية فقط. أي ملف؟ لا تنفّذ — اشرح أولًا.»

#### 3.4 قراءة شرح الملف في هذا الكتاب

| تريد تعدّل | اقرأ الفصل |
|------------|------------|
| محتوى | 4, 5, 10 |
| App.tsx | 3, 7, 12 |
| CSS | 8 |
| ترجمة | 9 |
| نشر | 13 |
| أخطاء | 11 |

#### 3.5 تشغيل الموقع **قبل** التعديل

```text
npm run dev
```

افتح `localhost:5173` — تأكد:
- home يفتح
- Console **بدون** أخطاء حمراء
- (اختياري) admin login يعمل

**الآن** عندك **خط أساس** — أي كسر = من التعديل الجديد.

---

### 4. كيف أطلب من Cursor تعديلًا صغيرًا؟

#### أمثلة أوامر **جيدة**

```
غيّر لون زر "about" في الصفحة الرئيسية فقط — 
class btn-pill — لا تلمس ملفات أخرى. اشرح قبل التنفيذ.
```

```
عدّل نص t.agriTitle في translations.ts للإنجليزية فقط.
لا تغيّر ar أو de. ملف واحد فقط.
```

```
أضف بطاقة واحدة في portal-grid نظريًا — 
اشرح أي سطر في App.tsx — لا تنفّذ بدون موافقتي.
```

```
حدّث mainImg لمشروع gfx واحد في data.json — 
لا تغيّر هيكل JSON — لا تلمس App.tsx.
```

```
لا تعدّل أي ملف كود — فقط PROJECT_LEARNING_BOOK.md
```

#### أمثلة أوامر **سيئة** (تجنّبها)

- «حسّن المشروع كاملًا»
- «أعد كتابة App.tsx»
- «حدّث كل المكتبات»
- «أصلح كل الأخطاء» (بدون تحديد)

#### عبارات magic تضيفها دائمًا

- **«ملف واحد فقط»**
- **«لا تلمس App.tsx»** (إن لم تكن تحتاجه)
- **«اشرح قبل التنفيذ»**
- **«لا Keep All تلقائي — انتظر مراجعتي»**

---

### 5. ما معنى Review و Keep All في Cursor؟

#### 5.1 Review (مراجعة)

عندما Cursor يقترح تعديلات — ترى **diff**:
- **أخضر** = سطر مُضاف
- **أحمر** = سطر محذوف

**مهمتك:** اقرأ **كل** ملف في القائمة — ليس الملف الأول فقط.

#### 5.2 Keep All

**Keep All** = «اقبل **كل** التغييرات في **كل** الملفات».

| متى **نعم** | متى **لا** |
|-------------|------------|
| فهمت **كل** diff | Cursor عدّل 5+ ملفات بدون طلبك |
| ملف **واحد** — تغيير بسيط | تغييرات في auth.php / schema |
| typecheck نجح بعدها | حذف imports أو refactor كبير |
| طلبتَ هذا بالضبط | «تحسين» غامض |

#### 5.3 متى أرفض؟

- تعديلات في ملفات **لم تطلبها**
- حذف كود **لا تفهم** لماذا
- تغيير **package.json** بدون سبب
- تعديل **vite.config** أو **data.php**

**Reject / Undo** أفضل من موقع معطّل.

#### 5.4 كيف أراجع الملفات المتغيّرة؟

1. **Source Control** في Cursor — قائمة الملفات المعدّلة
2. اضغط كل ملف — **اقرأ diff**
3. `git diff` في Terminal — إن Git موجود
4. بعد Keep — **شغّل dev** فورًا

#### 5.5 لماذا لا أقبل تعديلات كثيرة بدون فهم؟

Cursor **ذكي لكنه لا يعرف** كل سياق eng-alaa.com:
- قد يكسر `.cursorrules`
- قد يغيّر **هيكل** data.json
- قد يضيف dependencies غير مطلوبة

**أنت** المسؤول عن الموقع live — ليس AI.

---

### 6. ما الملفات **الآمنة نسبيًا** للتعديل؟

| النوع | الملف / الطريقة | ملاحظة |
|-------|-----------------|--------|
| **محتوى كتب/مقالات/تصاميم** | `public/data.json` | لا تغيّر `library`/`articles`/`designs` جذريًا — SFTP |
| **محتوى عبر admin** | ContentAdmin + حفظ | **الأسلم** — بدون كود |
| **نصوص واجهة UI** | `translations.ts` | أضف key للـ **3** لغات + interface T |
| **صور محتوى** | URLs في data.json / admin | Drive أو upload — الفصل 10 |
| **صور static** | `public/favicon.svg`, LogoAlaa | تحتاج build للرفع |
| **CSS بسيط** | `index.css` | **فقط بطلب صريح** — `.cursorrules` |
| **كتب CSV** | content-manager.html + templates | للمحتوى bulk |

#### ترتيب الأمان (من الأكثر للأقل)

1. **ContentAdmin** (حفظ server)  
2. **data.json** SFTP  
3. **translations.ts** (نص UI)  
4. **public/** static  
5. **index.css** (بحذر)  
6. **defaultCatalog.ts** (بيانات افتراضية — dev فقط)

---

### 7. ما الملفات **الخطيرة** — لا تلمسها كمبتدئ

| الملف | لماذا خطير؟ |
|-------|-------------|
| **`App.tsx`** | قلب الموقع — portal, state, كل الأقسام, handleCvSave |
| **`appData.ts`** | أنواع البيانات, fetch, merge data.json, upload |
| **`data.php`** | كل حفظ/تحميل MySQL — خطأ = موقع بلا بيانات |
| **`auth.php`** | تسجيل دخول admin — JWT |
| **`schema.sql`** | هيكل DB — خطأ = جداول تتلف |
| **`vite.config.ts`** | build, proxy, aliases — خطأ = لا deploy |
| **`package.json`** | dependencies — خطأ = npm install يفشل |
| **`handleCvSave`** | دالة الحفظ المركزية في App.tsx |
| **`loginToApi`** | في appData.ts — admin access |
| **`config.php`** | credentials DB — **سري** |

**قاعدة:** إن لم تفهم **90%** من الملف — **لا تعدّله** — اطلب شرحًا في Cursor **بدون تنفيذ**.

---

### 8. كيف أعرف أن التعديل **نجح**؟

#### checklist بعد كل تعديل

1. **`npm run dev`** — يعمل بدون crash؟
2. **افتح الصفحة** المتأثرة — home / agri / graphics…
3. **جرب الزر/الحقل** الذي عدّلته
4. **F12 Console** — لا أخطاء حمراء جديدة؟
5. **Network** — data.php 200 إن لمس البيانات؟
6. **احفظ** من admin — POST ناجح؟ — Ctrl+F5 — التعديل ظاهر؟
7. **لغة ثانية** — ar/en/de
8. **جوال** — F12 responsive أو هاتف حقيقي
9. **`npm run typecheck`** — إن عدّلت TypeScript
10. **قبل live:** build:hostinger نظريًا — الفصل 13

**نجاح =** كل ما **فوق** الذي ينطبق — **نعم**.

---

### 9. كيف أرجع للخلف إذا خرب شيء؟

| الطريقة | متى؟ | كيف؟ |
|---------|------|------|
| **Ctrl+Z (Undo)** | مباشرة بعد التعديل في المحرر | تراجع سطر/ملف |
| **Reject في Cursor** | قبل Keep All | ارفض diff |
| **Git restore** | بعد commit سابق | `git checkout -- file` أو `git restore` |
| **Git reset** | commit أنت صاحبه ولم يُ push | حذر — اقرأ الفصل 11 |
| **نسخة ZIP** | backup قبل التعديل | استبدل المجلد أو الملف |
| **data.json من Hostinger** | كسرت JSON | ارفع النسخة القديمة SFTP |
| **phpMyAdmin export** | كسرت DB | استورد backup |

**بعد live:**  
- ارفع **hostinger_upload** من backup قديم  
- أو **ملف واحد** (data.json) — أسرع

**لا تصاب بالذعر:** مع backup — **دائمًا** يمكن الرجوع.

---

### 10. أخطاء مبتدئين شائعة

#### 10.1 قبول كل تعديلات Cursor بدون قراءة

**النتيجة:** 8 ملفات تتغير — لا تعرف ماذا — موقع مكسور.  
**الحل:** diff لكل ملف — Keep **انتقائي**.

#### 10.2 تعديل 5 ملفات مرة واحدة

**النتيجة:** لا تعرف أي ملف سبب المشكلة.  
**الحل:** القاعدة الذهبية — **واحد**.

#### 10.3 حذف import

**النتيجة:** `X is not defined` — صفحة بيضاء.  
**الحل:** لا تحذف import إلا إن **أنت** حذفت Component.

#### 10.4 تغيير اسم متغير

**مثال:** `portal` → `page` — **50** مكان يتكسر.  
**الحل:** Find All References قبل أي rename.

#### 10.5 كسر JSON

**مثال:** فاصلة زائدة في data.json — `{,` أو `"title"` بدون فاصلة.  
**الحل:** JSON validator — انسخ المحتوى إلى jsonlint.com قبل الرفع.

#### 10.6 حذف قوس أو فاصلة في TSX

**النتيجة:** syntax error — build fail.  
**الحل:** Cursor يظهر خط أحمر — **لا** Keep حتى يختفي.

#### 10.7 رفع نسخة غير مجربة إلى Hostinger

**النتيجة:** eng-alaa.com معطّل للزوار.  
**الحل:** **دائمًا** dev محلي → build → اختبر → ثم رفع.

#### 10.8 تعديل live site مباشرة

**النتيجة:** File Manager على production — خطأ واحد = down.  
**الحل:** عدّل محليًا — ارفع **بعد** اختبار.

#### 10.9 تعديل CSS «لتحسين» بدون طلب

**النتيجة:** يكسر `.cursorrules` وتنسيق 5000+ سطر index.css.  
**الحل:** محتوى أولًا — CSS **بطلب صريح**.

#### 10.10 تغيير أسماء أقسام data.json

**مثال:** `designs` → `graphics` — App **لا يقرأ**.  
**الحل:** **لا** تغيّر `library`, `articles`, `designs`.

---

### 11. تشبيه: السيارة

**المشروع = سيارة تعمل جيدًا.**

- **تغيير لون الباب** = تعديل نص أو صورة في data.json ✅  
- **تغيير لوحة العدادات** = translations.ts ✅  
- **فك المحرك** = App.tsx + appData + data.php ❌  
- **تغيير نوع الوقود** = package.json ❌  
- **رسم خط جديد على الخريطة بدون GPS** = قسم جديد بدون فهم portal ❌

**عدّل الجزء المطلوب فقط** — واختبر «هل السيارة ما زالت تمشي؟»

---

### 12. مخطط: تعديل آمن

```
فكرة التعديل (ماذا بالضبط؟)
        ↓
backup (ZIP / Git / data.json)
        ↓
npm run dev — baseline يعمل
        ↓
حدّد ملفًا واحدًا
        ↓
اطلب من Cursor — صغير — اشرح أولًا
        ↓
Review diff — لا Keep All أعمى
        ↓
اختبار (صفحة + Console + حفظ + جوال)
        ↓
نجح؟ → commit / SFTP
        ↓
فشل؟ → Undo / restore backup
```

---

### 13. نهاية الفصل

#### 13.1 ملخص

1. الملفات **مرتبطة** — تعديل صغير قد يكسر كثيرًا.
2. **شيء واحد** في كل مرة.
3. **backup** قبل — **dev** baseline — **اختبار** بعد.
4. **آمن:** data.json, admin, translations (بحذر).
5. **خطير:** App, appData, PHP, schema, package, vite.
6. **Review** كل diff — **لا Keep All** أعمى.
7. **Undo / Git / ZIP** للرجوع.
8. **لا live** بدون اختبار محلي.
9. **`.cursorrules`** — محتوى قبل CSS/هيكل.
10. **أنت** المسؤول — Cursor مساعد.

#### 13.2 قائمة فحص قبل أي تعديل

- [ ] فهمت **ماذا** سأغيّر بالضبط
- [ ] backup (ZIP أو Git أو data.json)
- [ ] `npm run dev` يعمل **قبل** التعديل
- [ ] حدّدت **ملفًا واحدًا** (إن أمكن)
- [ ] قرأت الفصل المناسب في الكتاب
- [ ] طلبت Cursor: «اشرح قبل التنفيذ»
- [ ] راجعت **كل** diff
- [ ] اختبرت Console + الصفحة + (حفظ)
- [ ] جوال / responsive
- [ ] commit أو SFTP **بعد** النجاح فقط

#### 13.3 جدول: تعديل آمن / تعديل خطر

| تعديل | مستوى | ملاحظة |
|-------|-------|--------|
| كتاب جديد عبر ContentAdmin | ✅ آمن | الأفضل |
| data.json SFTP | ✅ آمن | لا تكسر JSON |
| نص UI في translations | ⚠️ متوسط | 3 لغات + interface T |
| صورة URL في mainImg | ✅ آمن | الفصل 10 |
| لون في index.css | ⚠️ متوسط | بطلب صريح فقط |
| defaultCatalog.ts | ⚠️ متوسط | dev / أول تشغيل |
| App.tsx portal | ❌ خطير | مبتدئ لا |
| appData.ts types | ❌ خطير | |
| data.php / auth.php | ❌ خطير | |
| schema.sql | ❌ خطير | |
| package.json | ❌ خطير | |
| vite.config.ts | ❌ خطير | |
| قسم جديد كامل | ❌ خطير | نظريًا أولًا — الفصل 12 |

#### 13.4 أهم 10 قواعد

1. **واحد** في كل مرة.
2. **backup** دائمًا.
3. **dev** قبل وبعد.
4. **لا Keep All** بدون diff.
5. **محتوى** قبل **كود**.
6. **data.json** — لا تغيّر جذور JSON.
7. **لا live** بدون اختبار.
8. **Console** بعد كل تعديل.
9. **اسأل** Cursor — لا «حسّن كل شيء».
10. **تعلّم** من كل خطأ — الفصل 11.

#### 13.5 أسئلة تدريبية

1. لماذا App.tsx خطير؟
2. ما القاعدة الذهبية؟
3. ما أأمن طريقة لتعديل كتاب؟
4. متى Keep All؟
5. كيف ترجع backup؟
6. ما .cursorrules تقول عن CSS؟
7. فرق data.json و appData.ts للتعديل؟
8. ماذا قبل npm run dev؟
9. لماذا 5 ملفات مرة واحدة سيء؟
10. ما checklist النجاح؟

#### 13.6 تمرين نظري: تغيير لون زر بدون تخريب

**الهدف:** زر «about» (`btn-pill`) — لون مختلف — **على ورقة فقط**.

1. **backup:** ماذا تنزّل؟
2. **أي ملف؟** index.css أم inline؟ — ماذا تقول `.cursorrules`؟
3. **className** الحالي في App.tsx — ابحث `btn-pill` — أي سطر؟
4. **طلب Cursor:** اكتب الأمر **الكامل** — ملف واحد — اشرح أولًا.
5. **Review:** ماذا ترفض إن Cursor عدّل App.tsx أيضًا؟
6. **اختبار:** 5 خطوات بعد التعديل.
7. **فشل:** كيف Undo؟
8. **live:** هل تحتاج build؟ نعم/لا — لماذا؟
9. **جوال:** هل اللون يؤثر responsive؟
10. **document:** لون قديم → جديد — commit message مقترح.

---

> **الفصل التالي (17):** «Git والنسخ الاحتياطي» — *(هذا الفصل أدناه)*

---

## الفصل السابع عشر: Git والنسخ الاحتياطي — كيف أحمي مشروعي وأرجع للخلف؟

> **هدف الفصل:** أن تفهم **كيف تحفظ** نسخًا من مشروع eng-alaa.com — وت **رجع** إذا خرب شيء — **بدون خوف**.

---

### مقدمة — هل مشروعي يستخدم Git؟

نعم — مجلد مشروعك **Git repo** (مستودع Git).  
هذا يعني: Git **يتتبع** التغييرات في ملفاتك — ويمكنه **حفظ نقاط** (commits) وال **رجوع** إليها.

**ملف `.gitignore` في مشروعك** يخبر Git: «**لا تتتبع** هذه الملفات» — مثل:

```
node_modules/
dist/
hostinger_upload/
.env.local
hostinger_deploy/api/config.php   ← أسرار MySQL
```

**مهم:** `config.php` **مستبعد** من Git — **عمدًا** — لأنه فيه كلمة مرور قاعدة البيانات.

---

### 1. ما معنى نسخة احتياطية (Backup)؟

**النسخة الاحتياطية** = **نسخة إضافية** من مشروعك (أو ملف) — تُحفظ **في مكان آمن** — لتستخدمها إذا **ضاعت** أو **انكسرت** النسخة الأصلية.

**أنواع في مشروعك:**

| النوع | مثال |
|-------|------|
| **ZIP** | `eng-alaa-backup-2026-06-22.zip` |
| **Git commit** | نقطة حفظ داخل Git |
| **data.json من Hostinger** | نزّل قبل التعديل |
| **Export MySQL** | من phpMyAdmin |

**تشبيه:** صورة فوتوغرافية لغرفتك قبل الترتيب — إن لم يعجبك الترتيب، ترجع للصورة.

---

### 2. لماذا النسخ الاحتياطي مهم جدًا؟

1. **Cursor** قد يعدّل 10 ملفات — Keep All — **كسر**.
2. **data.json** — فاصلة واحدة = كل المكتبة لا تُقرأ.
3. **Hostinger** — رفع build خاطئ = موقع live معطّل.
4. **جهازك** — عطل قرص — ضياع بدون نسخة.
5. **الوقت** — استرجاع backup = دقائق — إعادة بناء من الذاكرة = أيام.

**قاعدة:** «لست محترفًا حتى عندك backup» — حتى للمبتدئ.

---

### 3. الفرق: حفظ عادي vs نسخة احتياطية

| | **حفظ عادي (Ctrl+S)** | **نسخة احتياطية** |
|--|----------------------|-------------------|
| **ماذا؟** | يكتب فوق الملف الحالي | يحفظ **نسخة منفصلة** |
| **الرجوع؟** | Undo محدود | ترجع **كامل المشروع** |
| **متى؟** | كل ثوانٍ أثناء العمل | **قبل** تعديل كبير + **بعد** نجاح |
| **مثال** | حفظ App.tsx | ZIP + Git commit |

**Ctrl+S** = «احفظ مسودة الآن»  
**Backup / commit** = «احفظ **لقطة** يمكن العودة لها لاحقًا»

---

### 4. ما معنى Git؟

**Git** = برنامج **يتتبع تاريخ** ملفات مشروعك — **محليًا** على جهازك (وممكن ربطه بـ GitHub).

**ماذا يفعل؟**
- يعرف **ماذا تغيّر** في كل ملف
- يحفظ **commits** (نقاط حفظ)
- يسمح **بالرجوع** لcommit قديم
- يعمل **branches** (خطوط عمل متوازية)

**في مشروع eng-alaa.com:** Git يتتبع `src/`, `public/`, `hostinger_deploy/`…  
**لا** يتتبع `node_modules/` (حسب `.gitignore`).

---

### 5. ما معنى GitHub؟

**GitHub** = موقع على الإنترنت **يستضيف** نسخ Git — **سحابة** + تعاون + نسخة إضافية.

| Git | GitHub |
|-----|--------|
| على **جهازك** | على **الإنترنت** |
| `git commit` | `git push` يرفع commits |
| يعمل **بدون** إنترنت | يحتاج حساب |

**للمبتدئ:** يمكنك استخدام Git **محليًا فقط** — commit على جهازك — **بدون** GitHub.  
**GitHub مفيد:** نسخة ثانية إذا تعطل جهازك — **لكن** لا ترفع أسرارًا.

---

### 6. ما معنى commit؟

**Commit** = **نقطة حفظ** رسمية في Git — لقطة **لحالة كل الملفات** في لحظة.

**مكوّنات commit:**
- **التغييرات** (أي ملفات)
- **رسالة** تصف **لماذا** (مثل: «Update homepage text»)
- **معرّف** (hash) — مثل `a3f9c2d`

**تشبيه Git:** نقاط حفظ في لعبة — إذا **خسرت**، ترجع لآخر نقطة حفظ.

**الأمر (في Terminal):**

```bash
git add .
git commit -m "Update homepage text"
```

---

### 7. ما معنى branch؟

**Branch** = **خط فرع** — نسخة موازية من المشروع للتجربة.

**مثال:**
- `main` — النسخة **الرئيسية** المستقرة
- `feature/contact` — تجربة قسم تواصل — **بدون** كسر main

**كمبتدئ:** اعمل على **`main`** — branch لاحقًا عندما تتقدّم.

**تشبيه:** طريق رئيسي + مسار تجريبي بجانبه — إن فشل التجريبي، الرئيسي **سليم**.

---

### 8. Rollback — الرجوع للخلف

**Rollback** = **التراجع** لنسخة سابقة.

| الطريقة | متى؟ |
|---------|------|
| **`git restore file`** | ملف واحد — آخر commit |
| **`git checkout -- .`** | كل الملفات — **حذر** |
| **`git reset --hard HEAD~1`** | حذف آخر commit — **خطير** |
| **استرجاع ZIP** | بدون Git |
| **Reject في Cursor** | قبل commit |

**قاعدة مبتدئ:** إن **لم ت push** — `git restore` آمن لملف واحد.  
**اسأل Cursor** قبل `reset --hard`.

---

### 9. كيف أعرف الملفات التي تغيّرت؟

#### في Cursor

1. **Source Control** (أيقونة Y — الشجرة/الفرع) في الشريط الجانبي
2. قائمة **Changes** — كل ملف معدّل
3. اضغط الملف — **diff** (أخضر/أحمر)

#### في Terminal

```bash
git status          # ملخص — modified / untracked
git diff            # التفاصيل سطر بسطر
git diff App.tsx    # ملف واحد
git log --oneline   # آخر commits
```

**قبل commit:** **دائمًا** `git status` — تأكد **لا** config.php في القائمة.

---

### 10. Git داخل Cursor — خطوة بخطوة

#### 10.1 فتح Source Control

- **Ctrl+Shift+G** — أو أيقونة Source Control

#### 10.2 بعد تعديل ملفات

1. ترى الملفات تحت **Changes**
2. **+** بجانب الملف = **Stage** (تحضير للcommit) — مثل `git add`
3. اكتب **رسالة commit** في الصندوق فوق
4. **Commit** ✓ — حفظ نقطة

#### 10.3 Push (إن GitHub مربوط)

- **Sync / Push** — يرفع لـ GitHub  
**لا push** حتى تفهم — commit محلي **يكفي** للbackup.

#### 10.4 الرجوع من Cursor

- **Timeline** على ملف — نسخ قديمة  
- **Discard Changes** — يرمي تعديلات غير committed  
- Terminal: `git restore <file>`

---

### 11. متى أعمل commit؟

| **نعم — commit** | **لا — انتظر** |
|------------------|----------------|
| بعد تعديل **ناجح** + اختبار dev | وسط تعديل **نصف مكتمل** |
| **قبل** تعديل كبير (backup commit) | Cursor عدّل 10 ملفات — **لم تراجع** |
| قبل `build:hostinger` / نشر | الموقع **لا يعمل** محليًا |
| بعد إضافة ترجمة keys كاملة | فيه `config.php` أو `.env` |

**قاعدة عملية:**

```
قبل تعديل كبير  → commit: "Backup before ..."
بعد نجاح        → commit: "Fix / Add / Update ..."
إذا خرب         → rollback لآخر commit ناجح
```

---

### 12. ماذا أكتب في رسالة commit؟

**جيدة:** قصيرة — **إنجليزية** غالبًا — تصف **لماذا** لا **ماذا** فقط.

#### أمثلة من مشروعك

| الرسالة | متى؟ |
|---------|------|
| `Update homepage text` | غيّرت نصًا في translations |
| `Fix design section image` | صحّحت mainImg في gfx |
| `Add contact section` | أضفت قسم contact (نظري/فعلي) |
| `Improve admin dashboard layout` | تحسين AnalyticsDashboard |
| `Backup before Hostinger deployment` | **قبل** نشر — نقطة رجوع |
| `Update data.json library books` | محتوى كتب SFTP-ready |
| `Fix save handler regression` | إصلاح handleCvSave |

**سيئة:**
- `fix`
- `update`
- `asdf`
- `تعديلات كثيرة`

---

### 13. لماذا لا أرفع أسرار MySQL؟

**`hostinger_deploy/api/config.php`** يحتوي:

- اسم قاعدة البيانات
- username
- **password**
- host

**إن رُفع على GitHub:**
- **أي شخص** قد يراه (إن public)
- **bots** تسرق credentials
- **اختراق** MySQL — حذف/سرقة بيانات

**مشروعك محمي:** `.gitignore` **يستبعد** config.php.

**`.env.local`** أيضًا مستبعد — **لا ترفعه**.

**على Hostinger:** config.php **يدوًا** على السيرver فقط — **ليس** من Git.

---

### 14. ما الملفات التي لا يجب رفعها (Git/GitHub)؟

| ملف/مجلد | لماذا؟ |
|----------|--------|
| **`node_modules/`** | ضخم — يُعاد بـ npm install |
| **`dist/`**, **`hostinger_upload/`** | يُعاد بـ build |
| **`config.php`** | **أسرار** |
| **`.env`, `.env.local`** | API keys, passwords |
| **`*.log`** | ملفات log |
| **`.DS_Store`, `Thumbs.db`** | ملفات نظام |

**يمكن رفعها (عادة):**
- `src/`, `public/`, `hostinger_deploy/` (بدون config.php)
- `package.json`, `vite.config.ts`, `schema.sql`

---

### 15. ما معنى `.gitignore`؟

**`.gitignore`** = ملف نصي في **جذر المشروع** — قائمة «**تجاهل** هذه الأ paths».

**Git **لا** يتتبع** ما في `.gitignore` — **لا** يظهر في commit — **لا** يُ push.

**من مشروعك:**

```gitignore
node_modules/
dist/
hostinger_deploy/api/config.php
```

**إن أضفت ملفًا للignore:** Git **يتوقف** عن تتبعه — **لكن** الملف **يبقى** على قرصك.

**لا تحذف** `.gitignore` — **لا تزيل** سطر config.php منه.

---

### 16. خطة بسيطة — احفظها

```
┌─────────────────────────────────────────┐
│  قبل تعديل كبير                         │
│  → ZIP أو git commit "Backup before …"  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│  عدّل — اختبر — npm run dev             │
└─────────────────────────────────────────┘
                    ↓
         ┌─────────┴─────────┐
         │                   │
      نجح ✓               فشل ✗
         │                   │
         ↓                   ↓
  git commit            git restore
  "Update …"            أو ZIP قديم
         │
         ↓
  (اختياري) push GitHub
         │
         ↓
  قبل Hostinger:
  commit "Backup before deployment"
```

---

### 17. تشبيه: Git = نقاط حفظ في لعبة

**اللعبة** = مشروع eng-alaa.com  
**نقطة الحفظ** = commit  
**التحميل** = rollback / restore  
**GitHub** = نسخة cloud من نقاط الحفظ  

**إذا خسرت** (كسر الموقع) — **لا تبدأ اللعبة من الصفر** — ارجع لآخر commit ناجح.

---

### 18. Git vs ZIP — متى أيّهما؟

| | **Git commit** | **ZIP** |
|--|----------------|---------|
| **سهل الرجوع لملف** | ✅ | ⚠️ استبدال كامل |
| **تاريخ التغييرات** | ✅ | ❌ |
| **بدون تعلم Git** | ⚠️ | ✅ |
| **قبل نشر Hostinger** | ✅ + ✅ **كلاهما** | |

**الأفضل للمبتدئ:** **ZIP قبل أي شيء كبير** + **Git commit** عندما تتعلّم.

---

### 19. نهاية الفصل

#### 19.1 ملخص

1. **Backup** = نسخة للرجوع — ليس Ctrl+S فقط.
2. **Git** = تاريخ + commits + rollback.
3. **GitHub** = cloud اختياري — **لا أسرار**.
4. **commit** = نقطة حفظ + رسالة واضحة.
5. **`.gitignore`** يحمي config.php و node_modules.
6. **قبل كبير** → backup commit — **بعد نجاح** → commit.
7. **Cursor Source Control** = add + commit + diff.
8. **`git status`** قبل كل commit.
9. **ZIP + Git** معًا قبل Hostinger.
10. **rollback** = restore — لا panic.

#### 19.2 جدول مصطلحات Git

| المصطلح | المعنى البسيط |
|---------|---------------|
| **Repository (repo)** | مجلد المشروع + تاريخ Git |
| **Commit** | نقطة حفظ |
| **Branch** | خط عمل فرعي |
| **Stage (add)** | تحضير ملف للcommit |
| **Push** | رفع commits لـ GitHub |
| **Pull** | جلب من GitHub |
| **Diff** | الفرق بين نسختين |
| **Restore** | استرجاع ملف من commit |
| **Reset** | الرجوع لcommit — **حذر** |
| **.gitignore** | ملفات Git يتجاهلها |
| **Untracked** | ملف Git لا يعرفه بعد |
| **Modified** | ملف تغيّر منذ آخر commit |

#### 19.3 خطوات حماية المشروع (Checklist)

- [ ] `.gitignore` موجود — config.php مستبعد
- [ ] commit قبل تعديل ContentAdmin / App / data.json كبير
- [ ] ZIP شهريًا لمجلد المشروع
- [ ] export MySQL دوري من Hostinger
- [ ] نزّل data.json من live قبل تعديل bulk
- [ ] **لا** commit لـ `.env` أو passwords
- [ ] `git status` — راجع القائمة
- [ ] رسالة commit **واضحة**
- [ ] بعد commit — `npm run dev` ما زال يعمل
- [ ] قبل deploy — commit «Backup before Hostinger deployment»

#### 19.4 أخطاء شائعة

| خطأ | النتيجة | الحل |
|-----|---------|------|
| لا backup أبدًا | لا رجوع | commit + ZIP |
| commit كل 5 ثوانٍ بلا معنى | تاريخ فوضى | commit عند **نجاح** |
| رفع config.php | اختراق محتمل | .gitignore |
| Keep All بدون commit قبل | لا rollback | commit **قبل** |
| `reset --hard` بدون فهم | فقدان commits | restore ملف واحد |
| push أسرار | كارثة | git status — never config |
| ignore node_modules محذوف | commit ضخم | أعد `.gitignore` |

#### 19.5 أسئلة تدريبية

1. فرق Ctrl+S و backup؟
2. ما commit؟
3. لماذا config.php في .gitignore؟
4. كيف ترى الملفات المتغيرة في Cursor؟
5. متى commit «Backup before…»؟
6. فرق Git و GitHub؟
7. ما rollback؟
8. 3 ملفات لا ترفعها؟
9. ماذا يعني Stage؟
10. ما خطة: قبل / بعد / فشل؟

#### 19.6 تمرين نظري: نسخة قبل تعديل لوحة التحكم

**السينario:** تريد تعديل **ContentAdmin.tsx** — **على ورقة فقط**.

1. **لماذا backup؟** — 3 أسباب.
2. **ZIP:** اسم الملف المقترح؟
3. **Git:** اكتب الأوامر بالترتيب:
   - `git status`
   - commit backup — **الرسالة بالإنجليزية**
4. **تعديل** (نظري) — ContentAdmin — سطر واحد.
5. **اختبار:** 4 خطوات.
6. **نجاح:** رسالة commit ثانية؟
7. **فشل:** `git restore ContentAdmin.tsx` — ماذا يحدث؟
8. **git status** قبل commit — ماذا تتأكد **لا** يظهر؟
9. **Hostinger:** هل Git يستبدل SFTP؟ (نعم/لا + لماذا)
10. **Timeline:** كيف ترى نسخة قديمة من ملف في Cursor؟

---

> **الفصل التالي (18):** «كيف أطلب من Cursor تعديلات صحيحة؟» — *(هذا الفصل أدناه)*

---

## الفصل الثامن عشر: كيف أطلب من Cursor تعديلات صحيحة وواضحة؟

> **هدف الفصل:** أن تكتب **طلبات واضحة** — فيعدّل Cursor **ما تريد** — **ولا** يكسر eng-alaa.com.

---

### تشبيه: عامل الصيانة الذكي

**Cursor** = عامل صيانة **ذكي** — سريع — لكنه **يفترض** إن لم تكن دقيقًا.

| ما تقول | ما قد يحدث |
|---------|------------|
| «صلّح البيت» | يغيّر كهرباء + دهان + أبواب + نوافذ |
| «بدّل **مقبض** الباب **فقط**» | يبدّل المقبض — الباقي **سليم** |

**الدرس:** **الدقة في الطلب** = **الدقة في النتيجة**.

---

### 1. لماذا طريقة كتابة الطلب مهمة؟

Cursor يقرأ **كلماتك** — لا يقرأ **ذهنك**.

**طلب غامض** → Cursor **يخمّن**:
- قد يعدّل **App.tsx** + **index.css** + **package.json**
- قد يكسر **`.cursorrules`** (مثل تعديل CSS بدون طلب)
- قد يقترح **Keep All** لـ 8 ملفات

**طلب واضح** → Cursor **يحدّ** نفسه:
- ملف **واحد**
- تغيير **واحد**
- **شرح** قبل أو بعد

**أنت** تتحكم بالنتيجة — **بكلماتك**.

---

### 2. لماذا لا أقول فقط: «حسّن الموقع»؟

**«حسّن الموقع»** = **لا معنى تقني** — لكل شخص «تحسين» مختلف:

| Cursor قد يفهم | أنت ربما تريد |
|----------------|---------------|
| إعادة تصميم CSS كامل | تغيير **جملة واحدة** |
| refactor App.tsx | إصلاح **صورة** واحدة |
| تحديث dependencies | **لون زر** |
| إضافة features | **لا شيء** — فقط شرح |

**النتيجة:** تعديلات **ضخمة** — Review **مستحيل** — موقع **مكسور**.

**بدلًا من:** «حسّن الموقع»  
**قل:** «عدّل نص `t.agriTitle` في translations.ts للإنجليزية فقط — ملف واحد»

---

### 3. كيف أطلب تعديلًا صغيرًا ومحددًا؟

#### صيغة ذهبية (4 أجزاء)

```
1. ماذا بالضبط؟     (لون / نص / URL)
2. أين؟             (ملف + قسم + سطر إن عرفت)
3. قيود             (لا ملفات أخرى)
4. بعد التعديل      (اشرح / اختبر)
```

#### مثال من مشروع eng-alaa.com

```
عدّل فقط public/data.json:
غيّر mainImg لمشروع id "gfx-001" إلى هذا الرابط:
https://drive.google.com/file/d/XXXX/view
لا تغيّر هيكل JSON (library, articles, designs).
لا تلمس App.tsx أو أي ملف src.
```

#### كلمات magic للتصغير

- **«فقط»** / **«only»**
- **«ملف واحد»**
- **«سطر واحد»**
- **«لا refactor»**
- **«لا تلمس X»**

---

### 4. كيف أطلب شرحًا **قبل** التعديل؟

**لماذا؟** تفهم **أين** المشكلة — **قبل** أن يمس Cursor الكود.

#### أمثلة

```
أريد تغيير عنوان قسم الزراعة. 
لا تعدل أي ملف.
قل لي: أي ملف؟ أي key؟ ar أم en أم الثلاث؟
```

```
قبل إضافة صورة لتصميم:
اشرح خطوة بخطوة — ContentAdmin أم data.json؟
لا تنفّذ — شرح فقط.
```

```
ما الفرق بين handleCvSave و loadSiteContentFromJson؟
لا تعدل الكود — مثال من مشروعي.
```

**عبارات مفيدة:**
- «**لا تنفّذ**»
- «**شرح فقط**»
- «**Ask mode**» / «**بدون تعديل**»
- «**PROJECT_LEARNING_BOOK فقط**» (مثل فصول هذا الكتاب)

---

### 5. كيف أطلب ألا يلمس ملفات معينة؟

#### قائمة استثناءات صريحة

```
عدّل translations.ts فقط.
ممنوع لمس:
- App.tsx
- appData.ts
- أي ملف CSS
- data.php
- package.json
```

#### استخدم `.cursorrules` في طلبك

```
اتبع .cursorrules:
لا تعدّل CSS ولا هيكل App.tsx.
المطلوب: تحديث محتوى في data.json فقط.
```

#### لماذا تسمي الملفات؟

في مشروعك **الخطيرة:** App.tsx, appData.ts, auth.php, schema.sql, vite.config.ts — **الفصل 16**.

---

### 6. كيف أطلب تعديل **ملف واحد** فقط؟

```
عدّل ملف واحد فقط: src/translations.ts
المطلوب: غيّر t.homeWelcome.en إلى "Welcome to my portfolio"
لا تغيّر ar أو de.
لا تلمس interface T إلا إذا typecheck يطلب.
بعد التعديل: اذكر السطر الذي تغيّر.
```

**تكرار «ملف واحد»** في **بداية** و **نهاية** الطلب — يقلّل تعديلات Cursor الجانبية.

---

### 7. كيف أطلب **خطة** قبل التنفيذ؟

```
أريد إصلاح: الصور لا تظهر في قسم graphics على Hostinger.
لا تعدل أي ملف الآن.
أعطني:
1. الأسباب المحتملة (3–5)
2. الملفات للفحص
3. خطة إصلاح خطوة بخطوة
4. أي خطوة خطيرة على live
انتظر موافقتي قبل التنفيذ.
```

**متى تستخدم الخطة؟**
- أخطاء **500**, **404**, صفحة **بيضاء**
- **قبل** نشر Hostinger
- **قبل** لمس App.tsx

---

### 8. كيف أطلب شرح **التغييرات بعد** التنفيذ؟

```
بعد التعديل:
1. اذكر كل ملف تغيّر (يجب أن يكون ملفًا واحدًا)
2. ماذا فعلت في كل ملف — 3 جمل
3. كيف أختبر (npm run dev + أي صفحة)
4. ماذا لو فشل — كيف أرجع (git restore)
```

**أو في نفس الطلب:**

```
عدّل X. بعد الانتهاء اشرح ماذا فعلت كأني مبتدئ.
```

---

### 9. كيف أطلب **فحص بدون تعديل**؟

#### القالب الجاهز (من طلبك)

```
افحص هذه المشكلة ولا تعدل أي ملف.
أخبرني ما السبب المحتمل والملفات المرتبطة.
```

#### أمثلة لمشروعك

```
المشكلة: زر الحفظ في ContentAdmin لا يعمل على eng-alaa.com.
افحص — لا تعدل.
أخبرني: data.php؟ auth؟ JWT؟ Network؟
```

```
افحص لماذا data.json لا يُدمج بعد SFTP.
لا تعدل — اذكر appData.ts و loadSiteContentFromJson.
```

**كلمات:** «افحص»، «diagnose»، «read-only»، «لا تعدل»

---

### 10. كيف أطلب إصلاح خطأ **بدون تخريب** باقي الموقع؟

```
لدي هذا الخطأ في Console:
TypeError: Cannot read properties of undefined (reading 'title')
at App.tsx:1844

افحص السبب في App.tsx حول السطر 1844.
قبل أي تعديل أعطني خطة — تعديل minimal فقط.
لا refactor.
لا تغيّر handleCvSave أو data loading.
لا تنفّذ حتى أوافق.
```

**قواعد إصلاح آمن:**
- **Minimal fix** — أصغر patch
- **لا «while we're here»** — Cursor يحب تحسينات إضافية — **ارفضها**
- **اختبر** portal home + agri + graphics بعد fix

---

### 11. قوالب أوامر جاهزة

#### 11.1 فحص بدون تعديل

```
افحص هذه المشكلة ولا تعدل أي ملف.
أخبرني ما السبب المحتمل والملفات المرتبطة.
```

#### 11.2 تعديل صغير

```
عدّل فقط ملف X.
المطلوب Y.
لا تغيّر أي ملف آخر.
بعد التعديل اشرح ماذا فعلت.
```

**مثال filled:**

```
عدّل فقط ملف public/data.json.
المطلوب: أضف كتابًا واحدًا في library.books — انسخ هيكل كتاب موجود.
لا تغيّر articles أو designs.
بعد التعديل اشرح id و nodeId الذي استخدمته.
```

#### 11.3 إصلاح خطأ

```
لدي هذا الخطأ: [الصق رسالة Console أو Terminal]
افحص السبب.
قبل أي تعديل اعطني خطة.
لا تنفّذ حتى أوافق.
```

#### 11.4 تحسين تصميم

```
حسّن شكل هذا القسم فقط، ولا تغيّر منطق البيانات أو الحفظ.
```

**مثال filled (مشروعك):**

```
حسّن شكل بطاقات portal-grid في الصفحة الرئيسية فقط —
index.css — class portal-card.
لا تغيّر App.tsx structure ولا data loading.
لا تلمس handleCvSave.
```

#### 11.5 شرح

```
اشرح هذا الملف كأني مبتدئ، ثم أعطني خريطة بالدوال والمتغيرات.
```

**مثال filled:**

```
اشرح src/appData.ts كأني مبتدئ.
لا تعدل.
خريطة: pickML, loadSiteContentFromJson, uploadMediaFile, handleCvSave relation.
```

#### 11.6 نشر

```
افحص ملفات النشر فقط، ولا تعدل الكود.
قل لي ما الملفات التي يجب رفعها.
```

**مثال filled:**

```
افحص npm run build:hostinger و hostinger_upload/.
لا تعدل.
قائمة: ماذا أرفع إلى public_html؟ ماذا لا أرفع؟ config.php؟
```

---

### 12. متى أقبل تعديل Cursor؟

| **اقبل** | **السبب** |
|----------|-----------|
| طلبتَ **بالضبط** هذا التعديل | matches طلبك |
| **ملف واحد** (أو ما اتفقت) | Review سهل |
| **diff** واضح — تفهمه | لا surprises |
| `npm run dev` **يعمل** بعد Keep | اختبار نجح |
| **لا** config.php / package.json | آمن |
| commit **قبل** التعديل (الفصل 17) | rollback جاهز |

---

### 13. متى أرفض؟

| **ارفض** | **السبب** |
|----------|-----------|
| ملفات **لم تطلبها** | scope creep |
| **حذف** imports أو دوال | خطر crash |
| تعديل **App.tsx** كبير | بدون خطة |
| **Keep All** لـ 5+ ملفات | لا تفهم الكل |
| يمس **auth.php / schema** | خطير |
| **«تحسين»** غامض | لم تطلبه |

**كيف ترفض:** Reject — أو Undo — أو `git restore`

---

### 14. كيف أتعامل مع تعديلات **كثيرة**؟

1. **قسّم الطلب** — «الآن ملف 1 فقط — الباقي لاحقًا»
2. **لا Keep All** — Keep **ملف ملف**
3. **رتّب الأولوية:** محتوى → UI text → CSS → logic
4. **commit** بعد كل ملف ناجح
5. **اطلب إعادة تقسيم:**

```
عدّلت أكثر مما طلبت.
ارجع للخطة: خطوة 1 فقط — translations.ts — key واحد.
```

---

### 15. كيف أقول «ارجع كما كان»؟

#### قبل Keep

```
توقّف. لا Keep.
ارجع للاقتراح السابق أو اشرح فقط.
```

#### بعد Keep

```
git restore src/App.tsx
```
(أو الملف المعني — **الفصل 17**)

```
Undo — Ctrl+Z في الملف
```

```
استعد من backup ZIP
```

#### لـ Cursor في المحادثة التالية

```
آخر تعديلك كسر الموقع.
لا تصلّح بمزيد من التعديلات.
اعطني خطوات git restore للملفات التي غيّرتها فقط.
```

---

### 16. كيف أطلب **تبسيط الشرح**؟

```
اشرح كأني مبتدئ تمامًا — بدون مصطلحات.
جمل قصيرة. مثال من eng-alaa.com.
```

```
لم أفهم. أعد الشرح بدون TypeScript jargon.
استخدم تشبيه من الحياة.
```

```
اكتب الشرح في PROJECT_LEARNING_BOOK.md — لا في المحادثة فقط.
```

---

### 17. جدول: طلب سيء → طلب جيد

| ❌ سيء | ✅ جيد |
|--------|--------|
| حسّن الموقع | عدّل t.homeWelcome.en في translations.ts |
| أصلح الأخطاء | [الصق خطأ Console] — خطة — لا تنفّذ |
| عدّل App | عدّل سطر openPortal في App — لا غيره — اشرح أولًا |
| أضف قسم | **نظريًا** — خطوات — الفصل 12 — لا كود |
| حدّث كل شيء | build:hostinger — **فحص فقط** — قائمة رفع |

---

### 18. نهاية الفصل

#### 18.1 ملخص

1. **الطلب الواضح** = نتيجة آمنة.
2. **لا** «حسّن الموقع» — **نعم** «ملف X — تغيير Y».
3. **شرح قبل** — **خطة قبل إصلاح** — **شرح بعد**.
4. **«لا تعدل»** — **«ملف واحد»** — **«لا تلمس Z»**.
5. **Review** — **لا Keep All** أعمى.
6. **commit قبل** — **restore بعد** فشل.
7. Cursor = **عامل** — **أنت** المشرف.
8. استخدم **قوالب** جاهزة.
9. **`.cursorrules`** في الطلب للمحتوى/CSS.
10. **قسّم** التعديلات الكبيرة.

#### 18.2 جدول أوامر جاهزة (مرجع سريع)

| الهدف | بداية الطلب |
|-------|-------------|
| فحص | «افحص — **لا تعدل** — السبب والملفات» |
| تعديل صغير | «**ملف واحد فقط:** … المطلوب: …» |
| خطأ | «الخطأ: … **خطة قبل التنفيذ**» |
| تصميم | «**قسم/ class فقط** — لا data ولا حفظ» |
| شرح | «**كأني مبتدئ** — خريطة — **لا تعدل**» |
| نشر | «**build:hostinger فحص** — قائمة رفع — لا تعدل» |
| رجوع | «**git restore** / Undo — لا إصلاح جديد» |
| تبسيط | «**جمل قصيرة** — تشبيه — مثال من المشروع» |

#### 18.3 أهم 10 قواعد لطلب التعديلات

1. **حدّد** — ماذا، أين، كم ملف.
2. **«لا تعدل»** عند الفحص والشرح.
3. **«لا تلمس»** للملفات الخطيرة.
4. **خطة** قبل الإصلاح والنشر.
5. **لا** أوامر غامضة («حسّن»، «أصلح كل شيء»).
6. **Review** كل diff.
7. **commit** قبل التعديل الكبير.
8. **اختبر** dev بعد Keep.
9. **قسّم** طلبات كبيرة.
10. **`.cursorrules`** للمحتوى vs CSS.

#### 18.4 أخطاء شائعة في كتابة الطلبات

| خطأ | النتيجة |
|-----|---------|
| طلب واسع | 10 ملفات |
| بدون «لا تعدل» عند الفحص | كسر while diagnosing |
| نسيان اسم الملف | Cursor يخمّن App.tsx |
| Keep All بدون قراءة | كارثة |
| طلب CSS + logic معًا | entangled diff |
| عدم لصق رسالة الخطأ | إصلاح خاطئ |
| «حسّن الأداء» | dependency upgrade |
| عدم ذكر eng-alaa.com context | حل generic |

#### 18.5 أسئلة تدريبية

1. لماذا «حسّن الموقع» سيء؟
2. صيغة طلب تعديل صغير (4 أجزاء)؟
3. كيف تطلب فحص بدون تعديل؟
4. متى تطلب خطة؟
5. متى Keep vs Reject؟
6. كيف «ارجع كما كان»؟
7. ماذا تستثني في طلب data.json؟
8. قالب نشر — ماذا تسأل؟
9. كيف تطلب تبسيط شرح؟
10. تشبيه عامل الصيانة — الدرس؟

#### 18.6 تمرين نظري: طلب تغيير لون زر فقط

**اكتب الطلب الكامل** (بدون إرسال لـ Cursor — تمرين):

**الهدف:** زر `btn-pill` في header (زر about) — لون خلفية `#005599` — **فقط**.

**طلبك يجب أن يتضمن:**
1. اسم الملف المتوقع (index.css؟ App inline؟ — **ماذا تقول .cursorrules؟**)
2. «ملف واحد فقط»
3. «لا تلمس App.tsx logic»
4. «اشرح قبل التنفيذ» أو «بعد التعديل اشرح»
5. خطوات اختبار (dev + Ctrl+F5)
6. ماذا لو Cursor عدّل App.tsx — **ماذا ترفض؟**
7. رسالة commit مقترحة
8. هل تحتاج build لل live؟
9. **backup** قبل — commit message؟
10. سطر واحد: «لا تغيّر handleCvSave» — **لماذا تذكره؟**

**مثال إجابة نموذجية (للمقارنة بعد أن تكتب):**

```
قبل أي تعديل: اشرح أين يُعرّف لون btn-pill — index.css أم inline في App.tsx.
اتبع .cursorrules: CSS فقط بطلب صريح — هذا الطلب صريح.

عدّل ملف واحد فقط: src/index.css
المطلوب: غيّر لون خلفية .btn-pill (أو modifier للزر about إن وُجد) إلى #005599.
لا تلمس App.tsx ولا appData.ts ولا data.php ولا handleCvSave.
لا تغيّر padding أو layout — اللون فقط.

بعد التعديل: اذكر selector CSS الذي غيّرته + سطر تقريبي.
اختبار: npm run dev → header → زر about → F12 → تأكد لا Console errors.

commit: "Update about btn-pill color to #005599"
قبل التعديل: commit "Backup before btn-pill color change"
live: يحتاج npm run build:hostinger + رفع assets — CSS في bundle.
```

---

> **الفصل التالي (19):** «كيف أختبر الموقع قبل النشر؟» — *(هذا الفصل أدناه)*

---

## الفصل التاسع عشر: كيف أختبر الموقع قبل النشر؟

> **هدف الفصل:** أن **تجرّب** eng-alaa.com **قبل** أن يراه الزوار على Hostinger — وتكتشف المشاكل **عندك** — لا **على الطريق**.

---

### تشبيه: تجربة السيارة قبل السفر

**النشر على Hostinger** = **رحلة** — الزوار على متن «الموقع».

**اختبار الموقع** = **تجربة السيارة** قبل السفر:
- المحرك يعمل؟
- الفرامل؟
- الإضاءة؟
- GPS؟

**لا تنتظر** أن تكتشف **العطل في الطريق** — اختبر **في المرآب** (localhost) **ثم** في **محطة تجريبية** (build محلي) **ثم** بعد الرفع (live).

---

### 1. ما معنى «اختبار الموقع»؟

**الاختبار** = **أنت** تتصرف كـ **زائر** و **مدير** — وتتحقق:

| ماذا تفحص؟ | مثال |
|------------|------|
| **هل يفتح؟** | الصفحة الرئيسية — لا بيضاء |
| **هل يعمل؟** | أزرار، تنقل، حفظ |
| **هل يظهر؟** | صور، نصوص، 3 لغات |
| **هل هناك أخطاء؟** | Console حمراء — Network 404/500 |

**ليس** اختبارًا: «فتحت الموقع مرة — seems OK»  
**نعم** اختبار: **checklist** — كل قسم — جوال — admin — حفظ.

---

### 2. لماذا لا أرفع قبل الاختبار؟

1. **eng-alaa.com live** — كل زائر يرى **الخطأ**.
2. **Rollback** على Hostinger **أبطأ** من Undo محلي.
3. **صفحة بيضاء** = انطباع سيء + SEO.
4. **data.php 500** = **لا حفظ** — تعديلات admin **ضائعة**.
5. **build فاشل** محليًا = **أكيد** فاشل live.

**قاعدة:** **محلي → build → live** — **لا تتخطّى**.

---

### 3. فرق الاختبار المحلي vs Hostinger

| | **محلي** `localhost:5173` | **Hostinger** `eng-alaa.com` |
|--|---------------------------|------------------------------|
| **الأمر** | `npm run dev` | بعد SFTP / build:hostinger |
| **PHP/MySQL** | proxy لـ live أو محدود | **حقيقي** |
| **الملفات** | `src/` hot reload | `dist/` + `api/` |
| **localStorage** | **جهازك** | كل زائر جهازه |
| **الصور uploads** | قد proxy | `/uploads/` على السيرver |
| **متى؟** | **أولًا** — كل تعديل | **أخيرًا** — قبل «انتهيت» |

**مهم:** «يعمل محليًا» **لا يكفي** — API و uploads و paths **تختلف** على live (الفصل 11).

---

### 4. كيف أختبر الصفحة الرئيسية؟

#### قبل البدء

```text
npm run dev
→ http://localhost:5173
→ F12 مفتوح
```

#### خطوات (portal = "home")

1. **هل تفتح؟** — لا صفحة بيضاء
2. **Hero / اسم** — HeroNameDisplay يظهر؟
3. **بطاقات الأقسام** — portal-grid — 4–5 بطاقات؟
4. **النقر** — كل بطاقة تفتح قسمًا؟
5. **اللغة** — بدّل ar / en / de — النص يتغير؟
6. **Console** — **لا** أخطاء حمراء جديدة
7. **bottom-nav** — على عرض ضيق — يظهر؟

**بعد build (اختياري قبل live):**

```text
npm run build
npm run serve
→ localhost:4173 (أو المنفذ الذي يظهر)
```

كرّر نفس الخطوات — **نسخة production** أقرب لل live.

---

### 5. كيف أختبر التنقل بين الأقسام؟

**الأقسام في مشروعك** (`type Portal`):

| portal | ماذا تجرّب؟ |
|--------|-------------|
| `home` | العودة — goHome |
| `about` | صورة، نص، CV link |
| `agri` | مكتبة، مقالات، PlantDiagnostic |
| `graphics` | تصنيفات، مشاريع، صور |
| `software` | مشاريع، playground |
| `cv` | عرض سيرة، PDF |
| `admin` | بعد login فقط |

#### Checklist تنقل

- [ ] من home → كل بطاقة → **يفتح**
- [ ] bottom-nav — كل أيقونة
- [ ] **رجوع** home — زر أو logo
- [ ] **لا** crash عند التبديل السريع
- [ ] **URL** — الموقع SPA — قد لا يتغير path — **طبيعي**

---

### 6. كيف أختبر الصور؟

1. **F12 → Network → Img** — Ctrl+F5
2. **graphics** — بطاقات — `mainImg` — Status **200**؟
3. **agri** — مقالات — `images[]`
4. **مكتبة** — BookCover — `thumbnail`
5. **Drive** — هل `resolveImageSrc` يحوّل؟ — thumbnail يحمّل؟
6. **uploads/** على live — `/uploads/gfx/...` — 200؟
7. **placeholder** — كتاب بدون thumbnail — 📚 يظهر؟
8. **علامة مائية** — watermark على gfx — إن مفعّلة

**فشل شائع:** 404 في Network — الفصل 10.

---

### 7. كيف أختبر لوحة التحكم؟

1. **الدخول admin** — login form — `loginToApi`
2. **`portal === "admin"`** — ContentAdmin يظهر؟
3. **تبويبات:** agri, gfx, lab, site settings…
4. **ContentAdmin modes** — افتح gfx — قائمة مشاريع؟
5. **AnalyticsDashboard** — تبويب إحصائيات؟
6. **FileExplorerAdmin** — قائمة ملفات؟ (إن JWT + upload)
7. **Logout** — يخرج — admin يختفي؟

**محليًا:** قد تحتاج **connectServer** — JWT — نفس live.

---

### 8. كيف أختبر الحفظ؟

#### خطوات حاسمة

1. **login** admin — JWT في localStorage
2. **عدّل** حقلًا **واضحًا** — مثل عنوان كتاب — «TEST-SAVE-123»
3. **احفظ** — onSave → handleCvSave
4. **Network → POST** `./api/data.php` — **200**؟ Response OK؟
5. **Ctrl+F5** — هل «TEST-SAVE-123» **ما زال**؟
6. **Incognito** / جهاز آخر — **نفس** النص؟  
   - **نعم** = حفظ server ✅  
   - **لا** = localStorage فقط ❌
7. **GET data.php** — التعديل في JSON؟

**قبل live:** commit backup — الفصل 17.

---

### 9. كيف أختبر تسجيل الدخول؟

1. **auth.php** — Network POST — **200** + token؟
2. **كلمة مرور خاطئة** — رسالة خطأ — **لا** crash
3. **adminLoggedIn** — true — admin UI يظهر
4. **refresh** — هل يبقى logged in؟ (JWT/localStorage)
5. **على live:** `./api/auth.php` — **ليس** localhost فقط
6. **phpMyAdmin** — `admin_users` — المستخدم موجود؟

**500 على auth** → config.php / MySQL — الفصل 6.

---

### 10. كيف أختبر الإحصائيات؟

**AnalyticsDashboard** داخل admin:

1. افتح تبويب **Analytics** / إحصائيات
2. **page views** — تصفّح home + agri — هل تزيد؟ (`trackPageView`)
3. **GPS tab** — إن وُجد — بيانات موقع؟
4. **CV downloads** — تنزيل PDF — يُسجّل؟
5. **Network** — `./api/analytics.php` — 200؟
6. **لا** تختبر analytics **وأنت adminLoggedIn** فقط — analytics **يتوقف** لـ admin في App — تصفّح **كزائر** في tab آخر/incognito

---

### 11. كيف أختبر على الموبايل؟

#### طريقة 1: DevTools responsive

- F12 → **Toggle device toolbar** (Ctrl+Shift+M)
- iPhone / Pixel — **rotate** portrait/landscape
- **bottom-nav** — usable؟
- **نص** — لا overflow مقطوع

#### طريقة 2: هاتف حقيقي

```text
npm run dev -- --host 0.0.0.0
```
(مشروعك: `"dev": "vite ... --host 0.0.0.0"`)

- نفس WiFi — افتح `http://IP-جهازك:5173`
- **live:** eng-alaa.com من 4G — **اختبار حقيقي**

#### ماذا تفحص على الجوال؟

- [ ] home + nav
- [ ] صور تحمّل
- [ ] admin (إن لزم)
- [ ] لغة RTL/LTR

---

### 12. كيف أختبر السرعة بشكل بسيط؟

**كمبتدئ — بدون أدوات معقدة:**

1. **F12 → Network** — **Disable cache** — Ctrl+F5
2. **DOMContentLoaded** — وقت في أسفل Network — < 3s محلي؟
3. **أكبر ملفات** — JS في `assets/` — حجم معقول؟
4. **صور ضخمة** — Img > 1MB — **بطّئ** — ضغط (الفصل 10)
5. **live:** Lighthouse في Chrome — F12 → Lighthouse → Performance (اختياري)
6. **Three.js background** — هل يبطّئ جوال قديم؟ — جرّب على جهاز حقيقي

**لا تحتاج** perfection — **لكن** لا 10MB صورة رئيسية.

---

### 13. كيف أفتح Console؟

| المتصفح | الطريقة |
|---------|---------|
| **Chrome / Edge** | **F12** أو Ctrl+Shift+J |
| **Firefox** | F12 → Console |

**ماذا ترى:**
- **أحمر** = Error — **اقرأ أول سطر**
- **أصفر** = Warning — غالبًا OK
- **رمادي** = log

**فلتر:** اكتب `error` — أو **Errors only**.

---

### 14. كيف أفتح Network؟

1. **F12**
2. تبويب **Network**
3. **Ctrl+F5** — reload كامل
4. **فلاتر:** All | Fetch/XHR | Img | JS | CSS

**مهم لمشروعك:**

| Name | ماذا تتوقع |
|------|------------|
| `data.php` | GET 200 — JSON |
| `auth.php` | POST عند login |
| `data.json` | GET — merge محتوى |
| `assets/index-*.js` | 200 — live |
| صور Drive/uploads | Img 200 |

---

### 15. ماذا أبحث في الأخطاء؟

| ما تراه | المعنى | أين |
|---------|--------|-----|
| **404** | ملف غير موجود | assets, uploads, api |
| **500** | خطأ سيرver | data.php, auth.php |
| **CORS** | طلب محظور | proxy, img-proxy |
| **JSON parse** | رد ليس JSON | data.php error HTML |
| **undefined** | بيانات ناقصة | App.tsx — Console stack |
| **Failed to fetch** | API لا يصل | api/ ناقص — offline |
| **401** | غير مصرّح | JWT منتهي — login |

**اقرأ:** Message + File + Line — **الفصل 11**.

---

### 16. Checklist — قبل النشر

- [ ] **الموقع يفتح** — dev + serve بعد build
- [ ] **لا صفحة بيضاء** — Console نظيف
- [ ] **كل الأقسام** — home, about, agri, graphics, software, cv
- [ ] **الصور تظهر** — Network Img 200
- [ ] **الأزرار تعمل** — تنقل، لغة، PDF
- [ ] **لوحة التحكم** — login + ContentAdmin
- [ ] **الحفظ يعمل** — POST 200 + incognito يرى
- [ ] **Console** — لا errors حمراء **جديدة**
- [ ] **جوال** — responsive أو هاتف حقيقي
- [ ] **`npm run build:hostinger`** — **بدون فشل**
- [ ] **typecheck** (اختياري): `npm run typecheck`
- [ ] **Git commit** — «Backup before deployment»

---

### 17. Checklist — بعد النشر (live)

- [ ] **https://eng-alaa.com** يفتح
- [ ] **Ctrl+F5** — hard refresh
- [ ] **الصور** — live URLs — uploads/Drive
- [ ] **`./api/data.php`** — JSON في تبويب جديد
- [ ] **auth** — login admin
- [ ] **حفظ** — يظهر **لزائر آخر** / incognito
- [ ] **لا 404** — assets/, api/
- [ ] **لا 500** — data.php, auth.php
- [ ] **data.json** — إن رفعته — merge يعمل
- [ ] **جوال 4G** — ليس WiFi فقط
- [ ] **analytics** — زيارة test من incognito

---

### 18. أخطاء شائعة أثناء الاختبار

#### 18.1 يعمل محليًا — لا يعمل بعد النشر

| السبب | الفحص |
|-------|-------|
| api/ غير مرفوع | File Manager |
| config.php | data.php 500 |
| assets 404 | build + رفع dist |
| base path | vite base `/` |

#### 18.2 يظهر عندي فقط — localStorage

**الأعراض:** أنت ترى التعديل — incognito لا.  
**السبب:** لم يُحفظ server — أو data.json local فقط.  
**الاختبار:** **incognito** دائمًا بعد «حفظ».

#### 18.3 API لا يستجيب

**Failed to fetch** — Network أحمر.  
**فحص:** api/ موجود؟ CORS؟ mixed content http/https?

#### 18.4 الصور لا تظهر

Network Img **404** — URL خاطئ — uploads ناقص — Drive بدون resolveImageSrc.

#### 18.5 CSS لا يحمل

Network **CSS 404** — assets/*.css — build قديم — cache — Ctrl+F5.

#### 18.6 قاعدة البيانات

data.php **500** — phpMyAdmin — schema — config credentials.

---

### 19. جدول: اختبار قبل النشر (ملخص)

| # | الاختبار | الأمر/الأداة | نجاح = |
|---|----------|--------------|--------|
| 1 | تشغيل | npm run dev | صفحة home |
| 2 | Console | F12 | لا errors |
| 3 | أقسام | portal grid | 6 أقسام |
| 4 | صور | Network Img | 200 |
| 5 | لغات | switch ar/en/de | نص صحيح |
| 6 | admin | login | ContentAdmin |
| 7 | حفظ | POST + incognito | نفس البيانات |
| 8 | build | build:hostinger | exit 0 |
| 9 | preview | npm run serve | مثل live |
| 10 | جوال | responsive / 4G | usable |

---

### 20. جدول: اختبار بعد النشر (ملخص)

| # | الاختبار | URL/طريقة | نجاح = |
|---|----------|-----------|--------|
| 1 | الرئيسية | eng-alaa.com | 200 |
| 2 | data.php | /api/data.php | JSON |
| 3 | auth | login admin | JWT |
| 4 | حفظ | incognito | يرى التعديل |
| 5 | assets | Network JS/CSS | 200 |
| 6 | uploads | صورة gfx | 200 |
| 7 | data.json | /data.json | JSON valid |
| 8 | errors | Console live | لا 500 |
| 9 | PDF/CV | download | يعمل |
| 10 | mobile | هاتف 4G | OK |

---

### 21. نهاية الفصل

#### 21.1 ملخص

1. **اختبار** = checklist — ليس نظرة سريعة.
2. **لا رفع** قبل dev + build ناجح.
3. **محلي ≠ live** — اختبر **كلاهما**.
4. **Console + Network** — أدواتك الأساسية.
5. **incognito** — يكشف localStorage-only.
6. **حفظ** = POST 200 + زائر آخر يرى.
7. **admin analytics** — زائر من tab منفصل.
8. **جوال** — responsive + 4G.
9. **build:hostinger** — قبل SFTP.
10. **سيارة قبل السفر** — لا عطل على الطريق.

#### 21.2 أهم 10 أشياء يجب فحصها

1. لا **صفحة بيضاء**
2. **Console** نظيف
3. **6 portals** تعمل
4. **صور** 200
5. **3 لغات**
6. **login** admin
7. **حفظ** server-side
8. **build** ناجح
9. **data.php** JSON على live
10. **جوال**

#### 21.3 أسئلة تدريبية

1. فرق اختبار محلي و live؟
2. كيف تفتح Network؟
3. كيف تتأكد الحفظ server وليس localStorage؟
4. ماذا يعني 404 في assets؟
5. لماذا incognito؟
6. متى npm run serve؟
7. كيف تختبر analytics؟
8. checklist قبل النشر — 5 نقاط؟
9. CSS لا يحمل — أين تفحص؟
10. تشبيه السيارة — الدرس؟

#### 21.4 تمرين نظري: خطة اختبار قبل رفع نسخة جديدة

**السينario:** عدّلت **translations.ts** + **صورة gfx** في admin — تريد رفع live.

**اكتب على ورقة (بدون تنفيذ):**

| # | ماذا تكتب في خطتك |
|---|-------------------|
| 1 | backup — Git message؟ |
| 2 | أوامر terminal بالترتيب (dev, build, serve…) |
| 3 | 5 صفحات تفتحها محليًا |
| 4 | 3 فحوصات Network |
| 5 | اختبار حفظ — نص test — incognito |
| 6 | checklist قبل SFTP — 10 ✓ |
| 7 | ما ترفع — hostinger_upload — 5 مجلدات |
| 8 | checklist بعد live — 8 ✓ |
| 9 | ماذا لو data.php 500 بعد الرفع؟ |
| 10 | متى **rollback** — ZIP أو git؟ |

**مثال إجابة مختصرة للنقطة 6 (قبل SFTP):**

```
✓ dev — home + gfx — صورة جديدة
✓ Console clean
✓ ar/en/de — key المعدّل
✓ admin save — POST 200
✓ incognito — يرى
✓ build:hostinger — OK
✓ serve — preview
✓ جوال responsive
✓ commit backup done
✓ config.php على server — لم أمسه
```

---

> **الفصل التالي (20):** «مشروع تدريبي — قسم تواصل معنا» — *(هذا الفصل أدناه)*

---

## الفصل العشرون: مشروع تدريبي كامل — إضافة قسم جديد خطوة خطوة نظريًا

> **هدف الفصل:** أن **تمشي** في مشروع كامل — **على الورق** — لإضافة قسم **«تواصل معنا»** (ContactSection) — **بدون لمس الكود**.

> **تحذير:** هذا **تدريب نظري فقط**. لا تنفّذ في Cursor حتى تكون جاهزًا — الفصول 16–19 — ومع **backup** — الفصل 17.

---

### تشبيه: غرفة جديدة في البيت

**إضافة قسم** = **إضافة غرفة** لبيت eng-alaa.com:

| جزء البيت | في الموقع |
|-----------|-----------|
| **فكرة الغرفة** | هدف قسم «تواصل معنا» |
| **اسم على الباب** | زر + portal `"contact"` |
| **الأثاث** | ContactSection.tsx — JSX |
| **الدهان** | index.css — `.contact-section` |
| **لافتة بالعربي/إنجليزي** | translations.ts |
| **دفتر العناوين** | appData — phone, email |
| **ربط بالكهرباء** | App.tsx — `{portal === "contact" && ...}` |
| **تجربة قبل السكن** | الفصل 19 — اختبار |

**بدون باب** (زر) — لا أحد **يدخل** الغرفة.  
**بدون ربط** — الغرفة **معزولة** عن البيت.

---

### نظرة على المشروع الحالي (قبل التدريب)

**الأقسام الموجودة** في `type Portal`:

```tsx
type Portal = "home" | "about" | "agri" | "graphics" | "software" | "cv" | "admin";
```

**سنضيف نظريًا:** `"contact"`

**التنقل اليوم:**
- **portal-grid** — بطاقات home
- **bottom-nav** — شريط جوال
- **openPortal(p)** — يغيّر `portal` state

---

### الخطوة 1: تحديد هدف القسم

#### 1.1 ماذا سيعرض؟

**قسم «تواصل معنا»** — صفحة واحدة للزائر:

| عنصر | مثال محتوى |
|------|------------|
| **عنوان** | «تواصل معنا» / Contact Us |
| **وصف قصير** | «نسعد باستفساراتكم…» |
| **بريد** | info@eng-alaa.com |
| **هاتف** | +49… |
| **واتساب** | رابط wa.me/… |
| **عنوان** | (اختياري) المدينة، ألمانيا |
| **روابط social** | LinkedIn, YouTube — من siteSettings أو contact |

#### 1.2 لماذا نحتاجه؟

- زائر يريد **استشارة** أو **سؤال** — لا يجد طريقًا واضحًا
- **احترافية** — portfolio كامل
- **SEO / ثقة** — معلومات اتصال ظاهرة

#### 1.3 ما النصوص؟

**نوعان** (الفصل 9):

| النوع | أين | مثال |
|-------|-----|------|
| **UI ثابت** | translations.ts | «راسلنا»، «الهاتف» |
| **محتوى قابل للتعديل** | appData.contactSection | الرقم الفعلي، email |

#### 1.4 هل فيه نموذج تواصل (form)؟

**المرحلة 1 (بسيط):** **لا form** — فقط **روابط**:
- `mailto:email`
- `tel:phone`
- زر واتساب يفتح wa.me

**المرحلة 2 (متقدم — لاحقًا):** form يرسل POST لـ PHP — **خارج** هذا التدريب.

#### 1.5 واتساب أو بريد؟

**نعم — الاثنان** في البيانات:

```json
"whatsapp": "https://wa.me/491234567890",
"email": "info@eng-alaa.com"
```

**ContactSection** يعرض **أزرار** — نقرة → تطبيق البريد / واتساب.

---

### الخطوة 2: الملفات التي قد نحتاجها

| الملف | الدور في مشروع Contact | خطورة |
|-------|-------------------------|-------|
| **`src/ContactSection.tsx`** | **جديد** — واجهة القسم | ⚠️ متوسط |
| **`src/App.tsx`** | portal + import + render + nav | ❌ عالي |
| **`src/translations.ts`** | نصوص UI — 3 لغات | ⚠️ متوسط |
| **`src/appData.ts`** | نوع `contactSection` + default | ❌ عالي |
| **`src/defaultCatalog.ts`** | قيم افتراضية أول تشغيل | ⚠️ |
| **`src/index.css`** | `.contact-*` styles | ⚠️ (.cursorrules) |
| **`src/ContentAdmin.tsx`** | تحرير phone/email (اختياري) | ❌ عالي |
| **`public/data.json`** | **لا** — إلا إن جعلت contact محتوى JSON | ✅ |
| **`hostinger_deploy/api/data.php`** | يحفظ contact في MySQL | ❌ — متقدم |

**كمبتدئ للتدريب:** ركّز على **ContactSection + translations + App (نظري)** — **appData** إن أردت رقمًا قابلًا للتعديل.

---

### الخطوة 3: تصميم البيانات (نظريًا)

#### 3.1 شكل البيانات في appData

```tsx
contactSection: {
  title: { ar: "تواصل معنا", en: "Contact Us", de: "Kontakt" },
  description: { ar: "...", en: "...", de: "..." },
  phone: "+49 123 456789",
  email: "info@eng-alaa.com",
  whatsapp: "https://wa.me/491234567890",
  address: { ar: "...", en: "...", de: "..." },
  socialLinks: [
    { id: "1", label: "LinkedIn", url: "https://linkedin.com/in/..." }
  ]
}
```

#### 3.2 ملاحظات مهمة

- **title / description / address** = **ML** (ar, en, de) — مثل المقالات
- **phone / email / whatsapp** = **string** — غالبًا **نفس** الرقم كل اللغات
- **socialLinks** = مصفوفة — مثل `siteSettings.socialLinks` الموجود

#### 3.3 أين تُحفظ؟

| خيار | متى |
|------|-----|
| **translations فقط** | نصوص ثابتة — رقم في translations — **بسيط للتدريب** |
| **appData + MySQL** | admin يعدّل — **احترافي** |
| **siteSettings** | دمج في إعدادات موجودة — **بدون** type جديد |

**للتدريب:** ابدأ **translations للعناوين** + **appData.contactSection للقيم**.

---

### الخطوة 4: إنشاء Component نظريًا

#### 4.1 ماذا يعني ContactSection.tsx؟

**ملف React منفصل** — **غرفة** القسم — **لا** 500 سطر داخل App.tsx.

#### 4.2 ماذا يستقبل (props)؟

```tsx
// نظري — لا تنفّذ
ContactSection({
  lang: LangCode,           // "ar" | "en" | "de"
  t: T,                     // translations[lang] — تسميات UI
  data: ContactSectionData, // من appData
})
```

| prop | لماذا |
|------|-------|
| **lang** | pickML للعنوان والوصف |
| **t** | «البريد»، «اتصل»، «واتساب» |
| **data** | email, phone, whatsapp |

#### 4.3 ماذا يعرض؟ (نسخة بسيطة)

```
┌─────────────────────────────────────┐
│  [pickML(data.title, lang)]         │
│  [pickML(data.description, lang)]   │
│                                     │
│  📧  data.email  → mailto:          │
│  📞  data.phone  → tel:             │
│  💬  [t.contactWhatsApp] → wa link  │
│                                     │
│  [social icons row]                 │
└─────────────────────────────────────┘
```

#### 4.4 البساطة في البداية

- **لا** form
- **لا** خريطة Google
- **لا** animation معقد
- **نعم** div + h1 + p + 3 أزرار + classNames موجودة (`glass-panel`, `btn-pill`)

---

### الخطوة 5: ربط القسم مع App.tsx (نظريًا)

#### 5.1 توسيع Portal

```tsx
type Portal = ... | "contact";
```

**بدون هذا** — TypeScript **يرفض** `openPortal("contact")`.

#### 5.2 import

```tsx
import { ContactSection } from "./ContactSection";
```

**نسيان import** = `ContactSection is not defined` — **خطأ شائع #1**.

#### 5.3 متى يُعرض؟

```tsx
{portal === "contact" && (
  <ContactSection
    lang={lang as LangCode}
    t={t}
    data={data.contactSection}
  />
)}
```

**App يعرف** من **`portal` state** — مثل `{portal === "agri" && ...}`.

#### 5.4 زر التنقل

**أ) بطاقة في portal-grid (home):**

```tsx
{ id: "contact", icon: "fa-envelope", num: "05", ... }
```

**ب) bottom-nav:**

```tsx
{ key: "contact", icon: "fa-envelope", label: t.contactNav }
```

**ج) openPortal:**

```tsx
onClick={() => openPortal("contact")}
```

#### 5.5 أخطاء يجب تجنبها

| خطأ | النتيجة |
|-----|---------|
| نسيت `"contact"` في Portal | typecheck fail |
| نسيت import | crash |
| `{portal = "contact"}` بدل `===` | syntax / logic bug |
| نسيت bottom-nav | الجوال لا يصل للقسم |
| عدّلت 20 مكان portal بدون خطة | App.tsx فوضى |

---

### الخطوة 6: إضافة الترجمة

#### 6.1 في translations.ts

**لكل لغة** — ar, en, de:

```tsx
contactNav: "تواصل معنا",
contactTitle: "...",        // أو من ML — UI labels
contactEmailLabel: "البريد",
contactPhoneLabel: "الهاتف",
contactWhatsAppLabel: "واتساب",
contactAddressLabel: "العنوان",
```

**interface T** — **نفس keys** — **الثلاث** — وإلا typecheck fail.

#### 6.2 داخل ContactSection

```tsx
// تسميات الأزرار — من t
<span>{t.contactEmailLabel}</span>

// عنوان الصفحة — من pickML إن في data
<h1>{pickML(data.title, lang)}</h1>
```

**لا تخلط:** `t.*` = UI — `pickML(data.*)` = محتوى.

#### 6.3 checklist ترجمة

- [ ] ar + en + de لكل key
- [ ] interface T محدّث
- [ ] بدّل lang في الموقع — **3** اختبارات

---

### الخطوة 7: إضافة التصميم CSS (نظريًا)

#### 7.1 class خاص

```css
.contact-section {
  max-width: 720px;
  margin: 0 auto;
  padding: 24px 16px;
}

.contact-card {
  /* glass-panel style — أو extend existing */
}

.contact-actions {
  display: flex;
  flex-direction: column;
  gap: 12px;
}
```

**`.cursorrules`:** CSS **بطلب صريح** — إضافة قسم = **استثناء مخطّط**.

#### 7.2 البطاقة

- استخدم **`glass-panel`** الموجود — **قبل** كتابة CSS جديد
- **أزرار** — `btn-pill` — **لون** واحد — الفصل 18

#### 7.3 الجوال

```css
@media (max-width: 640px) {
  .contact-section { padding: 16px 12px; }
  .contact-actions { gap: 10px; }
}
```

**اختبار:** F12 responsive — bottom-nav **لا** يغطي المحتوى.

---

### الخطوة 8: ربط لوحة التحكم (نظريًا)

#### 8.1 هل نحتاج ContentAdmin؟

| | |
|--|--|
| **لا** | رقم/email **ثابت** في appData/defaultCatalog — تعدّل كود أو JSON مرة |
| **نعم** | تريد **تغيير** الهاتف **بدون** Cursor — مثل site settings |

#### 8.2 كيف نظريًا؟

1. **mode جديد** `'contact'` في ContentAdmin — **أو**
2. **تبويب** في site settings — حقول phone, email, whatsapp
3. **onSave** → handleCvSave → data.php
4. **حقول ML** — MlNameInputs للعنوان والوصف

#### 8.3 مخاطر ContentAdmin

- App.tsx **3200+** سطر admin — **خطأ** = admin **كامل** معطّل
- **handleCvSave** — partial خاطئ — **يمسح** بيانات أخرى
- **كمبتدئ:** **لا** ContentAdmin في **أول** تنفيذ — **appData يدوي** أو siteSettings

---

### الخطوة 9: اختبار القسم (نظريًا)

**اتبع الفصل 19:**

| # | اختبار |
|---|--------|
| 1 | `npm run dev` |
| 2 | home → بطاقة «تواصل» — **أو** bottom-nav |
| 3 | `portal === "contact"` — ContactSection **يظهر** |
| 4 | **ar / en / de** — عنوان + labels |
| 5 | **mailto** — يفتح بريد؟ |
| 6 | **wa.me** — يفتح واتساب؟ |
| 7 | **F12 Console** — **لا** أخطاء |
| 8 | **Network** — لا 404 |
| 9 | **جوال** — readable |
| 10 | **home + agri + …** — **لا** انكسر |

**بعد build:**

```text
npm run build:hostinger
npm run serve
→ كرّر 2–10
```

---

### الخطوة 10: أخطاء شائعة وحلولها

| خطأ | السبب | الحل |
|-----|-------|------|
| **نسيان import** | ContactSection | import في App |
| **نسيان portal** | type Portal | أضف `"contact"` |
| **اسم Component خطأ** | ContactSection vs Contact | تطابق import/export |
| **props ناقصة** | lang undefined | مرّر من App |
| **كسر CSS** | selector عام | `.contact-*` scoped |
| **نسيان en/de** | key واحدة | translations × 3 |
| **كسر JSON/ types** | contactSection shape | طابق appData interface |
| **Keep All** | Cursor عدّل App كامل | Review — الفصل 18 |
| **live بدون test** | — | الفصل 19 |

---

### مخطط المشروع التدريبي (كامل)

```
فكرة القسم
   «تواصل معنا» — email, phone, whatsapp
        ↓
بيانات القسم
   contactSection في appData + ML
        ↓
ContactSection.tsx
   props: lang, t, data — JSX بسيط
        ↓
ربط في App.tsx
   Portal + import + {portal === "contact" && ...}
        ↓
إضافة زر التنقل
   portal-grid + bottom-nav + openPortal("contact")
        ↓
إضافة ترجمة
   translations.ts — contactNav, labels × 3
        ↓
إضافة CSS
   .contact-section — responsive
        ↓
اختبار
   dev + Console + 3 لغات + جوال
        ↓
(اختياري) ContentAdmin
        ↓
نشر لاحقًا
   build:hostinger — الفصل 13 — checklist 19
```

---

### جدول الملفات ودور كل ملف

| الملف | الدور | الترتيب النظري |
|-------|-------|----------------|
| appData.ts | نوع + default contactSection | 1 |
| ContactSection.tsx | UI القسم | 2 |
| translations.ts | labels UI | 3 |
| App.tsx | portal + nav + render | 4 |
| index.css | styles | 5 |
| ContentAdmin.tsx | admin edit | 6 (اختياري) |
| defaultCatalog.ts | defaults | مع appData |
| data.php / MySQL | persist | مع admin |

---

### Checklist — إضافة قسم جديد (Contact)

#### التخطيط
- [ ] هدف القسم واضح
- [ ] sketch على ورقة
- [ ] backup Git — «Backup before contact section»

#### البيانات
- [ ] contactSection shape
- [ ] ML لل title/description/address
- [ ] phone, email, whatsapp

#### الكود (عند التنفيذ لاحقًا)
- [ ] ContactSection.tsx
- [ ] Portal type `"contact"`
- [ ] import + render block
- [ ] portal-grid item
- [ ] bottom-nav item
- [ ] translations × 3 + interface T
- [ ] CSS minimal
- [ ] typecheck
- [ ] dev test — الفصل 19
- [ ] build:hostinger
- [ ] live test — incognito

---

### ملخص الفصل (المشروع التدريبي)

1. **حدّد الهدف** — ماذا يرى الزائر.
2. **صمّم البيانات** — ML + strings.
3. **Component منفصل** — ContactSection.
4. **App.tsx** — portal + import + شرط + nav.
5. **translations** — UI — 3 لغات.
6. **CSS** — بسيط — responsive.
7. **ContentAdmin** — **اختياري** — متقدم.
8. **اختبر** — قبل live.
9. **أخطاء** — import, portal, props, ترجمة.
10. **غرفة + باب + ربط** — تشبيه البيت.

---

### أسئلة اختبار نهائية (الفصل 20 + مراجعة الكتاب)

1. ما portal `"contact"`؟
2. فرق `t.contactNav` و `pickML(data.title, lang)`؟
3. أي ملف **الأخطر**؟
4. لماذا Component منفصل؟
5. checklist قبل live؟
6. كيف تختبر incognito للحفظ؟
7. ما `.gitignore` يحمي؟
8. قالب Cursor «ملف واحد فقط»؟
9. فرق data.json و appData؟
10. ماذا تفعل إن صفحة بيضاء بعد deploy؟

---


---

## خاتمة: ماذا أصبحت أفهم الآن؟

**تهانينا** — أكملت **20 فصلًا** عن مشروع **eng-alaa.com (Portfolio Hub)**.

### أصبحت أفهم — على الأقل نظريًا:

| # | ماذا أفهم؟ | الفصل |
|---|-----------|-------|
| 1 | **خريطة المشروع** — من الملفات إلى Hostinger | 2, 6, 13 |
| 2 | **أين يبدأ التشغيل** — index.html → main.tsx → App.tsx | 1 |
| 3 | **وظيفة App.tsx** — portal, state, كل الأقسام | 3 |
| 4 | **React و Components** — props, state | 7 |
| 5 | **من أين تأتي البيانات** — appData, data.json, MySQL | 4 |
| 6 | **لوحة التحكم** — ContentAdmin, auth, handleCvSave | 5 |
| 7 | **السيرفر و Hostinger** — data.php, auth.php, schema | 6, 13 |
| 8 | **التعديل بحذر** — ملفات آمنة/خطيرة, Review | 16 |
| 9 | **الاختبار قبل النشر** — Console, Network, incognito | 19 |
| 10 | **طلبات Cursor الصحيحة** — ملف واحد, خطة, لا Keep All أعمى | 18 |

### ماذا أستطيع أن أفعل بثقة أكبر؟

- تشغيل `npm run dev` وفهم F12  
- تعديل **محتوى** عبر ContentAdmin أو data.json  
- تشخيص **صور 404** و **أخطاء Console**  
- **commit** backup قبل التعديل  
- **checklist** قبل Hostinger  
- **تخطيط** قسم جديد (الفصل 20)  

### الخطوة التالية

1. **خطة 30 يوم** — الفصل 15  
2. **تطبيق Contact** — ملفًا ملفًا — backup — اختبار  
3. **الملاحق** أدناه — للمراجعة اليومية  

---

## ملحق أ: أوامر جاهزة أستخدمها مع Cursor

### 1. افحص بدون تعديل

```
افحص هذه المشكلة ولا تعدل أي ملف.
أخبرني ما السبب المحتمل والملفات المرتبطة.
```

### 2. اشرح هذا الملف

```
اشرح ملف [اسم الملف] كأني مبتدئ تمامًا.
لا تعدل أي كود.
أعطني خريطة بالدوال والمتغيرات المهمة.
```

### 3. عدّل ملفًا واحدًا فقط

```
عدّل فقط ملف [X].
المطلوب: [Y].
لا تغيّر أي ملف آخر.
بعد التعديل اشرح ماذا فعلت وكيف أختبر.
```

### 4. أصلح خطأ بدون تغيير ملفات أخرى

```
لدي هذا الخطأ: [الصق رسالة Console أو Terminal]
افحص السبب.
قبل أي تعديل اعطني خطة minimal.
لا تنفّذ حتى أوافق.
```

### 5. اكتب خطة قبل التنفيذ

```
أريد [الهدف].
لا تعدل أي ملف الآن.
أعطني خطة خطوة بخطوة + الملفات + المخاطر.
انتظر موافقتي.
```

### 6. راجع التعديلات قبل Keep All

```
هذه قائمة الملفات التي عدّلتها: [list].
اشرح كل ملف في 2 جمل.
أي ملف يمكن رفضه بأمان؟
```

### 7. ساعدني أنشر على Hostinger

```
افحص npm run build:hostinger و hostinger_upload/.
لا تعدل الكود.
ما الملفات التي أرفع إلى public_html؟
ما الذي لا أرفعه؟
```

### 8. اشرح خطأ Console

```
هذا الخطأ من F12 Console: [الصق]
لا تعدل.
اشرح المعنى + الملف المحتمل + أول 3 خطوات فحص.
```

---

## ملحق ب: قائمة الملفات المهمة في المشروع

| الملف | الوظيفة | آمن للمبتدئ؟ | ملاحظات |
|-------|---------|--------------|---------|
| `index.html` | باب الموقع — `#root` | ⚠️ محدود | لا تحذف `#root` |
| `src/main.tsx` | يشغّل React | ❌ خطير | نقطة دخول |
| `src/App.tsx` | قلب الموقع — portal, state | ❌ خطير | 3600+ سطر |
| `src/appData.ts` | types, fetch, merge, upload | ❌ خطير | |
| `src/defaultCatalog.ts` | بيانات افتراضية | ⚠️ متوسط | |
| `src/translations.ts` | نصوص UI — 3 لغات | ⚠️ متوسط | interface T |
| `src/index.css` | تنسيق الموقع | ⚠️ متوسط | `.cursorrules` |
| `src/ContentAdmin.tsx` | لوحة تحكم المحتوى | ❌ خطير | |
| `src/AnalyticsDashboard.tsx` | إحصائيات | ❌ خطير | admin |
| `src/PlantDiagnostic.tsx` | تشخيص نبات | ⚠️ متوسط | agri |
| `src/CvRenderer.tsx` | عرض CV | ⚠️ متوسط | |
| `public/data.json` | كتب, مقالات, تصاميم | ✅ آمن نسبيًا | SFTP |
| `hostinger_deploy/api/data.php` | MySQL GET/POST | ❌ خطير | |
| `hostinger_deploy/api/auth.php` | login, JWT | ❌ خطير | |
| `hostinger_deploy/api/analytics.php` | تتبع زوار | ❌ خطير | |
| `hostinger_deploy/schema.sql` | جداول MySQL | ❌ خطير | |
| `scripts/prepare-hostinger.mjs` | hostinger_upload | ⚠️ متوسط | |
| `package.json` | scripts, deps | ❌ خطير | |
| `vite.config.ts` | build, proxy | ❌ خطير | |

---

## ملحق ج: قائمة فحص قبل أي تعديل

- [ ] أخذ نسخة احتياطية
- [ ] تشغيل الموقع قبل التعديل (`npm run dev`)
- [ ] تعديل شيء واحد فقط
- [ ] قراءة الملفات المرتبطة في الكتاب
- [ ] طلب خطة من Cursor قبل التنفيذ
- [ ] مراجعة التغييرات (diff)
- [ ] اختبار الموقع
- [ ] عدم الضغط على Keep All بسرعة
- [ ] عدم رفع الموقع قبل الاختبار

---

## ملحق د: قائمة فحص قبل النشر على Hostinger

- [ ] build ناجح (`npm run build:hostinger`)
- [ ] الصور تعمل
- [ ] التنقل يعمل (كل portals)
- [ ] لوحة التحكم تفتح
- [ ] تسجيل الدخول يعمل
- [ ] data.php يستجيب (JSON)
- [ ] MySQL متصل (config.php)
- [ ] الحفظ يعمل (incognito يرى)
- [ ] الموقع يعمل على الجوال
- [ ] لا أخطاء Console واضحة على live

---

> **هذا الكتاب هو دليل تعليمي لفهم المشروع، وليس بديلًا عن النسخ الاحتياطي أو مراجعة التعديلات قبل قبولها.**

---

*آخر تحديث: يونيو 2026 — الفصول 1–20 + ملاحق — مشروع eng-alaa.com Portfolio Hub*

*نهاية الكتاب التعليمي — للتعلّم فقط*
