# ── Local development (Vite) ─────────────────────────────────────
# PHP files (ai_proxy, api, PDF) do not run in Vite — requests are proxied to Hostinger.
# Change this if your live site uses a different domain:
VITE_PHP_PROXY_TARGET=https://eng-alaa.com

# Optional: override AI proxy URL directly (skip Vite proxy path)
# VITE_AI_PROXY_URL=https://eng-alaa.com/ai_proxy.php

# ── Hostinger deploy (npm run deploy) ────────────────────────────
# ftp = منفذ 21 | sftp = منفذ 65002 (مفتاح أو باسورد)
DEPLOY_METHOD=ftp
SFTP_HOST=YOUR_SERVER_IP
SFTP_PORT=65002
FTP_PORT=21
SFTP_USER=uXXXXXXXX
SFTP_PASSWORD="YOUR_FTP_PASSWORD"
SFTP_REMOTE_PATH=/home/uXXXXXXXX/domains/YOUR-DOMAIN.com/public_html
SFTP_PRIVATE_KEY=.deploy-keys/id_ed25519
